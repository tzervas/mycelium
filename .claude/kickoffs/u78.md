# Kickoff `u78` — unsafe-code hardening (`mycelium-mlir`; DN-21 / M-678)

> The DN-21 unsafe-hardening epic (`docs/notes/DN-21-Unsafe-Code-Hardening-Survey.md`).
> **Fully disjoint from `lex`** (touches `mycelium-mlir` + the check tooling, **never**
> `mycelium-l1`) — runs as a **parallel Sonnet swarm**, in parallel with `lex`. Read `CLAUDE.md` +
> `.claude/kickoffs/README.md` (the tiered `dev → integration → main` workflow) first.

## ⚡ RESUME HERE

**Branch off `dev`.** Behaviour-preserving, **zero-new-dependency** hardening of the workspace's
*only* `unsafe` — the **6 dynamic-linking FFI blocks** in `crates/mycelium-mlir` (DN-21 §2:
`jit.rs` dlopen/dlsym/dlclose/transmute-call · `bitnet.rs:349` · `specialize.rs:128`). Runs as a
**parallel-leaf Sonnet swarm** (the children are mostly disjoint). Land feature → `dev` →
`integration` → `main` per the tiered workflow.

**▶ FIRST: M-679 — strengthen the 3 thin JIT-FFI `// SAFETY:` comments** and add
`debug_assert!(!ptr.is_null())` at the dlsym/transmute-call sites (`jit.rs`, `bitnet.rs:349`,
`specialize.rs:128`). Pure robustness — no behaviour change, no new dependency.

## Chain (DN-21 §6 priority/risk order)
| # | Issue | What | Tree |
|---|---|---|---|
| 1 | **M-679** | strengthen the 3 thin SAFETY comments + `debug_assert!(!ptr.is_null())` at the FFI call sites | `crates/mycelium-mlir/**` |
| 2 | M-680 | forbid-pin the trusted base + the **11 zero-unsafe** `mycelium-mlir` submodules (`#![forbid(unsafe_code)]`) | `crates/mycelium-mlir/**` |
| 3 | M-681 | `just safety-check` — a SAFETY-adjacency gate (every `unsafe` block carries a `// SAFETY:`) | `scripts/checks/**` + `justfile` |
| 4 | M-682 | in-house **`Sym<'lib,T>`** lifetime-binding newtype (maintainer-chosen over `libloading`; **zero new dep**) so a JIT'd fn-ptr cannot outlive its `dlopen` handle — the top co-location risk (DN-21 §4) | `crates/mycelium-mlir/**` |
| 5 | M-683 | document the `audit_wild` (`.myc`) vs `clippy` (`.rs`) unsafe-scope split (ADR-014) | append-only notes |

## Ownership / method
- **Owns:** `crates/mycelium-mlir/**`, `scripts/checks/**` + `justfile` (the safety gate), and the
  ADR-014 / Security-Checks-Contract **append-only** notes. **Never touches `crates/mycelium-l1`**
  (that is `lex`). **Read-only / FLAG up:** `tools/github/issues.yaml`, `CHANGELOG.md`,
  `docs/Doc-Index.md`, workspace `Cargo.toml`.
- **Parallel-leaf Sonnet swarm:** M-679 / M-681 / M-683 are disjoint and can fan out; M-680 + M-682
  both edit `mycelium-mlir` source — **serialize those two**. Per task: a small auditable change +
  its SAFETY-adjacency comment + a test → review → land.
- **Honesty (DN-21 §7 — the irreducible floor):** the unsafe that **must remain** is calling JIT'd
  fn-pointers, the `dlopen` constructor, and the ABI claim — **do NOT** rewrite the SIMD in Rust
  intrinsics (FR-C3: the SIMD lives in JIT-compiled LLVM IR **by design**, inspectable). Every
  `unsafe` keeps an adequate `// SAFETY:` (none silently dropped — G2). **Done** = M-679 → M-683
  landed on `main`; `just safety-check` green; the unsafe-block **count is unchanged** but each is
  forbid-pinned-around and lifetime-bound where possible; every issue body + status updated.
