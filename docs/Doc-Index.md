# Mycelium — Document Index & Status

**Start here for current state:** `docs/CURRENT-STATE.md` — the dense "what's true now" pointer
index (gate state, corpus digest, implementation state, open maintainer decisions). This document
remains the authoritative status resolver for every individual RFC/ADR/DN.

**Date:** June 08, 2026 *(table living — last refreshed 2026-06-23)*
**Purpose:** the authoritative map of the document corpus — every RFC/ADR/DN with its status and the dependency DAG. The design corpus is Accepted/Resolved and **Rust-first implementation is underway** (Phases 0–3, 5, 7 complete; 4, 6, 8 in progress — see `docs/planning/phase-*.md`). Per the honesty rule the per-module stdlib specs were held at *"implemented (Rust-first), pending ratification"* — **not** silently `Accepted` — until the maintainer ratification passes (2026-06-20 DN-07 moved 23 specs to `Accepted`; **2026-06-21** additionally ratified `runtime` (v0 R1 surface) and the newly-written `sys` spec → **25/25** crate specs `Accepted`). Only `self-hosting-readiness` (the M-502 gate doc, not a crate) stays `Draft`. **2026-06-23:** RFC-0009/RFC-0010/RFC-0019/ADR-015/ADR-018/ADR-019 moved Accepted → **Enacted** (maintainer-approved; see §1 rows and individual doc changelogs). This index is append-only (§6).

---

## 1. Corpus & status (post-solidification)

| Doc | Role | Status |
|---|---|---|
| **Prior-Art Survey & Synthesis** | evidence base (pass 1) | Recorded → `research/01-prior-art-survey-RECORD.md` (full narrative is a conversation artifact) |
| **Research Findings (T0/T1/T2)** | evidence base (pass 2) | Recorded → `research/02-research-findings-RECORD.md` (full narrative is a conversation artifact) |
| **Project Foundation (r4)** | charter, scope, requirements, ADR-001…009, roadmap, risks; **r4** records Rust-first implementation status (Phases 0–3/5/7 complete, 4/6/8 in progress), extends §6 roadmap with Phases 4–8, refreshes §10 | **Living — updated** |
| **RFC-0001 — Core IR & Metadata Schema** | value model, `Repr`, `Meta`, guarantee lattice, content-addressing; **r3** adds data (registry Σ, `Datum`/`CoreValue`, `Construct`/flat `Match`, WF6/WF7/WF8); **r4** adds `Lam`/`App`/`Fix` (full L1-in-Core-IR; closed-closure value model; R7-Q1/Q3) | **Accepted (r4)** |
| **DN-01 — Packing Placement** | tradeoff study → schedule-staged decision | **Resolved → folded into RFC-0001 §4.1 + RFC-0004 §5** |
| **DN-02 — Fungal Lexicon & Reserved Words** | the naming law + ratified surface vocabulary (themed vs conventional) feeding RFC-0006's grammar | **Resolved** (ratified 2026-06-10) |
| **DN-03 — Lexicon Amendment: Surface Additions & Runtime Names (one name each)** | amends DN-02: adopt `consume`/`grow`, decline `embody` (keep `impl`), reserve `for`; **one name per term** (flat — rejects ADR-012 §7.6's canonical+alias scheme); the single Runtime names (`hypha`/`fuse`/`xloc`/`cyst`/`graft`/`forage`/`backbone`/`mesh`/`tier`/`reclaim`) | **Resolved** (2026-06-10) |
| **DN-04 — Optional Structured Diagnostics (DynEL-inspired)** | evaluate DynEL's feature set as **opt-in** structured/graded/dual-format diagnostics over Mycelium's existing explicit errors; governing constraint = diagnostics are *additive*, never a substitute for a never-silent error (G2); imports the contracts (levels / human+JSON / reified per-fn `{exceptions, message, tags}`), excludes the anti-patterns (`eval`-on-config, env-dump, exception-swallowing); tooling-layer, kernel untouched (KC-3) (M-345) | **Resolved** (2026-06-21; RFC-0013 enacted M-345 — diagnostics §4 landed in `crates/mycelium-lsp/src/diagnostics`; all Q1–Q5 decisions absorbed; M-648 editorial sweep) |
| **DN-05 — AOT Recursion Execution Strategy** | make the AOT env-machine recursion stack-robust without bloat; **empirically grounded** (`xtask recursion-probe`: env-machine aborts at ~600 `Fix`-unfolds vs the interpreter's O(1) stack); priority = (1) bank native MLIR→LLVM stack-robustness as a design requirement [libMLIR-gated, M-348], (2) explicit control stack / trampoline [near-term, makes never-silent total], (3) tail-call detection [cautious, optional, KC-3/YAGNI] (M-347) | **Resolved** (2026-06-21; M-347 trampoline + M-349 dynamic DepthBudget both enacted; `EvalError::DepthLimit` explicit (never abort/hang); G2 holds; M-648 editorial sweep) |
| **DN-06 — Static Organization & Dynamic Grouping Lexicon** | introduces `phylum` (library-scale) / `nodule` (basic unit, replaces "module") and reassigns `colony` to the **dynamic** runtime grouping of `hypha` (RFC-0008 §4.7); supersedes DN-02 §2's `colony` line (append-only); nodule status declared by header comment, not filename; keyword migration = M-358 | **Resolved** (2026-06-16) |
| **DN-07 — RFC-0016 Ratification-Readiness** | the ratification-*readiness* assessment that framed RFC-0016's maintainer ratification pass: the 23 specs by Ring/Tier, a recommended disposition per §8 question, the residual `BF16→F32` FLAG, and the standing §7 grounding obligation (M-501) | **Resolved** (2026-06-17; the pass ran — RFC-0016 → Accepted; Q1/Q2/Q5/Q6 + `BF16→F32` accepted, Q3 direction + M-540, Q4 Phase-7, §7 discharged via `research/08`) |
| **DN-08 — Maturation Granularity** | captures the maintainer intent that `matured` apply at module (`nodule`) / library (`phylum`) / program scope — coarse-grained, at a stable point — with per-`fn` maturation *atypical* and selective *de*-maturation (shift one subcomponent back to interpreted) the rare fine-grained operation; grounded in RFC-0007 §4.5, Glossary §2.10, DN-06 | **Resolved** (2026-06-18; ratified by **RFC-0017** — all five §3 questions closed) |
| **DN-09 — The KC-2 Verdict** | the maintainer verdict on the M-002 LLM-leverage run: measured leverage is *weak-but-recoverable* (best arm 7B+examples: 40% first-attempt → 70% eventual, +30pp edit-to-fix), so KC-2's "irrecoverable collapse" kill criterion is **not triggered**. **Verdict: proceed.** Selects the RFC-0006 Q1 L3 strategy = **committed text syntax + co-equal projection layer (M-380)**; records the "usability over theming" surface bias (§3.2); names the honest open follow-up (the T3.6 retention-ratio ablation) as a research prompt. Closes the standing KC-2 gate corpus-wide | **Resolved** (2026-06-18) |
| **DN-10 — Remaining L1 Gaps** | a planning capture (advisory, DN-08 posture) of two L1 gaps in dependency order, **both now enacted**: **R7-Q3** mutual-recursion *surface elaboration* (identity already fixed at RFC-0001 r5 FixGroup → purely additive; **landed in `mycelium-l1::elab` via M-391**) and **R7-Q4** the prim table Π → content-addressed declarations (ADR-003; **migrated via M-390**); each had a build-task framing + spike prompt | **Resolved** (2026-06-21; R7-Q3/Q4 enacted by M-391/M-390; M-648 editorial sweep) |
| **research-prompts.md — Standing Research Prompts** | the consolidated index of standing research prompts for variant passes (RP-1 KC-2 T3.6 retention-ratio ablation · RP-2 stage-1 grading implicit-flows · RP-3 traits coherence + Repr-poly · RP-4 projection ergonomics G11 · RP-5 maturation enactment spikes · RP-6 R7-Q3 surface grammar · RP-7 R7-Q4 prim BoundBasis · RP-8 performance optimization + tool-extraction spike), each with question / falsifier / which decision it feeds; append-only "Resolved Prompts" stub | **Living index** (2026-06-21) |
| **DN-11 — Next-Wave Plan** | a planning capture (advisory, DN-08 posture) that sorts the post-#194 KC-2-unblocked work into three dependency-ordered tracks with gates — **A** DN-10 L1 completion (mints **M-390** R7-Q4 / **M-391** R7-Q3), **B** RFC-0018…0021 ratification (each held Draft until its RP-1…RP-4 spike), **C** Phase-5 stdlib (gated on M-501/M-502) — names the leading item (Track A → R7-Q4) and records the stdlib spec-vs-code status nuance honestly. **2026-06-21 §5 appended (M-650):** Phase-5-complete summary (all 25 std crates done, RFC-0016/0017/0021 Enacted), remaining gate items (M-649 DEFERRED post-1.0; M-502 not-yet), and Phase-6 roadmap (Stage-1 generics/traits, effect annotations, native codegen, self-hosting expansion, open research gaps) | **Resolved** (2026-06-21; Phase-5 summary + Phase-6 roadmap appended §5; M-650 editorial sweep) |
| **DN-12 — RFC-0020 Ratification-Readiness** | a readiness assessment (advisory, DN-07 posture) recording that RFC-0020 (L2 surface) carries **no research gate**; maps its deferred items onto the now-research-discharged RFC-0018 (RP-2) / RFC-0019 (RP-3) and the enacted RFC-0001 r5 `FixGroup`; recommends a **scoped** ratification with a carve-out (the RFC-0006 r5 precedent) | **Resolved** (2026-06-21; RFC-0020 ratified Accepted scoped (2026-06-18) per DN-12 recommendation; §4.2/§4.5 carve-outs recorded; M-647/M-648 editorial sweep) |
| **DN-13 — RP-6 Surface Grammar for Mutual Recursion** | the surface-grammar decision for ≥2 mutually-recursive top-level functions (RP-6 / RFC-0007 §8 R7-Q3, surface half): **nodule-wide mutual visibility, no new syntax** — every top-level `fn` in a `nodule` is mutually visible and the elaborator auto-groups each call-graph SCC into the RFC-0001 r5 `FixGroup` (M-343 lowering); the inferred grouping is materialized as that L0 node (no black box). Adds no grammar production (KISS · DN-09 §3.1 append-only); confirmed by **M-391** (differential + identity + never-silent) | **Resolved** (2026-06-19; M-391) |
| **DN-14 — Self-Hosting Gate** (`docs/notes/DN-14-Self-Hosting-Gate.md`) | an honest feature-readiness assessment of whether Mycelium can currently be used to author its own stdlib modules (dogfooding); enumerates 11 surface-language features required, scores each `present` / `gate-fails` / `missing` against actual source evidence (primarily `crates/mycelium-l1/src/checkty.rs` + `ast.rs`); verdict: **5 present, 5 gate-fails, 1 partially missing** — self-hosting is not yet established. Gate-fails: generics (monomorphic-only), trait interfaces (deferred from v0), effect annotations (no AST syntax), `wild`/FFI blocks (LR-9 denied by default), static guarantee index (stage-0 only). Non-blocking: RFC-0012 ambient direction discharged as M-540; runtime phylum Phase-7-gated. Grounded in DN-13, RFC-0007, `mycelium-l1` | **Draft** (2026-06-19; M-502) |
| **DN-15 — Native-Path Direct-LLVM Decomposition** (`docs/notes/DN-15-Native-Path-Direct-LLVM-Decomposition.md`) | the honest decomposition of **M-348** into two halves with distinct unblock conditions: the **libMLIR-gated** real `ternary`→`arith`/`vector`→LLVM dialect lowering (`dialect.rs` is a textual skeleton; stays **blocked**, VR-5) vs the **direct-LLVM-advanceable** `llvm.rs` extension sanctioned by RFC-0004 §2's "lighter direct-LLVM backend" revisit clause; a per-increment table (bit/trit done · non-recursive Construct/Match this wave · closures+heap · recursion+DN-05 #1 stack-robustness · real MLIR dialect blocked) marking each "needs libMLIR? / tractable now? / risk". Grounds the **M-373** increment; cites RFC-0004 §2/§6, DN-05, ADR-009, `llvm.rs`/`dialect.rs` | **Draft** (2026-06-19; M-373) |
| **DN-16 — Stdlib Ratification-Readiness** (`docs/notes/DN-16-Stdlib-Ratification-Readiness.md`) | an advisory (DN-07 posture) per-spec survey diffing each `docs/spec/stdlib/*.md` against its landed crate, classifying each **ratification-ready / divergent / not-yet-implemented** with grounded citations; **surfaces recommendations** for a maintainer ratification call — moves **no** spec to Accepted (VR-5). Flags the live divergences (fmt/io `from_json` tag framing; dense/math/vsa provisional `Proven` rows unverified; swap §3 surface-reconcile) — the actionable divergences were then **grounded + closed** (M-377): dense Q1 finalized (elementwise `Proven`, accumulation `Empirical`), fmt/io kept scope-distinct, swap §3 pinned, math/vsa verified honest; per-spec *ratification* stays the maintainer's call | **Resolved** (2026-06-19; M-374 + M-377) |
| **DN-17 — Codebase Housekeeping & DRY Survey** (`docs/notes/DN-17-Codebase-Housekeeping-DRY-Survey.md`) | a planning capture (advisory, DN-08 posture) — a grounded read-only DRY/duplication survey of the ~43-crate Rust workspace with a priority-ordered, risk-tagged extraction plan for a future **behaviour-preserving** housekeeping wave (M-376). P1 `[workspace.dependencies]` dedup (serde/serde_json/blake3) + xtask path-dep versions; P2 MLIR differential test-helper consolidation; P3 a shared `assert_is_std_error` helper, with the error-**type** unification **deferred** (YAGNI — error shapes still evolve per spec). Marks intentional duplication (M-540 ambient docstrings, per-module guarantee matrices) as **not** targets. Standard Rust conventions now; Mycelium-native conventions deferred (DN-14) | **Draft** (2026-06-19; M-376) |
| **DN-18 — Native Deploy & Spore Schema** (`docs/notes/DN-18-Native-Deploy-Spore-Schema.md`) | the M-620 design note: how a native-compiled `mycelium-mlir::deploy::NativeArtifact` (content-addressed identity = the program's `ContentHash`, ADR-003; carries the dumpable LLVM IR + the M-630 VR-4 attestation) embeds into a deployable Spore (ADR-013). Records the two impl-pending, ADR-level pieces: the `mycelium-spore` ← `mycelium-mlir` workspace dependency (acyclic, §4.1) and the Spore wire-schema `native` component (RFC-0008-class, deferred per ADR-013 §4) | **Draft** (2026-06-20; M-620) — *design-complete, impl-pending* |
| **DN-19 — Road to 1.0.0** (`docs/notes/DN-19-Road-to-1.0.0.md`) | the prioritized, dependency-ordered remaining-gaps roadmap to a kernel/core 1.0.0: 1.0.0 = closing the open ADR-021 Gate A/B rows (GAP-1 B1 RFC-status confirm; GAP-2 A2 Medium-findings ledger; GAP-3 A4 `cargo deny`/`audit` in `just check`; GAP-4 A3 WS8 durability mutants/proptest/fuzz; GAP-5 A5 KC-4 budget; GAP-6 ADR-021 ratification — DONE 2026-06-21). Everything else (surface language, self-hosting M-502, native codegen, JIT, projections, arms 3/5) is post-1.0/1.x. Names which gaps parallelize | **Draft** (2026-06-21; planning capture, DN-08/DN-11 posture) |
| **DN-20 — Tiered Testing & Change-Scoping** (`docs/notes/DN-20-Tiered-Testing-and-Change-Scoping.md`) | the local↔CI test discipline: **three change-scoped, heavy-gated test tiers** — `just test-fast` (Tier 0, pre-commit: change-scoped crates' unit/regression tests only) · `just check` (Tier 1, default + CI: change-scoped crates **+ reverse-deps**, all targets + proptest at LOW cases + the always-on non-test gates) · `just check-full` (Tier 2, release/durability: full workspace + proptest at HIGH cases + cargo-mutants + cargo-fuzz smoke — the M-654/ADR-021 Gate A3 surface) — run through **cargo-nextest** with a `cargo test` parity fallback, plus a `cargo test --doc` pass (nextest skips doctests). Change-scoping (`scripts/checks/changed-crates.sh`) maps the diff to crates via `cargo metadata` + their reverse-dep closure, **conservatively widening to `--workspace`** on any shared/root-file change or detection failure (over-test, never under-test). **Honesty guardrail (VR-5):** property/bound tests are never dropped — only their *case count* is tiered (low every commit, high on release); coverage is focused, never removed; KC-3 small-kernel + local↔CI parity preserved (the change is in check tooling, not the kernel). Fixes the `mycelium-numerics` proptest `PROPTEST_CASES` hardcode | **Accepted** (2026-06-21; maintainer-ratified) |
| **DN-21 — Unsafe-Code Hardening Survey** (`docs/notes/DN-21-Unsafe-Code-Hardening-Survey.md`) | a planning capture (advisory, DN-17 posture) — a grounded read-only audit of all 6 workspace `unsafe` blocks (every one dynamic-linking FFI in `crates/mycelium-mlir`: `jit.rs` dlopen/dlsym/dlclose/transmute-call, `bitnet.rs`, `specialize.rs`), confirming each carries an adequate ADR-014 `// SAFETY:` justification (3 thin, none wrong). Identifies the `BitnetDotKernel` `*mut c_void` co-location dangling-ptr risk as the top hardening target; scopes a 5-issue behaviour-preserving, zero-new-dependency epic (M-678 → M-679 SAFETY-comment+assert hardening · M-680 forbid-pin the trusted base + 11 zero-unsafe mlir submodules · M-681 `just safety-check` adjacency gate · M-682 in-house `Sym<'lib,T>` lifetime-binding newtype · M-683 document the audit_wild/.rs scope split) enacting ADR-014's named follow-ons. §7 names the irreducible unsafe floor (calling JIT'd fn-ptrs / dlopen ctor / ABI claim) — do NOT rewrite the SIMD in Rust intrinsics | **Draft** (2026-06-22; planning capture, DN-17 posture) |
| **DN-22 — Compact Diagnostic Code Representation** (`docs/notes/DN-22-Compact-Diagnostic-Code-Representation.md`) | a design capture (advisory, DN-17 posture) for a **compact diagnostic code** — a packed status **byte** (`(component<<3)\|reason`; `echo $?` decodes) plus a variable-length **base-36 string** (`F<c><r>`, e.g. `Fb2`/`Fn2`) — as a **lossless, `EXPLAIN`-able projection** (G11) of an RFC-0013 reified diagnostic, optimized for token-efficient consumption by the paired AI dev assistant (NFR-2/SC-5b/M-330) at high granularity. Governed by DN-04's rule (diagnostics additive over explicit errors, never substitutive — G2): the byte is a coarse glance over an unbounded b36 handle; a **registry is the single source of truth** with codes *derived* (not hand-assigned), `EXPLAIN`-decodable (rule #2), **append-only versioned** (ADR-018, like the guarantee tags), severity-encoded. Reference implementation already landed in `scripts/checks/all.sh`. 6 open questions (byte layout, b36 grammar, registry home, RFC-0013 derivation, payload tier, KC-3 boundary) | **Draft** (2026-06-22; design capture, DN-17 posture) |
| **DN-23 — Operator Syntax** (`docs/notes/DN-23-Operator-Syntax.md`) | advisory (DN-17 posture) — records the design space for basic-mathematics / operator syntax: **words** (status quo — `add`/`xor`/`neg`; a word operator is a named fn, first-class once RFC-0024 lands) vs **symbols** (buys precedence/associativity + the `-` disambiguation). Recommendation: keep words canonical in the kernel; if symbols are wanted, add them as pure surface **sugar** that desugars to word functions (frontend-only, no kernel change — KC-3), its own RFC-0025. Grounded: `-`/`->`/`=>` are distinct atomic tokens; identifiers are snake_case (`is_ident_continue`), nodule names dotted — so a `-` operator forces no casing change (kebab-case is git-branch-only). Deferred leg E7-5 (after E7-3/E7-4, before `dfb`) | **Draft** (2026-06-23; advisory, DN-17 posture) |
| **DN-24 — Editor Syntax Highlighting & Grammar Distribution** (`docs/notes/DN-24-Editor-Syntax-Highlighting.md`) | advisory (DN-17 posture) — records the design space for **editor syntax highlighting** (coloring `.myc`, which renders as plain text everywhere today) and the recommended **layered** stack: a **TextMate** grammar baseline (VS Code/Sublime/Zed/IntelliJ + GitHub Linguist) + a **tree-sitter** grammar (Neovim/Helix/Emacs + GitHub) + **LSP semantic tokens** in `mycelium-lsp` (type-aware overlay; no provider today) + packaging (reference VS Code extension + Linguist registration). Single source of truth (honesty): grammar token classes are **generated** from the lexer `keyword()` (`token.rs`) + drift-checked — a projection of the real lexer, never hand-drifted (G2); KC-3 untouched (highlighting reads tokens, changes no semantics). Binding format/scope-name decision deferred to **RFC-0026** (now **Accepted** — this note's recommendation adopted). Leg **E9-1** (Phase 8; parallel-safe with `dfb`) | **Resolved** (2026-06-23; decided by RFC-0026 Accepted) |
| **DN-25 — Road to Full-Language 1.0.0** (`docs/notes/DN-25-Road-to-Full-Language-1.0.0.md`) | the operational program map for ADR-022: every gap → track (T1–T9) → epic (E10-1…E18-1) → child issues (M-700…M-743) → design stub → kickoff, with the dependency graph + sequencing (core gate ∥ surface first; stdlib-in-Mycelium the heart; self-hosting the capstone) and the parallelizable heads (`c10`/`s10`/`r10`/`lib10`/`ffi10`/`aot10`/`tool10`/`rel10`/`boot10`). The route; ADR-022 is the gate | **Draft** (2026-06-23; planning capture, DN-17 posture) |
| **DN-26 — Self-Hosting Bootstrap Plan** (`docs/notes/DN-26-Self-Hosting-Bootstrap-Plan.md`) | (Draft stub, feeds E18-1/T9) the staged plan to port the toolchain (`crates/mycelium-l1/src/`) + beyond-core libs to Mycelium (`.myc`) and meet the DN-14 self-hosting gate — tentative order stdlib → lexer → parser → checker → elaborator/mono, each differential-checked against the Rust host; the capstone criterion of full-language 1.0.0 | **Draft** (2026-06-23; planning stub) |
| **DN-27 — Post-1.0.0 Repository Decomposition & Public MIT Release** (`docs/notes/DN-27-Post-1.0.0-Repository-Decomposition.md`) | (Draft planning capture; ADR-022 §10) the post-`lang 1.0.0` plan — decompose the monorepo into **component repos** + **phylum re-export repos** (a friendly interface over phyla/nodules) and **flip to public, MIT-licensed repos** (a corpus for devs/users/models). Gated on the 1.0.0 gate closing; binding ADR drafted when 1.0.0 nears; grounded in ADR-003 (content-addressed identity) + DN-06 (phylum/nodule model) | **Draft** (2026-06-23; planning capture, DN-17 posture) |
| **DN-28 — Registry Architecture & Reconstruction-Based Distribution** (`docs/notes/DN-28-Registry-Architecture-and-Reconstruction-Distribution.md`) | (Draft planning capture; M-732/E16-1) the registry direction — store a phylum's **content-hash DAG** (the dense, verifiable map), fetch the **source bytes from a git forge / object store**, and **verify against the hashes** on reconstruct (cheap registry, more consumer compute). v0 backend = GitHub artifacts/Releases; honest correction that a hash *addresses+verifies* content stored elsewhere, it is not self-reconstructing; secrecy/obfuscation + source-to-publish policy = explicit future research, not MVP. Grounded in ADR-003/ADR-013; evolves the M-732 seam | **Draft** (2026-06-23; planning capture, DN-17 posture) |
| **DN-29 — Tunable Certification & Honesty Modes** (`docs/notes/DN-29-Tunable-Certification-and-Honesty-Modes.md`) | (Draft deliberation anchor — advisory) the maintainer's intended shift: make certification/honesty/hashing **tunable off → full** via scoped config instead of mandatory-everywhere, *while preserving honesty*. Draws the two-axis split (certification **depth** vs cheap never-silent **failure semantics**), a strawman level ladder (L0 Raw…L3 Proven) + the content-hash sub-toggle (spores/hot-inject depend on it), the knob's home (reuse RFC-0012 ambient-scoping + `mycelium-proj` manifest/header `@certification`, ADR-003 → no hash perturbation), and the honesty argument (generalizes **KC-4**; systematic flagged downgrade to `Declared` per **VR-5**; **G2** never-silent about the mode). Enacts nothing; the binding decision **RFC-0034 + ADR-032** is now **drafted (Proposed, 2026-06-24)** — this note stays the deliberation anchor (rev. 4, §9 settled) until that act is Accepted (then DN-29 → Superseded) | **Superseded** (2026-06-24, by RFC-0034 + ADR-032 — Enacted-design; retained as the rationale record) |
| **DN-30 — Security Scanning Toolkit** (`docs/notes/DN-30-Security-Scanning-Toolkit.md`) | (Draft direction capture — advisory) ship a **security scanning toolkit** as a first-class toolchain component inherited by downstream devs (like the testing toolkit): **automated** vulnerability detection over the inspectable Core IR (not solely documented findings; runs in conjunction with them), **standard machine-consumable reporting** (SARIF + CWE + CVE/OSV + VEX, each finding honestly confidence-tagged on the guarantee lattice), **find-once-report-to-community** to **two sinks** — the CLI report **and the registry**, which (DN-28) hosts the findings alongside packages as **screened/anonymized/privatized** entries (vulnerable logic minimized to a content-addressed pattern fingerprint; what / severity / affected versions / logic-retaining mitigations), keyed by the content-hash DAG, and **honest, semantics-preserving safe auto-fix** reusing the **RFC-0002** refinement certificate (proves the fix removes the vuln **and** refines the original modulo it; `Declared` fixes always flagged + human-gated — no black-box rewrites, G2/VR-5). Native + scoped (project/nodule/granular, reusing the RFC-0034 §6 `@certification` resolution). Enacts nothing; feeds a future security-scanning RFC + epic **E22-1** | **Draft** (2026-06-24; direction capture) |
| **DN-31 — Delimiter & Operator Deconfliction** (`docs/notes/DN-31-Delimiter-and-Operator-Deconfliction.md`) | (Draft direction capture — advisory) the decided reallocation of the four bracket families + return arrow + trit literals into one collision-free scheme: **`<>` → comparison/shift operators only** (`< > << >>`; `<=`/`>=` retire to word ops `lte`/`gte`), **`[]` → type args + sized/repr types + list literals** (`List[T]`, `Binary[64]`, `[1,2,3]`; position-disambiguated, clean because indexing is `get()` not `arr[i]`), **`()` → calls/grouping/tuples/ctors**, **`{}` → blocks/effects/match/maps**, return arrow **`->`→`=>`**, trit literals **`<+-0>`→`0t+-0`** (types `trit[N]`/`tryte[9]`, extends to `byte[N]`). Frees the **triple-loaded `<>`** onto the **near-empty `[]`**, **resolving M-745 by reallocation** (no speculative parsing). Proposes to **supersede RFC-0019 §4.1** (Enacted) + update RFC-0030/0025; empty-`{}` **resolved** (`{}`=block, `{:}`=empty map); list-at-statement-start (§4-Q2) still open. Enacts nothing | **Draft** (2026-06-24; direction capture) |
| **DN-32 — Three-Layer Hybrid Memory Architecture** (`docs/notes/DN-32-Three-Layer-Hybrid-Memory-Architecture.md`) | (Accepted-pending-ratification — maintainer strategic direction; resolves RFC-0027 OQ-1 + OQ-4) the runtime memory architecture as a **three-layer hybrid**: (1) **affine/linear ownership PRIMARY** for unique data (move-not-share, zero/near-zero cost, the default path), (2) **optimized reference counting only for EXPLICIT sharing** (static uniqueness analysis removes RC ops; non-atomic intra-hypha, atomic after cross-hypha transfer; `rc==1` in-place FBIP reuse — single-owner only, OCC the concurrent path), (3) **region-based allocation & reclamation within scopes** (region-local alloc, batched scope-exit reclamation). Parent–child reclamation **TOTAL**; sibling reclamation **CONCURRENT by default** (weak/partial coupling — RT7 already concurrent, LR-9 makes it safe), strong/total coupling **opt-in** for high-assurance subsets. Goals: max performance/throughput + memory safety + "stupid easy" ergonomics, preserving LR-8/LR-9/RT7. Roadmap Phases 1–3; refs Perceus PLDI'21 / Smith 2018 / Lorenzen 2021 thesis / Koka/Lean 4 / structured-concurrency lit. **Honest scope (VR-5):** all performance figures are **`Declared` goals**, not measured; Layer-2 static uniqueness analysis is the hardest leg + a real **KC-3 tension** (watch+measure); OQ-1 resolved **by argument** (safety `Proven`-modulo-LR-9), the weak-over-strong throughput benefit **`Declared`** (not measured); names the **open sub-question** — Layer-2 "atomic RC after cross-hypha transfer" vs RFC-0027 §7.3 "no cross-hypha refcount" (shared value crosses → atomic RC, vs sole ownership → affine move). Enacts nothing; **status move to full `Accepted` requires maintainer ratification** | **Accepted-pending-ratification** (2026-06-25; maintainer strategic direction) |
| **DN-33 — Layer-1 Static Uniqueness Analysis (MEM-4) & Cross-Hypha Reconciliation** (`docs/notes/DN-33-Layer1-Static-Uniqueness-Analysis.md`) | (Draft direction capture — advisory; research-backed; feeds DN-32 §2.2/§7 + RFC-0027 §12) the design direction for the deferred **MEM-4** leg: a **static uniqueness analysis** as an **additive, semantics-preserving compiler lowering pass** that elides provably-redundant RC ops, with the runtime `RcCell` probe (MEM-2) as the **sound fallback** — so MEM-4 is **sound-but-may-be-incomplete** (a bug costs throughput, never safety). LR-8 immutability + LR-9 acyclicity + content-addressing shrink the problem below Rust-style borrow checking (no mutation-alias tracking, no cycle detector). KC-3 kept by a **lowering-pass-not-type-checker** architecture + additive-only + watch+measure (DN-32 §6b). Incremental decomposition: (1) **non-escaping borrow elision** [smallest; testable as a refcount-invariant static check] → (2) `rc==1` reuse annotation → (3) full **FIP** static guarantee [Phase 3, deferred]. **Cross-hypha sub-question recommendation:** **Option A** (sole-move-only / affine-channel boundary — Pony-iso / Rust-`Box`; `RcCell` stays `!Send`) for **R1**, **Option B** (shared-crosses-atomic-RC — `Arc` / Pony-`val`) deferred to **R2** (`xloc`/`mesh`). All Mycelium-specific claims **`Declared`**; external prior art (Perceus PLDI'21 / Lorenzen ICFP'22+thesis / Koka FP² / ASAP / Pony / Rust / Verona) **`Empirical`**; cross-hypha recommendation is an argument, its ergonomic cost the named open risk. **Ratified Draft → Accepted (2026-06-25, §8.1):** Q1 → Option A (sole-move cross-hypha; `RcCell` stays `!Send`; B → R2), Q2 → separate RC-annotated IR (Core IR stays pristine — KC-3), Q3 → differential + structural-invariant soundness (`Empirical`). Design ratified; enacts no code — the MEM-4 build epic implements it | **Accepted** (2026-06-25; ratified) |
| **Research Record 16/17 — Env-machine reclamation (DN-35 evidence base)** | the ground truth for threading *actual* Mycelium-level reclamation into the AOT env-machine (the deferred big step past the §9 audit trail): **16** = internal repo dossier (the exact `eval_machine` seam in `crates/mycelium-mlir/src/aot.rs`, the `RcCell` probe, the reference RC-evaluator as correctness oracle, the open design decisions); **17** = external prior-art survey (Perceus/Frame-Limited-Reuse, Counting Immutable Beans, FP² FIP, Lean 4 RC, RC⊕region coupling, the content-address-vs-in-place-reuse obligation), adversarially verified, cited. Internal claims `Empirical`/`Declared`; external mechanisms `Empirical` (primary-source-checked) | Recorded → `research/16-env-machine-reclamation-internal-RECORD.md`, `research/17-env-machine-reclamation-prior-art-RECORD.md` (back **DN-35**) |
| **DN-35 — Env-Machine Reclamation (the Deferred Big Step past the §9 Audit Trail)** (`docs/notes/DN-35-Env-Machine-Reclamation.md`) | (Draft, advisory — research-backed by `research/16`+`17`) how *actual* Mycelium-level reclamation threads into the AOT env-machine (`eval_machine`, `crates/mycelium-mlir/src/aot.rs`), the big step deferred past the **§9 audit-trail bridge** (`rc_plan.rs`, today audit-only — the env-machine lets Rust manage values). Principle: **static decisions, dynamic verification** — the compiler statically plans drop-guided / **Frame-Limited reuse** (Lorenzen–Leijen) and inserts reclamation points; the runtime's **`is-unique` gate** (the `RcCell` probe, MEM-2) is the **sound safety valve** that makes a wrong static guess cost throughput, never safety. Records (1) the **in-place-reuse-vs-content-address obligation** — reuse a cell in place only at `rc==1`; a **weak intern table** + **evict-or-copy** keeps content-addressing (ADR-003) sound when a reused cell was interned; (2) **RC⊕region exactly-once** (Gay–Aiken) — region batch-reclaim and RC drop must not double-free; (3) the **`fast`/`certified` split** (ADR-032/RFC-0034) — `fast` plans reuse and trusts the static plan + the `is-unique` valve; `certified` additionally emits per-reclamation audit records (the §9 trail) and can run the reference RC-evaluator (`eval.rs`) as oracle. Open-question gating set carried into the build epic (Q4 reuse-granularity / Q6 intern-eviction-policy / Q2 region-RC-ordering). Internal claims `Empirical`/`Declared`; external mechanisms `Empirical`/`Proven`-in-literature (primary-source-checked). Enacts nothing | **Accepted** (2026-06-26; **ratified** — §10 design direction; enacts no code, no guarantee upgraded; §5/§6 obligations stay `Empirical`/`Declared` pending prototype; build = E12 Inc 3 / task #6) |
| **DN-34 — Rust→Mycelium Transpiler Strategy (Self-Hosting Acceleration)** (`docs/notes/DN-34-Rust-to-Mycelium-Transpiler-Strategy.md`) | (Draft strategy capture — advisory; feeds DN-26 / DN-27 / RFC-0028 / M-502) how a **Rust→Mycelium transpiler** accelerates the Mycelium self-hosting rewrite (the stdlib-in-Mycelium long pole): mechanically transpile the project's Rust crates to Mycelium surface, **flagging** (never guessing) the hard residue for manual refinement. Seeded from the maintainer's **py2rust** (AST-walk transpilation + a never-silent `CompatibilityAnalyzer`) and **py-rust-bridge** (Python↔Rust FFI/SFI bridge) projects; retargets `syn` (Rust AST) → Mycelium. Rust ownership/borrow facts come from a **rustc/rust-analyzer front-end** (authoritative = rustc MIR `mir_borrowck`; `syn` is syntax-only) — **MEM-4 (`mycelium-mir-passes`) is *not* that analyzer**, it is a downstream RC-insertion/elision **optimizer over Mycelium Core IR** (the Rust→affine mapping is the transpiler's own job; corrected 2026-06-25 audit). Construct-mapping sketch (fn/struct/enum/match/borrows/Result/traits/`unsafe`→`wild`); **flag-don't-guess** analyzer as the load-bearing G2 principle; phasing = isolated branch → incremental interop-bridged transpile-then-refine → differential verify → DN-27 component-repo decomposition. **Gated** on the Mycelium surface being a viable target (post-core-1.0); enacts nothing, ships no code. All effort/coverage claims `Declared`; seed-architecture `Empirical`, transfer `Declared` | **Draft** (2026-06-25; strategy capture) |
| **Research Record 18/19 — Rust→Mycelium transpiler (DN-34 evidence base)** | the ground truth for the DN-34 transpiler strategy: **18** = internal dossier (the two maintainer seed projects [py2rust AST-walk + `CompatibilityAnalyzer`; py-rust-bridge FFI] with reuse verdicts, Mycelium-as-transpile-target readiness [grammar ~70–85% ready; gaps are semantic, not syntactic], the Rust→Mycelium construct-mapping table, simplest-first crate corpus); **19** = external prior-art survey (front-end choice — `syn` syntax-only vs rust-analyzer HIR vs rustc MIR `mir_borrowck` as the ownership oracle; c2rust/SACTOR preserve-first architecture; never-silent residue-reporting with an `Applicability`-style confidence lattice; Rust-ownership→affine mapping; the Laertes ~11% automated-safe-translation ceiling as an honest caution; DDC bootstrap trust), adversarially verified, cited. Internal `Declared`; external `Empirical`/`Declared` (second-hand figures flagged) | Recorded → `research/18-rust-to-mycelium-transpiler-internal-RECORD.md`, `research/19-rust-to-mycelium-transpiler-prior-art-RECORD.md` |
| **Research Record 20 — Corpus-alignment audit (all 92 DNs/ADRs/RFCs)** | an exhaustive read-only doc↔code alignment audit of the entire design corpus (34 DN + 23 ADR + 35 RFC), one auditor per doc, cross-checking every status + normative claim against the landed tree. Result: **0 Critical, 8 High, 7 Medium, 37 Low**; verdicts **76 honest / 6 internally-inconsistent / 6 stale / 3 over-claims**; **no VR-5 guarantee-tag violation anywhere** (every drift is status/fact/locator staleness, not a strength over-claim). Headlines: ADR-018 over-claims a false "all crates `0.0.0`/`publish=false`" premise (3 crates at `0.1.0` — flagged for a maintainer reconcile), ADR-020 jumped to Enacted without the append-only Accepted/Enacted entries, DN-23 "no symbolic operators" is false (lexer tokenizes `+ - * / %`), DN-16/17/19 stale planning surveys, the 2026-06-24 ADR-029/030/031 in-body `## Status: Proposed` hygiene cluster. Includes a per-status roster (every doc once) + a consolidated open-question ledger (~9 near-term-gating, concentrated in the core/full-language 1.0.0 gate). Code/lexicon treated as ground truth; recommendations are append-only-safe (recommend-only) | Recorded → `research/20-corpus-alignment-audit-RECORD.md` |
| **Research Record 21/22 — Safe & high-performance iteration (DN-36 evidence base)** | the ground truth for iteration in the value-semantics model: **21** = internal repo dossier (the `for`→`Fix` fold sugar; `while`/`loop` removed-by-construction with a teaching diagnostic; the trampoline + three never-silent budgets = safety built `Exact`; the narrow native `lower_tail_fix` phi-loop [scalar `Binary{8}` only]; MEM-4's `Fix`/`FixGroup` refusal = the FBIP gap); **22** = external prior-art survey (TRMC [Leijen–Lorenzen] = constant stack, Perceus/FBIP reuse = constant heap → within 10% of C++ `std::map`; Roc/Koka/Lean/Futhark/Clojure surface forms; region-per-iteration; bounded never-silent non-termination; `tailcc`/`musttail` native codegen), adversarially verified, cited. Internal `Exact`/`Declared`; external mechanisms `Proven`-in-literature / `Empirical` (primary-source-checked) | Recorded → `research/21-safe-iteration-internal-RECORD.md`, `research/22-safe-iteration-prior-art-RECORD.md` (back **DN-36**) |
| **DN-36 — Safe & High-Performance Iteration in the Value-Semantics Model** (`docs/notes/DN-36-Safe-and-High-Performance-Iteration.md`) | (Draft, advisory — research-backed by `research/21`+`22`) how iteration stays **functional + safe + high-performance** without a mutable loop variable: the tension dissolves by keeping value-semantics in the *surface* and sourcing mutation from the *lowering* (TRMC + Perceus reuse — Koka's lesson). Records (1) **current state** — safe iteration is *built* (`Exact`): bounded `for`→`Fix` fold, O(1)-host-stack trampoline, three never-silent budgets, `while`/`loop` removed-by-construction; (2) the **two-tier surface** (the maintainer's "both") — a bounded total-by-construction Tier-1 idiom (`fold`/`map`/`for n`/`for x in xs`, termination `Proven`) + a sugared budget-gated Tier-2 open `loop`/`while` (catchable `BudgetExhausted`, `Declared`), both desugaring to one tail-recursive `Fix`; `recur`-checked tail position; folds in DN-31's grammar input (line-breaks/indentation, `,` delimiter); (3) the **high-perf roadmap** making the recursion-aware FBIP increment (E12 Increment 3 / task #6) load-bearing — RC across `Fix`, recursive verifying evaluator, value-affecting reuse, native structural accumulators, region-per-iteration, interpreter TRMC parity, benchmarks; (4) never-silent perf (`EXPLAIN` per-loop reuse status). Safety `Exact`; perf mechanism `Proven`-in-literature / `Empirical`-`Declared`-for-Mycelium; surface `Declared`. Enacts nothing | **Accepted** (2026-06-25; **ratified** — two-tier surface + §6 roadmap *direction*; enacts no code, upgrades no guarantee) |
| **Research Record 23/24 — Object & behavior model (DN-37 evidence base)** | the ground truth for how "objects"/OOP render in the value-semantics model: **23** = internal repo dossier (data via one keyword `type`; traits+impls+coherence+monomorphization = built `Exact`; super-traits/default-methods/delegation/dynamic-dispatch = deferred/greenfield; `FieldSpec` is `Repr\|Data` only — no function field; the complete free-vs-taken sigil table); **24** = external prior-art survey (Cook objects-vs-ADTs + the expression problem; typeclasses-as-interfaces; the inheritance-emulation menu — default methods/super-traits/Kotlin-`by`-delegation/decorator/row-poly/record-of-closures; Swift existentials; mono-vs-dynamic dispatch), adversarially verified, cited. Internal `Exact`/`Declared`; external mechanisms `Empirical`/`Proven`-at-source | Recorded → `research/23-object-behavior-model-internal-RECORD.md`, `research/24-object-behavior-model-prior-art-RECORD.md` (back **DN-37**) |
| **DN-37 — Object & Behavior Model (and the Sigil Category Scheme)** (`docs/notes/DN-37-Object-and-Behavior-Model.md`) | (Draft, advisory — research-backed by `research/23`+`24`) how objects render + how to emulate ergonomic inheritance, composition-aligned, in a value-semantics + trait language. Frames via **Cook's duality** — Mycelium sits on the **ADT / static-dispatch horn** by construction (strong at adding *operations*/binary methods; constrained at *open representations*). Records (1) **built `Exact`**: data (`type` record/sum, `Construct`/`Match`, content-addressed structural equality), traits+`impl`+bounded-generics+**coherence** via **monomorphization**, nodule `pub`; (2) **deferred/greenfield** (the ergonomic-inheritance layer is mostly future work) — super-traits (designed RFC-0019 §4.3, unparsed), default bodies, delegation, decorator, dynamic dispatch / record-of-closures (`FieldSpec` `Repr\|Data` only); (3) **content-addressing forces coherence** (ADR-003) — firmer than Haskell; (4) the **ranked emulation menu** (default methods → super-traits → `~>`-delegation → `@`-decorator → embedding → row-poly → record-of-closures escape hatch, whose `fix`-over-`self` *is* the cyclic back-edge LR-9 excludes), with the never-silent caveat (forwarding-without-late-binding is honest; avoid Rust's silent `Deref`); (5) the **settled sigil-category scheme** (`@` wrap · `#` identity · `$` splice · `?` fallibility · `~` approx / `~>` delegate · `!` effects · `&` conjoin [not borrow] · `` ` `` reserved), verified non-conflicting; (6) bespoke object systems live at the surface (L2/L3)+traits+derives, not the kernel (KC-3); (7) perf — monomorphization default, Swift-existential for future dynamic dispatch. Foundation `Exact`; menu+sigils+sugar `Declared`. Enacts nothing | **Accepted** (2026-06-25; **ratified** — framing + menu + sigil scheme *direction*; delegation keyword → **`via`** [DN-38 §8.1]; §8 build-order/`class`/dynamic-dispatch questions remain open) |
| **Research Record 25 — Layered lowering & generative-sugar prior art (DN-38 evidence base)** | external prior art (two passes): pit-of-success (Mariani/Abrams), progressive disclosure (Nielsen), make-illegal-states-unrepresentable (Minsky); Racket "languages as libraries" + the **language tower**; **nanopass** (Sarkar/Dybvig — many tiny verified passes, Chez 50+) + CompCert per-pass simulation; inspectable desugaring (cargo-expand caveat / Racket Macro Stepper / Lean `pp.all`); generative lowering good-vs-Lombok; Hylo/Val value-semantic delegation; Unison/Nix content-addressed generation (dedup-as-singleton); translation-validation. Cited, adversarially verified | Recorded → `research/25-layered-lowering-and-generative-sugar-prior-art-RECORD.md` (backs **DN-38**) |
| **DN-38 — The Layered-Lowering Atlas & Generative Sugar (one seamless gradient to L0)** (`docs/notes/DN-38-Layered-Lowering-Atlas.md`) | (Draft, advisory — research-backed by `research/25`) the **unifying** note the feature DNs hang off. **Thesis:** Mycelium is **one seamless language** — the L0–L3 "levels" are how the *compiler lowers*, not modes the *programmer declares*; a program freely intermixes high-sugar and low-explicit forms because they are the same program at different points on a **desugaring gradient** (RFC-0006 §3; Racket "languages as libraries" taken further — the low level is the *same* language less-sugared). The batteries-included feature **superset** (macros/traits/super-traits/delegation/generative-lowering/iteration+object sugar) **abstracts L0 away — abstracted, never hidden** (always `reveal`-able). **The lowering law:** every feature lowers to L0 with the same observable meaning; each pass small, IL-grammar-checked, semantics-preserving; kernel never grows (KC-3) — the "seamless gradient" and the "verified tower" (nanopass/CompCert) are one property from two seats (RFC-0012 observational-identity + NFR-7). **Honest refinement:** levels invisible, but `wild`/`!{io}`/`@matured`/guarantee-tags stay explicit + **level-independent** (surface *what the code does*). **Generative lowering:** terse-params → explicit, inspectable, **content-addressed** L0 artifact (`derive`; Lombok-magic structurally impossible — only output is an L0 term; same-intent→same-identity free via ADR-003; Dynel/RFC-0013 errors woven in). **Inspectable desugaring:** `reveal` shows the *real* L0 term (not a lossy text render), layer-hideable, `delaborate∘lower=id` round-trip gated by `certified` (ADR-032). Includes the **per-feature Lowering Map** (living checklist: iteration/objects/sigils/traits/effects/ambient/maturation/value-model/generative → surface→layer→lowering→L0→status). Layering+observational-identity+built lowerings `Exact`; framing+construct+naming `Declared`. Enacts nothing. **§8.1 naming settled** (delegation `via` · generative `derive` · inspector `reveal`) | **Accepted** (2026-06-26; **ratified** — thesis + lowering law + construct set + naming *direction*; §8 architectural questions [nanopass-vs-`elab`, `reveal` sequencing] remain open) |
| **DN-39 — Kernel-Promotion Review (KC-3 trusted-core audit)** (`docs/notes/DN-39-Kernel-Promotion-Review-KC3.md`) | (Draft, advisory — maintainer-commissioned KC-3 review) should any non-kernel functionality be promoted into the trusted core? **Recommendation: NO — zero promotions; the kernel boundary stays UNCHANGED** (L0 Core IR + reference interpreter + content-addressing primitive + guarantee lattice + swap engine; mlir/mir-passes validated-not-trusted; `mycelium-spore` above the kernel). The four-clause default-DENY bar held **on merit**; the one candidate (spore `content_address`) is a decisive **KEEP-OUT** (fails clause 2 *verifiable ⇒ verify-not-trust* + clause 3). Records the **security finding** it surfaced — a `v0` content-address injectivity flaw / supply-chain substitution vector, now **`Proven`-and-FIXED** (PR #617: `v1` length-prefixed single canonical encoder + adversarial witness) — and the generalizable principle: *a deterministic encoding is the most testable artifact, so the last thing to axiomatize into the kernel* (VR-5 applied to the trust boundary). Reaffirms L0/interpreter-**trusted** vs deploy-encodings-**verified** (NFR-7). Per-verdict tags `Proven`/`Declared` held at basis; enacts nothing | **Accepted** (2026-06-26; **ratified** — no promotions, kernel boundary UNCHANGED; bar + principle adopted; enacts no code) |
| **DN-40 — Input-Validation Architecture (only-intended-inputs across the stack)** (`docs/notes/DN-40-Input-Validation-Architecture.md`) | (Draft, advisory — maintainer-commissioned input-validation review) answers both halves of the direction: **(a) WHERE** validation is needed — a ranked, file:line-grounded **gap ledger** over 5 boundaries; **(b) the ARCHITECTURE** — one closed-grammar **recognizer per boundary** minting an immutable/canonical/bounded **typed value**, which doubles as the **lock-free concurrency fan-out** (validate-once-then-trust ⇒ no TOCTOU, data-parallel at memory bandwidth). Patterned on the spore `content_address` `v1` length-prefix fix (DN-39 §5) as the reference. **Three `Proven` security gaps lead the ledger:** A1 **CRITICAL** type-subgrammar parser stack-overflow DoS (`mycelium-l1/src/parse.rs:685-771` — no `MAX_EXPR_DEPTH` charge → uncatchable `SIGABRT` on attacker `.myc`); A2 **HIGH** pattern-subgrammar DoS (`parse.rs:1125-1146`); A3 **HIGH** parse-don't-validate gap on the identity-bearing **dependency hash** (free-text `String`; the existing `ContentHash::parse` smart constructor unused). Six principles (parse-don't-validate · canonicalization · bounds · allowlist · never-silent G2 · illegal-states-unrepresentable). Exhibited gaps `Proven`; architecture `Declared`; prior-art (Parse-don't-validate / LANGSEC / simdjson) `Empirical`/`Proven`-at-source. Enacts nothing — records gaps + recommends fixes (each a separate forward decision) | **Accepted** (2026-06-26; **ratified** — six principles + ledger prioritization + recognizer-per-boundary architecture adopted; fixes A1/A2/A3-first **authorized** via swarms; enacts no code) |
| **DN-41 — Width-Cast Prim (`bit.width_cast`)** (`docs/notes/DN-41-Width-Cast-Prim.md`) | (**Proposed** 2026-06-26 — Rust-first impl landed waveN2-p1) the never-silent `Binary` width-cast kernel prim wave-n1 flagged as missing: re-width unsigned `Binary{N}`→`Binary{M}` so a `Binary{8}` index compares against a `Binary{32}` length. Widen = zero-extension `Exact` (ADR-028 sign-free); narrow = keep low `M` bits iff dropped high bits are zero, else never-silent `Overflow` (G2). Surface `width_cast(v, into)` / kernel `bit.width_cast`; task M-798. Unblocks M-717 multi-byte UTF-8 + `byte_at`. 12 three-way tests; code Rust-first, spec **Proposed**. | **Accepted** (2026-06-26; **ratified** — width-cast prim sanctioned; impl Rust-first) |
| **DN-42 — Width-Generics (M-753)** (`docs/notes/DN-42-Width-Generics.md`) | (**Proposed** 2026-06-26 — design only, enacts no code) const-generic-over-representation-width functions (`fn f<N>(x: Binary{N})`). Recommends **Option A** — width as a const-generic param bound at monomorphization (faithful analogue of the type-param machinery; surface feature per RFC-0032 §5 D5; keeps L1≡L0≡AOT closed). Rejects runtime-width-tag (B); Option C (fixed-width allowlist) only a flagged interim. Open for maintainer: surface syntax (Q1) + width-generic-instance coherence (Q5, hardest — v1 likely free-fns only). Unblocks M-718 generic math + generic-key collection lookup; impl gated on ratification (E11-1/s10). | **Accepted** (2026-06-26; **ratified** — Option A, v1 = width-generic free-fns; instance coherence deferred) |
| **DN-43 — Bytes Slice/Concat Surface** (`docs/notes/DN-43-Bytes-Slice-Concat-Surface.md`) | (**Proposed** 2026-06-26) surfaces the **already-landed** kernel prims `bytes.slice`/`bytes.concat` (M-750) to `.myc` as `bytes_slice(b,start,end)`/`bytes_concat(b1,b2)` — the missing surface mapping (`prim_kernel_name`), closing **FLAG-text-3** (a *surfacing* gap, NOT a missing prim — correcting the wave-n1 misdiagnosis). Never-silent out-of-range/inverted slice → refusal (G2). Task M-799; Rust-first impl + 11 three-way tests. | **Proposed** (2026-06-26; maintainer ratifies → Accepted) |
| **DN-44 — Codebase Security Posture** (`docs/notes/DN-44-Codebase-Security-Posture.md`) | (**Proposed** 2026-06-26) a **consolidating reference** for the security posture of Mycelium's *implementation* (Rust kernel + interpreter + toolchain) — ties ADR-014/RFC-0034/RFC-0035/Security-Checks-Contract/DN-21 into one grounded map: the **thesis** (only developer-introduced vulns; nothing inherent to the implementation), the **hardening floor** (trusted base `forbid(unsafe)`; the 8-block confined unsafe surface; never-silent failability), the **gate map**, the **explicit trade-offs** (unsafe-confined-not-forbidden; panic-on-overflow over panic-freedom; bans-warn pre-1.0; fast-default/certified-on-request), the **env supply-chain caveat** (git-proxy `deny` artifact → OSV.dev primary), and the **hardening ratchet** (§6). Lands advisory `cargo-machete` + kernel-hardening clippy (47 trusted-base panic-path sites) into `just scan` (Rust-first). Makes NO new normative decision (cites the Enacted basis). Task M-678. | **Proposed** (2026-06-26; maintainer ratifies → Accepted) |
| **DN-45 — OSV Scanning of Mycelium Programs** (`docs/notes/DN-45-OSV-Scanning-Of-Mycelium-Programs.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F1) records the maintainer's intent to extend `osv-scanner` + the in-env supply-chain posture (DN-44) to scan **actual `.myc` programs** and give accurate security guidance on real Mycelium code. Maintainer-set sequencing: **high-level** scan once the language is fully usable (~1.0); **low-level** once fully self-hosted. Plan-only — all scope/mechanism are open questions. Complements RFC-0035 (native scanning toolkit) + DN-44. Task M-800. | **Accepted** (2026-06-28; single L0 kernel ratified; FixGroup FLAG-B open; → Enacted once FLAG-B resolved) |
| **DN-46 — Honest-Insecurity-Disclosure Operationalization** (`docs/notes/DN-46-Honest-Insecurity-Disclosure-Operationalization.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F2) the follow-on DN-44 §1.1 explicitly names: operationalize the never-silent residual-insecurity discipline into a **standard disclosure block** + a **gate** asserting every `wild`/FFI/intentional-escape surface carries one. Preference order firm — fix-in-language first; disclosure is the fallback. All block/gate/tooling design open. Tasks M-801 + gate M-806. | **Draft** (2026-06-27; capture-only) |
| **DN-47 — Projection in Mycelium** (`docs/notes/DN-47-Projection-In-Mycelium.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F3) frames research into what "projection" is in SE/PL broadly and how it relates to Mycelium — primary open question whether **RFC-0021** (Semantic-Level Projection Framework, Enacted) is the same notion / a subset / adjacent. If absent: want it, and the 2nd/3rd/4th-order effects. If present: provable, more economic/performant/safe? Deliverable (later): research record + a DN; possibly an RFC. Task M-802. | **Draft** (2026-06-27; capture-only) |
| **DN-48 — L4 & the "Reveal" Lowering** (`docs/notes/DN-48-L4-And-Reveal-Lowering.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F4) frames whether an **L4** layer is appropriate, what it would mean, and how layers **reveal** (the maintainer's vernacular for lowering downward) — open question whether **L4 can reveal straight to L0** (all layers lower to L0). Deliverable (later): DN/RFC + research. Referenced by DN-49 (F6). Task M-803. | **Draft** (2026-06-27; capture-only) |
| **DN-49 — Post-Critical Quality Passes** (`docs/notes/DN-49-Post-Critical-Quality-Passes.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F6) records the maintainer's ordered post-1.0 quality passes: **testing refactor first** (parameterized/fixture/chaos; ties M-797 + CLAUDE.md §Test-layout) → **language-level DRY/efficiency** (retaining tunable certification + explicit swap) → **public/private no-black-boxes** resolution → **L3 review + the L4 question** (DN-48). Sequenced after the language is fully usable. Task M-804. | **Draft** (2026-06-27; capture-only) |
| **DN-50 — Parsable-vs-Runnable Gap Analysis** (`docs/notes/DN-50-Parsable-vs-Runnable-Gap-Analysis.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F7) frames a systematic analysis of the **gap between *parsable/checkable* and *runnable/instantiable* code** — the frontier where the parser/checker accepts a construct but it does not yet run three-way (L1≡L0≡AOT) or instantiate. Generalizes the recurring "type-checks but doesn't run" pattern (traits before M-673, `iter` HOFs M-715, width-generics M-753). Open: the "runnable" definition, whether a standing gate is warranted, how to enumerate the frontier (OQ-1..6). Ties M-719 (one slice), the three-way differential harness, the `Residual` mechanism. Task M-807. | **Draft** (2026-06-27; capture-only) |
| **DN-51 — Binary Width-Arithmetic: Promotion / Hybrid-Overflow / Explicit-Narrowing** (`docs/notes/DN-51-Binary-Width-Arithmetic-Promotion-and-Narrowing.md`) | (**Accepted** 2026-06-27, maintainer-ratified in-session; enacts no code) the cross-width `Binary{N}` arithmetic policy: **operand promotion** (auto-widen the narrower to the wider — DN-41 zero-extend, `Exact`/lossless; accuracy-first), **hybrid overflow** (promotion-only default keeps the never-silent `Overflow` refusal; `widening_*` growth-to-fit is an explicit opt-in), **explicit narrowing** in two named forms (DN-41 checked-narrow + new `truncate`), and **per-instance** guarantee tags (resolves the width-generic grading fork). Extends **DN-41**; supersedes the **DN-42 §4** mixed-width *refusal* for arithmetic (follow-on impl). Grounded in ADR-028 (Binary sign-free, *Accepted 2026-07-01 — was Proposed at this note's authoring*) via DN-41; G2/VR-5/KC-3 | **Accepted** (2026-06-27; design — impl is a follow-on E11-1/E13-1 wave) |
| **DN-52 — Parsable-vs-Runnable Census** (`docs/notes/DN-52-Parsable-vs-Runnable-Census.md`) | (lang-design wave; the DN-50 DoD deliverable, M-807) the census classifying ~50 accepted construct categories into *runs three-way* / *explicit-`Residual`* / *silent-gap*, by reading the accept corpus + the `Residual` sites. **KEY: no silent-gaps found** — the never-silent (G2) floor holds across the v0 surface; two rows **Undetermined** (Dense three-way, cross-nodule three-way — not yet in the differential corpus). Recommends the narrow standing gate (accept ⇒ runs OR explicit `Residual`). `Empirical` | **Draft** (2026-06-27; census record) |
| **DN-53 — Object-Composition Surface & Granular Visibility** (`docs/notes/DN-53-Object-Composition-Surface-And-Granular-Visibility.md`) | (lang-design wave; DN-37 Q1/Q4 follow-ons, M-811) the honest non-OOP object-composition sugar — recommended keyword **`object`** (passes the DN-02 three-test gate where `class`/`struct`/`record` fail; one-to-one desugar to `type`+`impl`+`via`, KC-3, `reveal`-able) — and **granular item-level `pub`** (`pub fn`/method/`let`; field-level `pub` deferred until named fields). Gated on the grammar wave (RFC-0037) | **Accepted** (2026-06-27, R5) — implemented (Rust-first), pending ratification (2026-06-28; M-811 in `mycelium-l1` — `object`/`via` active keywords, structural desugar, three-way differential; item-level `pub fn` inside body deferred) |
| **DN-54 — User-Extensible Generative-Lowering Surface** (`docs/notes/DN-54-User-Extensible-Generative-Lowering-Surface.md`) | (lang-design wave; DN-38 extensibility ruling, M-812) the user-extensible "macro" surface — recommended keyword **`lower`** (`macro` fails T-map) — where a user defines a surface→L0 lowering rule whose RHS is explicit typed L0, so **`reveal` works on every user extension by construction** (no opaque step); held to the same §6 verification (differential + hygiene + round-trip) as built-in passes; no kernel node (KC-3). Gated on RFC-0037 + the verification harness | **Accepted** (2026-06-27, R5) — implemented (Rust-first, surface + structural checks); KC-3 + IL-grammar + RHS-elaboration pending — pending ratification (2026-06-28; M-812 in `mycelium-l1` — `lower`/`derive` active keywords + never-silent structural checks; RHS elaboration to L0 + §4.1 IL-grammar check + §6 KC-3 guard deferred to M-812-cont, so `derive` emits no L0 today — an honest residual) |
| **DN-55 — Static Specialization: The Polymorphism Model** (`docs/notes/DN-55-Static-Specialization-The-Polymorphism-Model.md`) | (lang-design wave, M-814; maintainer ratified "static comp") names Mycelium's single polymorphism mechanism **static specialization** — type `[A]` / width `{N}` / bounded `[A: Trait]` all resolve via `mono.rs` to closed monomorphic L0, dictionary-free. **Load-bearing: polymorphism costs ZERO kernel primitives** (pure frontend; `mycelium-core` untouched) — the one exception is dynamic dispatch (ADR-033). Documents the existing M-673/M-753 mechanism (`Empirical`, three-way); Zig-`comptime` analogue; conventional surface terms. Feeds DN-56 | **Proposed** (2026-06-27; design) |
| **DN-56 — Kernel-Completeness & Freeze Criterion** (`docs/notes/DN-56-Kernel-Completeness-And-Freeze-Criterion.md`) | (lang-design wave **capstone**, M-815) the thesis as a concrete gate: a minimal, eventually-**frozen** L0 kernel such that every construct (incl. user pseudo-macros) lowers transparently **by construction**, so a black box is *impossible by design*. States the **completeness criterion** (every accept/reject/invariant enumerated, zero silent gaps, closed lowering surface incl. extensions) and the **freeze gate** (five checkable conditions). Synthesizes DN-52/ADR-033/DN-54/DN-55; **extends DN-39**, complements **RFC-0036**, feeds **ADR-021/022**. Declares no freeze — defines when one is permissible (VR-5/G2) | **Proposed** (2026-06-27; design — capstone) |
| **DN-57 — Delimiter Semantics: `:` ascribe · `,` separate · `;` terminate** (`docs/notes/DN-57-Delimiter-Semantics-Ascribe-Separate-Terminate.md`) | the delimiter role split (`:` ascribes · `,` separates siblings · `;` terminates a component/operation), refining RFC-0037 layout-independence (FLAG-2) so whitespace-free / **streamable** source is legal alongside the indented form. The **mandatory `;` terminator** is implemented (AST-transparent — adds no node; `mycelium-l1` + `mycelium.ebnf`), with `mycfmt --flatten` (M-819) and `myc --stream` (M-820) realising the streamable form. §6 records the Enactment | **Enacted** (2026-06-29; M-818 mandatory terminator + M-819 --flatten + M-820 --stream landed; was Accepted 2026-06-27) |
| **DN-58 — Runtime-Vocabulary Surface Forms (`fuse`/`reclaim`/`tier`)** (`docs/notes/DN-58-Runtime-Vocabulary-Surface-Fuse-Reclaim-Tier.md`) | (ratification group G1; M-667/M-710) the L1 surface forms RFC-0027 §10.5/ADR-020 deferred — `fuse` (lawful binary merge over a declared `Fuse` semilattice trait, meet-on-guarantee, RT6), `reclaim` (a reified reclamation/supervision policy on a structured scope `reclaim(policy){…}` → `std.runtime` supervision, RT7), `tier` (a per-definition execution-mode **attribute** `@tier(compiled\|interpreted)` bridging RFC-0004 `ExecutionMode`, observable-equivalent NFR-7). Runtime machinery already landed (M-709/711/713). Unblocks the `r4v` wave. `Declared` surface | **Enacted** (2026-06-28, prm wave — M-817 landed / M-710 closed; §A `fuse` + §B `reclaim` execute three-way `Empirical`: `fuse_join:binary` meet prim · data-fuse → resolved `Fuse::join` call · `reclaim` sequential ref + `mycelium_mlir::run_reclaim` RT7 supervision driver; honestly deferred: F-A3 non-`Binary` meets, F-B2 policy→bounds. Prior **Accepted** 2026-06-28 maintainer-ratified — F-C1/F-C2/F-C3) |
| **DN-59 — Reclamation Strategy & Cross-Hypha Sharing** (`docs/notes/DN-59-Reclamation-Strategy-And-Cross-Hypha-Sharing.md`) | (ratification group G3, post-DN-32 follow-on) consolidates the deferred memory/reclamation questions for batch ratification: cross-hypha sharing (sole-ownership move, R1), ownership-mode representation (DN-33 §8 Q2 — MEM-4 prerequisite), Layer-2 uniqueness, drop-latency SLO/fuel (the one genuinely-open axis), rc==1 EXPLAIN-only, sweep-coupling, env-machine wiring. Mostly **confirm-the-record** over the Accepted RFC-0027/DN-32/33/35 cluster; routes R2-distributed reclamation to DN-61. `Declared` | **Accepted** (2026-06-28; 7 axes ratified; DN-62 fuel-model note COMMISSIONED) |
| **DN-60 — Effect-System Surface Direction (Phase-2)** (`docs/notes/DN-60-Effect-System-Surface-Direction.md`) | (ratification group G6, direction-capture) records the Phase-2 direction for enriching effects past the v0 declared-set: dynamically-resolved budgets, effect-row polymorphism + minimal-set inference, hypha-in-effect-row (RFC-0008 R8-Q2) in concert with RFC-0018 grading. Decides nothing now (KISS/YAGNI, not a 1.0 gate); the vehicle is a future RFC-0014 revision. `Declared` direction-capture | **Accepted** (2026-06-28; G6 direction ratified; RFC-0014 revision COMMISSIONED) |
| **DN-61 — Concurrency & Distribution Runtime Semantics** (`docs/notes/DN-61-Concurrency-And-Distribution-Runtime-Semantics.md`) | (ratification group G8) Part A (ratifiable): R1 scheduler normativity = RT2-determinism + fuel-cooperative stepping + RT7 no-orphans, parallel as a validated perf path (RFC-0008 R8-Q1). Part B (agenda-only, each needs a research pass): R2 distributed — clocks (R8-Q3), Byzantine (R8-Q4), distributed reclamation (RFC-0027 OQ-2), network-xloc, concurrent fuse-merge. `Declared` | **Part A: Accepted** (2026-06-28; R1 scheduler normativity ratified) · **Part B: Draft** (R2 distributed — open research agenda) |
| **DN-62 — Reclamation Fuel-Model** (`docs/notes/DN-62-Reclamation-Fuel-Model.md`) | (DN-59 follow-on, commissioned 2026-06-28) the research/measurement note for reclamation drop-latency: the fuel-bound question (O(n) deep-tree drop vs. honest bound), a candidate per-epoch fuel model (units: nodes-reclaimed / RC-decrements / region-sweeps; fixed-budget R1 default; defer-with-EXPLAIN on exhaustion), and the measurement plan (P99/P999 + a reclamation-debt/epoch-lag counter) that would move the drop-latency SLO from `Declared` to `Empirical`. Until measured, "no silent pause" is an EXPLAIN-recorded honesty commitment, NOT a latency SLO (VR-5). Anchored on RFC-0027 §11 OQ-3. `Declared` | **Draft** (2026-06-28; research/measurement) |
| **DN-63 — RFC-0008 R2 Distribution Vocabulary Decomposition** (`docs/notes/DN-63-RFC-0008-R2-Distribution-Vocabulary-Decomposition.md`) | (M-668; E7-2 R2 planning) decomposes the six RFC-0008 §4.6 R2 constructs (`xloc`, `mesh`, `cyst`, `graft`, `forage`, `backbone`) into per-construct implementation RFC tracks; per-construct `Declared` typing/elaboration strategies; dependency ordering (`forage`→`backbone`→`mesh`→`graft`→`xloc`→`cyst`); honest guarantee tags (`mesh` probabilistic RT5/T4.2; `xloc` fallible RT4; `cyst` soundness RT2); R2 gated on R1/M-667; 16 construct FLAGS plus 3 orchestrator FLAGS (RFC-number assignment; mesh research lead). Grounded in RFC-0008, DN-61, RFC-0005, RFC-0027/0028/0023. `Declared` | **Draft** (2026-06-29; planning capture, M-668) |
| **Research Record 26 — DN-64 type-system R&D (graded-soundness proof path · bound composition · memory ergonomics · reclamation · per-instantiation grades)** (`research/26-dn64-typesystem-rnd-RECORD.md`) | R&D planning for DN-64 §7 dispositions OQ-A/C/D/E/S (M-827/829/830/831/844): a near-term mechanization path for the RFC-0018 noninterference proof (Lean 4 / Liquid Haskell / Agda), a tractable E2-1 bound-composition rule, three-layer memory "stupid-easy" ergonomics, substrate/hypha reclamation interaction, and per-instantiation guarantee-tag context through monomorphization. All proposals `Declared`; external mechanisms `Empirical` at source | Recorded → `research/26-dn64-typesystem-rnd-RECORD.md` (backs **DN-64 §7**) |
| **Research Record 27 — DN-64 ergonomics R&D (forage/backbone activation + policy capture · guards · short-keyword scope · annotation-burden wrappers · composite aggregation · proposal-time gate · record-literal shadowing)** (`research/27-dn64-ergonomics-rnd-RECORD.md`) | R&D planning for DN-64 §7 dispositions OQ-B/G/H/I/J/R/T (M-828/833/846/834/835/843/845): activate `forage`/`backbone` + mechanized EXPLAIN-able SelectionPolicy capture; guard clauses degrade the arm by the meet rule; short keywords bind type-literals-only; ergonomic wrappers for the stage-1a annotation burden (no silent upgrade, S2); composite-op aggregation = meet; the three-test gate at proposal time; and record-literal `{x,y}` shadowing (default-reject on YAGNI). All proposals `Declared` | Recorded → `research/27-dn64-ergonomics-rnd-RECORD.md` (backs **DN-64 §7**) |
| **Research Record 28 — Starting the Mesh/Xloc/Cyst Research Track (Plan Only, Not the Survey)** (`research/28-mesh-xloc-cyst-distributed-runtime-research-start-RECORD.md`) | (M-913; kickoff `enb`, epic E28-1, Phase-I H1) the **start** of a long-lead research track for the three heaviest RFC-0008 R2 distribution constructs — `mesh`, `xloc`, `cyst`: a prior-art survey plan, problem statement, open-question ledger, and a skeleton for the future design note. Not the survey itself; implements nothing. `Declared` | Recorded → `research/28-mesh-xloc-cyst-distributed-runtime-research-start-RECORD.md` (backs a future R2 mesh/xloc/cyst DN) |
| **Research Record 29 — Recursion Depth & Stack Safety (DN-84/M-979 Phase-1)** (`research/29-recursion-depth-and-stack-safety-RECORD.md`) | (M-979 solve-now; 14-agent workflow + orchestrator confirm/correct) the evidence base for the explicit-work-stack + unified-deterministic-budget RFC. Five decisive facts: (1) the `myc run` engine `mycelium-interp` has **no depth budget at all** (`DepthLimit` defined-but-never-constructed — orchestrator-verified) — the biggest, remotely-reachable hole; (2) a real recursive-`Drop` stack bomb on the **frozen** core types; (3) **no TCO** + non-tail stdlib idiom ⇒ a `.myc` port can't process big worklists without solving depth first; (4) the AOT `Vec<Frame>` env-machine is a differential-validated design-D reference to mirror; (5) prior art (rustc/serde_json/CEK) says convert the *evaluators*, guard the *frontend*. Surfaces a deterministic-vs-AOT-dynamic-budget contradiction (DN-05) + four maintainer plan-decisions + the DN-84 §3 corrections owed. Confidence-tagged; `[V]`=verified | Recorded → `research/29-recursion-depth-and-stack-safety-RECORD.md` (backs **DN-84 / M-979 RFC**) |
| **DN-64 — Mycelium Language Design: Synthesis Exploration Note** (`docs/notes/DN-64-Mycelium-Language-Design-Synthesis-Exploration.md`) | advisory research synthesis (5-facet corpus sweep: surface ergonomics, Mycelium-unique types/constructs, unique application capabilities, the signed/cert-gated hot-inject security model, conventions/idioms). Maps each unique construct to a traditional paradigm and frames it as an extension. **§7 (2026-06-29) — maintainer dispositions on all 20 open questions OQ-A…OQ-T** (OQ-H supplied last): the hot-inject production mode `sealed`→**`inoculated`** ratified as a new RFC-0034-orthogonal axis (→ **RFC-0038**); `forage`/`backbone` to be made active; graded-proof, bound-composition, memory-ergonomics, VSA-`Proven`-bounds (GPU experiment) and more routed to R&D (M-827…M-846, `research/26`+`27`). Proposes nothing normatively — every claim `Declared` | **Draft** (2026-06-29; §7 dispositions appended, append-only) |
| **DN-65 — Scoped-PR Decomposition & Per-PR Toolchain Scoping** (`docs/notes/DN-65-Scoped-PR-Decomposition-And-Toolchain-Scoping.md`) | (workflow policy, maintainer-directed) large work is done at any scale but **lands as logical, closely-scoped PRs** (soft ~1–2k-LOC-delta rule of thumb — cohesion over a line count; a 50k-line wave lands as a fan/sequence of small, individually `/pr-review`'d PRs). **Workspace prep before a unit:** sync off the latest tip + pre-install the change-kind's toolchain (§2.3 map: Rust→`just setup`; Python→`uv sync`; docs→markdownlint/`doc_refs`; proofs→`z3`/LH/Lean). The PR-landing twin of DN-20 (change-scoping) + the swarm file-ownership partition. Distilled into CLAUDE.md/CONTRIBUTING.md + `/dev-workflow`/`/land`/`/kickoff`; scoped-setup automation = M-848. Process decision; enacts no code | **Accepted** (2026-06-29; maintainer-directed) |
| **DN-66 — Stdlib Stable-API Freeze & Rust-Crate Retirement Status** (`docs/notes/DN-66-Stdlib-Stable-API-Freeze-And-Rust-Crate-Retirement-Status.md`) | (M-719 broader-closure note, W1-719 leaf) freezes the current stable public-API baseline for all 26 `mycelium-std-*` crates as a single dated, grounded snapshot (spec status + `GUARANTEE_MATRIX` location + public-surface size per crate) and assesses the RFC-0031 §5 D6 retirement trigger per crate — finding none yet cleared (the 5 same-named `.myc` prototypes are disjoint subsets, not full ports, so no crate is retirement-eligible); adds additive stability doc-comments to all 26 crates' `src/lib.rs`/README, no `#[deprecated]` attributes, no crate removed. Flags the per-op audit (§4b), the `mycelium-std-runtime` load-bearing exception (§4c), and shared-file registration (§4e, closed by this reconciliation) as explicitly deferred. Advisory input to the maintainer's append-only ratification/retirement call (DN-07 posture); not self-ratified to Accepted. `Declared` | **Accepted** (2026-07-01, maintainer-ratified via ADR-035; was Draft) — **§6 currency note appended 2026-07-02** (kickoff `acy`): the §4.c `mlir → std-runtime` load-bearing basis is void post-M-883/M-884 (`mycelium-rt-abi` extraction); §4.c's original text unchanged, append-only |
| **DN-67 — Persistent Work-Stealing Pool** (`docs/notes/DN-67-Persistent-Work-Stealing-Pool.md`) | (M-864) ratifies the `Scheduler::run_indexed` `'static` job-closure contract change (`F: Send + 'static`, `T: Send + 'static`) required to replace the per-call `std::thread::scope` (fresh OS threads every call) with one process-wide, persistent, bounded pool (`mycelium-sched::pool`, sized to `available_parallelism()`); documents the Cilk/TBB/Rayon-style help-stealing nested-join wait loop (`Pool::help_while`) and its structural deadlock-freedom argument for arbitrarily deep nested `run_indexed` submission on a fixed-size pool; audits every current caller (M-860's `emit_llvm_ir_many_with_swap_mode`, M-862's `eval_top_batch`, plus two callers not named in the original issue — M-711's `run_dataflow_scheduled` and M-713's `run_supervised`) and how each was adjusted from a borrow to an owned/`Arc`-shared value. RT2 spawn-order determinism is unchanged throughout. `Empirical` (checked by nested deep-chain/wide-fan-out/mixed-shape stress tests + a Linux thread-count regression witness), never `Proven` (no mechanized proof) | **Draft** (2026-07-01; M-864 leaf) |
| **DN-68 — The Acyclic-Deps Invariant** (`docs/notes/DN-68-Acyclic-Deps-Invariant.md`) | (M-885, kickoff `acy` H0) documents the structural dependency invariant already encoded by `xtask/src/deps/` + `xtask/deps-strata.toml` (M-877/M-878/M-879): the 53-crate workspace's strictly-downward acyclic dependency graph, checked two independent ways — the fine per-crate `[strata]` map (`Empirical`, longest normal-dep chain from a sink) as a forward-looking regression gate, and the coarse `[tiers]` bucket (`Declared`, `core < std < tools`) catching upward-tier anomalies `[strata]` cannot see by itself. Names the two checked cross-boundary rules (`no-interp-std-dep`, `no-upward-tier-edges`), where the gate lives (`cargo run -p xtask -- deps`, Tarjan SCC over normal+dev+build), and the change-procedure (a stratum/tier change is its own reviewed PR — extraction, not loosening the rule; PR #864 precedent). Ratifies nothing new. `Exact`/`Empirical`/`Declared` per-claim (see file header) | **Draft** (2026-07-02; M-885 leaf, `acy/m885-invariant-dn`) — **2026-07-02 currency note appended** (M-971, PR #1015): the Phase-I H1 wave regressed the gate to 12 violations; fixed by structural extraction (no whitelist) back to 0, adding two crates to the graph — `mycelium-std-conformance` (tier `std`, stratum 0) and `mycelium-vsa-decode` (tier `std`) — and re-deriving `mycelium-vsa`'s stratum 2 to 1. The note's own text is unchanged (append-only, house rule #3); its body's 53-crate figure is now superseded by the workspace's current crate count |
| **DN-69 — DN-39 Promotion Dossier: the Scalar-Float Value Form (Route ii)** (`docs/notes/DN-69-Scalar-Float-Kernel-Promotion-Dossier.md`) | (M-895 companion to ADR-040) a review instance of the DN-39 four-clause default-DENY kernel-promotion bar applied to the scalar-float kernel entry. **Recommendation: PROMOTE** — the first candidate to clear all four conjunctive clauses (DN-39's sole prior candidate was a decisive KEEP-OUT); default-DENY overcome on merit, per-clause tags. Enacts no code alone — kernel entry rides the ADR-038 §2.6 double gate (this verdict + ADR-040) and only via M-896…M-900 | **Accepted** (2026-07-02, maintainer-ratified; PROMOTE confirmed alongside ADR-040) |
| **DN-70 — M-828 D-lite Scope Split: the R2 Usability Subset (H1) vs the Maturity Remainder (H2)** (`docs/notes/DN-70-M828-D-lite-Scope-Split.md`) | (M-905; kickoff `enb`) splits M-828's "activate forage/backbone" scope into the D-lite H1 usability subset (D1: `@forage(policy)` placement annotation, landed M-906) versus the H2 maturity remainder (full runtime constructs + mechanized SelectionPolicy capture, routed to the R2 lane / DN-78). §4/FLAG-D3: M-825 landed the backbone **decision**, not an executing construct (M-907). `Declared`/`Empirical` per-claim | **Accepted** (2026-07-02, maintainer sign-off; FLAG-D1 cleared; M-906/M-907 unblocked) |
| **DN-71 — `Substrate`/`consume` Execution Model: Confirm-Memo** (`docs/notes/DN-71-Substrate-Consume-Execution-Model-Confirm-Memo.md`) | (M-901; kickoff `enb` Gap E) confirms **Model S** for the affine `Substrate`/`consume` construct: an interpreter-level opaque affine handle, static use-once checking with a never-silent runtime backstop, identity-move `consume` lowering through existing paths (no new L0 node). Cross-checked against the `grm` M-918 DN-54 dossier (same model, not forked). `Exact` (source-read) + `Declared` (proposal) | **Accepted** (2026-07-02, maintainer sign-off; Model S **executes at L1-eval** — M-902…M-904 landed) |
| **DN-72 — Integer-Prim Surface Naming: the `_u`/`_s` Signedness Convention** (`docs/notes/DN-72-Integer-Prim-Surface-Naming.md`) | (kickoff `enb`) fixes the integer-prim surface naming to an explicit `_u`/`_s` signedness suffix convention (never-silent about signedness), enacted in the same change that authors the note. `Declared` (convention) | **Accepted** (2026-07-02, maintainer-ratified "clean up now"; enacted in-change) |
| **DN-73 — Tuple-Type Decision Ratification** (`docs/notes/DN-73-Tuple-Type-Decision-Ratification.md`) | (M-920; kickoff `grm`) inventories the landed M-822/M-826 tuple-type state, enumerates the residual §4A.8 surface (D1–D5) and options A–D, recommends **Option A** (ratify landed state; reconciliation-only delta). `Empirical`/`Declared` per-claim | **Accepted** (2026-07-02, wave-orchestrator under the maintainer's delegation; Option A) |
| **DN-74 — ADR-033 FLAG-1 `FieldSpec::Fn` Soundness Disposition** (`docs/notes/DN-74-ADR-033-FLAG-1-FieldSpec-Fn-Soundness-Disposition.md`) | (M-922; kickoff `grm`) verifies the landed Path A full-signature encoding + property suite, frames four options, recommends **Option A** (Path A final; primitive IN at `Empirical`; surface lowering + differential ride M-923). Steps no other doc's status. `Empirical`/`Declared` per-claim | **Accepted** (2026-07-02, wave-orchestrator under delegation; Option A — ADR-033 untouched) |
| **DN-75 — DN-54 Completion Audit** (`docs/notes/DN-75-DN-54-Completion-Audit.md`) | (M-917; kickoff `grm`) audits the `lwd`-landed DN-54 M-812-cont completions against the tree (not the changelog): §7 harness re-run green, per-section audit table, one editorial discrepancy (E-1), residual ledger all-routed. Recommends **Option 1** (audit stands; DN-54 remains Accepted). `Empirical` | **Resolved** (2026-07-02, wave-orchestrator under delegation; Option 1) |
| **DN-76 — Kernel-Freeze Four-Condition Scorecard** (`docs/notes/DN-76-Kernel-Freeze-Four-Condition-Scorecard.md`) | (M-958; kickoff `frz` Lane A) scores the DN-56 four freeze conditions at `dev` — 0/4 green with cited `Empirical` evidence — and recommends **Option A** (accept as the M-969 freeze-gate instrument; execute M-969 only on 4/4 green). Freezes nothing | **Accepted** (2026-07-02, wave-orchestrator under delegation, as the M-969 gate instrument; kernel NOT frozen — M-969 stays gated) |
| **DN-77 — Inject-Mode Build-Scope Dossier** (`docs/notes/DN-77-Inject-Mode-Build-Scope-Dossier.md`) | (M-960; kickoff `frz` Lane B) frames the RFC-0038 inject-mode build scope, recommends **Option B** (the Phase-I-buildable coherent slice: mode gating, `InjectCert`→`TrustRoot` verify via a `SignatureScheme` seam, never-silent `UnsignedCode`/`BadSignature`, `inject_mode` on `Resolution`, the `whole` grain + Phase-I manifest). `module`/`call` grains owned by M-847. `Empirical`/`Declared` per-claim | **Accepted** (2026-07-02, wave-orchestrator under delegation; Option B — built by M-961) |
| **DN-78 — frz Lane C: the M-828 R2 Remainder Buildable-vs-Research Split + RFC-0027/DN-32 Memory-Model Confirmation** (`docs/notes/DN-78-R2-Remainder-Split-and-Memory-Model-Confirmation.md`) | (M-962…M-964; kickoff `frz` Lane C) splits the M-828 R2 remainder into buildable-now vs research-track work and confirms the RFC-0027/DN-32 memory model. Decided under the maintainer's delegation of the R2-split/memory-model decisions (`Declared` as relayed). `Empirical`/`Declared` per-claim | **Accepted** (2026-07-02, under delegation) |
| **DN-79 — Guard-Clause Semantics: `when`-Guards and Guarantee Propagation** (`docs/notes/DN-79-Guard-Clause-Semantics-When-Guards-And-Guarantee-Propagation.md`) | (M-968; kickoff `frz` Lane D) options + a clear recommendation for `when`-guard clause semantics and guarantee propagation (M-833 / DN-64 OQ-G). Implements nothing — M-833's RFC-0020 amendment is post-acceptance. `Declared` | **Accepted** (2026-07-02, wave-orchestrator under delegation; §6 recommendation — impl held) |
| **DN-80 — The Reject-Ledger: an Exhaustive, Never-Silent Refusal Inventory** (`docs/notes/DN-80-Reject-Ledger-Exhaustive-Never-Silent-Refusal-Inventory.md`) | (M-959; kickoff `frz` Lane A — DN-56 condition 1) the cross-stratum `{construct, reason, alternative}` ledger of every construct the kernel *rejects* (parse corpus 30 fixtures · check-level 41 construct families / 217 sites · closed `AmbientError`/`EvalError`/`WfError` enums 1:1), with a regression guard (`mycelium-std-conformance/tests/reject_ledger.rs`) that fails never-silently on drift. Re-audited through the grm/frz wave (M-916/919/965/966/973). `Empirical` (mechanical inventory, VR-5) | **Accepted** (2026-07-02) |
| **DN-81 — DN-54 Attachment-Model Decision Dossier** (`docs/notes/DN-81-DN-54-Attachment-Model-Decision-Dossier.md`) | (M-972/M-972b; kickoff `grm`) frames the DN-54 §10 attachment-model decision, weighs Model A (sibling-item injection) vs Model B with grounded tradeoffs + an adversarial stress-test, and recommends **Model A**. `Empirical`/`Declared` per-claim | **Accepted** (2026-07-02, orchestrator under delegation; **Enacted** via M-973 — sibling-injection through the M-919 affine tracker) |
| **DN-82 — Readable-Format Style: a Scoped Canonical for `lib/std`** (`docs/notes/DN-82-Readable-Format-Style-Scoped-Canonical.md`) | (M-974; kickoff `grm`) scopes the new human-readable `mycfmt --readable` multi-line style as the canonical form for `lib/std` only (not a global flip), presentation-only + functionally inert (proven behavior-neutral); the `myc-fmt` gate passes `--readable` for the `lib/std` root. `Empirical`/`Declared` | **Accepted** (2026-07-02) |
| **DN-83 — Surface-Grammar Stability-Window Proposal** (`docs/notes/DN-83-Surface-Grammar-Stability-Window-Proposal.md`) | (M-924; kickoff `grm`) proposes an RFC/ADR-gated stability window for future surface-grammar changes now that the Phase-I grammar is feature-complete (operators, generics `[…]`, effects, guarantee annotations, retired-glyph deconfliction), citing RFC-0037/RFC-0030 + ADR-023, with explicit carve-outs. `Empirical`/`Declared` per-claim | **Proposed** (2026-07-02; maintainer decision pending) |
| **DN-84 — Dynamic Host-Stack Growth + Unified Tunable Recursion-Depth Budget** (`docs/notes/DN-84-Dynamic-Host-Stack-And-Unified-Depth-Budget.md`) | (M-978) design space for crash-proof, essentially-unbounded nesting: (B) wire the already-documented `mycelium-stack` grow-on-demand hybrid (`stacker::maybe_grow`, KC-3/ADR-014 intact) so the host stack is never the binding limit, + a unified **deterministic** tunable depth budget (unifying the scattered 256/64/4096 constants) as the portable, self-hosting primitive. Makes the honest tension explicit (§4: "limitless" bends to G2 + determinism + portability) and grounds realistic depth needs (§5: hand-authored logic is shallow; the real driver is *data-shaped* desugaring depth — RFC-0040 list literals — which should be routed through iteration/grow-on-demand, not a bigger control budget). Motivated by the ADR-041 near-miss. `Empirical`/`Declared` per-claim | **Draft** (2026-07-03; decides nothing normatively — feeds M-978's RFC/ADR) |
| **DN-85 — Multi-Language Transpilation and the Single-Language Full-Stack Goal** (`docs/notes/DN-85-Multi-Language-Transpilation-and-Single-Language-Full-Stack.md`) | (maintainer direction) generalizes DN-34's Rust-only transpiler into a **multi-source-language** program (Rust in flight → Python → C/C++/Fortran/Cython/CUDA as demand arises), flagship goal = a **single-language Mycelium full stack** (collapse a polyglot ecosystem's app + native extensions + kernels into one language/toolchain/guarantee model). Python-first: **pure Python first, gated on sound type inference**, then the extensions for full-library coverage. Interim: transpile the coverable layer + **FFI-bind** the native backend (PyTorch/TensorFlow C++/CUDA) until its transpiler lands. **Open-source constraint** + the provenance ladder (transpile vs. binding vs. reverse-engineered reimpl — a bound/reimpl artifact is *never* tagged a port, G2/VR-5). `Declared`/aspirational; not `1.0.0`-gating (ADR-036); architecture companion **DN-86** | **Proposed** (2026-07-06; maintainer direction — ratifies no mechanism; Resolved when the first Python-pure arm is minted as an RFC/ADR) |
| **DN-86 — Multi-Language Transpiler Front-Ends Architecture (Python · TypeScript · Java)** (`docs/notes/DN-86-Multi-Language-Transpiler-Front-Ends.md`) | the **architecture companion to DN-85** (which holds the strategy/vision; authored concurrently, reconciled to distinct numbers): the front-end-abstraction for ingesting Python (then TS/Java) alongside Rust. In the `mycelium-transpile` split, `vet.rs` + the `.myc` emitter are **source-agnostic** and `gap.rs`'s structure is reused, but its taxonomy carries Rust-source-shaped categories (`Trait`/`Impl`/`MacroDef`/`DeriveAttr`/…) a multi-language front-end must **generalize**; only `transpile.rs`/`emit.rs`/`map.rs` are `syn`-coupled. Transfers the trx2 M-1006 ladder as the method. Per house rule #4 states the boundaries: Python dynamic typing (§4.1), the **C/CUDA library-core boundary** (§4.2 — numpy/scipy/pytorch cores are compiled C/C++/CUDA, *not* Python source; transpilation yields the Python layer only, compute is a separate Mycelium-native/FFI track), follow-on bounded by surface coverage (§4.3). Self-hosted-`.myc` toolchain switch (§5) → `boot10`/DN-26 dep. `Empirical`/`Declared` per-claim | **Draft** (2026-07-06; decides nothing normatively — feeds proposed E35–E38 + RFC/ADR) |
| **DN-87 — mycelium-tero: The Transparent Memory Substrate & Agent Knowledge API** (`docs/notes/DN-87-Transparent-Memory-Substrate-And-Agent-Knowledge-API.md`) | (maintainer vision, captured near-verbatim) convert the corpus (methodologies, decisions, intents, language docs, tracker state) into a **generated, transparent, provenance-carrying encoding** that *supplements* the human-friendly format — search/access **improved upon RAG** (the claim `Empirical`-gated: M-1018's harness must beat/complement the baseline before it is made) — exposed as a **secure, platform-agnostic API** (MCP + HTTP, token-scoped) + an optimized skill set, usable by Claude Code, Grok, any platform. Hybrid substrate: deterministic structured index (the api-/lib-index pattern generalized, the floor) + a **VSA semantic layer** on `mycelium-vsa` (bind/bundle + resonator decode, `EXPLAIN`-able — the ceiling). Rust core + Python ingestion v0; the **Mycelium-lang package** phase-gated (M-1019 ⟂ M-993). Mandatory provenance (an uncited answer is a refusal); drift-gated freshness; MIT-only. Mints **E39-1 + M-1015…M-1019**; kickoff **`mem`** stowed — **the build wave fires when the maintainer names the project** | **Proposed** (2026-07-07; four §2 design forks orchestrator-resolved `Declared`, maintainer-overridable. **NAMED `mycelium-tero` + kickoff `mem` FIRED same day** — homage to Atsushi Tero; Resolved on the §2 confirmations + wave-1 landing) |
| **DN-88 — Component-Repo Decomposition & the Managerial Re-Export Topology** (`docs/notes/DN-88-Component-Repo-Decomposition-And-Managerial-Re-Export-Topology.md`) | (planning capture, DN-17 posture) the post-dogfood repository-decomposition plan: split the monorepo into component repos published as GHCR spores, with a managerial re-export topology so downstream consumers see a stable façade. The decomposition gate is where ADR-042's "zero foreign first-party languages" end-state is asserted. Advisory — decides nothing normatively; feeds follow-on RFC/ADR. `Declared` per-claim | **Proposed** (2026-07-07; planning capture — advisory) |
| **DN-89 — A Native AI/ML Library Ecosystem for Mycelium (Post-Self-Hosting)** (`docs/notes/DN-89-Native-AI-ML-Library-Ecosystem.md`) | (planning capture, DN-17/DN-85/DN-88 posture) the aspirational native AI/ML library ecosystem once self-hosting + the multi-language transpiler program (DN-85/DN-86) mature — a Mycelium-native alternative to the numpy/scipy/pytorch stack, replacing the interim FFI-bound compute track with native `.myc`. Advisory — decides nothing normatively. `Declared`/aspirational | **Proposed** (2026-07-07; planning capture — advisory) |
| **DN-90 — A Unified, Extensible Strategy for Arbitrarily-Large Values/Byte-Sizes Across the Value Model** (`docs/notes/DN-90-Unified-Arbitrary-Large-Values-And-Sizes-Strategy.md`) | (BACKLOG/plan-around) frames a unified, extensible strategy for arbitrarily-large values and byte-sizes across the value model. A design artifact — decides nothing normatively. Advisory, `Declared` throughout | **Draft** (2026-07-08) |
| **DN-91 — The CU-3 Float↔Int Conversion Surface: Reified Lossy-Conversion Swaps + Checked Signed Prims** (`docs/notes/DN-91-CU-3-Float-Int-Conversion-Surface.md`) | (trx2, DN-34 §8.16 follow-on) proposes the reified lossy-conversion swap surface ADR-040 §2.4/§5 rules for — a rounding `Binary{N}→Float{F64}` swap and a saturating `Float{F64}→Binary{N}` swap, each certificate-carrying — plus checked signed float-int prims, closing the CU-3/D4-D5 design gate. Works up the decision for maintainer ratification; decides nothing normatively | **Draft** (2026-07-08) |
| **DN-92 — CU-6 Bit-Rotation Surface: dedicated prims vs. a derived width-safe surface** (`docs/notes/DN-92-CU-6-Bit-Rotation-Surface.md`) | (trx2, DN-34 §8.16 `FLAG-math-3`) works up the CU-6 rotate/reverse decision — recommends Option A (a dedicated `bit.rotl`/`bit.rotr`/`bit.reverse`/`bit.swap_bytes` kernel-prim family) over Option B (a derived `std.math` surface), with the `n mod N` total rotate-amount convention (`n=0`/`n≥N` never refused). Enacts nothing | **Draft** (2026-07-08) |
| **DN-93 — The E20-1 Content-Address Rehash Settlement** (`docs/notes/DN-93-E20-1-Content-Address-Rehash-Settlement.md`) | recommends batching every identity-changing one-way-door change into a single content-address rehash rather than spending it piecemeal — a maintainer strategic-timing/coordination discipline. Decides nothing normatively; never spends the rehash | **Draft** (2026-07-08) |
| **DN-94 — Atomics & Memory-Model Direction** (`docs/notes/DN-94-Atomics-And-Memory-Model-Direction.md`) | (closes the CU-8/gate-D2 design question, DN-34 §8.16) recommends atomics stay a runtime-internal concern rather than a user-facing surface (per the LR-8/LR-9 value-semantics model + the RT1 no-shared-mutable-state invariant and the already-ratified sole-ownership cross-hypha move), routing the normative commit to the RFC-0027 follow-on. Enacts nothing | **Draft** (2026-07-08) |
| **DN-95 — Merge & Down-Propagation Pattern** (`docs/notes/DN-95-Merge-And-Down-Propagation-Pattern.md`) | (clean-base tiers vs. the squash-divergence funk) shows that squash-only + no-force-push + persistent accumulation tiers makes lower-tier graph divergence mathematically unavoidable (`Proven`-within-model) and works up an ephemeral-RC + periodic-`dev`-reset candidate pattern. Its standalone recommendation is superseded by DN-97's Rank-1 unification — append-only, not edited, only referenced | **Draft** (2026-07-09; consumed by DN-97) |
| **DN-96 — Forward-Development & Change-Sizing Pattern** (`docs/notes/DN-96-Forward-Development-And-Change-Sizing-Pattern.md`) | works up a spec-first staged pipeline (spec → public API → private API → components → code) as a context-windowing mechanism, with bite-sized changes (DN-65's ≈1–2k-LOC soft rule) and precursor doc/index branches for auto-generated bulk; proposes a `/forward` skill. Enacts nothing, ships no code, changes no `CLAUDE.md` | **Draft** (2026-07-09; consumed by DN-97) |
| **DN-97 — Unified Branch/Merge/Propagation Pattern** (`docs/notes/DN-97-Unified-Branch-Merge-Propagation-Pattern.md`) | reconciles DN-95 (down-propagation) + DN-96 (forward/up-flow) under the maintainer-ratified **Rank 1**: three same-content persistent trunks differing by rigor, not content, so every merge — up and down — is a plain, conflict-free `--no-ff`; lightweight production `main` is a `just package-release` (`git archive` + `.gitattributes export-ignore`) packaging artifact, not divergent tracked content. Operationalized by the `/forward` + `/sync-down` skills + `just package-release` (landed same day, PRs #1338/#1339) | **Accepted** (2026-07-09; maintainer-ratified in-session as Rank 1) |
| **DN-98 — Specialist Agent Personas** (`docs/notes/DN-98-Specialist-Agent-Personas.md`) | proposes a specialist-agent-persona system: reusable `.claude/agents/*.md` roles that pre-scope model tier + tool set + skills + discipline per recurring swarm role (six personas — `myc-porter`, `myc-leaf`, `pr-reviewer`, `security-reviewer`, `design-reasoner`, `integrator`) for token/usage-efficient swarms. Ships the persona files as Draft operational material; enacts nothing, moves no other doc's status | **Draft** (2026-07-09) |
| **DN-99 — Surface-Gap Closure Register & Plan (spw / RFC-0031 stdlib-port wave)** (`docs/notes/DN-99-Surface-Gap-Closure-Register.md`) | classifies the 92 surface gaps that block porting the Rust stdlib to self-hosted `.myc` into one closure register (per row: status · evidence cite · layer · closure approach · DoD · DN-flag · tracking · semcore-lane collision · size · priority), foregrounds the already-closed set so landed work is not re-opened (the Float-lesson correction — ADR-040 Enacted 2026-07-02, do-not-reopen; mitigation #14), ranks a two-track closure plan (language `enb` closures plus transpiler closures) toward the zero-hand-port north star under the whole-project unfrozen posture (ADR-045), and attests completeness honestly (§5: one spot-verify round; `Declared`, not `Proven`). The §8 `enb` backlog is filed as M-1024…M-1034 under epic E28-1. Enacts nothing; moves no other doc's status. `Empirical` where cited against the tree, `Declared` for unratified closure designs | **Draft** (2026-07-10) |
| **DN-100 — Macro Expand-First Transpiler Pre-Pass (the toolchain DN for M-1032 / ENB-9)** (`docs/notes/DN-100-Macro-Expand-First-Transpiler-Pre-Pass.md`) | (trx increment; DN-34 §8 register #51 / DN-99 §8 ENB-9) the toolchain DN M-1032 requires before implementation — decides **how** the transpiler expands Rust macros before `syn`-item translation: Alt A `cargo expand` · Alt B a vendored `macro_rules` engine · Alt C a hybrid/std-only shim · Alt D status-quo hand-expand, grounded in a whole-corpus macro profile (82 invocations across 18 files, ~93% custom `macro_rules!`). Honest ROI (no sycophancy): macro expansion is an **expressibility lever with an *uncertain* `checked_fraction` effect** — the dominant corpus macros expand into constructs the transpiler still gaps — not a guaranteed win. Recommends (ranked) opt-in `cargo expand` and a std-macro shim; enacts nothing, moves no other doc's status. Counts `Empirical`; unbuilt designs `Declared` | **Accepted** (2026-07-10, maintainer-ratified — Rank 1 (`cargo expand`, opt-in/off-by-default/never-gating) plus Rank 2 (std-macro shim) accepted as complementary; the §3 honest ROI framing (uncertain `checked_fraction` effect) carried forward unchanged; unblocks M-1032; originally Draft 2026-07-10) |
| **DN-101 — Cross-Nodule Runtime Link (the ENB-1 runtime-execution close)** (`docs/notes/DN-101-Cross-Nodule-Runtime-Link.md`) | (M-1024 / ENB-1; DN-99 register row #41 runtime half) the design of `PhylumEnv::link` — the runtime dual of the check-time import resolution (`resolve_imports`, M-662): folds every nodule's **checked** decls into **one** linked `Env` the existing `elab`/`mono`/`eval` pipeline consumes unchanged (KC-3 — no new L0 node; DRY — reuses the check-time registry). Verify-first finding (mitigation #14): **direct** cross-nodule execution already worked (a consumer's checked `Env` retains imported `pub` decls with bodies); the real gap closed here is **transitive** (a `pub` fn calling its home nodule's **private** helper). Records the **v0 flat-namespace** collision policy (one decl per simple name; a cross-nodule name collision is a never-silent `CheckError`, G2 — never a silent winner) — recommended, **not** ratified; qualified per-nodule scoping that would *disambiguate* a collision is the M-982 residual. Residual flagged (VR-5): AOT parity = M-1024 follow-up; `.myc` runtime mirror = N/A until the `.myc` evaluator (M-986/M-987). Renumbered from DN-100 at integration (that number is M-1032's macro-expand DN). `Empirical` where witnessed by `phylum_exec.rs`; `Declared` for unratified designs | **Accepted** (2026-07-11, maintainer-ratified — v0 flat-namespace baseline confirmed; the long-term option-B-vs-C choice left open for a dedicated planning pass, M-1048; originally Draft 2026-07-10) |
| **DN-102 — The `?` Try-Operator Desugar (the ENB-2 grammar-sugar close)** (`docs/notes/DN-102-Try-Operator-Desugar.md`) | (M-1025 / ENB-2; DN-99 register rows #60 + #52) the design of the `?` try-operator **surface + desugar**: a `Question` token, `e?` parsed as a postfix `Expr::Try` marker, and the desugar of `let x = e? in body` to a **type-directed `match`** over the operand's `Result`/`Option` channel with the continuation inside the binding arm — so it type-checks **without** an early-return or a never-type. §2 confronts head-on (no sycophancy) that the naive local-`match`-with-`return` desugar is **ill-typed** in this language (no `return`, no `!`; the arms `A` vs `Result[A,E]` do not unify), and §3 pins the **error-type unification rule** (the operand's error/absence channel must match the enclosing return channel — exact-match, no `From`-widening in v0). §5 records the **v0 position restriction** (`?` legal only as a `let`-binder RHS; any other position a never-silent refusal pointing at the deferred CPS lift). KC-3 (no new L0 node — reuses `Expr::Match` + `Result`/`Option`); a `Try` never survives checking. Residual FLAGs: FLAG-try-1 general-position CPS lift (gated on the never-type `-> !`, M-1030/#88), FLAG-try-2 `From`-error widening, FLAG-try-3 the `.myc` frontend represents-not-produces `?`. Enacts nothing; moves no other doc's status; maintainer ratifies (house rule #3). `Empirical` where witnessed by `tests/try_operator.rs`; `Declared` for the unratified desugar rule. **Second research pass appended (M-1049, 2026-07-10; PR #1395):** reconciles with DN-106/DN-109/DN-107 (all ratified since the first pass); **resolves FLAG-try-2** — implicit `From`-error widening scoped as a deliberate exclusion (DN-109 D13), mapped to the native `map_err(e, conv)?` idiom, no error-conversion-trait subsystem; recommends Rank 1 (ratify the landed v0 core + adopt the resolutions) | **Accepted** (2026-07-10, maintainer-ratified — SP.5 Rank 1 adopted: the landed v0 desugar (§2/§3/§5) plus the FLAG-try-2 (SP.2, deliberate exclusion of implicit `From`-widening → native `map_err`) and FLAG-try-1 (SP.3, CPS-lift follow-up decoupled from the never-type) resolutions; M-1049 closed; DN-99 §8 ENB-2 wording confirmed; originally Draft 2026-07-10, non-ratified 2026-07-11 pending a second pass) |
| **DN-103 — Impl-Level Generic-Parameter Slot (the ENB-3 grammar-slot close)** (`docs/notes/DN-103-Impl-Level-Generic-Parameter-Slot.md`) | (M-1026 / ENB-3; DN-99 register row #63 / §A2) the design of the impl-level **type-parameter slot** `impl[T] Foo[T] { … }` — surface, grammar, AST, checking, monomorphization. A `[T, …]` slot parsed **immediately after `impl`** via the existing unbounded-name parser (unambiguous — no base type begins with `[`); threaded into the inherent-impl AST (`InherentImplDecl.params`, not flattened at parse); and desugared at **Phase-0** by **prepending** the params to each lifted method's own `fn` type-params, so the method becomes an ordinary generic free function and monomorphization reuses the **existing** fn-generics path (KC-3/DRY — no new L0 node, no new mono pass, VR-5 no tag upgrade). §3 confronts the two forks (parse-time flatten vs AST slot; inherent-only vs generic trait-instance) without glossing. **v0 scope: inherent impls only** — a non-empty slot on a **trait instance** (`impl[T] Trait for Foo[T]`) is a never-silent refusal deferring generic trait-instance coherence to an RFC-0019 §4.5 amendment (§6 residual), a `: bound` in the slot is refused (bounds live only on `fn` type-params, RFC-0019 §4.1), and a duplicate param is the existing never-silent refusal (G2). Lifetime-erasure resolves to N/A (no lifetime surface in v0). Residual FLAGs: generic trait instances, width-param impls (DN-42). Enacts nothing; moves no other doc's status; maintainer ratifies (house rule #3). `Empirical` where witnessed by `tests/check.rs`; `Declared` for the unratified slot semantics | **Accepted** (2026-07-11, maintainer-ratified — §3 Fork 1 confirmed as option B, AST slot + Phase-0 desugar-prepend; originally Draft 2026-07-10) |
| **DN-104 — Per-Constructor Visibility Seal (the ENB-4 grammar-slot close)** (`docs/notes/DN-104-Per-Constructor-Visibility-Seal.md`) | (M-1027 / ENB-4; DN-99 register rows #37 + #69 field-visibility sub-case; DN-53 §B.5/§B.6 Q1) the design of the per-constructor **visibility seal** `pub type T = priv Mk(..)` — surface, grammar, AST, export-table semantics, the never-silent cross-nodule construction refusal. A `priv` **seal marker** on the `constructor` production (reserved keyword `Tok::Priv`, never a silent identifier — G2), threaded into the AST (`Ctor.sealed: bool`, not flattened); export-table semantics that export the type **NAME** (usable cross-nodule in signatures, `use`, and **pattern position**) but **withhold the constructor from cross-nodule CONSTRUCTION via an imported name** (a never-silent refusal for a well-behaved caller going through `use`), enforced by a per-nodule **withheld set** derived from the M-662/M-1024 export table (DRY — the exact twin of the `NoduleImports.ambiguous` machinery; own ctors subtracted) and checked at the two constructor-application sites. §3 confronts the DN-53 §B.6 Q1 fork (**binary seal vs full `pub(path)` scoped visibility**) on its merits and resolves it to the **BINARY seal** (KISS/YAGNI). Two further never-silent refusals: `priv` on a **non-`pub`** type (redundant seal) and `priv` inside an **`object`** body (out of scope in v0, refused at parse in Rust; the `.myc` mirror does not yet match — §6). **CRITICAL residual (found + pinned at integration review, M-1036 tracks the fix):** this is **NOT an enforced security/capability boundary** — Mycelium resolves types/ctors by bare name in the caller's own scope (own decls shadow imports, pre-existing RFC-0006 §4.3), so a foreign nodule's **same-named local (unsealed) shadow type**, never imported, bypasses the seal entirely (`check_phylum` accepts it; pinned by `tests/ctor_seal.rs::known_gap_a_same_named_local_shadow_type_bypasses_the_seal`). The seal as landed is an opt-in API-discipline nudge for well-behaved `use`-based callers, **not** the "unforgeable" FR-N3 capability-gate M-1023's `Approx::proven` port needs — that requires nodule-qualified type identity (M-1036). Also FLAGGED: the fully-opaque variant (seal pattern-matching too) deferred; `pub(path)` scoped visibility deferred (YAGNI); the `.myc` cross-nodule *enforcement* mirror rides the checkty `.myc` port (M-741) — this increment mirrors the `.myc` surface + AST + fingerprint (tag 110 parity) only. Enacts nothing; moves no other doc's status; maintainer ratifies the corrected, narrower scope (house rule #3). `Empirical` where witnessed by `tests/ctor_seal.rs` (11 witnesses incl. the pinned gap); `Declared` for the unratified seal semantics — explicitly **not** a checked capability/security claim | **Draft** (2026-07-10) |
| **DN-105 — `match` on a `Bytes` Scrutinee (the ENB-12 string-literal-pattern enabler)** (`docs/notes/DN-105-Match-On-A-Bytes-Scrutinee.md`) | (M-1035 / ENB-12; DN-99 register row #72; DN-34 §8.21; DN-26 dual) the design of allowing a `match` whose scrutinee is a `Bytes` value with **byte-string-literal** arms and a **required default arm**. **One-clause enabler (KC-3):** the single categorical block was `check_match`'s scrutinee-type gate admitting only `Data`/`Binary`/`Ternary`; lifting it to admit `Ty::Bytes` unblocks #72 and every string-`match` port target — everything downstream (normalize/usefulness/decision/eval/elab) already handles an open-domain literal column generically (`Bytes` was already `signature() → None`). Decides (for ratification): the pattern form is the **existing byte-string literal** in both spellings — `0x…` hex (`Literal::Bytes`) and `"…"` text (`Literal::Str`), which lower to the SAME `Repr::Bytes` value (no new pattern/AST node); **byte-content equality** (`"foo"` ≡ `0x666f6f`); **`Bytes` is an OPEN domain** so a wildcard/default arm is REQUIRED (a non-exhaustive `Bytes` match is a never-silent W7 refusal, witness `_`); the fork resolves to **literal-only equality patterns**, not structural byte patterns (deferred); the redundancy key stays per-surface-form (`by:`/`s:`), a conservative under-report of cross-form redundancy, never a silent miscompile. Residuals (VR-5/G2, honest): the `.myc` `0x…`-hex eval synthesis stays deferred (FLAG-semcore-25, explicit `Err`); the native-LLVM `Bytes`-match backend refuses explicitly (the trampoline AOT handles it); the trx #72 transpiler pin flips *gapped*→*emitted* as a separate follow-up. Enacts nothing; moves no other doc's status; maintainer ratifies (house rule #3). `Empirical` where witnessed by the three-way differential (`tests/enablement.rs`) + the `.myc` eval-match oracle (`compiler_stage5_evalmatch.rs`); `Declared` for the unratified semantics | **Accepted** (2026-07-11, maintainer-ratified as drafted; originally Draft 2026-07-10) |
| **DN-106 — Statement-Sequencing (`let _`) + Record-Update / Mutation Split: a Triage (ENB-10)** (`docs/notes/DN-106-Statement-Sequencing-And-Record-Update-Triage.md`) | (M-1033 / ENB-10; DN-99 register row #89; DN-71/M-903; DN-26 dual) the **triage** of M-1033 per mitigation #14 (verify a stale issue's claim against the codebase before implementing): finds that **both** ENB-10 sub-gaps' language side is **already closed at L1**, three-way witnessed (L1-eval ≡ elaborate→L0-interp ≡ trampoline-AOT), and that the real residual is entirely **transpiler-lane**. Part 1 (statement-sequencing `let _ = e in body`) is an ordinary `let` whose binder is the identifier `_` (grammatical `ebnf:291`/`ebnf:447`; parsed, checked, evaluated, elaborated) and is moreover the established affine drop/use-once surface (DN-71/M-903). Part 2 (functional field-update) is the already-expressible destructure-and-reconstruct `match base { Ctor(f0, …) => Ctor(f0, …, NEW, …) }` — Mycelium has **positional constructors, no named-field record literal and no field-projection by design** (KC-3/§2), so a Rust-style `{ ..base, field: v }` record-update literal is **intentionally absent** and its addition to L1 is **rejected** (§3, fork B); it is a never-silent parse refusal today (G2). Corrects the M-1033 issue body's over-scoped "grammar-`enb`, HIGH collision, touches `crates/mycelium-l1/**`" framing — DN-99 #89's own tags already agree (layer `tr`, collision `low`); **M-1033's L1/semcore residual is NIL**. Ships four pinning regression witnesses (`tests/enablement.rs`), **no** L1 code change. Enacts nothing; moves no other doc's status; maintainer ratifies (house rule #3). `Empirical` where three-way-witnessed by the §7 differentials; `Declared` for the unratified Part-2 fork resolution (VR-5) | **Accepted** (2026-07-11, maintainer-ratified — fork B reframed as a mechanically-lowering, reversibly-expandable surface sugar; establishes the surface-sugar-transparency + gap-closure-default principles; originally Draft 2026-07-10) |
| **DN-107 — Host-Effect Real-Syscall Registry + the Never-Type (`-> !`) Divergence Model (the ENB-7 close)** (`docs/notes/DN-107-Host-Effect-And-Never-Type.md`) | (M-1030 / ENB-7; DN-99 register rows #79 host-effect wild execution + #88 never-type divergence) a design-reasoner note working the M-1030 decision forward to a ranked recommendation, not a ratification. Separates the two ENB-7 sub-gaps (coupled at exactly one point, `exit`): **host-effects (#79)** close by granting real `wild:read/write/get_env/exit` prims into the `PrimRegistry` over the already-ratified RFC-0028 `wild`-block surface + the RFC-0014 `ffi` effect (no new representation) plus a fixture/sandbox differential; **the never-type (#88)** is recommended as **divergence-as-effect with NO bottom `Ty`** (Rank 1) — a new `diverges` effect on the RFC-0014 lattice, `wild:exit` carrying `!{ffi, diverges}`, a narrow never-silent checker admissibility rule ("an unconditionally-diverging expression is well-typed at any expected type"), and an honest totality-checker interaction (`!diverges` = non-total, flagged, never silent); a nominal `Never`+`absurd` eliminator (Rank 2) is the append-only upgrade path held in reserve; a true bottom subtype `⊥ <: T` (Rank 3) is rejected for v0 (forces subtyping into a structural-equality checker). Names a FORK for the maintainer: whether general-position `?` (DN-102 §6) actually depends on the never-type at all. Enacts nothing; moves no other doc's status; does not edit `issues.yaml`/`CHANGELOG.md`/`Doc-Index.md` itself (the integrating session owns those). `Empirical` for claims read against the tree at `5130badc`; `Declared` for every proposed-but-unratified design (VR-5 — nothing here is `Proven`) | **Accepted** (2026-07-11, maintainer-ratified — Rank 1 accepted for both sub-gaps; the §6 fork resolved to general-`?` independent of the never-type; originally Draft 2026-07-10) |
| **DN-108 — Numerics Transcendentals under the Transparency Rule (the ε/δ surface, ENB-5 / #42)** (`docs/notes/DN-108-Numerics-Transcendentals-Accuracy-Bounds.md`) | (M-1028 / ENB-5; DN-99 register row #42) a design-reasoner note working the DN-99 #42 decision forward to a ranked recommendation, not a ratification. Recommends transcendental/irrational-result functions (`sqrt`/`exp`/`ln`/`sin`/`cos`/`pow`) surface as **`flt.*` interpreter prims** returning a plain `Repr::Float` value whose accuracy bound rides the **existing `Bound`/`Approx` provenance certificate** (ADR-010) — no new numeric surface type, no new kernel node (KC-3). The v0 per-op guarantee tag is **`Declared`, not `Empirical`** — the ε is asserted from the host libm's documented ULP bound, there being no measured reference-case corpus yet (VR-5: no `Empirical` without trials). Out-of-domain inputs (`ln(x ≤ 0)`, `sqrt(x < 0)`, indeterminate `pow` forms) refuse never-silently (`NumErr::Domain`, G2); v0 refuses composition of an *approximate* transcendental input (`ApproxRule::Refuse`) since ε-propagation through a transcendental is nonlinear and has no checked rule yet. The `fast`/`certified` mode split (RFC-0034/ADR-032) is the growth path, not a v0 requirement (YAGNI). Enacts nothing; moves no other doc's status; does not edit `issues.yaml`/`CHANGELOG.md`/`Doc-Index.md`/`lib/**`/`crates/**` itself (the integrator owns those). `Empirical` where read against the tree at `5130badc`; `Declared` for any design not yet built/ratified (VR-5) | **Accepted** (2026-07-11, maintainer-ratified — Rank 1 accepted, OQ-3 vendored-libm + OQ-4 land-now resolved with recommendation, tracked via M-1053; originally Draft 2026-07-10) |
| **DN-109 — Layer-4 Idiom-Optimal Transpilation and Layer-5 Structural Remapping (module to nodule / crate to phylum)** (`docs/notes/DN-109-Idiom-Optimal-Transpilation-And-Structural-Remapping.md`) | (companion to `docs/planning/zero-hand-port-delta-ledger.md`, the zero-hand-port five-layer analysis swarm, 2026-07-10) recommends, for maintainer ratification: **(L4)** a three-bucket idiom-decision framework — Mechanical (auto-emit v0: int-width, Option/Result/`?`-to-match, struct/enum-to-data-decl, `&T`-erasure, `unsafe`-to-`wild`) / Heuristic (rule plus EXPLAIN flag) / Judgment (flag, never guess: `&mut`-to-value-semantics, Ternary/VSA selection, `as`-to-`swap`) — bound by a conjunctive no-silent-upgrade ratchet (semantics-preserving, no inferred `swap` (S1), no guarantee-tag upgrade (VR-5), EXPLAIN-recorded); and **(L5)** structure-preserving 1:1 module-to-nodule remapping plus a **mandatory remap manifest** (`remap.json`/`REMAP.md`, item-granular, unifying the L5 restructuring ledger with the L4 idiom-choice EXPLAIN trail) as the transpiler's provenance artifact of record, fixing a verified flat-emit stem-collision data-loss bug along the way (mitigation #14). Names 6 forks for the maintainer (borrowck-frontend acquisition F1/§7-b is pivotal). Enacts nothing; moves no other doc's status; filed alongside tracked issues M-1041…M-1047 (house rule #3). `Declared` throughout (design proposal, not a measurement) | **Accepted** (2026-07-11, maintainer-ratified — L4/L5 design accepted; fork F1/§7-b REFRAMED as an optional precision aid, not a porting blocker (D7/D8 map to native solutions via flag-with-suggested-idiom, M-1052); fork §7-d flagged as not cleanly resolved; originally Draft 2026-07-10) |
| **DN-110 — Mycelium's Native Facility for the Role Rust Fills with Macros (Generative-Lowering + Sugar Rules), and the Rust→Mycelium Native-Translation Taxonomy it Instantiates** (`docs/notes/DN-110-Native-Metaprogramming-And-Sugar-Lowering-Facility.md`) | decomposes Rust's macro role into five jobs (J1 derive-gen / J2 delegation / J3 expression sugar / J4 compile-time computation / J5 foreign-syntax DSL), finding J1/J2 already native+landed (`lower`/`derive`/`object`, M-811/M-812), J4 native via static specialization (DN-55), J5 a Bridge/flagged exclusion, and **J3 the single real gap**. Recommends (Rank 1, accepted) generalizing the landed `lower`/`derive` term-level lowering framework (DN-54/M-812) to expression position, with `reveal`/M-1051 as the mandatory transparency spine. Proposes a general Rust→Mycelium native-translation taxonomy (§2: Adaptation/Solution/Approximation/Bridge — ratified as provisional handles, **now retained as permanent aliases** for the canonical terms companion **DN-111** settled). §8.2's expression-position hygiene/scoping model is accepted as the basis via the companion `DN-110-8.2-hygiene-deepdive.md`. `Declared` throughout (design proposal); `Empirical` only where citing landed `lower`/`derive`/M-919 or register-verified DN-99 closures at their own source | **Accepted** (2026-07-10, maintainer-ratified — design ratification only, **NOT Enacted**: role decomposition + Rank-1 mechanism + §8.2 hygiene basis accepted; §2 taxonomy labels provisional pending DN-111; §8.1 (J5) and remaining §11 OQs stay open; facility guarantees stay `Declared` until implementation + E1/E3 experiments + `reveal`/M-1051 land; originally Draft 2026-07-11; **§2 taxonomy carve-out resolved 2026-07-10** — DN-111 Accepted, append-only pointer added at DN-110 §2) |
| **DN-110 §8.2 Hygiene Deep-Dive — The Expression-Position Hygiene/Scoping Model for Sugar Rules (Companion)** (`docs/notes/DN-110-8.2-hygiene-deepdive.md`) | a design-reasoner deep-dive working DN-110 §8.2 forward to a concrete hygiene model — def-site resolution of RHS free identifiers, `%`-namespace freshening of RHS binders (reusing the landed `Elab::fresh` gensym), partition-safe substitution of use-site argument terms, and affine/type checking on the expanded L0 (reusing the landed M-919 affine tracker) — plus a five-experiment validation plan (E1–E5) and a tractability verdict (keep DN-110 Rank-1; §8.2 downgraded from "sharpest open question" to "mechanism identified, two bounded residuals to prototype"). Corrects a "content-addressing gives hygiene for free" hypothesis (§6). `Empirical` where read against the codebase at dev `4b3e7e7f`; the proposed model + verdict are `Declared` until E1–E5 run | **Accepted as the basis for DN-110 §8.2** (2026-07-10, maintainer-ratified — §4 model + §7 experiment plan accepted, E1+E3 commissioned as the Rank-1 go/no-go for J3; §9 tractability verdict recorded; §10 OQs (OQ-H1…OQ-H6) NOT dispositioned, stay open; stands as a companion to DN-110, not folded into its text; originally Draft 2026-07-10) |
| **DN-111 — The Canonical Rust→Mycelium Native-Translation Taxonomy (the Native-Equivalence Spectrum)** (`docs/notes/DN-111-Canonical-Rust-To-Mycelium-Native-Translation-Taxonomy.md`) | the companion taxonomy DN deferred from DN-110 §2/§12.1: settles the **canonical Rust→Mycelium native-translation taxonomy** — a four-category native-equivalence spectrum generated by two orthogonal yes/no questions ({exact?} × {native?}): **Native Equivalent** (was Adaptation — renamed, "adaptation" is a Vinay-and-Darbelnet false friend meaning the opposite), **Idiomatic Remapping** (was Solution — renamed, "solution" is overloaded with the genus DN-109 F1 already uses; reuses DN-109's own "remapping" vocabulary), **Approximation** (kept — already the correct PL term), **Interop Bridge** (kept, qualified — canonical full form). Net: 2 renamed, 2 kept; DN-110's four handles retained as **permanent aliases**. Gives each category a decision test, an honesty-posture ceiling (VR-5 — Native Equivalent/Idiomatic Remapping may be `Exact`/`Proven`; Approximation capped `Empirical`/`Declared`, never-silent; Interop Bridge Judgment/never-silent, no native-semantics claim), and corpus-cited worked examples. Formalizes the reconciliation with DN-109's decidability axis (Mechanical/Heuristic/Judgment) as an **orthogonal 2-D grid**, re-deciding nothing in DN-109. Adversarially stress-tested: classification is of a **(construct, context) pair, not a construct** (the sharpest finding — `&mut` lands in different cells depending on aliasing, a fact `syn` cannot see); the Native-Equivalent/Remapping seam (`?`→`match`); the Approximation/Bridge seam and the `wild` fold (the native equivalent *is* the bridge); cells are **time-indexed** under the unfrozen kernel (a Bridge migrates to Native Equivalent as gaps close). Feeds `/native-translate` (M-1056, canonical enum, landed 2026-07-11) and the sugar-index `native_strategy` column (M-1058, contract settled, population landed 2026-07-11 — see the sugar-index entry below). `Declared` throughout (a vocabulary proposal); `Empirical` only where citing landed `lower`/`derive` or register-verified DN-99 closures at their own source | **Accepted** (2026-07-10, maintainer-ratified — see the note's own "Ratification (maintainer, 2026-07-10)" section. Ratifies the TERMINOLOGY + TAXONOMY only; individual classifications stay `Declared` projections until independently checked (VR-5); ships no code, **not Enacted**; originally Draft 2026-07-10) |
| **DN-112 — Nodule-Qualified Type Identity (the ctor-seal capability boundary, made real)** (`docs/notes/DN-112-Nodule-Qualified-Type-Identity.md`) | (M-1036; the residual DN-104 §6 left open) decides the type-identity mechanism that turns M-1027's landed `priv` constructor seal from a never-silent API-discipline nudge into an enforced capability boundary. Verifies the bypass: `Ty::Data` carries only a bare name, so a foreign nodule's own same-named local (unsealed) type impersonates the sealed one (pinned by `tests/ctor_seal.rs::known_gap_a_same_named_local_shadow_type_bypasses_the_seal`). Ranks three mechanisms (§4 objective table) and recommends **Rank 1 — a nodule-qualified name in the existing `Ty::Data` `String` slot**, stamped at `resolve_ty` from `DataInfo.home` (the declaring nodule): collision-free by construction for `PartialEq`/`subst_ty`/mangling, ~79 destructure sites untouched (KC-3-minimal). Decides the `type_head` impl-coherence twin gap **IN-SCOPE** (an automatic consequence of Rank 1). Adversarially stress-tests its own recommendation (§9): the sharpest finding is the **prelude/builtin uniform-home invariant** — a naive per-current-nodule qualification would fracture `Bool`/`Option`/`Result`/`Tuple$N` per-nodule, the strongest argument for Rank 2. `Empirical` where read against `dev@45927ea4`; `Declared` for the unimplemented mechanism | **Accepted** (2026-07-10, delegated ratification — maintainer delegated the option choice, orchestrator selected Rank 1 on the merits; design ratification only, **NOT Enacted**; a DoD condition added requiring a dedicated builtin/prelude uniform-home-invariant regression test, directly answering §9's sharpest finding; guarantee posture `Empirical` post-fix, `Declared` on a narrower point-patch; unblocks M-1036; originally Draft 2026-07-10) |
| **DN-113 — The Cross-Phylum (Crate→Crate) Import/Resolution Subsystem** (`docs/notes/DN-113-Cross-Phylum-Import-Resolution-Subsystem.md`) | designs the green-field cross-phylum import/resolution subsystem (0% wired today, §1 audit): a `::` phylum-boundary `use` head (`use dep::nod.sym`), an additive `Phyla` dep-graph type + `check_phylum_with_deps`, content-pinned resolution to a `(phylum_hash, qname)` def-site ref (grounds DN-110-8.2 OQ-H1's cross-phylum half), loaded v1 B2-from-verified-source-tree plus a generated lock (B4), layered over the M-1024 canonical linker via a phylum-qualifier key dimension (DRY, no third linker). Adversarially stress-tests: the diamond/version-conflict finding (§9.1) — two differently-pinned phyla of "the same" dependency are two different phyla, never silently SemVer-coalesced; a cyclic-phylum refusal forced by content-addressing itself; a re-export origin-hash rule specified now so v2 is not a silent gap. v1 defers separate-comp/re-export/glob/ranges never-silently. `Empirical` for the §1 audit; `Declared` for the design | **Accepted** (2026-07-10, delegated ratification — maintainer delegated the option choice, orchestrator selected the recommended v1 design on the merits: `::` syntax (Rank 1), B2+B4 loading (B3 deferred), `(phylum_hash, qname)` OQ-H1 granularity (per-function hashing deferred), strict no-silent-SemVer-coalescing diamond policy (OQ-CP-1, coalescing deferred as a possible explicit v2 opt-in); v1 design ratification only, **NOT Enacted**; mints **M-1060**; OQ-CP-5 (lock format/shape) stays open; originally Draft 2026-07-10) |
| **DN-114 — Validated Narrative Generation** (`docs/notes/DN-114-Validated-Narrative-Generation.md`) | proposes a sentence-level **faithfulness oracle** that gates the interpretive prose the Narrative-Authoring-Pipeline's `gen-book`/`gen-manual` generators emit (M-363), so the language book + reference manuals + a learning corpus can be **generated** while never hallucinating: constrained-from-extracted-facts generation, resolvable `doc_refs` per paragraph, an adversarial claim-grounding pass, and a **`validated_fraction`** (modeled on the transpiler `checked_fraction`) with commit-only-validated + never-silent drops; idempotent content-addressed generation for differential update. **Refines** (does not supersede) `Narrative-Authoring-Pipeline` §4/§4.1/§6 and the Capture note's draft-then-review floor; ships the `tools/llm-harness/narrate/` harness + `/gen-book` skill as Draft. No sycophancy: the shipped checker is a deterministic lexical stand-in (`validated_fraction` an `Empirical`/`Declared` proxy), output stays `Empirical`/`Declared`, the human review floor is preserved. `Declared` pending ratification | **Draft** (2026-07-11) |
| **DN-115 — M-1054 Stage 2 (OQ-H1): Def-Site Resolution of Free RHS Identifiers, Single-Nodule Fragment** (`docs/notes/DN-115-M1054-Stage2-Defsite-Resolution-Single-Nodule.md`) | a leaf-scoped design-reasoner scoping note for the M-1054 native-metaprogramming facility's Stage 2: finds single-nodule def-site resolution / referential transparency for a sugar rule's free RHS identifiers already holds **by construction** since Stage 1b (Pass-1 elaboration against the def-site env) — so Stage 2 is *prove it non-vacuously on the real elaborator + close two narrow check-phase gate-correctness gaps (G1 VSA/float-prim RHS over-refusal, G2 nullary-`lower`-rule value-position over-acceptance) + document the single-nodule invariant*, not *add a new resolution mechanism*; confirms the Stage-4 (cross-nodule/phylum) boundary stays refused (DN-113/M-1060's job). `Empirical` where read against the codebase and where the Ratification section's own landed tests are cited; `Declared` for anything not independently checked | **Accepted** (2026-07-11, delegated ratification — see the note's own "Ratification (maintainer-delegated, orchestrator-selected on the merits, 2026-07-11)" section; ratifies the §1 finding + the Q1/G2/§6 design decisions, design ratification only, **NOT Enacted**; the implementation landed in the same leaf's PR, tracked as **M-1069**; originally Draft 2026-07-11. **DN-114 ID collision RESOLVED at integration close-out (2026-07-11)**: the M-1054 Stage-1b leaf-scoped note this note cites has been renumbered **DN-116** (`docs/notes/DN-116-M1054-Stage1b-Check-Phase-Sugar-Accept.md`; the unrelated `docs/notes/DN-114-Validated-Narrative-Generation.md` keeps DN-114 per maintainer directive); this note's own prose citations were updated to DN-116 in the same pass, but every `crates/mycelium-l1/` source comment still citing "DN-114" is a residual, deliberately deferred so as not to touch `mycelium-l1` while a concurrent Stage-3 agent edits it — grep `DN-114` in that crate to find the set) |
| **DN-116 — M-1054 Stage 1b: Check-Phase Sugar-Call Accept (Option B, the Def-Time RHS Type-Scheme)** (`docs/notes/DN-116-M1054-Stage1b-Check-Phase-Sugar-Accept.md`) | *(renumbered from DN-114 at the 2026-07-11 integration close-out — collided with `docs/notes/DN-114-Validated-Narrative-Generation.md`, which keeps DN-114; see that note's own "DN-116 renumber" FLAG and DN-115's updated collision note)*. A leaf-scoped implementation note for the M-1054 native-metaprogramming facility's Stage 1b: makes the verified Stage-1 hygienic expander (`elaborate_lower_rule_with_args`, `crates/mycelium-l1/src/elab.rs`) reachable end-to-end from a value-parametric sugar-rule call site via the checker's `Cx::check_sugar_call` accept path (**Option B** — a monomorphic rule's RHS result type is fixed at *definition*, never re-inferred per call site) plus `Elab::app`'s new §5.2 dispatch, gated by two never-silent refusals (Stage 2/OQ-H1 free-identifier gate; Stage 3/OQ-H4 affine gate, including a composite/nested-`Substrate` structural walk fixed by an adversarial-verify finding). `Empirical` per the dual-oracle `reachability_stage1b.rs` corpus and its non-vacuity controls; Stage 2 (cross-nodule) and Stage 3 (full affine re-check) stay `Declared`/out of scope | **Draft** (2026-07-11 — records a leaf's implementation choice for orchestrator review; not itself a ratification of DN-110/DN-110-8.2, which stay `Accepted`; moves toward `Accepted` only via maintainer/orchestrator review) |
| **DN-117 — M-1054 Stage 3 (OQ-H4): Affine Soundness over the Substituted Expr — Lift the Stage-3 Gate from Refuse-All-Affine to Accept-Linear/Refuse-Duplicated** (`docs/notes/DN-117-M1054-Stage3-Affine-Over-Substituted-Expr.md`) | *(brought into the docs corpus at the 2026-07-11 integration close-out from the separately-building Stage-3 leaf, `claude/leaf/M1054-DN117-stage3-affine-scoping`; landed with the Stage-3 implementation via `claude/leaf/m1054-stage3-affine`'s own pull-down merge, same date.)* A design-reasoner build-plan scoping note recommending Stage 3 run the landed M-919 affine `Tracker` over the **substituted `Expr`** at check time inside `Cx::check_sugar_call` (Q1, FLAG-A/Rank 1), replacing the DN-116 §3.2 conservative structural approximations with a precise linear-use-count decision; records a grounded correction that a dropped affine value parameter is **ACCEPT** (runtime-backstopped, M-904), not a static refusal. `Empirical` where read against the codebase at `dev @ 3c7e85d7`; `Declared` for the proposed (unbuilt) Stage-3 mechanism | **Accepted** (2026-07-11, delegated ratification — see the note's own "Ratification (maintainer-delegated, orchestrator-selected on the merits, 2026-07-11)" section, mirroring the DN-115 precedent; ratifies the §1–§7 design decisions, NOT `Enacted` (house rule #3); implementation landed in the same leaf's PR (`crates/mycelium-l1/src/checkty.rs` + new `src/tests/affine_stage3.rs`, 12 tests). Originally Draft 2026-07-11. **Same-day Errata (Ratification point 11, CRITICAL, fixed):** a pattern-ctor name-collision false-accept (an ambiguous match-arm binder left unrenamed, letting a spliced argument be captured) was found and fixed — `Cx::stage3_substitute_pattern`/`stage3_substitute_arm` now refuse the whole sugar call in the ambiguous case rather than guess; FLAG-pattern-ctor-collision (the disambiguation gap itself) stays open) |
| **DN-118 — Closure-to-Value-Semantics Transpiler Enabler and Native-Conformance Contract** (`docs/notes/DN-118-Closure-To-Value-Semantics-Transpiler-Enabler-And-Native-Conformance-Contract.md`) | ratifies **Option A**: the transpiler gap for Rust closures is a **closure-EMIT gap**, not a defunctionalization gap (defunctionalization of env-capturing closures is already implemented in the language — RFC-0024 §4A/M-704, `done`), so `crates/mycelium-transpile` emits the Mycelium `lambda` surface and lets mono's whole-program `ClosureSpecialization` resolve captures; never builds its own defunctionalizer (KC-3/DRY). A closure that syntactically mutates a captured binding in place (FnMut/`&mut`-style — `syn` carries no borrowck facts) is conservatively FLAGGED (`Category::Closure`), never auto-emitted (DN-109 D5/D7 ratchet); Phase 1 is single-parameter-closures-only (a verify-first narrowing of the note's own original plan — a directly multi-param `lambda(x,y)=>…` checks fully curried, M-822/RFC-0024 §4A.8, which this transpiler's unchanged call-site emission cannot produce). P1 (the emit pass) lands with this note; P2 (a future RFC-0018-framework `@value_closures` native-conformance contract) and P3 (borrowck-backed capture-mutation checking) are scoped, not built. `Empirical` for every claim checked against the codebase/real toolchain (myc-check-verified emission); `Declared` for the general "every value-safe-classified closure is semantically faithful" claim and the unbuilt P2/P3 scope (VR-5) | **Accepted** (2026-07-11, orchestrator-ratified on the merits, delegated authority — the scoping agent's own attempt to file this note was guard-blocked; mirrors the DN-115/DN-117 precedent. Explicitly NOT Enacted — P1 code lands with this note's leaf (PR #1500, `crates/mycelium-transpile/src/{emit,visit,gap}.rs` + `src/tests/emit.rs`); P2/P3 are future work, house rule #3) |
| **DN-119 — L3 Comprehensive Surface Expressibility: Scoping, Reframe, and Phased Plan** (`docs/notes/DN-119-L3-Comprehensive-Surface-Expressibility-Scoping.md`) | a design-scoping reasoner note working the maintainer directive "implement the L3 concrete surface grammar comprehensively" forward: verifies the L3 grammar is **substantially complete** already (register-lag corrections to DN-99 rows #60/#73/#37), isolates the genuinely-open ~7-class L3-grammar residual (struct/named-field patterns, range/`@`-binding patterns, impl-level generics, general-position `?`, format-string residual) from the bulk of the delta-ledger gap mass that is **misattributed** to L3 (actually kernel type-vocabulary, trait semantics, runtime execution, or transpiler-lane work), and from the **deliberate-exclusion set** (`&mut`, unbounded `loop`, shared mutability, silent casts) that must NOT get L3 grammar without regressing value-semantics guarantees. Recommends (Rank 1) reframing "full native capability" to "L3 expresses every construct's native *answer*" (grammar ∪ flagged remapping), a lane-tagged leverage-ranked phased plan (A–D grammar, E–I downstream), and draws the honest `checked_fraction` boundary — L3 grammar alone moves the vet number ~0; the real lever is kernel/runtime (Phase E). Python explicitly flagged out of scope, needing its own DN. `Empirical` where read against integration tip `b36f0ef4`; `Declared` for the proposed reframe/plan | **Draft** (2026-07-11) |
| **DN-120 — Content-Addressed Identity vs. Temporary-Copy Mutation: Solved-by-Design (Verdict)** (`docs/notes/DN-120-Content-Addressed-Identity-vs-Temporary-Copy-Mutation-Verdict.md`) | a verdict note (not a new design — mitigation #14) recording that ADR-003's disclosed residual ("content-addressed identity ⇒ efficient temporary-copy mutation may remain to be solved") is **SOLVED-BY-DESIGN**: DN-35 §5 already is the content-address-coherence answer (reuse fires only at rc==1, no live alias of the old identity; the intern/hash-cons table modeled as a weak map, evict-or-copy on reuse). Corrects the landed/open boundary against the tree: the rc==1 **detection gate** is landed and `Exact` (`mycelium-std-runtime/src/rc.rs`'s `RcCell`/`RcProbe::UniqueOwner`); the actual **reuse-write** optimization stays `Declared` and unbuilt at both the interpreter and AOT tiers, with DN-35's own E12 Increment 3 / task #6 as its ratified, not-yet-executed build plan. Draws the boundary with the distinct DN-109 §6.1/D7 `&mut`-aliasing problem (routed to the DN-118 `FnMut` lane), so the two are not conflated. Adds forward cross-references at DN-32 §2.2 and DN-33 §6 (append-only). `Empirical`/`Declared` per DN-35 §5's own tag; no new mechanism proposed | **Draft** (2026-07-11) |
| **DN-121 — The Type-Vocabulary Lever: Scoping the Dominant `checked_fraction` Class** (`docs/notes/DN-121-Kernel-Type-Vocabulary-Lever-Scoping.md`) | a design-forward reasoner note scoping the ~40% "Other / type-coverage" gap class the zero-hand-port delta-ledger names as the dominant `checked_fraction` lever. Proposes a corrected framing — the dominant lever is the language's **type-reference-closure** layer, of which only a *bounded* residual needs a genuinely new kernel `Ty` variant; most of the 322-gap class closes on the **existing** kernel `Data`/`Bytes`/`Binary{N}` vocabulary via std ADTs + transpiler emit rules + idiom ratification (per DN-99's already-verified rows) — correcting the "missing *kernel* type-vocabulary" framing to *type-reference closure* on the existing kernel. Gives a phased, leverage-ranked build plan (P1–P7), names the already-closed/do-not-reopen set (signed-order ops, closures, string-literal pattern), and flags the outstanding Phase-0 re-measure that must precede any numeric target. `Empirical` where read against dev tip `b36f0ef4`; `Declared` for any design not yet built/ratified | **Draft** (2026-07-11) |
| **DN-122 — External-Trait Impls Across the Home Boundary (the top `checked_fraction` unlock)** (`docs/notes/DN-122-External-Trait-Impls-Across-The-Home-Boundary.md`) | a design-reasoner note working the M-876 external-trait-impl gap (DN-121's P1 lever) forward to a ratified recommendation. Ratifies **Alt 1 — foreign-trait import + closure-extended coherence**, tightened to a **build-ready §13 MVP** (single-parameter, param-only-signature foreign-trait-impl class, prelude-scoped first — **OQ-6 resolved: prelude-seed**, on KISS/YAGNI plus least-soundness-surface grounds) via a **transpiler rule-swap (WU-A) + target-trait availability (WU-B)**, riding the now-landed M-1060/M-1036 checker substrate with **zero new kernel/checker work**. Soundness: the MVP admits exactly the complement of the landed M-1060 cross-phylum bare-name-collapse guard, so it cannot reopen that collapse by construction. Unsupported shapes (two-type/`Self`-needing traits incl. the canonical `Widen` witness; concrete-type-in-sig) are refused never-silently, tracked as **M-1076**/**M-876** (WU-C, out of v1 scope). **OQ-7 (single-param vs two-type split of the 114-gap/12.4% class) stays an open honest residual**, unmeasured until the Phase-0 re-measure runs under DN-124's phylum-mode vet basis. `Empirical` read against `dev@b36ebdbe`; `Declared` for the ratified-but-unbuilt mechanism until WU-A/WU-B land + differential-witnessed | **Accepted** (2026-07-12, delegated ratification — maintainer authorized the orchestrator to "ratify based on objective reasoning and the project's needs/intents, keep to core principles, report results"; mirrors the DN-115/117/118 precedent. NOT Enacted (house rule #3) — WU-A/WU-B unbuilt, tracked as **M-1080**. Originally Draft 2026-07-11, re-verified + tightened to build-ready 2026-07-12) |
| **DN-123 — The Records / Named-Fields Surface Lever (P2): Sugar-over-Positional, Not a Kernel Primitive** (`docs/notes/DN-123-Records-Named-Fields-Surface-Lever.md`) | a design-forward reasoner note working DN-121's **P2 lever** (records / named-field surface, Struct 80/10%) forward to a ranked recommendation. Verify-first correction (register-lag, mitigation #14): records are **already substantially supported at the `checked_fraction` level** — the transpiler carries a field-name↔index map (`StructLayout = Vec<Option<String>>`), already desugars Rust struct literals / field-projection / struct-update to positional `Data`, and emits named-field structs positionally with names **recorded** as a never-silent `NamedFieldDrop` sub-gap; the blocker is an unmapped field TYPE, not named-fieldness. The genuine residual is **faithfulness** (dropped names) plus the **self-hosted `.myc` surface** (the DN-119 L3-G1 struct-pattern grammar gap). Recommends **Option A — a mechanically-lowering SUGAR** over positional `Ctor`/`Data` plus the name↔index map (per DN-106's ratified GP1/GP2 — the gap-closure default is the sugar, not a kernel primitive), with **Option C** (sugar plus inert `field_names` type-registry metadata) as the YAGNI-gated faithfulness upgrade and **Option B** (first-class named-field kernel variant) **rejected** (DN-106 fork B, KC-3, value-semantic positional design). Identity stays positional/structural (ADR-003) — names never in the hash. Adversarial OQ-1 (content-addressed identity vs field ORDER — canonicalize-to-declaration-order obligation — and NAMES off the hash), OQ-2 (cross-phylum name metadata, DN-112/DN-113/M-1060), OQ-3 (functional-update spread affine treatment, M-919), OQ-4 (struct-pattern exhaustiveness plus DN-104 seal). `Empirical` read against dev tip `46006994`; `Declared` for the proposed design until built plus differential-witnessed | **Draft** (2026-07-11) |
| **DN-124 — Vet-Harness Phylum Visibility: Partial Per-Nodule Verdicts, Phylum-Mode Vet Wiring, and the Measurement-Basis Honesty Question** (`docs/notes/DN-124-Vet-Harness-Phylum-Visibility-And-Measurement-Basis.md`) | a design-forward reasoner note working the gap-close-2 wave-2 vet-harness phylum-blindness finding forward to a ratified recommendation. Verify-first problem statement: the transpiler `--vet` loop and `gen/myc-drafts/regenerate.sh` check each emitted `.myc` in **oracle (single-file) mode**, which has zero phylum-import visibility, so a correctly-resolved cross-nodule `use checkty.Width;` validates CLEAN under `myc check --phylum` but FAILS under oracle mode — making the whole cross-nodule/cross-phylum lever family (Import, DN-122 external-trait impls, cross-phylum records) un-creditable even when the transpiler emits correctly. Ratifies **P-A** — sound partial per-nodule verdicts on `PhylumReport` via driver-level import-closure sub-phylum re-check that **reuses the kernel `check_phylum` unchanged** (zero kernel growth, KC-3), then switches the vet path to `myc check --phylum <dir>`; the **import-closure invariant** (a nodule is `Clean` only if its whole import closure is clean) is the load-bearing false-clean guard. Ratifies **M-A** — phylum-mode is the demonstrably-correct measurement basis (proven in-tree by `phylum_cross_nodule_reference_resolves`), but the `checked_fraction` jump on the switch is a **basis correction, not lever progress**: dual-report both bases one transition cycle, then re-baseline with `Δ_basis` attributed; the historical oracle §8 series is annotated, never rewritten (house rule #3). `Empirical` where read against `dev@b36ebdbe`/leaf `4f346da3`; `Declared` for the ratified-but-unbuilt mechanism | **Accepted** (2026-07-12, delegated ratification — maintainer authorized the orchestrator to "ratify based on objective reasoning and the project's needs/intents, keep to core principles, report results"; mirrors the DN-115/117/118/122 precedent. NOT Enacted (house rule #3) — Units 1–3 unbuilt, tracked as **M-1079**. Originally Draft, same day 2026-07-12) |
| **Glossary & Term Index** (`docs/Glossary.md`) | the dedicated, separately-maintained reference for unique Mycelium terms — a summarized Index over a detailed Glossary (fungal lexicon + honesty/architecture concepts), each entry citing its normative source | **Living** (2026-06-16) |
| **Language Reference & Tutorial** (`docs/reference/`) | user-facing docs for the full-language 1.0.0 docs gate (**M-735**): `language-reference.md` (full surface — lexical structure, nodules/phyla, types + ADTs, the guarantee lattice, functions/effects, expressions, match, swap, generics/traits, ambient paradigms, maturation/`thaw`, `wild`/FFI, keyword set, L0–L3 layers; grounded in `mycelium.ebnf` + the conformance corpus + `token.rs`, honest VR-5 staged-surface notes), `tutorial.md` (a complete program committed as the parser-verified fixture `accept/20-tutorial-classifier.myc`), `stdlib-api.md` (the **M-736** generated stdlib API docs — `lib/std` wired into `mycelium-doc`, `std.result` covered, coverage grows with E13-1), and `README.md` (index) | **Living** (2026-06-23; M-735/M-736) |
| **Nodule Header & Project Manifest** (`docs/spec/Nodule-Header-and-Project-Manifest.md`) | the structured in-file nodule header (closed-key `// @key: value`), the `mycelium-proj.toml` manifest (pyproject analogue), and explicit top-down inheritance; metadata ≠ identity (ADR-003), no ambient metadata (G2) | **Proposed** (2026-06-16; format choices flagged for sign-off) |
| **`mycfmt` Formatter Contract** (`docs/spec/Mycfmt-Formatter-Contract.md`) | the canonical formatter (M-364; M-142 grows up) as an **identity-preserving projection** (RFC-0001 §4.6/§4.8, ADR-003): three checked invariants (identity-preservation · idempotence · header-preservation), a never-silent error model (no partial rewrite, G2), the CLI + `[toolchain].format` reading, an `EXPLAIN` identity receipt, and an honest round-trip-safe v0 scope (refuse, never lossily reformat) | **Accepted** (2026-06-16; **enacted 2026-06-17** — `crates/mycelium-fmt`) |
| **`myc-check` Driver Contract** (`docs/spec/Myc-Check-Driver-Contract.md`) | the project-aware correctness/type-check driver (M-365; `myc-check` grows up): deterministic project resolution (manifest surface + deps + M-359 inheritance), whole-`phylum` diagnostic aggregation routed via the M-362 baseline (additive-only A1, EXPLAIN-able), honest per-op tags preserved (VR-5), CI exit semantics; trusted M-210 checker unchanged (KC-3) | **Accepted** (2026-06-16; **enacted 2026-06-17** — `crates/mycelium-check`) |
| **Lint + Auto-fix Contract** (`docs/spec/Lint-and-Autofix-Contract.md`) | the lint+fix tool (M-366): M-141 lints + RFC-0013 diagnostics + the RFC-0015 §9 "only logged — add a handler?" advisory as **actionable, reified, opt-in** fixes with a suggest/apply/**scaffold** boundary (a control-flow change is offered as a scaffold, never auto-applied — RFC-0014 I1/I5); hosts the M-363 §4.1 doc quality-bar lint (8 checks; unblocked by the §8 ratification); no silent rewrite (G2) | **Accepted** (2026-06-16; **enacted 2026-06-17** — `crates/mycelium-lint`) |
| **Security Checks Contract** (`docs/spec/Security-Checks-Contract.md`) | security checks as tooling (M-367): secrets (gitleaks) · supply-chain (cargo-deny/audit) · the **`wild`-block audit** (LR-9/S6 — inventory + ADR-014 `// SAFETY:` justification); the honesty crux — every finding cites *why*, a missing scanner is **reduced coverage, never a silent pass** (G2/VR-5); a new scanner is an ADR (KC-3) | **Accepted** (2026-06-16; **enacted 2026-06-17** — `crates/mycelium-sec`) |
| **Spore Build & Publish Contract** (`docs/spec/Spore-Build-and-Publish-Contract.md`) | packaging `mycelium-proj.toml` → `spore` (M-368; ADR-013): the build pipeline, the **identity-vs-metadata** split (ADR-003 — same code+deps ⇒ same spore hash), hash-authoritative dependency resolution, never-silent publish inputs (no partial artifact, G2), an `EXPLAIN` identity receipt; v0 single-project/hash-pinned (R2 wire-schema/signing/germination deferred) | **Accepted** (2026-06-16; **enacted 2026-06-17** — `crates/mycelium-spore`) |
| **Narrative & Automated Authoring Pipeline** (`docs/spec/Narrative-Authoring-Pipeline.md`) | the M-363 authoring pipeline: one content-addressed **doc IR → many renderers** (HTML/PDF/EPUB + JSON, no drift — ADR-003/G11); four projection generators; the §4.1 quality-bar lint; **§8 build stack ratified** (custom doc-IR + Typst + static HTML · Typst PDF · v0 single-version) | **Accepted** (2026-06-16; design ratified — building remains a separate task) |
| **Surface Stability Declaration** (`docs/spec/Surface-Stability-Declaration.md`) | the M-708 stage-1 surface audit: a per-feature catalogue of the L1 surface (generics, traits, effects, `wild`/FFI, phyla, grading, concurrency, static HOF, operators) marked **present** (12, with source/test refs) or **deferred** (with a never-silent refusal + forward issue ref each — dynamic HOF → M-704, angle-bracket ops → M-745, effect-budget → M-677, `consume`/`grow`/inherent-`impl` → M-664, runtime vocab → M-667/M-668, VSA/Substrate + `wild` exec → RFC follow-ups); audit found no silent-incorrect form (G2 holds) | **Draft** (2026-06-23; advisory audit, DN-17 posture) |
| **Narrative Capture & Automated Authoring** (`docs/notes/Narrative-Capture-and-Authoring.md`) | the capture discipline + pipeline for partially-to-fully automated **blog / language book / reference manual** authoring (free digital distribution) — synthesis from the cited corpus under the honesty rule; the append-only `docs/devlog/` fills the struggle/how gap | **Living — initial capture** (2026-06-16; full design = M-363) |
| **Lexicon Reference (v0.4)** | the three-tier vocabulary catalog (Surface/Runtime/Formal) with short mnemonic forms; extends DN-02 | **Draft** (review notes in ADR-012 §7) |
| **Example Programs Reference (v0.2)** | 20 worked examples demonstrating the lexicon and surface syntax against RFC-0001 semantics | **Draft** (grounding notes appended) |
| **Native-Ternary Forward-Compat (M-370)** | forward map: the ternary value-semantics contract → a future 3-state HW backend; the `ternary` dialect as the stub target (ADR-005 / R7) | **Living note** (2026-06-15) — doc + stub, no backend built |
| **RFC-0002 — Swap Certificate & Split Regime** | certificate, legal pairs, bijection semantics, shared checker | **Accepted** |
| **RFC-0003 — VSA Submodule Boundary** | boundary, guarantee matrix, sparsity refinement, manifest | **Accepted (r4)** — r3 §4.1 erratum; r4 §6 resonator decode params (RFC-0009) |
| **RFC-0004 — Execution Model & "Stable Component"** | MLIR backbone, shared checker, schedule-staged packing; **r2** adds §9 interpreted↔compiled continuum + build-target profiles (`interpret`/`--slim`/`--target`/`--fat`) + §10 open questions | **Accepted (r2)** |
| **RFC-0005 — Selection-Policy Language** | total cost-based policy + EXPLAIN | **Accepted** |
| **RFC-0006 — Surface Language & Term-Language Layering** | L0–L3 layering, syntactic honesty invariants, Rust-class-and-beyond capability targets; committed L3 = text syntax + projections co-equally (DN-09); Q8 `unsafe`-class spelling separately tracked | **Accepted** (r5, 2026-06-18; r4 layering/S1–S6/LR-1…LR-9/grammar ratified; Q1/Q3/Q6 discharged — DN-09 KC-2 verdict + RFC-0018 stage-1 grading; residual: Q8 `unsafe`-class L3 *spelling* — non-KC-2-gated, tracked separately — §10) |
| **RFC-0007 — The L1 Kernel Calculus** | ten-node term budget, registry data declarations, totality/`matured` gate, v0 elaboration fragment | **Accepted** (r4, 2026-06-15; v0 calculus §4.1–4.8 ratified; stage-1 grading / R7-Q1…Q4 / surface syntax deferred — §10; **§11 (2026-06-22, M-656)** discharges the §4.4 generics deferral → RFC-0019, pinning the stage-1 generics surface for `mycelium-l1` v1; **§12 (2026-06-22, M-658)** pins the stage-1 trait/`impl` surface (+ reserves `impl`) → RFC-0019 — append-only, no calculus change) |
| **ADR-010 — Verified-Numerics Foundation** | two bound kernels + shared certificate | **Accepted** |
| **ADR-011 — BoundBasis is universal** | `basis` is a companion of every `Bound` (supersedes RFC-0001 r1 §4.3) | **Accepted** |
| **ADR-012 — Layered Lexicon & Fungal Runtime Model** | three-tier lexicon + fungal runtime vocabulary (Surface/Runtime/Formal tiers) | **Accepted** (2026-06-20; all §7 flags resolved — see ADR-012 §8) |
| **Research Record 04 (T4.1–T4.6)** | evidence base (pass 4): concurrency, merge/mesh, mobility/placement, durability, failure, mode switching | Recorded → `research/04-runtime-concurrency-RECORD.md` |
| **RFC-0008 — Runtime & Concurrency Execution Model** | RT1–RT7 runtime invariants; deterministic-fragment-first; placement as the third policy site; lawful fusion; honest probabilistic guarantees; Runtime vocabulary grounded but reserved | **Accepted** (2026-06-16 — maintainer sign-off; RT1–RT7 and §4 model normative; enactment staged: M-353/M-354/M-356 budget/observability/concurrency slices; M-357 `mycelium-mlir::runtime` v0 fork/join; Phase 7 complete) **2026-06-21 — R1 `hypha`/`colony` L1 constructs implemented (Rust-first, pending ratification; M-666):** `colony { hypha … }` parses, type-checks, and lowers to real concurrency via `run_colony` / `Scope::join_all`; `run_interleaved` is the sequential trusted base (RT2 differential Empirical, not Proven); `colony` and `hypha` are now active keywords; RT7 enforced (colony cannot exit before all hypha complete); `ColonyError` variants are never-silent (G2). RFC-0008 stays **Accepted** (not Enacted — full enactment requires remaining R1 vocabulary M-667 + R2 planning M-668). |
| **RFC-0009 — Resonator-Network Factorization** | opt-in VSA factorization of bind products into unknown codebook factors; iterative resonator loop; probabilistic-only (`Empirical` ceiling, never `Proven`); never-silent termination verdicts; reuses `VsaModel`/cleanup/`EmpiricalProfile` (M-350) | **Enacted** (2026-06-23, M-350; `resonator.rs` present; never-silent `NoConverge` verdict; measured `MAPI_RESONATOR_PROFILE`; full convergence corpus ≥100-vector/regime is build-phase deferred) |
| **RFC-0010 — Decode-Methodology Selection** | choose the decode methodology (brute-force `Exact` vs resonator `Empirical` vs refuse) as a third site of the one RFC-0005 selection mechanism; reified, `EXPLAIN`-mandatory decision table over exact metadata; guarantee tag read off the chosen arm; cleanup-variant selection deferred (Hebbian dominates) (M-350) | **Enacted** (2026-06-23, M-350; `decode_select.rs` present; ADR-015 closed §8 `enum_budget`; Hebbian cleanup-variant is the normative default; other variants deferred out-of-scope) |
| **RFC-0011 — L0 `Match` & the L1-in-Core-IR Revision** | the named RFC-0001 revision (RFC-0006 §4.4 step 2): fold the L1 data-and-matching core (`Construct` + flat `Match` + content-addressed registry; WF6/WF7/WF8) into frozen L0 as **r3**, staged ahead of an r4 (`Lam/App/Fix`); flat `Match` is the kernel node (Maranget tree stays untrusted); unblocks M-320 leaf-emission + M-310 sync (M-320) | **Enacted (r3)** (2026-06-15; RFC-0001 → r3, code in `mycelium-core`/`-interp`/`-l1`, M-210 differential covers the data fragment; `Lam/App/Fix` → r4) |
| **RFC-0012 — Ambient Representation & Scoped Overrides** | a **surface-only** declared, scoped, **paradigm-only** representation default + override/conversion blocks, to offset honesty's verbosity (tension A) without a black box: two normative invariants — the ambient emits no `Swap` (I1) and resolution is observationally the identity (I2, identical L0 ⟹ identical hash) — defended by a meaning-preservation differential (NFR-7); cross-paradigm edges stay explicit `Swap`s, a missing one is an explicit `MissingConversion`; kernel untouched (KC-3); exported signatures are concrete reprs (ADR-016) (M-344) | **Accepted** (ratified 2026-06-16; design normative, enactment gated — M-344) |
| **RFC-0013 — Structured Diagnostics & Reified Error Policy** | additive presentation over explicit errors (never substitutive — I1): content-addressed diagnostic record, dual human/JSON projection (G11), graded levels + allowlisted detailed tier, the reified `on <ErrorClass> => {…}` policy, the representation-crossing audit view; routes → RFC-0008 sinks (§8, M-354); tooling-layer (KC-3) (M-345) | **Enacted** (2026-06-16; `crates/mycelium-lsp/src/diagnostics`) |
| **RFC-0014 — Declarative Error Recovery & Bounded Effects** | the recovery half: errors trigger functionality (fallback/retry/escalate/cleanup) via an explicit handling site (L0 `Match` — no new kernel node) + a reified policy; declared + **bounded** effects (the unified budget mechanism, §4.8/M-353); single-task boundary lifted to concurrency (§8/M-356); additive over the explicit error (I1), honest (VR-5) (M-352) | **Enacted** (2026-06-16; `crates/mycelium-lsp/src/recover`) |
| **RFC-0015 — Automatic Baseline Diagnostics & Recovery** | the DynEL *automated-baseline* layer over RFC-0013/0014: a zero-config baseline diagnostic/logging policy auto-derived from the structured mapping + auto-applied (QoL), light overrides → fully manual; honesty boundary — auto = additive presentation/logging (I1), auto-recovery is opt-in/declared/bounded (I3/I4/I5), reified + EXPLAIN-able (M-362) | **Enacted** (2026-06-16; `crates/mycelium-lsp/src/baseline.rs`; prior art in `research/06-…`) |
| **RFC-0016 — Core Library & Standard Library** | the standard-library **scope + contract**: the per-op contract every stdlib op must meet (C1–C6 — never-silent, honest tags, EXPLAIN, content-addressed, above-the-kernel, declared/bounded effects), the module taxonomy (Tier-A *differentiator* modules — swap/numerics/vsa/ternary/dense/select/diag/recover/runtime/spore/content; Tier-B *common* modules — collections/text/math/iter/error/io/fs/serialize/time/rand/cmp+convert/fmt/testing), the ring layering (KC-3), the per-module guarantee matrix, and the Rust-first→Mycelium-lang migration; decomposes into Phase-5 tasks M-510…M-534 (`docs/planning/phase-5.md`) (M-346/M-501) | **Enacted** (2026-06-21; 25 `mycelium-std-*` crates landed Rust-first, M-501–M-534/540/541 done; 23-module guarantee matrices asserted in tests; G2/VR-5 hold; self-hosting migration half stays M-502-gated; M-648 editorial sweep) **Accepted** (2026-06-17, per DN-07; full 23-module v0 taxonomy; §8 Q1/Q2/Q5/Q6 + `BF16→F32` resolved; Q3/Q4 deferred-with-direction; §7 discharged via `research/08`; **2026-06-20 erratum** — `spore` Ring 1/Tier A reconciled) |
| **RFC-0017 — Maturation Scope & De-maturation** | lifts `matured` from **per-definition** to **scope** granularity (ratifies DN-08): a `nodule`/`phylum` is matured via its header (`// @matured: true`), a program/package via the `mycelium-proj.toml` manifest; **`matured fn` is retired**; reserves **`thaw`** (Surface; `germinate` is taken by spore-germination, ADR-013) as the in-source de-maturation marker (keep one def interpreted in a matured scope). **Supersedes RFC-0007 §4.5 *granularity*** — the `matured ⟹ total` soundness argument is unchanged, just quantified over the scope (§4.2); reifies a per-scope `EXPLAIN`-able maturation record (the M-311 cert roll-up, §4.4) | **Enacted** (2026-06-21; `thaw`/scope-`matured` gate implemented + tested in `crates/mycelium-l1/`; DN-08 five questions closed; M-648 editorial sweep) **Accepted** (2026-06-18, maintainer sign-off) |
| **RFC-0018 — Stage-1 Static Guarantee Grading** | drafts the static graded judgment RFC-0006 Q3 / RFC-0007 §4.3 deferred: the guarantee lattice moved into the type system as a **graded coeffect modality** over the meet-semilattice (`Γ; pc ⊢ e : τ @ g`), with `Swap`+certificate the sole controlled-endorsement point (static VR-5). Surfaces the **implicit-flows decision** (pc-index vs data-lineage; R18-Q1) as a required maintainer choice + a pre-ratification research prompt; addresses R7-Q2 | **Enacted** (2026-06-22, **stage 1a** landed in `mycelium-l1` — M-663: `grade.rs` static grading pass; Accepted 2026-06-18; RP-2 discharged via `research/09`; R18-Q1 = **Design A** (data-provenance integrity), R18-Q4 = reference-at-type-level; supersedes RFC-0007 §4.3 stage-1 deferral, discharges RFC-0006 §8 Q3; stages 1b/2 future RFCs. Soundness = Declared-with-argument, not machine-checked) |
| **RFC-0019 — Traits & Parametric Polymorphism (LR-2)** | drafts the trait/typeclass + bounded-generics layer v0 deferred: familiar `impl Trait for T` surface, **dictionary-passing** elaboration to existing L1 nodes (kernel budget unchanged, KC-3), coherence (global-uniqueness/orphan rule under content-addressed identity). **Repr-polymorphism (LR-5)** and **guarantee-indexed methods (LR-6)** flagged-novel with research prompts | **Enacted** (2026-06-23, M-657/M-659/M-662/M-673; stage-1 surface complete incl. `checkty.rs`/`mono.rs`/`elab.rs`; trait checking + coherence + bounded generics + L0 elaboration via monomorphization + dictionary-free static resolution, EXPLAIN-reified; literal §4.5 runtime-dictionary `Construct` form deferred — gated on abstract-parameter `FieldSpec` trusted-core change; multi-param/assoc types deferred to v2; coherence stays Declared-with-argument) · **Accepted** (2026-06-18; R1/R2 discharged via `research/10`; coherence = orphan-rule + global-uniqueness + reject-overlap, the Repr-poly restriction set normative; newtype waivers rejected in v1, multi-param/assoc types deferred to v2. Soundness = Declared-with-argument, not machine-checked) |
| **RFC-0020 — The L2 Surface Term Language** | scopes the programmer-facing surface as **elaboration-defined** (no independent semantics — RFC-0006 §3): within-definition HM inference (monomorphic v0), content-addressed modules (ADR-003/LR-3), pattern sugar → Maranget-flat L1 `Match`, derived forms, the RFC-0012 ambient as the model invariant-preserving sugar; usability-first (DN-09 §3.2); a conformance-corpus plan | **Accepted (scoped)** (2026-06-18; DN-12; no research gate — the §4.1/§4.3/§4.4/§4.6/§4.7/§4.8/§4.9 core Accepted, deferred sections (§4.2/§4.5/R20-Q1…Q5) carved out, unblocking via the now-Accepted RFC-0018/0019 + RFC-0001 r5) |
| **RFC-0021 — Semantic-Level Projection Framework** | drafts M-380/FR-C1/G11: projections as total, inspectable **views** over content-addressed defs (RFC-0001 §4.6/ADR-003) — the same definitions render for human and **LLM-facing canonical** surfaces (FR-S5), the lever to lift the real-but-modest text-surface leverage (DN-09). Invariants P1–P6 (meaning/tags/identity preserved, S2/S6); gated on the G11-ergonomics + T3.6 research prompts | **Enacted (framework)** (2026-06-21; M-380 `LlmCanonical` renderer landed in `crates/mycelium-lsp/src/project.rs`, total over 11 L1 node kinds, P1–P6 unit-tested; LLM-leverage stays Declared/open in M-381 non-blocking track; M-648 editorial sweep) **Accepted (framework)** (2026-06-18; framework + P1–P6 + `LlmCanonical` design + §4.7 supersession Accepted; ergonomics/feasibility demonstrated in code; LLM-leverage carved out → RP-1/M-381, no result asserted) |
| **RFC-0022 — Web-Tooling Phylum** (`docs/rfcs/RFC-0022-Web-Tooling-Phylum.md`) | designs a `web` phylum (HTTP client/server/routing/JSON — nodules `web.http` / `web.json` / `web.route` / `web.server` / `web.client`); held to the RFC-0016 §4.1 C1–C6 never-silent contract throughout; v1 Rust-first as `mycelium-web`; the server surface (`colony { hypha … }`) gated on E7-2 (M-666); the typed `Json<T>` handler surface gated on E7-1 (M-657/M-659); evidence in `research/12-web-phylum-RECORD.md` (T12.1.x–T12.4.x) | **Accepted** (2026-06-21, maintainer ratification — RP-10 discharged clean against primary specs [RFC 9110/9112 · RFC 8259 · WHATWG-URL], `research/12` §8; design agreed, **Enacted** gated on the `mycelium-web` build + E7-1/E7-2; `web.server` on the Mycelium runtime, IDNA pinned-at-build [nontransitional, fail-closed], HTTP/2-3 + TLS + smuggling = v1 non-goals) **Draft** (2026-06-21; gated on RP-10 + E7-1/E7-2) |
| **RFC-0023 — Agent Development Kit Phylum** (`docs/rfcs/RFC-0023-Agent-Development-Kit-Phylum.md`) | designs an `adk` phylum (a Mycelium port of Google's Agent Development Kit — nodules `adk.agent` / `adk.tool` / `adk.session` / `adk.runner` / `adk.model`); **honesty-as-differentiator**: every tool/model output tagged Declared/Empirical, never Proven; every tool call a never-silent `Result`; wires to the existing LLM harness (`GrokLlmReport`); v1 Rust-first; evidence in `research/13-adk-phylum-RECORD.md` | **Accepted** (2026-06-21, maintainer ratification — RP-9 discharged + §3 concept-map repaired via §3.7 [ADK 2.0 graph Workflow Runtime; §3 pinned v2.3.0], `research/13` §6; design agreed, **Enacted** gated on the `mycelium-adk` build + E7-1/E7-2; runtime `mycelium-mlir::runtime`, `ToolError` budget→`TaskOutcome::BudgetExhausted`, Session snapshot-v0) **Draft** (2026-06-21; gated on RP-9 + E7-1/E7-2) |
| **RFC-0024 — Higher-Order Functions via Static Defunctionalization** (`docs/rfcs/RFC-0024-Higher-Order-Functions-via-Static-Defunctionalization.md`) | adds a minimal HOF surface — the function type `A -> B` + named top-level functions as first-class values — lowered by **static defunctionalization** in the L1 frontend (`mono.rs`), so the L0 kernel stays first-order (**no `mycelium-core` change — KC-3**); narrows RFC-0007 §4.4's no-function-values clause for the named-fn case (append-only); first consumer = `std.result` `map`/`and_then`/`fold` (M-649); scope boundary keeps closures / dynamic fn-flow / partial application / generic-fn-as-value as never-silent `Residual` (full Reynolds defunctionalization deferred) | **Accepted** (2026-06-28; ratified in-session; currying + generic-fn-as-arg IN SCOPE for M-704; §5 updated; → Enacted once M-704 lands) |
| **RFC-0025 — Operator Syntax** (`docs/rfcs/RFC-0025-Operator-Syntax.md`) | (Proposed; M-705/E11-1) adopts DN-23's hybrid — optional symbolic infix/prefix sugar (`a + b` → `add(a, b)`, `-a` → `neg(a)`) desugaring at parse time to canonical word functions, frontend-only, no kernel change (KC-3). Binding Rust-derived precedence table (§4.1) + desugaring map (§4.2); the desugared App node is the EXPLAIN record (§4.4). Implemented Rust-first in `mycelium-l1`; angle-bracket ops `< <= > >= << >>` deferred → M-745 (§4.3) | **Enacted** (2026-06-28; M-745 operator wiring landed Rust-first — PR #723, `crates/mycelium-l1`, `just check` green; stepped through Accepted first per house rule #3) |
| **RFC-0026 — Editor Syntax Highlighting Grammar** (`docs/rfcs/RFC-0026-Editor-Syntax-Highlighting-Grammar.md`) | (feeds E9-1/E16-1) the binding decision DN-24 deferred — §3.1 artifact scope (TextMate + tree-sitter + LSP semantic tokens ship at the gate; VS Code extension + Linguist → M-697), §3.2 the scope-name table (standard TextMate/tree-sitter/LSP names with a `.mycelium` suffix, over lexer-derived buckets), §3.3 the single-source-of-truth/drift contract (`tools/grammar/generate.py` + `just drift-check`). Implemented by M-731/M-730 | **Accepted** (2026-06-23; Draft→Proposed→Accepted; →Enacted when M-697 packaging ships) |
| **RFC-0027 — Memory Management and Reclamation** (`docs/rfcs/RFC-0027-Memory-Management-and-Reclamation.md`) | (feeds E12-1) the runtime memory/reclamation model — ownership/lifetime/reclaim semantics + the "no silent GC pause" honesty stance (G2). **Mechanism decided: reference counting** (LR-9 acyclicity *is* Perceus's garbage-free precondition — no cycle detector; single-owner intra-hypha, cross-hypha rides the affine channel §7.3) with the reclamation EXPLAIN record (§9), RC-unifies-reclamation+copy/mut (§10). **OQ-1 + OQ-4 now resolved by DN-32 (§12):** weak/partial sibling coupling (concurrent) default + strong opt-in; `rc==1` reuse EXPLAIN-record-only. RC-soundness `Proven`-modulo-LR-9; throughput `Declared`. **Status move Proposed → Accepted is GOVERNANCE-FLAGGED for maintainer ratification** (OQ-2/5/6 deferred non-blockers; `Enacted` not claimed — no in-repo impl/proof) | **Accepted-pending-ratification** (2026-06-25; Draft 2026-06-23 → Proposed 2026-06-24 → Accepted proposed 2026-06-25; flagged) |
| **RFC-0028 — FFI and System Interface** (`docs/rfcs/RFC-0028-FFI-and-System-Interface.md`) | (feeds E14-1) the build-time `@std-sys` capability model + the `wild` host-execution floor (lowers to `Op{wild:…}`, no new Core-IR node; dispatched via the prim-registry capability handle — DN-14 row 9 *executes*) + syscall binding strategy (io/fs/sys/rand/time, `Declared`) + the Mycelium-level `just safety-check` audit | **Accepted** (2026-06-23; §4.4 signed off 2026-06-28; DN-40 A1/A2/A3 COMMISSIONED before E14-1) |
| **RFC-0029 — AOT Optimization, Codegen Maturity & JIT** (`docs/rfcs/RFC-0029-AOT-Optimization-Codegen-Maturity-and-JIT.md`) | (Accepted, feeds E15-1) §5 resolved against the live `crates/mycelium-mlir/` (E6-1 `done`: JIT M-340 / BitNet M-360 / MLIR-dialect M-601 / three-way native diff M-602 already landed; ADR-019 Enacted; ADR-009 already sanctions JIT). §7 sanctions inlining/CSE/DCE as EXPLAIN-able never-silent passes (M-726 — the one genuinely-new mechanism), JIT as a never-silent first-class mode (M-727), the BitNet capability gate (M-728), and the mutant-witnessed `interp ≡ AOT ≡ JIT` durability gate (M-729); the interpreter stays the trusted base | **Accepted** (2026-06-23) |
| **RFC-0030 — Concrete Surface Grammar / L3 Ratification** (`docs/rfcs/RFC-0030-Concrete-Surface-Grammar-L3-Ratification.md`) | (Draft stub, feeds E11-1) a committed EBNF reference grammar + conformance corpus for the full L3 surface, resolving RFC-0006 Q3/Q8; relates RFC-0007/0020/0025 | **Enacted** (2026-06-27; M-745 wiring COMMISSIONED 2026-06-28) |
| **RFC-0031 — Self-Hosted Standard Library Composition** (`docs/rfcs/RFC-0031-Self-Hosted-Standard-Library-Composition.md`) | (Accepted, gates E13-1 — the heart of full-language 1.0.0) how the stdlib + core libs are written in Mycelium (`.myc`) and layer on the bare Rust core — §5 D1 irreducible-Rust boundary, D2 phylum layout, D4 surface-readiness-tiered migration order (Tier-0 core executable now; collections/text/math blocked on kernel prims — VR-5), D5 per-op stability bar; builds on RFC-0016 + `lib/std/result.myc`/`option.myc`/`cmp.myc` (M-715) | **Accepted** (2026-06-23; M-714) |
| **RFC-0032 — Kernel Self-Hosting Enablement Surface** (`docs/rfcs/RFC-0032-Kernel-Self-Hosting-Enablement-Surface.md`) | (Accepted, M-746 — gates E19-1) the kernel value-representations + primitive operations the self-hosted `.myc` stdlib bottoms out on to complete E13-1 Tier-1/Tier-2 — §5 D1 `eq`/`lt` prims, D2 binary arithmetic, D3 `Repr::Seq`, D4 `Repr::Bytes`, D5 width-generics → E11-1/`s10`, D6 **in `core 1.0.0`** (extends ADR-022 T1 — enacted append-only by the focused amending ADR-024), D7 sequencing; the kernel surface RFC-0031 §5 D4 sequences the blocked tiers behind | **Accepted** (2026-06-23; M-746) |
| **RFC-0033 — Value-Model Collections & Precision** (`docs/rfcs/RFC-0033-Value-Model-Collections-and-Precision.md`) | (Accepted, E20-1/M-785 — the value-model collections + precision design, ratified) §3 `Seq`/`Bytes` (aligned with RFC-0032 D3/D4), §4 Binary sign-free · Ternary arbitrary-width (`BigTernary`, **removes the cap** — `core::ternary` is already cap-honest) · Dense granularity-descriptor quant **in `Repr`** + scale arrays **in `Payload`** · VSA explicit element-space + block-sparsity + complex carrier; §6 swap/guarantee reconciliation **extends** the ratified **ADR-011** `bound.basis` (OQ-3 already closed, **not** reopened); §7 content-address identity + dogfood gate (single rehash M-780). Two decisions (Dense §4.3, VSA §4.4) deliberately **disagree** with the input draft on never-silent grounds. Evidence: `research/14-value-model-integration-report-RECORD.md` + `research/15-embeddonator-leverage-map-RECORD.md`; plan `docs/planning/value-model-implementation-plan.md` + `…-backlog.md`; decisions ADR-025…031 (all Accepted). **V1–V5 implementation (M-760…M-784) deferred post-1.0** | **Accepted** (2026-07-01, maintainer-ratified; was Proposed 2026-06-24; M-785) |
| **RFC-0034 — Tunable Certification & Transparency Modes** (`docs/rfcs/RFC-0034-Tunable-Certification-and-Transparency-Modes.md`) | (Proposed — the binding mechanism from the settled DN-29 deliberation) makes certification/hashing/tag machinery a **tunable policy** over RFC-0001/0002/0005: the knob matrix (§4); two **first-class modes** `fast` (default) / `certified` + `balanced` intermediate (§5); `global/phylum/nodule` resolution reusing **RFC-0012** scoping + a `@certification` attribute, no hash perturbation per ADR-003 (§6); the **provenance tag as an adjustable unit** (`fast` omits `Empirical`/`Proven`) + the **generation≠consumption** signal split (§7); the **compile/runtime phase split** — spores survive a cert-off runtime (§8); memory-safe-by-default + explicit per-use escape sharpening **ADR-014** (§9); the named `wrapping` Axis-B opt-out (§10); the never-silent mode invariant (§3). Implementation-focused — the charter/north-star reframe + whole-corpus honesty→transparency reword are carried by **ADR-032** and applied via the never-silent `tools/dn29_apply.py` + `docs/notes/dn29-amendment-manifest.json` (§13). Anchor: DN-29 | **Enacted (design-driven)** (2026-06-24; design ratified + corpus amendments landed/governing; runtime mode mechanism = paired TDD, pending — never claimed implemented, VR-5/G2) |
| **RFC-0035 — Security Scanning Toolkit** (`docs/rfcs/RFC-0035-Security-Scanning-Toolkit.md`) | (Proposed — the binding security-scanning design that binds **DN-30** + the maintainer's 2026-06-24 answers to DN-30 §7's five open questions) a native, inherited, scope-configurable security toolkit composed over the existing substrate (RFC-0001 inspectable Core IR + content-addressing, RFC-0002 refinement certificate, RFC-0034 §6 scoped resolution + honest-tag lattice, RFC-0013/0014 diagnostics/recovery, DN-28 reconstruction-on-render): **automated detection** of a v0 **fixed base of vuln classes WITH an extensibility seam** (§2/D1, never silently folded); **standard reporting** SARIF+CWE+OSV+VEX with **versioned-pinned, immutable-once-pinned** schemas (§3/D2, append-only); **two sinks** (CLI + registry) where the registry hosts **screened/anonymized** advisories as a **second content-addressed catalog** reconstructed-on-render (§4, tamper-evident); **honest, RFC-0002-certificate-backed safe auto-fix** with **per-class fix-strength + a pedantic mode** + a **certified patch registry** (§5/D3,D5; `Declared` fixes always human-gated, no black-box rewrites); **screening policy** configurable-with-defaults, **mandatory for high-security classes** (§6/D4); native + scoped reusing RFC-0034 §6 (§7); the five answers tabled as Decisions (§10). **Designs the toolkit; implements nothing** — every runtime claim is `Declared` for **E22-1** to discharge; the two **worked examples** (safe-fix refinement-cert; screening case study) are deferred pre-Accepted work, **not fabricated** (§9, VR-5/G2). Anchor: DN-30 | **Proposed** (2026-06-24; awaiting ratification) |
| **RFC-0036 — Kernel & Primitives Consolidation** (`docs/rfcs/RFC-0036-Kernel-Primitives-Consolidation.md`) | (**Draft** 2026-06-27 — capture-only, decides nothing; `rsm` F5) frames consolidating the kernel + its primitives toward a **frozen/locked/pinned 1.0 kernel** — minimal *because targeted* (only primitives, only what must be done in primitives) and the most heavily tested/benchmarked/chaos-engineered artifact in the project; includes the **"what must be a primitive vs built on top"** boundary review and the open structural question **one kernel vs several** (e.g. binary/ternary/embedding/VSA kernels). Grounded in KC-3, DN-39 (zero promotions, boundary unchanged), RFC-0001/0003. Deliverable (later): an RFC + research. Task M-805. | **Draft** (2026-06-27; capture-only) |
| **RFC-0037 — Surface-Grammar Deconfliction & Layout-Independence** (`docs/rfcs/RFC-0037-Surface-Grammar-Deconfliction-and-Layout-Independence.md`) | (**Proposed** 2026-06-27, M-809 — the binding enactment of DN-31; enacts no code) the kind-split bracket allocation (`[T]` type params/lists, `{N}` const/width **and repr types unchanged** — short keywords `bin`/`tern`/`emb`/`hvec`, `vec` rejected; `<>` operators only, `<=`/`>=` → `lte`/`gte`), `->` → `=>`, trit `0t`, the **`lambda`** keyword, and a **layout-independent grammar** (newlines formatting-only; `,`-delineation; type-vs-value position resolves the type-app-vs-list edge with no newline rule). **Supersedes RFC-0019 §4.1**; updates RFC-0030/RFC-0025 (resolves M-745). §7 open: exact delimiter set, short-keyword lexer status, full `lambda` semantics. The lexer/parser/corpus migration is the follow-on epic | **Proposed** (2026-06-27; ratification + migration epic pending) |
| **RFC-0038 — Inject-Mode Security Axis** (`docs/rfcs/RFC-0038-Inject-Mode-Security-Axis.md`) | (**Proposed** 2026-06-29 — ratifies the DN-64 §7 hot-inject dispositions OQ-K…OQ-Q; enacts no code) a hot-inject security axis **orthogonal** to the fast/certified cert axis (RFC-0034 §8): `loose` (local-dev — unsigned injection permitted, every injected call G2-tagged) vs **`inoculated`** (production — a valid `InjectCert` required, `InjectError::UnsignedCode` never-silent refusal, gating the interpreter-fallback path too). The **`InjectCert` IS the spore's signature component** (ADR-013 §2 comp. 4) — `myc-prepare` signs a spore that is simultaneously the deployable unit and the inject gate (signature over `content_hash ‖ vr4_attestation_digest`, fusing security with the VR-4 no-opaque-lowering attestation); a receiving colony verifies it valid/trusted/unexpired/unsuperseded against its **own** `TrustRoot` (germination-set, immutable). Signing authority project-scoped + graded by scope-of-work. §K.2/§L/§M (key-management detail, replay/expiry, inject-mode scoping) named as open R&D (M-836…M-842). **§8.4–§8.7 (2026-06-29 maintainer direction, M-847):** an **enforcement-granularity** axis (`whole`/`module`/`call` — `inoculated` is *not* per-call by default; `whole`-app compile/load-time signature is the application default), a **scope hierarchy** (global ⊃ … ⊃ line) with **auto-decoration** + **granular override** + a never-silent **default-plus-deviations manifest**, **defaults by project kind** (scripts/interpreted → `loose`; app → `inoculated`/`whole`; trusted-computing → `inoculated`/`call`), and **opt-in per-inject signing** on the interpreted path (`BadSignature` added). **§8.8 (M-849):** a **colony trust topology** — **controller mode** (a redundant, regionally-partitioned controller stack distributing the `TrustRoot`, for enterprise-scale central management) vs **masterless** (each colony self-manages trust against its own store) vs **node invalidation/blacklist** (config-driven, never-silent) — framed by the no-black-box inspectability thesis (`reveal`/`EXPLAIN`/provenance) for safe AI mesh auto-development. `Declared` | **Accepted** (2026-06-29; maintainer-approved — **design ratified, mechanism unbuilt** → every mechanism claim stays `Declared` until Enacted Rust-first; open R&D §K.2/§L/§M + M-849 carry forward) |
| **RFC-0039 — Native Dense & VSA Codegen** (`docs/rfcs/RFC-0039-Native-Dense-and-VSA-Codegen.md`) | (**Accepted** 2026-06-30, maintainer-ratified — the design vehicle ADR-034 §6 names for native codegen of `Repr::Dense` + `Repr::Vsa` and the dynamic-VSA JIT; the gap RFC-0029 §3 excludes) §5.1 native Dense element-wise lowering (`mycelium-dense` reference) with the ADR-030 quant granularity / accumulator width / packing schedule as inspectable `Meta.physical` (DN-01) — F32/BF16 un-quantized first, widening to the full int/fp8/TF32 set as **E20-1** lands `QuantDesc` (OQ-2/OQ-4); §5.2 native VSA bind/bundle/permute over the 1.0.0-mandatory standard models **MAP-I/BSC/HRR/FHRR** (niche SBC/MAP-B extend post-mandate — OQ-3) honoring ADR-031's element-space/sparsity/complex carrier, preserving the RFC-0003 §4.1 per-op guarantee matrix **only where the checked basis holds** (VR-5 — single-op MAP-I `bundle` Proven via `proofs/lh-bundle`/`capacity.rs`; the multi-hop M-832 work is in-progress research that emits undischarged obligations and never stamps Proven); §5.3 the dynamic-VSA JIT (data-dependent dim, runtime model selection, cleanup/resonator loops) as an explicit never-silently-selected `ExecMode`; §5.4 the M-210-checked, `cargo-mutants`-witnessed, interpreter-referenced honesty contract; §6 records the ADR-009 dynamic-VSA JIT deferral lift append-only — OQ-1 resolved: §6 IS the vehicle, no separate ADR-009 amendment. Removes the live Dense/VSA refusals (`llvm.rs:2170`, `dialect/native.rs:349-350`). Ratifies design only — asserts no implementation; M-853/854/855 stay design-gated on it; the full quantized/element-space coverage is additionally gated on **E20-1**. Feeds E25-1 (M-853 Dense · M-854 VSA · M-855 dynamic-VSA JIT) | **Accepted** (2026-06-30, maintainer-ratified; the four OQs resolved — was Proposed 2026-06-30; M-853) |
| **RFC-0040 — `Vec` List-Literal Desugaring** (`docs/rfcs/RFC-0040-Vec-List-Literal-Desugaring.md`) | (M-977; 2026-07-03) type-directed elaboration: a `[e1,…,en]` literal against a cons-list-shaped `Vec[T]` (a nullary nil + a recursive `Cons(A, Self)`) desugars to the `Cons` chain and is re-checked as it; `Seq{T,N}` and non-list ADTs are untouched/refused (no ambiguity). **No grammar/parser/L0 change** — frontend elaboration only (`checkty::check_list`), a lowering onto the frozen kernel (freeze-safe, DN-56 §6). Behavior-neutral by **AST identity** (`Empirical` — the desugared body is byte-for-byte the hand-written chain). Delivers the each-item-closed `lib/std` static tables (`matrix()` etc.); resolves DN-82 FLAG-976-1. | **Accepted** (2026-07-03; Rust-first implemented + green, `Enacted` is the maintainer's — VR-5; pursued under the maintainer's explicit directive) |
| **RFC-0041 — Recursion-Depth Safety: Explicit Work-Stack Evaluators + Unified Deterministic Budget** (`docs/rfcs/RFC-0041-Recursion-Depth-Safety-Explicit-Work-Stack-And-Unified-Budget.md`) | (M-979 solve-now; grounds on `research/29`) implements DN-84's decided direction: convert the L1 evaluator + L0 reference interpreter to explicit `Vec<Frame>` work-stack machines (mirroring the differential-validated AOT env-machine); one **global deterministic** depth budget (default 4096) + a **byte** co-ceiling; fine-grained, runtime-gated `stacker::ensure_sufficient_stack` growth (Rev 2 — supersedes the coarse per-pass `maybe_grow` of Rev 1); **iterative `Drop`** on the frozen core via a DN-39 review (closes the drop-bomb); **TCO** in evaluators with EXPLAIN-able elided frames; frontend stays guarded+grow (close the guard holes; `check_list` flat-loop). **No new L0 node/prim/surface — freeze-clean** (KC-3/DN-56). Amends DN-05 §4.4 (deterministic floor + dynamic headroom). Seven-wave staged plan; secure-by-design DoD. **Rev 2** hardened by an 8-lens adversarial review (4 Critical + 15 High resolved: source-call-boundary depth metric, error-parity differential, real memory ceiling + process arena, TCO post-work precondition, full iterative-destruction class, extract-budget-only, within-freeze channel). | **Accepted** (2026-07-03, maintainer-ratified Rev 2; per-stage Enacted as the 7 waves land differential + error-parity green — VR-5) |
| **Standard-Library module specs** (`docs/spec/stdlib/`) | the per-module design specs decomposing RFC-0016: each fixes a module's scope + boundary + exported-op surface and ships the load-bearing **guarantee matrix** (RFC-0016 §4.5 — ops × {tag · fallibility · effects · EXPLAIN}) proving the §4.1 contract per op; a uniform `_TEMPLATE.md` enforces single-template conformance; cross-module seams reconciled in the index §5; lives above the kernel (KC-3) (M-510…M-534) | **Living index** (2026-06-17; first wave **landed** — Drafts `core`/`swap`/`ternary`/`dense`/`select`/`content` + `iter`/`math`/`error`/`cmp`/`fmt`, plus the M-502 self-hosting-readiness gate; remainder anticipated). **2026-06-18 — Batch P5-A Rust-first enactment:** the seven Tier-A/Ring-0 crates `mycelium-std-{core,ternary,swap,dense,select,vsa,content}` (M-515/517/516/518/519/513/523) now exist as code (guarantee matrix asserted in tests; 230 tests; fmt/clippy/test green), so those specs read **implemented (Rust-first), pending ratification** — *not* `Accepted`; the Mycelium-lang migration half (M-502-gated) stays open. **2026-06-18 — Batch P5-B Rust-first enactment:** the twelve Tier-B/Ring-2 commons crates `mycelium-std-{collections,error,cmp,iter,math,text,fmt,testing,io,fs,time,rand}` (M-511/527/532/526/525/524/533/534/514/528/529/531) likewise land as code (matrices asserted; 722 tests; fmt/clippy/test green) and read **implemented (Rust-first), pending ratification**; the effectful modules FLAG the audited `wild`/FFI floor to the `std-sys` phylum (M-541) and pure `std` stays leak-free (LR-9). **2026-06-18 — Tier-A completion Rust-first enactment:** the four remaining Tier-A/Ring-1 crates `mycelium-std-{numerics,diag,recover,spore}` (M-512/510/520/522, #153/151/156/163) land as code (matrices asserted; full `clippy --all-targets`/`test --workspace` green, 1883 tests), reading **implemented (Rust-first), pending ratification** — `recover` is the Rust-first half (self-hosting stays P5-C/M-502), `spore` the library/manifest half (native deploy stays M-620). A maintainer-resolved FLAG extracts the canonical RFC-0013 record into a new **`mycelium-diag` kernel crate** (small, deliberate trusted-base growth; the std crate re-exports it, KC-3). This completes the Tier-A differentiator surface (only Phase-7-gated `runtime` M-521 remains). **2026-06-20 — Maintainer ratification (DN-07):** the **23** implemented Rust-first specs move *pending ratification → `Accepted`* — guarantee matrices asserted in tests, never-silent fallibility holds, open §7/§8 questions are design/scope calls (not contract violations), and **no guarantee tag was upgraded without a checked basis** (the dense/math/vsa/numerics `Proven` rows were verified/aligned by M-377/M-512 — VR-5). Two pre-ratification fixes landed: `cmp` trait naming (`MycEq`/`MycOrd`/`MycPartialOrd`) documented in §3 (parity note, not honesty), and `spore` §7-Q1 ring placement reconciled to Ring 1/Tier A via the RFC-0016 §4.2 erratum. **Deferred (stay `Draft`):** `runtime` (Phase-7 / RFC-0008 constructs) and `self-hosting-readiness` (M-502 gate; verdict "not yet established") |
| **ADR-013 — `spore` Is the Deployable Unit** | spore = content-addressed code+values+metadata; the RFC-0003 manifest is one component; `spore(v)` is the degenerate case | **Accepted** |
| **ADR-014 — `unsafe` Rust policy** | relax the workspace `unsafe_code` lint from `forbid` to permitted-but-warned (explicit + justified + `// SAFETY:` annotation); never-silent (G2); a new scanner / new `unsafe` site is itself an ADR (KC-3) | **Accepted** (2026-06-15) |
| **ADR-015 — Decode `DEFAULT_ENUM_BUDGET` = 4096** | fixes the RFC-0010 decode-selector default at the *guarantee-maximal* enumeration budget (4096 = `MAPI_RESONATOR_PROFILE.max_capacity`) — every in-regime request is also enumerable, so the brute-force `Exact` arm dominates the whole validated envelope — over the cost-optimal ≈128; resolves RFC-0010 §8 | **Enacted** (2026-06-23, M-350; applied in `decode_select.rs`; closes RFC-0010 §8) |
| **ADR-016 — Interpreted↔compiled ABI** | dispatch a compiled stable component by its content hash; cross `CoreValue`s in the RFC-0001 §4.8 self-describing wire form (canonical), zero-copy fast-path later; resolves RFC-0004 §10 OQ-1 | **Accepted** (ratified 2026-06-16) |
| **ADR-017 — Hot-inject recompiled definitions** | hash-keyed dispatch + content-addressed dynamic linking; immutable-by-construction (a change is a new hash under a new entry) dissolves the atomicity hazard; recompile = the changed dependency-closure by hash reachability; resolves RFC-0004 §10 OQ-2; in-process prototype in `mycelium-mlir::inject` (M-341) | **Accepted** (ratified 2026-06-16; prototype building) |
| **ADR-018 — Versioning policy** | per-crate `0.x` SemVer across the workspace + **source-only** distribution (no crates.io publish during the design phase); the `CHANGELOG` `[Unreleased]` → release-cut mapping; grounded in ADR-007 + the squash-only linear-history discipline + RFC-0017 §4.1 (M-383; release-plz dry-run wiring M-384) | **Enacted** (2026-06-23, M-383/M-384; workspace pinned `0.0.0`; CHANGELOG `[Unreleased]` discipline in force; release-plz dry-run wired; first release cut deferred as a separate future act) |
| **ADR-019 — libMLIR Toolchain** | libMLIR (`mlir-opt`/`mlir-translate`, version-matched to the installed LLVM) is the build dependency of the OFF-by-default `mlir-dialect` feature — the real bit/trit element-wise `arith`/`func`→LLVM lowering (`mycelium-mlir::dialect::native`, M-601); durably provisioned via `scripts/setup-mlir.sh` (the dedicated `just setup-mlir` recipe, kept out of the default `just setup`; skip-gracefully); the default build/test stay green without it; resolves M-348 on Linux (M-603) | **Enacted** (2026-06-23, M-348/M-603; `scripts/setup-mlir.sh` present; `mlir-dialect` feature OFF by default; default build green without libMLIR; real dialect lowering present M-601; MLIR version drift/platform provisioning = ongoing maintenance, not open design) |
| **ADR-020 — `runtime`/`colony` Phylum Placement** | resolves RFC-0016 §8-Q4: `runtime` and `colony` live in a dedicated `runtime` phylum (`crates/mycelium-std-runtime`) with a thin `std.runtime` re-export facade inside `std`; construct-by-construct activation at the Phase-7 gate; v0 R1 surface (`Colony`/`Scope`, `Task`, `TaskCtx`, `Poll`, `SweepOrder`, `Deadlock`, `Network`, `Sender`, `Receiver`, `TrySend`, `TryRecv`) with per-op guarantee matrix; all RFC-0008 §4.5 reserved vocabulary (`hypha`/`fuse`/`xloc`/`cyst`/`graft`/`forage`/`backbone`/`mesh`/`tier`/`reclaim`) stays Glossary ⟂; `runtime` v0 is `wild`-free; Option C hybrid (phylum decouples release cadence + embedded targets from `std`; facade preserves Tier-A discoverability); grounded in RFC-0008 §§4.3/4.5–4.7, RFC-0016 §§4.1–4.3/8-Q4/8-Q6, DN-06, ADR-003/013/014/018; implements M-521 (Rust-first implemented, 21 tests, 16-row guarantee matrix) | **Enacted** (2026-06-20; M-521 implementation landed, PR #277) |
| **ADR-021 — 1.0.0 Release-Readiness Gate** | defines what "the kernel stabilizes" concretely means: the kernel/core 1.0.0 release criteria, partitioned into Gate A (honesty-integrity + durability — High findings closed, mutants/fuzz, `just check` incl. `cargo deny`, KC-4 budget) and Gate B (decision/external — RFC-0003 reconciliation; **KC-2 verdict now MET**, DN-09 §10 determinate retention ratio); surface language scoped to a tracked `1.x`; Phase-3+ maturation (native codegen, JIT, projections, self-hosting) explicitly out of scope; grounded in the 2026-06-14 deep-review remediation roadmap §5, ADR-018, Foundation §6; defines the gate, does NOT declare a release (house rule #3) — archived verbatim, now on the `archive` git branch (was docs/archive/adr/ADR-021-1.0.0-Release-Readiness-Gate.md in-tree, 2026-07; extracted to the branch 2026-07-09) | **Superseded by ADR-022** (2026-06-23; was Accepted 2026-06-21 — kernel Gate A/B preserved as ADR-022 track T1) |
| **ADR-022 — Full-Language 1.0.0 Release-Readiness Gate (dual-version)** | supersedes ADR-021; establishes a **dual-version model** (`core` axis ⟂ `lang` axis — the Rust kernel may reach `core 1.0.0` first on ADR-021's now-track-T1 criteria, while the **full language** stays `< 1.0.0` until its whole gate closes); defines the full-language 1.0.0 Definition of Done as tracks **T1–T9** → epics **E10-1…E18-1** (core gate · surface · runtime · **stdlib in Mycelium** · FFI · native AOT · toolchain · docs/stability · self-hosting); headline criterion: the stdlib + every lib/phylum beyond the bare Rust core is written in Mycelium, stable, fully usable; establishes 3 conventions (user stories + Definition of Done on every epic/issue + ADR/RFC/DN; MIT-only first-party licensing); program map DN-25 | **Accepted** (2026-06-23; maintainer-ratified scope; →Enacted at the tagged full-language 1.0.0) |
| **ADR-023 — Stability & API-Compatibility Guarantees** | (feeds E17-1/T8; **authored M-737**) what "stable" means at full-language 1.0.0 — §3.1 four-layer API-compatibility scope (surface syntax · Core-IR/cert/interp · LSP wire · Rust crate API) with explicit carve-outs; §3.2 dual-version model (full-language 1.0.0 is a *release-event* — tag + CHANGELOG + ADR-022 gate record — **not** a crate/workspace version, ADR-018 upheld); §3.3 release-based never-silent deprecation (≥1 minor, removal at 2.0.0); §3.4 **MIT-only** legal-readiness gate (six first-party *shipped* violations — `lib/std/result.myc` + 5 `examples/**` programs — Apache→MIT fixed; `mycelium-proj` test fixtures excepted by design). All §3 claims `Declared` (VR-5). →Enacted at the tag (M-738) | **Accepted** (2026-06-23; M-737 — was Proposed/Draft 2026-06-23; →Enacted at the full-language 1.0.0 tag) |
| **ADR-024 — Core 1.0.0 Gate (Track T1) Scope Amendment** | (amends ADR-022 T1) the focused, house-rule-correct (supersede-to-change-criteria) capture of RFC-0032 §5 D6: adds epic **E19-1** (the kernel self-hosting-enablement surface — `eq`/`lt` prims, binary arithmetic, `Repr::Seq`, `Repr::Bytes`) to T1's Definition of Done, so the stdlib is fully `.myc`-self-hosted at the `core 1.0.0` tag. The ADR-021 Gate A/B rows in T1 are **unchanged + remain met**; `M-703` now `depends_on` E19-1; ADR-022 carries only an append-only "amended by ADR-024" pointer. All criteria `Declared` until each E19-1 leaf lands differential-tested (VR-5). →Enacted with ADR-022 T1 at the `core 1.0.0` tag | **Accepted** (2026-06-23; amends ADR-022 T1) |
| **ADR-025 — Seq/Bytes length lives in the type** (`docs/adr/ADR-025-Seq-Bytes-Length-In-Type.md`) | (E20-1; grounds RFC-0033 §3.1) `Seq{elem,len:u32}`/`Bytes{len:u32}` — length in the `Repr` type; growables are higher structures over a fixed-capacity `Seq` (consistent with RFC-0032 D3/D4). One-way door on the fixed-size invariant (A1) | **Accepted** (2026-07-01, maintainer-ratified; was Proposed 2026-06-24) |
| **ADR-026 — Seq elements are repr-values only** (`docs/adr/ADR-026-Seq-Elements-Are-Repr-Values.md`) | (E20-1; grounds RFC-0033 §3.1.1/§3.1.4) homogeneous repr-value elements only; heterogeneous/`Vec<Struct>`/`Map` are ADTs above the kernel; `Map` never a kernel repr. One-way door (A2) | **Accepted** (2026-07-01, maintainer-ratified; was Proposed 2026-06-24) |
| **ADR-027 — `get` returns (Repr, in_bounds bit); Option lifted** (`docs/adr/ADR-027-Get-Returns-Inbounds-Bit-Option-Lifted.md`) | (E20-1; grounds RFC-0033 §3.1.2) `get → (elem_repr, in_bounds:Binary{1})`; single blessed `lift_option` adapter above the kernel; mirrors the ratified RFC-0032 D1 `eq → Binary{1}` + Bool-lift (A3) | **Accepted** (2026-07-01, maintainer-ratified; was Proposed 2026-06-24) |
| **ADR-028 — Binary is sign-free; signedness is operations** (`docs/adr/ADR-028-Binary-Is-Sign-Free.md`) | (E20-1; grounds RFC-0033 §4.1; **amends the input draft**) `Binary{width}` stores a bitvector with no signedness; shared `add/sub/mul/neg`, signedness-split `div/cmp/shift/overflow`; `signed`-in-`Repr` is a separate one-way door (default: no). Matches SMT-LIB/Z3 (B-Binary) | **Accepted** (2026-07-01, maintainer-ratified; was Proposed 2026-06-24) |
| **ADR-029 — Ternary arithmetic is arbitrary-width now** (`docs/adr/ADR-029-Ternary-Arithmetic-Is-Arbitrary-Width.md`) | (E20-1, high priority; grounds RFC-0033 §4.2) digit-serial `BigTernary` (grows instead of overflowing) reconciled into `core::ternary` (DRY `add_with_carry`; no new crate, no duplicate `Trit`); **removes the cap** — `core::ternary` is already cap-honest (the silent-overflow defect is `embeddonator`'s, do-not-lift); `Limb27`/`PackedTernary` YAGNI follow-on (B-Ternary) | **Accepted** (2026-06-24; maintainer-ratified — V0 `BigTernary` landed #535) |
| **ADR-030 — Dense quant is a granularity descriptor in Repr; scales in Payload** (`docs/adr/ADR-030-Dense-Quant-Granularity-Descriptor.md`) | (E20-1; grounds RFC-0033 §4.3; **disagrees with the input draft**) expand `Dtype` (int/fp8/tf32); `QuantDesc{scheme,symmetric,scale_dtype}` **in `Repr`** (content-address identity), scale/zero-point **arrays in `Payload`**; a scalar scale is silently wrong for per-block. Dequant `Bounded` cert **extends ADR-011** `bound.basis` with block structure. Content-address one-way door (B-Dense) | **Accepted** (2026-06-24; maintainer-ratified — impl in the E20-1 rehash, pre-persistence) |
| **ADR-031 — VSA element space in Repr; block-sparse + complex carrier** (`docs/adr/ADR-031-VSA-Element-Space-Block-Sparse-Complex.md`) | (E20-1; grounds RFC-0033 §4.4; **disagrees with the input draft**) `VsaElem{Binary,Bipolar,Integer,Real,Complex}` + `VsaSparsity{Dense,SparseGlobal,SparseBlock}` in `Repr::Vsa`, `HypervectorC` `Payload` arm; unblocks FHRR/MAP-I/SBC/BSDC the old shape can't honestly hold; `embeddonator/block_sparse.rs` is the reference impl. Content-address one-way door (B-VSA) | **Accepted** (2026-06-24; maintainer-ratified — impl in the E20-1 rehash, pre-persistence) |
| **ADR-032 — Tunable certification supersedes always-on; transparency reframe** (`docs/adr/ADR-032-Tunable-Certification-Supersedes-Always-On-and-Transparency-Reframe.md`) | (Proposed; the superseding ADR for RFC-0034) supersedes the **unconditional** reading of **SC-3/FR-M3** + RFC-0001 §3.4/§4.6, RFC-0002 §2, RFC-0005 §2 — they hold **at the active mode** (`certified`), the mode itself never-silent (mechanisms unchanged); reframes **"honesty" → "transparency & auditability"** whole-corpus (mechanism/lattice/VR-5/G2 unchanged — only the wording); repositions the **north star** toward a fast, memory-safe, ergonomic language with certification optional (the Foundation **mission sentence is already transparency-framed** — unchanged); **sharpens ADR-014** to safe-by-default + explicit per-use escape; adds append-only **§-end footnotes** to RFC-0001/0002/0005 + ADR-010/011/013/016/017. Amendments **applied** 2026-06-24 (21 amendments / 13 files via `tools/dn29_apply.py`); the runtime mechanism it adopts is RFC-0034's paired TDD (pending, never claimed done). Grounds RFC-0034; anchor DN-29 | **Enacted** (2026-06-24) |
| **ADR-033 — Abstract-Function-Field for Dynamic Dispatch** (`docs/adr/ADR-033-Abstract-Function-Field-For-Dynamic-Dispatch.md`) | (**Proposed** 2026-06-27, M-810 — DN-37 Q3 "plan ahead"; enacts no code, gated on maintainer KC-3 sign-off) designs the `FieldSpec::Fn` abstract-function-typed field that unblocks the RFC-0019 §4.5 runtime-dictionary form (dynamic dispatch) — the **one** polymorphism form that cannot fully monomorphize (DN-55). **KC-3-significant** trusted-base growth (like ADR-024). **FLAG-1 (open soundness):** arity-only encoding hashes two different function types identically — requires a checked soundness argument *or* a minimal type descriptor before Enacted (a freeze-gate item per DN-56). Reified/`reveal`-able dispatch (no hidden vtable); three-way differential verification | **Accepted** (2026-06-28; FLAG-1 resolved-pending-impl — Path A full-sig encoding; → Enacted once full-sig lands) |
| **ADR-034 — Full-Language 1.0.0 Gate (Track T6) Re-Gating: Native AOT** (`docs/adr/ADR-034-Full-Language-1.0.0-Gate-T6-AOT-Re-Gating.md`) | (amends ADR-022 T6; maintainer 2026-06-30) the focused, house-rule-correct (supersede-to-change-criteria) **reversal of ADR-022 §8 Q4**: re-gates epic **E15-1** (native AOT) INTO `lang 1.0.0` as a **hard gate row**, scope expanded to **full-language native-codegen coverage** (closures · non-tail/mutual recursion · `trit.mul` · `Swap` · Dense · VSA · JIT for dynamic VSA/HDC), delivered "through the lowers." Umbrella epic **E25-1** (M-850…M-863); `M-738` now `depends_on` E15-1; ADR-022 §3/§5/§8 Q4 carry append-only "re-gated by ADR-034" pointers. The interpreter stays the trusted-base reference; the AOT path stays outside the kernel (KC-3); all coverage claims `Declared` until each leaf lands differential-checked (VR-5). →Enacted with ADR-022 at the `lang 1.0.0` tag | **Accepted** (2026-06-30; amends ADR-022 T6) |
| **ADR-035 — Full-Language 1.0.0 Gate (Track T4) Scope Amendment: Stdlib-in-Mycelium** (`docs/adr/ADR-035-T4-Stdlib-in-Mycelium-Scope-Amendment.md`) | (amends ADR-022 T4; maintainer 2026-07-01) the focused, house-rule-correct (supersede-to-change-criteria) **narrowing** of ADR-022 track T4: the `lang 1.0.0` bar is met by the documented **stable-API freeze** (**DN-66**, Draft→Accepted by this ADR) + the **core-lib self-host slice** (M-714…M-719), **not** full RFC-0031 §5 D6 retirement of all 26 `mycelium-std-*` crates — grounded in DN-66's finding that zero crates clear the D6 trigger today (six same-named `.myc` nodules are structurally disjoint prototypes; `mycelium-std-runtime` is load-bearing). Full D6 retirement **deferred to the post-1.0 long-term arc** (ADR-022 §10; new backlog issue minted, `status:todo`), mirroring how §8 Q1 already narrowed T9 and how ADR-024 narrowed T1. ADR-022 §5 T4/§8 Q1 carry append-only "narrowed by ADR-035" pointers; RFC-0031 §5 D6 carries an append-only scope note (mechanism unchanged). →Enacted with ADR-022 T4 at the `lang 1.0.0` tag | **Accepted** (2026-07-01; amends ADR-022 T4) |
| **ADR-036 — Dogfooding and Public-Release Strategy** (`docs/adr/ADR-036-Dogfooding-and-Public-Release-Strategy.md`) | (additive — does NOT amend ADR-022 §5/§8 Q1 criteria; maintainer 2026-07-01) fixes the `lang 1.0.0` **tag** and the project's **public release** as two distinct milestones: the tag is cut on the **Rust reference implementation** — self-hosting gates it only at the existing **core-lib self-host slice** (ADR-022 §8 Q1, unchanged); **comprehensive dogfooding** (progressively rewriting the whole toolchain/stdlib/kernel *in* Mycelium, beside the Rust originals — E18-1's full scope beyond the slice) is a first-class **within-1.0.0**, non-tag-gating, **parallel** track; each Mycelium reimplementation is **Rust≡Mycelium differential-validated** (extending the interp≡AOT≡JIT discipline, RFC-0029 §7.5/M-210) and **replaces** its Rust original only once tested/benched/validated and maintainer-satisfied; the repository **stays private** until dogfooding is complete + validated, with the **public release** gated on that completion — refining the trigger condition **DN-27** (Draft, untouched) deferred to "a future ADR." ADR-022 §8 Q1/§10 carry append-only "see ADR-036" pointers (their text unchanged); E18-1 and `docs/planning/self-hosting-port-ledger.md` carry clarifying, non-status-changing notes. →Enacted at the **public-release** milestone, not the tag | **Accepted** (2026-07-01; additive, not a §5 amendment) |
| **ADR-037 — Spore Registry v0 Remote Backend: GHCR/OCI Dense-Map** (`docs/adr/ADR-037-Spore-Registry-v0-Remote-Backend-GHCR-OCI-Dense-Map.md`) | (backend decision — does NOT amend any ADR-022 criterion; maintainer 2026-07-01) fixes the spore registry's **remote/networked backend**: a published spore is an **OCI 1.1 artifact** in the **GitHub Packages container registry (GHCR)** (`ghcr.io/<owner>/<phylum>:<version>`), decomposed into the **DN-28 dense-map** — each source object → one BLAKE3-addressed OCI blob (deduped by digest), the dense-map DAG → the OCI config blob, `name@version` → the OCI tag; resolve is the DN-28 §3 fetch-and-verify (every object BLAKE3-checked, `spore_id` recomputed via the canonical `content_address` — never-silent, G2). `oras` is the v0 wire-transport driver behind an `OciTransport` trait (pure-Rust client is append-only future work). Realizes the direction **DN-28** (Draft, advisory) recorded and refines its one sketched detail (GitHub *Releases* → the Packages *container* registry the release strategy names); DN-28 gains an append-only forward pointer and is otherwise unchanged. Grounds: ADR-036 (host packaging in GitHub Packages, no crates.io), ADR-003/ADR-013, DN-28 §1–3, OCI 1.1, KC-3 (no new runtime dep), G2/VR-5. Tracked as **M-871** (epic **E26-1**). →Enacted on a local + live-GHCR round-trip of the example phyla | **Accepted** (2026-07-01; backend decision) |
| **ADR-038 — Pragmatic Dogfooding: the Function-First Release Strategy** (`docs/adr/ADR-038-Pragmatic-Dogfooding-Function-First-Release-Strategy.md`) | (strategy decision, authored 2026-07-01 from the maintainer's session directives — **Accepted, 2026-07-01, maintainer-ratified; FLAG-V1/V2 resolved — public sub-`1.0.0`/`0.x` at usability; `1.0.0` = fully-rewritten-where-appropriate**) records the North Star **"Rust where appropriate, Mycelium everywhere else"** (pragmatic dogfooding, not zero-Rust dogmatism) and the **function-first** strategy: **Phase I → reach functional usability, then go public at a `0.x`** — the public release is gated on functional usability and is **version-independent** (§2.8 — happens at whatever semver fits, **well before `1.0.0`**, NOT at a version milestone); close the below-grammar enabler gaps (readiness-§0 list: binary arithmetic/signed ops, dense/vsa prims, scalar-float `Repr` route (ii), `Substrate`/`consume` execution, R2-lite vocabulary, plus `myc run`/string-literal/`hash.*`) so a Mycelium program exercises the whole surface + stdlib end-to-end; repo **private + `0.0.0` + `publish = false`** until the flip. **Versioning axis (§2.8):** the public semver **tracks the Mycelium-rewrite progress**, climbing `0.x → 1.0.0` in the open, with **`1.0.0` ≡ "fully rewritten into Mycelium (where appropriate) and 100% operational"** (Phase II's terminal); **for now the version stays `0.0.0` and the concrete semver scheme is deferred until publish-time.** **Phase II → post-public progressive rewrite** — compiler self-hosting a **deferred, doubly-conditional aspiration** (only if demonstrably better on stability/perf; only after the transpiler is 100% polished). **Refines ADR-036 §2.4** (append-only: release gate = usability, version-independent, not Rust-replacement; §2.1–§2.3 unchanged) and **records ADR-036's supersession of RFC-0031 §5 D1's "compiler stays Rust forever"** (append-only note there; D1 operative through Phase I). Transpiler doctrine: progressive hardening, pre-port polish, manifest transcoding only where ROI-positive — accelerant, never gate. Float route (ii) coordinated with the deferred ADR-030/031 doors for a **single rehash** at first value-persistence (RFC-0033 §7 honored). Execution doctrine (§2.7): **Fable-class models reserved solely for planning + complex design; implementation + lighter work on Opus/Sonnet/Haiku scoped to task complexity**, PM prep (user stories + DoD) before any implementation agent. **Resolved by the maintainer 2026-07-01 (§2.8):** the `lang 1.0.0` label collision (FLAG-V1 — public release is sub-`1.0.0`/`0.x`, so no collision) and whether `1.0.0` requires compiler self-hosting (FLAG-V2 — `1.0.0` = fully dogfooded/self-hosted/rewritten-where-appropriate). Companion roadmap: `docs/planning/road-to-1.0.0-and-mycelium-rewrite.md` (revised same day from the `rcp` plan) | **Accepted** (2026-07-01, maintainer-ratified; FLAG-V1/V2 resolved — public sub-`1.0.0`/`0.x` at usability; `1.0.0` = fully-rewritten-where-appropriate) |
| **ADR-040 — Scalar-float value form (route ii): first-class `Repr::Float`, IEEE-754 binary64** (`docs/adr/ADR-040-Scalar-Float-Value-Form-Route-II.md`) | (M-895; kickoff `enb` Gap A) fixes the float *design* inside ADR-038 §2.6's already-decided route (ii): `Repr::Float{width}`, **F64/binary64 only** at introduction on a frozen-tag width registry; IEEE-754 **round-to-nearest-even only** (rounding is a property of operations, ADR-028 parallel); **canonical quiet-NaN** at construction, bit-distinct signed zeros; in-band propagating specials (±inf/NaN, FLAG-2) with **never-silent conversion boundaries** (float↔int/parse → explicit `Option`/error); content-address impact documented-not-spent (single E20-1 rehash deferred to first value-persistence, RFC-0033 §7). Kernel entry via the ADR-038 §2.6 double gate (this ADR + DN-69). `Exact`/`Empirical`/`Declared` per-claim | **Enacted** (2026-07-02 — the value form landed M-896…M-900; was Accepted 2026-07-02 maintainer-ratified, drafted Proposed same date; append-only) |
| **ADR-042 — Rust-Base Freeze Now, Full Mycelium Self-Hosting (Kernel Included)** (`docs/adr/ADR-042-Rust-Base-Freeze-Mycelium-First-Expansion.md`) | (maintainer directive) records the **Rust-base freeze + Mycelium-first expansion** policy over two horizons: **(a) NOW** — freeze new Rust surface (new functionality authored in `.myc`); **(b) END-STATE** — the entire first-party project **including the kernel** is rewritten to `.myc` (**zero foreign first-party languages** by the DN-88 decomposition gate). Rust kernel is the current trusted base, itself slated for a `.myc` rewrite as the last self-hosting step. One sanctioned exception: `mycelium-tero`'s Rust PoC (M-1015→M-1018), then rewritten (M-1019). North Star + wild-free-kernel aspiration `Declared`. **Enacts nothing** — freeze/port/kernel-rewrite are ongoing | **Accepted** (2026-07-07; `Accepted → Enacted` only when the whole first-party project is `.myc`) |
| **ADR-043 — Rust Retirement & Legacy Archival** (`docs/adr/ADR-043-Rust-Retirement-And-Legacy-Archival.md`) | (maintainer directive) records how a Rust crate/lib is **retired** once its `.myc` port is fully validated: the retire-when-proven gate, the dedicated **archive branch** preserving the full legacy Rust for posterity + audit, and the per-crate housekeeping that follows removal. **Enacts nothing** — retirement is per-crate as ports clear the gate | **Accepted** (2026-07-07; `Accepted → Enacted` only when the whole migration is complete and the archive branch holds the full legacy — same posture as ADR-042) |
| **ADR-044 — Async Runtime (tokio + axum) for the mycelium-tero HTTP Front** (`docs/adr/ADR-044-Async-Runtime-For-Tero-HTTP-Front.md`) | (M-1017 / DN-87 §2.3) adopts **`tokio` + `axum`** as the async runtime + web framework for the `mycelium-tero` HTTP API front — the **first async runtime in the workspace**. Scope confined to `mycelium-tero` (`tools` tier); the kernel and every other crate stay synchronous/std-only. `Empirical`/`Declared` per-claim | **Accepted** (2026-07-07; `Accepted → Enacted` once the front lands with `cargo build`/`cargo test` green, `cargo deny` green, `THIRD-PARTY-LICENSES.md` regenerated) |
| **ADR-045 — Whole-Project Unfreeze (L0→L3 grammar + kernel + stdlib lexicon) for Early Gap-Closure** (`docs/adr/ADR-045-Kernel-And-Lexicon-Unfreeze-For-Early-Gap-Closure.md`) | (maintainer directive, 2026-07-10) temporarily + boundedly **UNFREEZES the whole project** — the L0 Core IR (RFC-0001), the L1 kernel primitive/type set, the L2/L3 surface grammar / projections, and the stdlib public-API "lexicon" — so the language can be **optimized while its expressibility gaps are closed early**, before the DN-88 decomposition turns the same closure into a hundred-module lift, in service of the **zero-hand-port north star**. Amends via append-only status pointers (decision bodies unchanged): ADR-042 §2.1(a), DN-56 §9/§6, DN-66 §2, and lifts RFC-0001's L0-frozen floor; the ADR-042 §2.1(b) END-STATE + the DN-39 TCB-admission boundary + the never-silent/honesty discipline are **retained**. Re-freeze on §2.4 (the DN-99 residual worklist M-1024…M-1034 exhausted + a DN-56/DN-76-successor re-score green + a follow-up ADR). §4 argues the case honestly (Option B is the runner-up a short worklist would have compelled). `Empirical`/`Declared` per-claim | **Accepted** (2026-07-10, maintainer-ratified whole-project unfreeze; `Accepted → Enacted` only when the §2.4 re-freeze conditions are met — the window is open) |

## 2. Dependency DAG

```
Survey ─► Foundation ─► RFC-0001 (r5) ─► { RFC-0002, RFC-0003, RFC-0004 (r2), RFC-0005 }
                               │
                               ├─► DN-01 (Resolved) ─► RFC-0001 §4.1, RFC-0004 §5
                               │
  Surface/term track:          └─► RFC-0006 (r5) ─► RFC-0007 (r4) ─► RFC-0011 (r3 Enacted → r4 in RFC-0001)
                                        └─► RFC-0018 (stage-1 grading) · RFC-0019 (traits) · RFC-0020 (L2 surface) · RFC-0021 (projections, M-380)

VSA decode track:      RFC-0003 ─► RFC-0009 (resonator factorization) ─► RFC-0010 (decode-method selection; reuses the ONE selection mechanism)
Diagnostics/recovery:  RFC-0013 (diagnostics) ─► RFC-0014 (recovery + bounded effects) ─► RFC-0015 (automatic baseline)
Ambient & ABI:         RFC-0012 (ambient) · ADR-016 (interp↔compiled ABI) ─► ADR-017 (hot-inject)
Runtime:               RFC-0008 (RT1–RT7 runtime & concurrency) ─► ADR-020 (runtime phylum placement, M-521)
Standard library:      RFC-0016 (scope + per-op contract) ─► stdlib module specs (docs/spec/stdlib/) ─► Phase-5 `std-*` crates
Maturation:            RFC-0007 §4.5 ─► RFC-0017 (maturation scope & de-maturation; supersedes the granularity)

ADR-010 ─► RFC-0001 §4.7,  RFC-0002 (swap bounds),  RFC-0003 (VSA bounds);   ADR-011 (BoundBasis is universal)

Shared machinery (decided):
  • ONE certificate checker   ⇄  RFC-0002 (swaps) & RFC-0004 (interp-vs-compiled)
  • ONE selection mechanism   ⇄  RFC-0002 (swap target), RFC-0004 (packing schedule) & RFC-0010 (decode method)
  • TWO bound kernels (ε, δ)  ⇄  ADR-010 → RFC-0001/0002/0003   (one shared certificate)
```

## 3. What the research resolved (decisions now baked in)
- **KC-1 (VSA-in-core viability): PASSED → confirmed (build) 2026-06-09.** Proven non-asymptotic bundling bounds exist (Clarkson-Ubaru-Yang 2023; Thomas-Dasgupta-Rosing 2021) → VSA stays in core with honest `Proven` tags for MAP-I/sparse `bundle`. (T0.2) The confirming Liquid-Haskell `bundle` probe (`proofs/lh-bundle/`, M-001) is **SAFE** (Z3 discharged), ratifying the cited-theorem + checked-instantiation strategy.
- **Bounds: two kernels, one certificate** — ε via affine arithmetic (Daisy/FloVer), δ via union-bound/apRHL; `strength` composes by meet. (ADR-010 / T0.1)
- **Packing: schedule-staged**, not in the type; cost-model+exhaustive selector over the fixed bitnet.cpp set (I2_S/TL1/TL2). (DN-01 / T1.4)
- **Sparsity: declared class = static refinement; capacity = axiomatized-theorem + checked instantiation; observed sparsity = runtime metadata.** (T1.3)
- **Backend: MLIR backbone → LLVM**, Rust interpreter as reference + trusted base. (T1.5)
- **Validation: one translation-validation certificate checker** for swaps *and* interpreter-vs-compiled equivalence; graded (differential testing + per-artifact TV for stable components). (T1.1)
- **Bijection: `LosslessWithinRange`** (total bijection impossible at fixed widths), `Option`-typed inverse, Exact-within-range, never silent. (T2.1)
- **Selection: total, non-learned cost policy + mandatory EXPLAIN**; avoids the DB cardinality-estimate trap because Mycelium's statistics are exact metadata. (T2.3)
- **VSA guarantee matrix** per model × operation encoded into RFC-0003; **HRR/FHRR unbind is the residual `Empirical` weak link.** (T1.2)

## 4. Remaining experiments (small; for the build phase, not blockers to design)
- ~~**LH bundling-bound instantiation**~~ — **DONE (2026-06-09).** MAP-I `bundle` capacity refinement encoded in Liquid Haskell (`proofs/lh-bundle/`, M-001); LH reports **SAFE**, Z3 discharged all constraints — ratifies the cited-theorem + checked-instantiation strategy (ADR-010 / KC-1). *(RFC-0003 §5)*
- **E1** staged-packing perf over the 5 schemes — **measured in Phase 3 (M-303).** · **E3** wrong-layout-tag soundness vs NFR-7 *(RFC-0004 §8)* — open. · **E4** LLM surface comparison ("packing in type" vs absent) — **subsumed by KC-2 / M-002; verdict proceed (DN-09).**

## 5. Build status — the live phase ladder
The corpus is Accepted and the build is underway; the dependency-ordered, priority-tagged tasks live in `tools/github/issues.yaml` (+ `idmap.tsv`) and the phase plans in `docs/planning/phase-*.md`. Current state:
- **Phases 0–3 (done):** the confirming LH probe; the Core IR + Rust reference interpreter + kernel data structures; the certified binary↔ternary swap + the single certificate checker; the `ternary` MLIR dialect + native LLVM/JIT; the VSA submodule (per the §4-matrix tags); the L1 calculus, projections, and acceleration.
- **Phase 5 (Rust-first done; self-hosting open):** the standard library — **25** `mycelium-std-*` crates implementing RFC-0016 (guarantee matrices asserted in tests); all 25 specs **ratified to `Accepted`** (2026-06-20 DN-07: 23 specs; 2026-06-21: `runtime` v0 R1 surface + `sys`). Only `self-hosting-readiness` (M-502 gate doc) stays `Draft`. The **E7-1 L1 stage-1 surface is complete incl. M-673 monomorphization → generics/traits run; DN-14 §3 rows 6/7 (`bounded generics` / `trait interfaces`) → `present`**. Self-hosting (M-502) is **not yet established**.
- **Phase 7 (done):** the RFC-0008 runtime/concurrency model (RT1–RT7).
- **Phases 4, 6, 8 (in progress):** the interpreted↔compiled ABI + AOT env-machine completion; native MLIR→LLVM codegen + deployable spores; the toolchain & release-engineering gates.
- **Zero-hand-port program (planning, 2026-07-10):** `docs/planning/zero-hand-port-delta-ledger.md` is the living, Draft register of the remaining delta between the ~85% hand-expressibility ceiling and the ~7.8% measured auto-transpile floor (`checked_fraction`), grounded in a five-layer analysis swarm — supporting inventories under `docs/planning/zero-hand-port/`; its L4/L5 idiom + structural design lands as **DN-109** (Accepted); the native-translation companion taxonomy lands as **DN-110**/**DN-111** (both Accepted). Force-multiplier progress on the filed issues: **M-1041** Scope-A (the pilot `ExprVisitor`/`TypeVisitor` over `mycelium-transpile`'s emit/map dispatch, `in-progress` — the `mycelium-l1` walker half + `prim_map.rs`/`Pat` dispatch remain), **M-1042** (structured source-tree-mirroring output + path-qualified nodule names, `done`), **M-1044** (the remap manifest folded into `BatchSummary`, `done`) — all three landed on `dev` 2026-07-10.

This index remains the map the board points back to.

## 6. Agent tooling index

`docs/api-index/` — committed, auto-generated agent-facing code index (Wave-4A, M-392..395; 2026-06-19):
- `index.json` — machine-readable symbol table (symbol, kind, crate, file:line, summary). `Empirical/Declared` heuristic — source is ground truth; use for navigation only.
- `INDEX.md` — grep-friendly table grouped by crate, for agent context lookups.

Regenerate with `just docs-index` (runs `tools/docgen/code_index.py`). The committed index must stay current; `just check` enforces this via a drift gate (`scripts/checks/doc-index.sh`). The orchestrator regenerates and commits the index after every octopus merge that touches a public API. See `CLAUDE.md §Auto-generated docs & the agent index` for the ownership and `doc_refs` grammar.

`docs/lib-index/` — committed, auto-generated self-hosted `.myc` reference index (M-1004/M-1005; kickoff trx2 E-C, 2026-07-06), the `docs/api-index/` analogue for `lib/std/` + `lib/compiler/`:
- `index.json` — machine-readable symbol table (phylum, nodule, kind, file:line, signature, summary, tag)
- `INDEX.md` — grep-friendly table grouped by phylum/nodule, for agent context lookups (`docs/lib-index/INDEX.md#<nodule>` anchors)

`Empirical/Declared` heuristic — source is ground truth; extraction limits (an unrecognised top-level construct, a missing nodule marker, a malformed header) appear in the `flagged` section, never silently dropped. Regenerate with `just lib-index-gen` (runs `myc-doc lib-index`, `crates/mycelium-doc/src/lib_index.rs`). The committed index must stay current; `just check` enforces this via a drift gate (`scripts/checks/lib-index.sh`, `just lib-index`). The integrating parent regenerates and commits the delta after any change under `lib/std/` or `lib/compiler/`.

`docs/tero-index/` — committed, auto-generated **whole-corpus Layer-1 citation index** (M-1015; DN-87 / E39-1 / `mycelium-tero`, 2026-07-07) — the same pattern generalized over docs (RFC/ADR/DN/spec/guide/planning, with status + guarantee tag + section anchors), research records, `tools/github/issues.yaml` (+idmap gh-numbers), `CHANGELOG.md` entries, and the skills:
- `index.json` — machine-readable rows (stable anchor, kind, file:line, status/tag where the source declares one, verbatim-ish summary)
- `INDEX.md` — grep-friendly table for agent context lookups

`Empirical/Declared` heuristic — source is ground truth; unextractable constructs go to the `flagged` section, never dropped. `docs/api-index/` and `docs/lib-index/` are referenced as sibling indices, never duplicated. Regenerate with `just tero-index-gen` (runs the published, checksum-pinned `tero-index` binary from `tzervas/tero-rs` — `crates/mycelium-tero` was extracted there and is no longer an in-tree crate; see `tools/tero-rs/PROVENANCE.md` and `scripts/fetch-tero-index.sh`); drift-gated in `just check` (`scripts/checks/tero-index.sh`, `just tero-index`). Integrator-regenerated, never hand-merged.

`docs/sugar-index/` — committed, auto-generated **surface-sugar / lowering registry** (M-1058, `tools/grammar/sugar.yaml` + `tools/grammar/sugar_index.py`; landed `dev` PR #1403, 2026-07-10) realizing DN-38 §6's per-feature Lowering Map as a drift-gated artifact rather than hand-maintained prose:
- `tools/grammar/sugar.yaml` — hand-authored source of truth (like an ADR/RFC/DN, NOT generated): one row per `token.rs::keyword()` identifier keyword (45/45), plus `glyph_sugars` (DN-102's `?`) and `not_lexed_sugars` (DN-38 §5's `reveal`), each tagged `build_status` (`Exact`/`Proven`/`Empirical`/`Declared`/`Gap` — VR-5) and cross-cited to its defining DN.
- rendered `docs/sugar-index/` — the committed, grep-friendly projection.

`sugar_index.py` mechanically cross-checks `sugar.yaml` against `token.rs::keyword()` (reusing `generate.py`'s `STRUCTURAL_KEYWORDS`, not re-derived) and fails loudly on drift. Regenerate with `just sugar-index-gen`; drift-gated in `just check` (`scripts/checks/sugar-index.sh`, `just sugar-index`). **`native_strategy` column populated (2026-07-11, PR #1422, M-1058 follow-up — supersedes the prior "deferred" note here, not a rewrite):** all 47 rows (45 keywords + `?` glyph + `reveal` not-lexed) classified against **DN-111**'s ratified canonical taxonomy via the new **`/native-translate`** skill (**M-1056**, `.claude/skills/native-translate/SKILL.md`) — 35 `NativeEquivalent`, 3 `IdiomaticRemapping`, 1 `Approximation`, 8 honestly `unclassified # needs-review` (no fabricated per-row guesses, VR-5).

### Reader guide — README topic docs (`docs/guide/`)

The root `README.md` is a lean landing page; its long-form content lives in linked topic docs
(added 2026-07-01, README/docs decomposition wave): `why-and-design.md`,
`guarantees-and-verification.md`, `workspace-map.md`, `comparisons.md`, `repository-structure.md`,
`status-and-roadmap.md`, `decisions-and-reading-order.md`, `glossary.md` (README-scope; canonical is
`docs/Glossary.md`), and `contributing-and-provenance.md`. Tooling long-form similarly split out:
`tools/llm-harness/{MODEL-ACQUISITION,TERMUX-SETUP,GROK-HARNESS}.md` and `experiments/KC2-RUNBOOK.md`.
`docsite-preview.md` (added 2026-07-11, docs-access wave) is the visual preview of `just docs-site`'s
local browsable docsite — screenshots of the home page, corpus nav tree, a code-bearing page, and a
rendered decision doc, each in both the light and the real (not capture-time-override) dark theme;
regenerated by `just docs-assets` (`docs/assets/`, drift-gated in `just check`).

## 7. Maintenance
Append-only with status transitions (Draft/Proposed/Preliminary → Accepted → Superseded), mirroring the ADR discipline. Keep `Proven | Empirical | Declared` tags honest per VR-5 — per model/op, never in aggregate. New non-asymptotic results may *upgrade* a tag; absence keeps it `Empirical`.
