# Rust Reference Completion and Acyclic Dependency Graph — MOVED

**This plan was revised (2026-07-01) into the two-phase, function-first umbrella roadmap governed
by ADR-038:** see **`road-to-1.0.0-and-mycelium-rewrite.md`** (same directory).

Content mapping (nothing dropped — see the roadmap's meta-changelog): Workstream A (acyclic-deps
hardening) → **H0**; the §8a below-grammar gap table → **H1** (the functional-usability enablers);
Workstreams B–G remainder → the **H2** lanes; grammar-stability items → the **H2a** mass-porting
gate; E18-1 porting → opportunistic Phase-I (§6) plus Phase II (§7).

This stub preserves the old path for external references (e.g. PR #913 and its review thread).
