# Course-correction program — 2026-07-18

| Field | Value |
|---|---|
| **Status** | Active execution program (maintainer-directed 2026-07-18, in-session): apply the alignment corrections identified by `../alignment-assessment-2026-07-18/ALIGNMENT-REPORT.md` against the committed steer pack (`../design-steer-2026-07-17/`), then fix the component repos to build, then transpile readiness → `*-myc` delivery → dual-side validation + apples-to-apples metrics → releases. **Human review comes post-fix**; no mid-way stop points, so every maintainer-grade choice is recorded in §2 and mirrored in the blockers file — proceed-on-recorded-default, never silent (G2). |
| **Honesty** | This ledger is `Declared` program state. Decision artifacts minted by this program land **Draft** (H1/H2 — no agent-side `Accepted`). Verification claims in the phase log carry their evidence class. |
| **Grounds** | `../design-steer-2026-07-17/PROGRAM-HANDOFF-DESIGN-STEER-2026-07-17.md` (the approved plan) · `../alignment-assessment-2026-07-18/` (findings F1–F19) · maintainer directive of 2026-07-18 (execute course correction; validate Rust + Mycelium; metrics; releases) |

## 1. Phase map

| Phase | Work | State |
|---|---|---|
| **A** | Monorepo alignment corrections: steer pack committed (A1) · record fixes (A3) · Phase-0 audit ledger (A2) · Phase-1 Draft captures (A4) · deviation captures + blockers (A5) · G-8 ledger entry (A6) | **complete 2026-07-18** (see phase log) |
| **B** | 46 component repos: workspace roots (train v0.464.0), git-pinned cross-repo deps in topo order, toolchain/supply-chain/CI/CROSS-REF/docs scaffolding, umbrella upgrade, per-repo green + identity suite | **complete 2026-07-18** (see phase log) |
| **C** | AX-core waves W-A..W-D in the monorepo; delta propagated to affected components via the §6.2-5 umbrella-PR protocol | **complete 2026-07-18** (W-A..W-D + C5, see phase log) |
| **D** | Transpile readiness: closable gap classes iterated, M-1006 remeasure, per-component DoD per steer Phase 4 | **complete 2026-07-18** (bounded pilot-scoped pass; see phase log + `PHASE-D-READINESS.md`) |
| **E** | `*-myc` delivery, leaf-first: graduated `lib/` nodules routed to twins where they exist; vetted drafts + honest gap ledgers where not; differential witnesses | **complete 2026-07-18** (see phase log) |
| **F** | Validation both sides + metrics report (perf/stability/QA/UX/DX; VSA/GPU rows desktop-held) | **complete 2026-07-18** (see phase log; report `docs/measurements/course-correction-dual-side-2026-07-18.md`) |
| **G** | Releases: per component + `*-myc` + umbrella (tag v0.464.0, GitHub Release, tarball); monorepo promotion PRs staged, terminal squash held for review | **staged 2026-07-18** — tag pushes 403-blocked by session policy; full manifest + cut procedure in `RELEASE-v0.464.0.md` (see phase log) |

## 2. Recorded defaults (maintainer-grade choices made to keep executing; ratify or reverse at post-fix review)

| # | Choice | Basis | Steer clause affected |
|---|---|---|---|
| D-1 | **Keep the executed 46-repo seam topology** (no re-extraction to PARTITION scope groups) | Maintainer directive wording: "correcting the alignment of the rust component repos … so they all properly work and compile" — the existing repos are the object | H§6.1 topology (deviation captured as Draft DN) |
| D-2 | **Clean-slice seeds stand; history deviation ratified as Draft** (filter-repo re-extraction offered post-review, not executed; monorepo retains all history) | Compile-ability is the stated goal; history re-extraction is orthogonal + layerable later | H§6.2-1 |
| D-3 | **AX-core W-A..W-C in scope**, sequenced: components fixed from archive first, AX-core lands in monorepo, delta propagated via umbrella-PR protocol | Steer gates transpile readiness on AX-core; §6.2-5 protocol gets exercised | H§5, H§6.2-5 |
| D-4 | **Transpile delivery is honesty-laddered**: graduated ports (differential-witnessed) where they exist; drafts + gap ledgers where not; coverage stated at measured strength | VR-5; M-991 verdict (instrument, not bulk porter); current all-7 `checked_fraction` 28.7% `Empirical` | H§7 per-component DoD |
| D-5 | **Version train v0.464.0** lockstep across components/`*-myc`/umbrella | Steer §6.2-2 "lockstep version train v0"; next minor over monorepo `0.463.1` | H§6.2-2 |
| D-6 | **Releases** = git tag + GitHub Release + `git archive` tarball per repo; **no crates.io publishing** (ADR-018 posture unchanged); monorepo terminal squash **held** for human review | Maintainer: "ensure releases are cut for each"; repo release policy is deliberately source-only | — |
| D-7 | **PR #1705 grows** with monorepo course-correction work (same designated branch); component-repo PRs agent-merged after review + green per the autonomous-PR workflow | Harness branch mandate; maintainer: "human review will come post fix" | — |
| D-8 | **`mycelium-lang` umbrella retained** (U1); the H§6.1 `mycelium`-as-front conversion **deferred** — it conflicts with the maintainer's standing requirement that `tzervas/mycelium` contain everything in the components | Assessment F8 tension, surfaced not silently resolved | H§6.1 front repo |

## 3. Blockers / ratification queue (EXPRESS-ORACLE pattern — maintainer answers collect at post-fix review)

| ID | Item | Default in force |
|---|---|---|
| CC-B1 | Which 2026-07-17 program governs long-term (steer pack vs PROGRAM-SELFHOST-DECOMPOSE) — supersession capture needed | Steer pack treated as the approved plan for this program (maintainer directive 2026-07-18); formal supersession doc left Draft |
| CC-B2 | Ratify D-1 topology + D-2 history deviations (Draft DN minted in A5) | In force |
| CC-B3 | Front-repo end-state (`mycelium` thin front per H§6.1 vs monorepo-keeps-everything) | Deferred (D-8) |
| CC-B4 | Phase-1 capture ratifications (Swap Ergonomics DN, DN-141, RFC-0013 amendment, RFC-0034 footnote, retention spec, W-1 capture) | All land Draft |
| CC-B5 | Version-train number v0.464.0 | In force |
| CC-B6 | G-8 `PolicySlot` uncapped logs (assessment F13): cap with the §1.4 retention store (Phase C), not ad hoc | Ledgered; code unchanged until C |
| CC-B7 | Monorepo integration→main terminal squash | Held for human review |
| CC-B8 | DN-80 §4 family-table re-derivation (per-family row sums lag the audited file totals since the M-966 row; the 2026-07-18 W-B Reconcile re-pinned file totals with fn-level attribution but deliberately did not re-derive 41 family rows ad hoc) | Ledgered in DN-80's 2026-07-18 Reconcile row; queued for a dedicated re-audit lane |
| CC-B9 | X3 `regime_type_lie` hard refusal: wiring it breaks three shipped, tested behaviors (bare-typed `dec` swaps in `differential.rs`/`runnable_gate.rs`/bench corpus). Accept the break now, or wait for X9 `to:`-elision as the non-breaking wiring point? | Classification + Diag builder landed, tested, unwired (DN-141 Appendix C); shipped behavior preserved until the maintainer decides |

## 4. Phase log (append-only)

| When | Entry |
|---|---|
| 2026-07-18 | Program opened. A1 done: steer pack committed verbatim (`../design-steer-2026-07-17/` + PROVENANCE). |
| 2026-07-18 | **Phase G STAGED (blocked-op disclosure) — program execution complete.** Annotated `v0.464.0` tags were prepared for all 91 component/twin repos + the umbrella, but **every tag push was refused by the session's scoped git relay (HTTP 403 on `refs/tags/*`** — branch pushes permitted, tag pushes not; a policy boundary handled per the blocked-op protocol: not retried, not circumvented, not reported as done). Additionally this session's GitHub toolset has **no release-object creation capability**. Deliverable instead: **`RELEASE-v0.464.0.md`** — the complete 92-row release manifest (repo → verified rev), the notes text, and the exact per-repo cut procedure, executable verbatim from any tag-authorized environment (maintainer desktop qualifies). Monorepo promotion: PR #1705 → working tier held for maintainer review; staging-tier + terminal-squash promotions follow it (CC-B7); the monorepo releases via the squash, not this train. **All seven phases (A–G) of the 2026-07-18 course-correction directive are now executed to the limit of this session's authority; the residuals are precisely: (1) maintainer review of PR #1705 + the ratification queue CC-B1..B9, (2) the tag cut per the manifest, (3) the desktop-held heavy tiers.** |
| 2026-07-18 | **Phase F COMPLETE** (leaf commit `135a8336` + close-out): dual-side validation + apples-to-apples report landed at `docs/measurements/course-correction-dual-side-2026-07-18.md`. Freshly measured: Rust side 1,901/1,901 tests green on l1/cert/core at tip; Mycelium side `myc-check` clean on `lib/std` (22 files, median 0.218 s) and `lib/compiler` (9 files, median 5.007 s); Rust↔Mycelium differentials 32/32 + runnable-gate + W-1 round-trip green against the live oracle; the `mycelium-bench` shared-corpus run (14 cases): JIT 3.5–4.9× over interp where lowerable, AOT-env 1.6–2.6× on recursion cases, **zero correctness divergences**; live diagnostic sample, LOC pair (std-swap: 613 Rust vs 358 `.myc`), toolchain-step table. Cited (same-session prior measurements): C5 all-45 green, Phase-D 28.7% checked_fraction, Phase-E twin results. VSA/GPU/proof rows desktop-held (`scripts/vsa-desktop-checks.sh`). **Correction (VR-5, surfaced by the F leaf's re-read and confirmed by recount of the delivery-run records): the Phase E twin figure is 26/46 CLEAN and 20/46 FINDINGS — the E row above and the umbrella lock header said 27/19, a miscount now corrected here, in the CHANGELOG, and in the lock headers (the per-repo DELIVERY.md files were always correct).** Next: Phase G (releases). |
| 2026-07-18 | **Phase E COMPLETE.** All **46 `*-myc` twins** delivered and PR-merged (one PR each, agent-merged per D-7): each twin refreshed to the AX-core-train graduated content where a hand-vetted monorepo nodule exists (the Grok seeds already carried train parity for unchanged nodules — verified per-file by byte comparison; real refreshes: `std-swap-myc` gains the W-1 `bin64_to_tern41`/`tern41_to_bin64` exports, `std-core-myc` gains `derive_prelude.myc`, `std-error-myc` gains `option.myc`+`result.myc` — the last two placements are recorded judgment calls) and every twin gains a **`DELIVERY.md` honesty record** (provenance, per-file guarantee basis, the honesty ladder). **Measured per-repo `myc-check --project lib` at delivery: 27/46 CLEAN — including the full 9-nodule self-hosted compiler surface in `mycelium-compiler-myc` — and 19/46 FINDINGS confined to `Declared` Grok-era seed drafts (disclosed per-repo; no tag upgraded; drafts stay quarantined per T6).** Umbrella lock: all 46 `*-myc` pins advanced to delivered revs (merged `5d01a6fd`); mirror updated. Session note: mid-phase disk exhaustion resolved by deleting the finished C5 build caches (deletes-free-space; no work lost). Next: Phase F (dual-side validation + metrics). |
| 2026-07-18 | **Phase D COMPLETE** (leaf commit `b2219f3f` + close-out; artifact `PHASE-D-READINESS.md`): fresh M-1006 remeasure at the post-AX-core tip — all-7 `checked_fraction` **28.7% (98/342) flat** vs post-G-β (the oracle's l1 changes did not shift the pilot; verify-first corrected the launch framing that called 28.7% a pre-advance baseline — VR-5). G-γ discard-statement/`let _` close landed (live-oracle witnessed; 0.0pp headline, residual advancement — 5 records now gap one construct deeper). Steer Phase-4 DoD evaluated per target, **pilot-scoped**: zero P0 (zero `CheckError` files), dual-report default, breadcrumbed worklist, T6 quarantine verified, no fabrication; ONESHOT claims stay HOLD. Residuals ledgered: kernel-prim/trait-impl/M-875 design gates, `Option::ok_or` surface absent (→ Phase E stdlib), if/else early-return + tuple-let deferred leaves, whole-corpus remeasure queued. **Program implication for Phase E (D-4 confirmed by measurement):** transpile is a gap-profiling instrument at 28.7% — `*-myc` delivery is honesty-laddered: graduated `lib/` nodules to twins where they exist, vetted drafts + honest gap ledgers elsewhere; no bulk-port claims. Next: Phase E. |
| 2026-07-18 | **Phase C / C5 COMPLETE — Phase C closed.** The full monorepo content delta (archive `aad96b7a` → branch `fff42708`/`ac789485` — the AX-core waves **plus** the post-archive dev-line work, incl. the G-α/G-β transpiler advances; delta scope deliberately the *full* archive→tip sync per the maintainer's superset/alignment requirement, a superset of the AX-core-only reading of D-3) propagated to the component repos via the §6.2-5 protocol: classified per-file (FAST-copy / 3-way-merge / NEW; classifier + driver in-session), applied in the same 9-wave topo order as Phase B, each repo verified **fmt/clippy `-D warnings`/test green standalone** at its pushed rev before its PR merged (43 repos advanced; cli-common + std-sys unchanged by design — no content, no moved deps). Both risky 3-way merges (std-conformance `reject_ledger.rs`, transpile `diff.rs`) resolved cleanly preserving the Phase-B adaptations. **Umbrella lock v2 re-pinned** (45 rev+tree pins, verified pin == origin/main for all; merged `39be661b`) and the monorepo mirror updated. **Two fix-forwards, disclosed:** (1) `mycelium-runtime` lacked the `docs/spec/schemas` slice its cert schema-pinned test reads — that test **cannot have passed** at the Phase-B rev, so Phase B's "cargo test green standalone" claim for runtime is **corrected here** (VR-5 downgrade of that one row): green held for all suites except that one deterministic failure; the slice now travels with the container. (2) `mycelium-transpile` `combinator.rs` carried pre-existing dev-line rustfmt drift (change-scoped monorepo gates never fmt-checked it); formatted in both the monorepo (`ac789485`) and the component. **Phase C exit state:** monorepo and all 45 Rust component repos carry identical AX-core content at lockstep train revs, all green standalone (`Empirical`, wave logs in-session; per-repo CI + the umbrella integration CI are the running witnesses). Next: Phase D (transpile readiness). |
| 2026-07-18 | **Phase C / W-D COMPLETE** (leaf commits `5749dfbb..9d9653f4` + close-out): E-W1/M-1119 ternary conversion-ceiling lift `i64`→`i128` (BREAKING on the DN-66 surface, recorded append-only in `ternary.md`; verify-first corrected the real ceiling to m=39 and closed a latent mid-decode overflow G2 gap); `lib/std/swap.myc` W-1 exports (32/21 + 64/41 pairs, myc-check clean, dual-witnessed); CC-B6 PolicySlot caps landed (CertStore pattern, `Declared`, not mode-gated — YAGNI disclosed); sizing pass upgrades measured retention rows `Declared`→`Empirical` (§5.1: L4 ≈608 B/rec, L1 ≈397 B/pair, new L8); DN-141 S1/S2 grounded addenda (S2's sequencing finding: phylum-wide meet before DN-113 lands = no wall at all). M-1119 → done with landed basis. Residuals disclosed: `bytes_len` canon move (kernel-primitive registry), L2/L3/`balanced` unmeasured, api baselines still awaiting cargo-public-api. **AX-core monorepo wave set (W-A..W-D) closed.** Next: C5 — propagate the delta to affected component repos (full lockstep re-pin train, umbrella-PR protocol). |
| 2026-07-18 | **Phase C / W-C COMPLETE** (leaf commits `bf1507a1..8cb1efd2` + close-out): X2 grade catalog (14 RFC-0018 §4.3 rules as data) + overclaim guard (`Exact` closed-form enumerations); X3 regime classification (direction-aware per RFC-0002 §4) + `RegimeTypeLie` Diag builder — hard refusal deferred to preserve shipped behavior (CC-B9); X4 meet-boundary table proven equivalent to `grade.rs` enforcement + the R4 structural guard; X5 l1→diag edge landed, `check_swap`'s two refusal branches now emit real envelope instances (DN-80 pins unchanged, verified). Verify-first avoided re-implementing R1/R2/R7. **AX-core status, honest:** X1–X5+X15 implementation surface landed; §7 probes 1/2/4/6 witnessed, 3 partial (needs X6, W-E), 5 residual (B6), 7 transpile-phase, 8 pre-existing checker-wide span gap. All gates green change-scoped + `cargo check --workspace`. **Disclosed residual:** the committed `docs/spec/api/*.txt` public-API baselines (the input `docs/api-index/` derives from) predate W-A..W-C's new pub symbols — regenerating them needs `cargo-public-api` + a nightly rustdoc, absent in this session (the `api.sh` gate skips gracefully, by design); the api-index regen here refreshes locations only. Baseline regen queued for the desktop lane / next tool-equipped session, before the dev→staging promotion. Next: W-D (sizing pass, E-W1 lift + W-1 sweep, S1/S2 spikes, CC-B6 PolicySlot cap). |
| 2026-07-18 | **Phase C / W-B COMPLETE** (leaf commits `b84e40b0..e1be3465` + close-out): `policy: ambient` surface + `default policy <name>;` nodule declaration, rejected vocabulary (`_`/`auto`/`default`) as hard errors naming `ambient`, scoped resolution mirroring `cert_scope` (nodule tier wired; unresolved = hard error), A1 legal-pair matrix (RFC-0002 §5 rows as data, wired into `check_swap`), and the exit criterion proven — five ambient/longhand pairs elaborate to identical content hashes (`Empirical`, `tests/ambient_policy.rs`). Close-out: DN-80 Part B re-audit performed (drift 103→115/116→135 attributed fn-by-fn; guard re-pinned; 9/9 green; family-table debt → CC-B8), CHANGELOG row, Doc-Index DN-142 row updated, api-index regenerated. Deferral disclosed: no l1→diag emitter edge yet (`PolicyResolve`/`LegalPairRefuse` reserved, unwired). Next: W-C (X2–X5). |
| 2026-07-18 | **Phase C / W-A COMPLETE** (commits `2818d372..864e214f`): Diag `FirstFaultEnvelope` (RFC-0013 A1, additive — pinned-golden backward compat), `swap_check` emitter + **G-3 fix** at the mode.rs seam, `Meta.cert` handle (hash-excluded, boxed), capped mode-gated `CertStore` (Declared caps, EXPLAINed drops), CertMode print on check/run, `first_fault_line()` exit criterion proven. ~537 change-scoped tests green; api-index regenerated; CHANGELOG row applied; audit-ledger G-3 disposition cites the landing. Judgment calls recorded in the W-A CHANGELOG entry. Next: W-B (`policy: ambient` + catalog + legal-pair matrix + elaboration-hash goldens). |
| 2026-07-18 | **Phase B COMPLETE.** All **45 Rust component repos** land buildable at train v0.464.0 — 9 waves (W1–W9), each repo verified `cargo fmt --check` + `clippy --all-targets -D warnings` + `cargo test` **green standalone** at its merged rev (`Empirical`, logs in-session; per-repo CI v2 is the running witness), PR-merged to its `main`. Umbrella `mycelium-lang` upgraded: **lock v2** (45 Rust pins with rev **+ tree hash**; 46 `*-myc` seed pins carried `Declared`; no-self-pin design note) + a **real integration CI** (lock-format assertions + apex `cargo check` transitively resolving the whole pinned graph — steer §6.2-4); merged (`123cac8c`). Monorepo mirror lock updated with the umbrella tip. Container conversions beyond the seeds: l1, doc, std-conformance (compile-time fixture parity); dual-layout fixture resolution in transpile + fmt (flagged for Phase-C upstreaming); DN-80 sibling-source audits layout-gated with printed skips (canonical audit stays monorepo-side). F14 resolves as documented-by-README (cli-myc declares dual provenance). **Identity invariant status (honest):** source byte-parity vs archive `Exact` (assessment parity engine) + content-hash/conformance suites green in the component layout (`Empirical`); the dedicated cross-layout golden-suite comparison artifact remains queued (Phase F). Next: Phase C (AX-core W-A). |
| 2026-07-18 | **Phase B in flight (checkpoint).** Select+vsa-decode → runtime move executed (maintainer-approved; DN-143 §4 updated — vsa-decode rides along, zero dependents). Generator `scripts/decompose/fix_component_workspace.py` landed (plan/apply; 9-wave DAG). **W1–W4 + std-error landed: 31/46 repos buildable, verified fmt/clippy/test green standalone, PR-merged to their `main`s.** Working rules learned + encoded: (1) **frozen lockstep pins** — one rev per dep repo train-wide (mixed revs split cargo package identity → E0308; generator re-pins drifted git lines); (2) dep lines carrying `version =` keys normalized (stale train pin breaks resolution); (3) owned fixture slices travel with their repo (core: `docs/spec/schemas`; l1: `lib/compiler`, `lib/std`, `docs/spec/grammar`); (4) l1 converted to container layout so monorepo-relative fixture paths resolve with zero source divergence; (5) RFC-0034 clause-(c) conformance split per-repo with delegation notes (runtime checks cert; core/value check their own trusted-base crates); (6) one CI failure (clippy `single_element_loop`) fixed forward same-hour. W5 l1 full suite running; W6–W9 (bench/proj/transpile → doc/fmt/lsp/spore/std-testing → check/cli/lint/std-spore → std-conformance) pending. |
| 2026-07-18 | **Phase A complete.** A2: audit-grok ledger — G-1/2/4 clean (`Exact`/`Empirical`; RFC-0012 goldens 12/12, mode tests 7/7, io.myc tags oracle-matched), G-3 one P2 latent pre-archive finding (mode.rs Certified/`check:None` — forward-fix scheduled with W-A), G-5..G-11 rowed. A3: program record corrected append-only + CHANGELOG rows + lock accounting. A4: all six Phase-1 captures landed **Draft** — DN-142, DN-141, RFC-0013 §10 Amendment A1, RFC-0034 §7 footnote, `Language-Retention-Policy.md`, W-1 amendments + **M-1119** (E-W1). A5: DN-143 (four deviation captures incl. the `select`→runtime repo-cycle fix). A6: G-8 `PolicySlot` ledgered (CC-B6). Gates at close: markdown 672 clean · doc_refs OK · links OK. Two agent-flagged grounded corrections recorded in the docs themselves (RFC-0018 §4.5 status; RFC-0013 I1-vs-I3 citation). Next: Phase B. |
