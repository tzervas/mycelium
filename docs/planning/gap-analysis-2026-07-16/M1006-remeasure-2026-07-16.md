# M-1006 pilot re-measure post G3 (Empirical, advisory)

> **Superseded for tip numbers by** [`M1006-remeasure-post-A1A2-2026-07-16.md`](./M1006-remeasure-post-A1A2-2026-07-16.md)
> (post ORACLE-R1 A1+A2 @ `6d61b3b8`: union **checked 8.5%** / expressible **18.6%**; cmp/rand Clean;
> std-time residual `Show`/`WallInstant`; eval residual `DEFAULT_FUEL`). This file remains the
> post-G3 / pre-companion baseline snapshot.

**Date:** 2026-07-16
**Base tip:** `8b35c2dfd7bd4e6b289058ee3d445be818a2d800` (`origin/dev`, includes M-1090 WU-3 #1630, M-1084 #1635, M-1037 #1636, G4-a #1634)
**Scope:** Representative `just transpile-vet` default set + explicit `mycelium-std-cmp` pilot — **not** the full M-1006 17-target / whole-corpus ladder (VR-5: do not treat these numbers as corpus-wide).

## Commands

```bash
git fetch origin dev
git checkout -B claude/leaf/M1006-remeasure-post-g3 origin/dev
just transpile-vet
just transpile-vet crates/mycelium-std-cmp/src
```

Both invocations built `myc-check` and `mycelium-transpile` once (`scripts/checks/transpile-vet.sh`); oracle via `MYC_CHECK_CMD`.

## Per-target results (`Empirical`)

| Target | non-test items | checked (myc-check-clean) | `checked_fraction` | emitted | `expressible_fraction` | First `myc check` blocker (oracle) |
|--------|---------------:|--------------------------:|-------------------:|--------:|-----------------------:|-------------------------------------|
| `crates/mycelium-l1/src/eval.rs` | 42 | 0 | **0.0%** | 5 | 11.9% | unknown trait `Default` (`impl` in `Default`) |
| `crates/mycelium-l1/src/fuse.rs` | 12 | 0 | **0.0%** | 0 | 0.0% | (no emission; parse OK) |
| `crates/mycelium-std-time/src` | 37 | 0 | **0.0%** | 17 | 45.9% | duplicate function `as_nanos` |
| `crates/mycelium-std-rand/src` | 34 | 0 | **0.0%** | 6 | 17.6% | unknown prim `rotate_left` (`rotl64`) |
| `crates/mycelium-std-cmp/src` | 111 | 0 | **0.0%** | 24 | 21.6% | unknown trait `Widen` (`impl` in `Widen`) |

**Union over the default 5 targets:** 0 / 236 items myc-check-clean → **`checked_fraction` 0.0%**; 52 / 236 emitted → **`expressible_fraction` 22.0%** (file-gated: one check error zeros the whole file numerator).

**Phylum-mode headline** (transpiler `--vet --phylum`, same denominators): `checked_fraction_phylum` **0.0%** on `std-time`, `std-rand`, and `std-cmp`; Δ_basis **+0.0pp** vs oracle on each — no false-fail recovery on these pilots.

### `mycelium-std-cmp` alone

Matches the default-set row: **0.0%** checked (0/111), **21.6%** expressible (24/111), `[CheckError=1]` — `unknown trait Widen`.

## Comparison to prior pilot baseline (`Empirical`, declared prior)

`docs/planning/self-hosting-port-ledger.md` Phase-0 default-set union (pre–G3 batch, different commit): **≈4.3%** checked (10/234), **≈12.8%** expressible (30/234). On this tip:

- **`checked_fraction`:** 0.0% vs ~4.3% — **no net rise** on the default pilots; eval/std-time previously had partial file-clean numerators, now file-gated to zero by trait/surface/prim errors above.
- **`expressible_fraction`:** 22.0% vs ~12.8% — **higher emission** (notably `std-cmp` 12.6% → 21.6% and `std-time` emission count), consistent with M-1037 widening expressible surface without yet producing myc-check-clean files.

Honesty tags: all fractions in this doc are **`Empirical`** (measured by `mycelium-transpile --vet` + real `myc check`). Interpretation of *why* levers did not move checked on these targets is **`Declared`** unless tied to a cited diagnostic line above.

## Residual still open (FLAG for integrating parent)

| Item | FLAG |
|------|------|
| **M-1090** | Re-measure **does not** satisfy a DoD that requires net **`checked_fraction`** gain on vet pilots; keep **`todo`** / do not close on this evidence alone. WU-3 lowering is landed; blockers on these targets are unrelated (`Widen`, `Default`, etc.). |
| **M-1084** | Landed on tip; default pilots still **0%** checked — Import net-close did not manifest as clean files on this set (first failures are trait/duplicate/prim, not unresolved import strings in the captured diagnostics). |
| **M-1037** | Landed on tip; **`expressible_fraction`** up on `std-cmp` vs older pilot note, **`checked_fraction`** still **0.0%** on `std-cmp` (same as `M1037-pilot-remeasure.md`). |
| **M-1006** | Full phased ladder / 17-target manifest re-measure **not run** here — only the M-1001 default 5-target profile. |
| **M-740** | L1 `.myc` self-host port remains the separate epic; not probed by this leaf. |

## Orchestrator actions suggested

1. Do **not** flip **M-1090** → `done` from this pilot alone.
2. Target next levers at **first oracle failures** on hot pilots (`Widen`, `Default`, duplicate-emit hygiene, `rotate_left` / std surface) or re-run vet on a corpus slice where M-1084/M-1037 were measured to bite (e.g. phylum-mode 24-target harness), if that harness is the authoritative acceptance basis.
3. Optional: append this path to **M-1006** `doc_refs` / body note at integration close-out (orch-owned `issues.yaml`).
