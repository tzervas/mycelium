# Spec — `std.cmp` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-532, #172;
> Ring 2, Tier B). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `cmp` / `convert` — ordering/equality + non-representation conversions (a representation change is `swap`, not a convert).
