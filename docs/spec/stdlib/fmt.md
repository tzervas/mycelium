# Spec — `std.fmt` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-533, #173;
> Ring 2, Tier B). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `fmt` — formatting / display as a dual human + machine (JSON) projection (G11); display is a projection, not identity.
