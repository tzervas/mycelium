# Spec — `std.dense` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-518, #160;
> Ring 1, Tier A). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `dense` — typed dim-tracked Dense{dim,dtype} tensors/embeddings, elementwise + similarity ops with per-op Exact/Bounded tags.
