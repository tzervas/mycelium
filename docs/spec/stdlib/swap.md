# Spec — `std.swap` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-516, #158;
> Ring 1, Tier A). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `swap` — the certified, never-silent representation-change library over the landed swaps + the one M-210 certificate checker.
