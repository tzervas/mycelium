# Spec — `std.core` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-515, #157;
> Ring 0, Tier A). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `core` / prelude — the honest value model (Value/Repr/Meta, Option/Result, the guarantee-lattice tags); Ring-0 re-exports of mycelium-core.
