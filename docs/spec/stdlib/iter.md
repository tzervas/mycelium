# Spec — `std.iter` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-526, #167;
> Ring 2, Tier B). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `iter` — iterator / fold / transducer combinators over the RFC-0007 §4.8 `for` fold; totality-preserving.
