# Spec — `std.select` (being authored)

> **Status:** Draft (needs-design) — authored in the Phase-5 design wave (task M-519, #161;
> Ring 1, Tier A). This spec follows `docs/spec/stdlib/_TEMPLATE.md` and conforms to the
> RFC-0016 §4.1 per-op contract (C1–C6). Index: `docs/spec/stdlib/README.md`.
>
> `select` / `explain` — the reified selection-policy DSL + mandatory EXPLAIN record (one mechanism, many sites).
