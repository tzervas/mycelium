"""VSA compositional bounds experiment (M-832, OQ-F).

Maps where the MAP-I single-hop `Proven` capacity formula extends to multi-hop
compositions (bind-chains, bundle-of-binds, nested unbind) and where it breaks down
to `Empirical`-only.

VR-5: measured RATES only — the verdict on candidate `Proven` subsets is maintainer analysis.
G2: never-silent backend selection and skip.
"""
