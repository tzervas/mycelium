{
  "source": "crates/mycelium-l1/src/elab.rs",
  "emitted_items": [],
  "gaps": [
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 32,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use std :: collections :: { BTreeMap , BTreeSet } ;",
      "reason": "`use` import (grouped `{{a, b}}`) — the transpiler has no cross-nodule symbol table, so it cannot confirm the imported path resolves to a declared Mycelium nodule; the M-1000 vet loop confirms such imports fail `myc check` name-resolution (a Rust `use extern_crate::Sym` names a crate, not a nodule). Flagged, not emitted — the same flag-don't-guess stance `map_type`/`emit_expr` take on qualified paths/calls (DN-34 §4/§8.2; VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 34,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use mycelium_core :: { operation_hash , Alt , CtorRef , CtorSpec , DataRegistry , DeclSpec , FieldSpec , FieldTyRef , FloatWidth , FnSig , Meta , Node , Payload , PolicyRef , Provenance , Repr , ScalarKind , SparsityClass , Trit , Value , } ;",
      "reason": "`use` import (grouped `{{a, b}}`) — the transpiler has no cross-nodule symbol table, so it cannot confirm the imported path resolves to a declared Mycelium nodule; the M-1000 vet loop confirms such imports fail `myc check` name-resolution (a Rust `use extern_crate::Sym` names a crate, not a nodule). Flagged, not emitted — the same flag-don't-guess stance `map_type`/`emit_expr` take on qualified paths/calls (DN-34 §4/§8.2; VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 40,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: ast :: { Arm , BaseType , Expr , Literal , Path , Scalar , Sparsity , TypeRef , WidthRef } ;",
      "reason": "`use` import (grouped `{{a, b}}`) — the transpiler has no cross-nodule symbol table, so it cannot confirm the imported path resolves to a declared Mycelium nodule; the M-1000 vet loop confirms such imports fail `myc check` name-resolution (a Rust `use extern_crate::Sym` names a crate, not a nodule). Flagged, not emitted — the same flag-don't-guess stance `map_type`/`emit_expr` take on qualified paths/calls (DN-34 §4/§8.2; VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 41,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: checkty :: { infer_type , normalize_pattern , prim_kernel_name , resolve_ty , Env , Ty } ;",
      "reason": "`use` import (grouped `{{a, b}}`) — the transpiler has no cross-nodule symbol table, so it cannot confirm the imported path resolves to a declared Mycelium nodule; the M-1000 vet loop confirms such imports fail `myc check` name-resolution (a Rust `use extern_crate::Sym` names a crate, not a nodule). Flagged, not emitted — the same flag-don't-guess stance `map_type`/`emit_expr` take on qualified paths/calls (DN-34 §4/§8.2; VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 42,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: decision :: { self , Head , Tree } ;",
      "reason": "`use` import (grouped `{{a, b}}`) — the transpiler has no cross-nodule symbol table, so it cannot confirm the imported path resolves to a declared Mycelium nodule; the M-1000 vet loop confirms such imports fail `myc check` name-resolution (a Rust `use extern_crate::Sym` names a crate, not a nodule). Flagged, not emitted — the same flag-don't-guess stance `map_type`/`emit_expr` take on qualified paths/calls (DN-34 §4/§8.2; VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 44,
      "col": 1,
      "category": "PayloadVariant",
      "rust_construct": "PayloadVariant",
      "snippet": "# [doc = \" Why a definition could not be elaborated to L0 — always explicit, never a partial artifact\"] # [doc = \" (RFC-0007 §4.6).\"] # [derive (Debug , Clone , PartialEq , Eq)] pub enum ElabError { # [doc = \" The body (or something it calls) is outside the evaluation-complete fragment; the program\"] # [doc = \" still *runs* — on the L1 fuel-guarded evaluator (RFC-0007 §4.6).\"] Residual { # [doc = \" The definition being elaborated when the refusal arose.\"] site : String , # [doc = \" Which construct fell outside the fragment, and why.\"] what : String , } , # [doc = \" The requested entry definition does not exist in the checked environment.\"] UnknownFn (String) , # [doc = \" A pass-internal AST traversal ([`crate::totality::walk_expr`], reused here for call-set\"] # [doc = \" collection over the call graph — M-641/DRY) exceeded its own explicit recursion-depth budget\"] # [doc = \" (M-674) on a pathologically-nested body. An operational resource refusal, distinct from\"] # [doc = \" [`ElabError::Residual`] (a semantic \\\"outside the fragment\\\" verdict) — refused cleanly rather\"] # [doc = \" than overflowing the host stack (banked guard 4).\"] DepthExceeded { # [doc = \" The definition being analyzed when the budget was exceeded.\"] site : String , # [doc = \" The exceeded budget.\"] limit : u32 , } , }",
      "reason": "enum `ElabError` variant `Residual` uses named fields — `constructor ::= Ident ('(' type_ref (',' type_ref)* ')')?` has no named-field/record form",
      "item_name": "ElabError"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 71,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for ElabError { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { ElabError :: Residual { site , what } => write ! (f , \"`{site}` is outside the evaluation-complete fragment (RFC-0007 §4.6): {what} — \\\n                 run it on the L1 evaluator\") , ElabError :: UnknownFn (name) => write ! (f , \"no function `{name}` in the checked nodule\") , ElabError :: DepthExceeded { site , limit } => write ! (f , \"`{site}`: AST nesting exceeds the call-graph analysis's own recursion-depth budget \\\n                 ({limit}) — an explicit budget (banked guard 4; M-674), refused cleanly rather than \\\n                 overflowing the host stack\") , } } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `fmt` signature: unsupported Rust type form `& mut core :: fmt :: Formatter < '_ >`",
      "item_name": "ElabError"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 90,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl std :: error :: Error for ElabError { }",
      "reason": "impl block has no items",
      "item_name": "ElabError"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 92,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn residual < T > (site : & str , what : impl Into < String >) -> Result < T , ElabError > { Err (ElabError :: Residual { site : site . to_owned () , what : what . into () , }) }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "residual"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 99,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Build the L0 [`Value`] of a representation literal (Q6: a literal *is* its representation —\"] # [doc = \" width = digit count). Bare integers and lists have no representation family and are refused\"] # [doc = \" (the typechecker already refuses them; this refusal keeps the bridge honest on its own).\"] pub fn lit_value (site : & str , l : & Literal) -> Result < Value , ElabError > { match l { Literal :: Bin (s) => { let bits : Vec < bool > = s . chars () . filter (| c | * c == '0' || * c == '1') . map (| c | c == '1') . collect () ; let width = u32 :: try_from (bits . len ()) . expect (\"digit count fits u32\") ; Value :: new (Repr :: Binary { width } , Payload :: Bits (bits) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed binary literal: {e}\")) , Ok ,) } Literal :: Trit (s) => { let trits : Vec < Trit > = s . chars () . map (| c | match c { '+' => Ok (Trit :: Pos) , '0' => Ok (Trit :: Zero) , '-' => Ok (Trit :: Neg) , other => Err (other) , }) . collect :: < Result < _ , _ > > () . map_or_else (| c | residual (site , format ! (\"non-trit char {c:?} in ternary literal\")) , Ok ,) ? ; let width = u32 :: try_from (trits . len ()) . expect (\"trit count fits u32\") ; Value :: new (Repr :: Ternary { trits : width } , Payload :: Trits (trits) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed ternary literal: {e}\")) , Ok ,) } Literal :: Bytes (s) => { let hex : String = s . chars () . filter (| c | * c != '_') . collect () ; let mut bytes = Vec :: with_capacity (hex . len () / 2) ; let chars : Vec < char > = hex . chars () . collect () ; for pair in chars . chunks (2) { let hs : String = pair . iter () . collect () ; match u8 :: from_str_radix (& hs , 16) { Ok (b) => bytes . push (b) , Err (_) => { return residual (site , format ! (\"non-hex byte pair {hs:?} in `0x…` literal\")) } } } Value :: new (Repr :: Bytes , Payload :: Bytes (bytes) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed byte literal: {e}\")) , Ok ,) } Literal :: Str (s) => Value :: new (Repr :: Bytes , Payload :: Bytes (s . as_bytes () . to_vec ()) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed string literal: {e}\")) , Ok ,) , Literal :: Float (s) => { let x : f64 = match s . parse () { Ok (x) => x , Err (_) => { return residual (site , format ! (\"internal: malformed float literal text {s:?} reached elaboration\") ,) } } ; Value :: new (Repr :: Float { width : FloatWidth :: F64 , } , Payload :: Float (x) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed float literal: {e}\")) , Ok ,) } Literal :: Int (_) => residual (site , \"a bare integer literal has no representation family (Q6)\" ,) , Literal :: AmbientInt (_ , _) => residual (site , \"internal: an unresolved ambient bare decimal reached elaboration — the checker \\\n             resolves its width before the L0 bridge runs (RFC-0012 §4.3)\" ,) , Literal :: List (_) => residual (site , \"internal: a list literal reached `lit_value` — it lowers through `expr_inner` (the \\\n             element expressions need elaboration; RFC-0032 D3)\" ,) , } }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "lit_value"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 242,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Resolve a surface [`TypeRef`] to a kernel [`Repr`] (swap targets). Only representation types\"] # [doc = \" resolve; named/data and `Substrate` types are explicit refusals.\"] pub fn type_repr (site : & str , t : & TypeRef) -> Result < Repr , ElabError > { match & t . base { BaseType :: Binary (WidthRef :: Lit (n)) => Ok (Repr :: Binary { width : * n }) , BaseType :: Binary (WidthRef :: Name (v)) => Err (ElabError :: Residual { site : site . to_owned () , what : format ! (\"width variable `{v}` reached elaboration — must be monomorphized first \\\n                 (DN-42 / M-753; Residual)\") , }) , BaseType :: Ternary (WidthRef :: Lit (m)) => Ok (Repr :: Ternary { trits : * m }) , BaseType :: Ternary (WidthRef :: Name (v)) => Err (ElabError :: Residual { site : site . to_owned () , what : format ! (\"width variable `{v}` reached elaboration — must be monomorphized first \\\n                 (DN-42 / M-753; Residual)\") , }) , BaseType :: Dense (d , s) => Ok (Repr :: Dense { dim : * d , dtype : match s { Scalar :: F16 => ScalarKind :: F16 , Scalar :: Bf16 => ScalarKind :: Bf16 , Scalar :: F32 => ScalarKind :: F32 , Scalar :: F64 => ScalarKind :: F64 , } , }) , BaseType :: Vsa { model , dim , sparsity , } => Ok (Repr :: Vsa { model : crate :: checkty :: vsa_kernel_model_id (model) , dim : * dim , sparsity : sparsity_class (sparsity) , }) , BaseType :: Seq { elem , len } => Ok (Repr :: Seq { elem : Box :: new (type_repr (site , elem) ?) , len : * len , }) , BaseType :: Bytes => Ok (Repr :: Bytes) , BaseType :: Float => Ok (Repr :: Float { width : FloatWidth :: F64 , }) , BaseType :: Substrate (tag) => residual (site , format ! (\"Substrate{{{tag}}} is not a representation type\") ,) , BaseType :: Named (name , _) => residual (site , format ! (\"`{name}` is not a representation type — no kernel Repr\") ,) , BaseType :: Ambient (_) => residual (site , \"internal: an unresolved paradigm-less repr `{…}` reached elaboration — the ambient \\\n             resolution pass fills it first (RFC-0012 §4.3)\" ,) , BaseType :: Fn (_ , _) => residual (site , \"function types (`A -> B`) are not a representation type and cannot be a swap target \\\n             (RFC-0024 §3, HOF stage 1 — defunctionalization is M-687)\" ,) , BaseType :: Tuple (_) => residual (site , \"tuple types are not a representation type and cannot be a swap target (M-826; G2)\" ,) , } }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "type_repr"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 333,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The v0 **policy-name reference**: a deterministic, domain-separated content address derived\"] # [doc = \" from the surface policy *name* (`policy: roundtrip`).\"] # [doc = \"\"] # [doc = \" Honesty note (Declared): RFC-0005 policy *objects* are content-addressed over their canonical\"] # [doc = \" serialization (`mycelium-select::SelectionPolicy::policy_ref`); binding surface names to\"] # [doc = \" registered policy objects is later integration work. Until it lands, this name-derived address\"] # [doc = \" keeps `Meta.policy_used` answerable and — because the evaluator and the elaborator share this\"] # [doc = \" one function — keeps every execution path's swaps on the *same* `PolicyRef`, so the NFR-7\"] # [doc = \" differential is meaningful. Domain-separated (`policy-name.v0:`) so it can never collide with\"] # [doc = \" a structural or operation hash.\"] # [must_use] pub fn policy_name_ref (policy : & Path) -> PolicyRef { operation_hash (& format ! (\"policy-name.v0:{}\" , policy . 0 . join (\".\"))) }",
      "reason": "fn parameter `policy` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "policy_name_ref"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 348,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" A surface name's elaboration binding: `(surface name, kernel variable, v0 type)`. The type lets\"] # [doc = \" the elaborator re-infer a `match` scrutinee's type (to lower its patterns) without a second\"] # [doc = \" inference pass over the whole body.\"] type Binding = (String , String , Ty) ;",
      "reason": "`type Binding = ...` alias — Mycelium's `type_item` always introduces a new nominal sum type via `'=' constructor ('|' constructor)*`; a bare alias to an existing type would fabricate a sum type where none exists semantically",
      "item_name": "Binding"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 353,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Elaborate the nullary function `entry` of a checked nodule to a closed L0 [`Node`].\"] # [doc = \"\"] # [doc = \" As of RFC-0001 r4 the evaluation-complete fragment is the **whole v0 calculus**: data + matching\"] # [doc = \" (r3) and now **functions + recursion** (`Lam`/`App`/`Fix`). Each reachable **self-recursive**\"] # [doc = \" function is bound once as `let f = Fix(f, λparams. body)` (callee-first), and a call to it\"] # [doc = \" elaborates to a curried `App`; every other call still inlines (the non-recursive call graph is\"] # [doc = \" acyclic). **Mutual recursion** lowers to a `FixGroup` (RFC-0001 r5; M-343 — R7-Q3); top-level\"] # [doc = \" functions in a nodule are mutually visible (RP-6 / DN-13 — no surface marker), so a mutual group is\"] # [doc = \" *inferred* from the call graph and **materialized as a `FixGroup` node** — inspectable in the\"] # [doc = \" elaborated term, never a black box. A guarantee index `@ g` is statically checked + erased since\"] # [doc = \" RFC-0018 (M-663), not a `Residual`. On success the result is a closed L0 term whose evaluation\"] # [doc = \" must agree with the L1 evaluator (NFR-7; the M-210 differential).\"] pub fn elaborate (env : & Env , entry : & str) -> Result < Node , ElabError > { mycelium_stack :: with_deep_stack (| | { let mono_env = crate :: mono :: monomorphize (env , entry) ? ; let (mut el , binders , fd) = elab_prelude (& mono_env , entry) ? ; let mut stack = vec ! [entry . to_owned ()] ; let entry_body = el . expr (& mut stack , & [] , & fd . body) ? ; Ok (wrap_in_binders (binders , entry_body)) }) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 385,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **ADR-033/DN-74 (M-923):** elaborate `entry` **without** the [`crate::mono::monomorphize`]\"] # [doc = \" pre-pass [`elaborate`] always runs first. Intended for an `env` that is already concrete\"] # [doc = \" (no generics, no traits, no unresolved widths) and whose `Ty::Fn` record fields should lower\"] # [doc = \" through the kernel `FieldSpec::Fn` primitive (`field_spec`'s new arm, Path A — the DN-74-ratified\"] # [doc = \" FLAG-1 disposition) rather than through `mono.rs`'s closure defunctionalization.\"] # [doc = \"\"] # [doc = \" **Why this exists, honestly (new evidence surfaced while implementing M-923).** `mono.rs`\"] # [doc = \" unconditionally rewrites *every* reachable `Ty::Fn` field into a closure tag-sum before\"] # [doc = \" `build_registry` ever runs (RFC-0024 §4A/M-704) — so calling `field_spec`'s new `FieldSpec::Fn`\"] # [doc = \" arm through the standard [`elaborate`] entry is unreachable by construction (defunctionalization\"] # [doc = \" already produces a closed, executing term for that case). Making `FieldSpec::Fn` reachable from\"] # [doc = \" a real, parsed-and-checked program therefore needs an elaboration path that does not run through\"] # [doc = \" `mono.rs` at all — this function is that path. It is deliberately **narrow**: it does not\"] # [doc = \" monomorphize generics or lower closures (a `lambda` literal reaching [`Elab::expr`] this way is\"] # [doc = \" still refused, per the `Expr::Lambda` invariant guard), so it only accepts programs that are\"] # [doc = \" already closed and whose function-typed field values are bare references to ordinary top-level\"] # [doc = \" functions (the `field_spec`/`expr` `Ty::Fn`/named-fn-as-value arms this leaf adds). This is\"] # [doc = \" **additive** — [`elaborate`]'s own behavior, and the existing differential corpus that depends on\"] # [doc = \" it (RFC-0024 §4A closures, NFR-7), is unchanged.\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" The same [`ElabError`] variants as [`elaborate`] — most notably [`ElabError::Residual`] for a\"] # [doc = \" generic entry/function or a construct this narrower path does not (yet) accept; **never** a\"] # [doc = \" silent, half-lowered term (G2/VR-5).\"] pub fn elaborate_direct (env : & Env , entry : & str) -> Result < Node , ElabError > { mycelium_stack :: with_deep_stack (| | { let (mut el , binders , fd) = elab_prelude (env , entry) ? ; let mut stack = vec ! [entry . to_owned ()] ; let entry_body = el . expr (& mut stack , & [] , & fd . body) ? ; Ok (wrap_in_binders (binders , entry_body)) }) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_direct"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 418,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **Per-hypha elaboration of a `colony` entry** for the *real-concurrency* execution path\"] # [doc = \" (RFC-0008 §4.7; M-666 redone with the `mycelium-mlir::runtime` executor). Where [`elaborate`]\"] # [doc = \" produces the single L0 `Node` whose body is the **RT2 spawn-order sequentialization** (a `Let`\"] # [doc = \" chain — the deterministic *reference* the concurrent run is validated against), this produces one\"] # [doc = \" **closed L0 `Node` per `hypha`**: each hypha body, elaborated under the entry's scope and wrapped\"] # [doc = \" in the **same** recursive-binder prelude (so a hypha may call the nodule's recursive functions).\"] # [doc = \" The `mycelium-mlir` colony driver spawns each as a concurrent `Task` in a `Scope`/`Colony`, runs\"] # [doc = \" the structured fork/join, and validates the concurrent observable **equals** the sequential\"] # [doc = \" reference (RT2; an inequality is an explicit divergence, never silent — G2/RT4).\"] # [doc = \"\"] # [doc = \" The colony's *observable* is its **last** hypha's value (the type rule, [`crate::checkty`]); the\"] # [doc = \" returned vector preserves spawn order, so element `N-1` is that observable. This adds **no L0\"] # [doc = \" concurrency node** — the trusted base stays sequential (RFC-0008 §4.2; KC-3); concurrency is\"] # [doc = \" scheduling layered *over* unchanged per-hypha L0 terms.\"] # [doc = \"\"] # [doc = \" Refuses with an explicit [`ElabError::Residual`] (never a fabricated accept) when the entry body\"] # [doc = \" is **not** a `colony`, or when any hypha body is outside the evaluation-complete fragment.\"] pub fn elaborate_colony (env : & Env , entry : & str) -> Result < Vec < Node > , ElabError > { mycelium_stack :: with_deep_stack (| | elaborate_colony_inner (env , entry)) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_colony"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 439,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn elaborate_colony_inner (env : & Env , entry : & str) -> Result < Vec < Node > , ElabError > { let mono_env = crate :: mono :: monomorphize (env , entry) ? ; let (mut el , binders , fd) = elab_prelude (& mono_env , entry) ? ; let Expr :: Colony (hyphae) = & fd . body else { return residual (entry , \"the entry body is not a `colony` — `elaborate_colony` lowers a colony to its per-hypha \\\n             closed L0 programs (the concurrent path); use `elaborate` for the sequentialized form\" ,) ; } ; if hyphae . is_empty () { return residual (entry , \"internal: an empty `colony` reached elaboration — the parser requires ≥ 1 hypha \\\n             (RFC-0008 §4.7)\" ,) ; } let mut stack = vec ! [entry . to_owned ()] ; let mut programs = Vec :: with_capacity (hyphae . len ()) ; for h in hyphae { let body = el . expr (& mut stack , & [] , & h . body) ? ; let node = match & h . forage { None => body , Some (policy) => { forage_reject_if_empty (entry , h) ? ; let policy_node = el . expr (& mut stack , & [] , policy) ? ; Node :: Let { id : el . fresh (\"forage_policy\") , bound : Box :: new (policy_node) , body : Box :: new (body) , } } } ; programs . push (wrap_in_binders (binders . clone () , node)) ; } Ok (programs) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_colony_inner"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 485,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **Policy + body elaboration of a `reclaim` entry** for the *real-supervision* execution path\"] # [doc = \" (DN-58 §B; RFC-0008 RT7; M-817). Where [`elaborate`] produces the single L0 `Node` whose body is\"] # [doc = \" the **sequential reference** (a `Let{_ = policy, body}` — evaluate the policy for its effect, then\"] # [doc = \" yield the body; the deterministic reference the supervised run is validated against), this produces\"] # [doc = \" the **policy** and **body** as two **closed L0 `Node`s**, each elaborated under the entry's scope\"] # [doc = \" and wrapped in the **same** recursive-binder prelude (so each may call the nodule's recursive\"] # [doc = \" functions). The `mycelium-mlir` reclaim driver ([`mycelium_mlir::run_reclaim`]) runs the body under\"] # [doc = \" `mycelium-std-runtime::supervise_with_restart` (re-evaluating it per restart — which is why the body\"] # [doc = \" is returned as an unevaluated node, not a value), threads a `SupervisionRecord` EXPLAIN trail, and\"] # [doc = \" validates the supervised observable **equals** the sequential reference on success (RT7). This adds\"] # [doc = \" **no L0 supervision node** — the trusted base stays sequential (KC-3); supervision is scheduling\"] # [doc = \" layered *over* unchanged body evaluation, exactly as the concurrent `colony` driver layers over\"] # [doc = \" per-hypha L0 terms.\"] # [doc = \"\"] # [doc = \" Refuses with an explicit [`ElabError::Residual`] (never a fabricated accept) when the entry body is\"] # [doc = \" **not** a `reclaim`, or when the policy/body is outside the evaluation-complete fragment.\"] pub fn elaborate_reclaim (env : & Env , entry : & str) -> Result < (Node , Node) , ElabError > { mycelium_stack :: with_deep_stack (| | elaborate_reclaim_inner (env , entry)) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_reclaim"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 505,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn elaborate_reclaim_inner (env : & Env , entry : & str) -> Result < (Node , Node) , ElabError > { let mono_env = crate :: mono :: monomorphize (env , entry) ? ; let (mut el , binders , fd) = elab_prelude (& mono_env , entry) ? ; let Expr :: Reclaim { policy , body } = & fd . body else { return residual (entry , \"the entry body is not a `reclaim` — `elaborate_reclaim` lowers a reclaim to its \\\n             (policy, body) closed L0 programs (the supervised path); use `elaborate` for the \\\n             sequential-reference form\" ,) ; } ; let mut stack = vec ! [entry . to_owned ()] ; let policy_node = el . expr (& mut stack , & [] , policy) ? ; let body_node = el . expr (& mut stack , & [] , body) ? ; Ok ((wrap_in_binders (binders . clone () , policy_node) , wrap_in_binders (binders , body_node) ,)) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_reclaim_inner"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 530,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **Elaborate a user-defined generative-lowering rule's RHS to a closed L0 [`Node`]** (DN-54\"] # [doc = \" §4.1/§6 / M-812-cont). This is the `crate::elab` site that **reads [`Env::lower_rules`]** — the\"] # [doc = \" completion the `low` (M-812) landing deferred: a `lower Name[…] = <rhs>` rule is no longer mere\"] # [doc = \" stored data, it elaborates to real L0.\"] # [doc = \"\"] # [doc = \" The mechanism is **DRY and observation-faithful by construction**: the rule's RHS is given the\"] # [doc = \" **exact same** lowering path a hand-written nullary `fn <rule>%rhs() = <rhs>` would take — it is\"] # [doc = \" inserted as a synthetic nullary fn into a *clone* of `env` and run through the ordinary\"] # [doc = \" [`elaborate`]. So the §7 differential `observe(derive Name for T) == observe(hand-lowered Name)`\"] # [doc = \" holds **because the two go through one code path**, and the rule's observational identity is\"] # [doc = \" `Empirical` (earned by running, never self-attested — VR-5).\"] # [doc = \"\"] # [doc = \" **§6 KC-3 (kernel-growth) — `Proven`-by-construction (narrow, checked sense):** the return type\"] # [doc = \" is [`Node`], a *closed* Rust enum (the frozen L0 grammar — `mycelium_core::node`). The elaborator\"] # [doc = \" can only ever *construct* one of those variants, so a `lower` rule **cannot** introduce a new\"] # [doc = \" kernel node — the type system is the side-condition that makes this `Proven`. (The checker's\"] # [doc = \" §4.6 `wild`-refusal closes the one *surface*-growth a rule could otherwise smuggle in.) This\"] # [doc = \" function does **not** *prove* an arbitrary RHS elaborates — only that **if** it does, it adds no\"] # [doc = \" kernel node, which is exactly KC-3.\"] # [doc = \"\"] # [doc = \" **Scope / honest residual (DN-54 underdetermination — FLAGGED).** v0 elaborates a **nullary,\"] # [doc = \" monomorphic** rule (the landed surface — `lower Name = <expr>`): its RHS is a closed term. A\"] # [doc = \" **parametric** rule (`lower Name[T] = <rhs over T>`) whose RHS mentions the type parameter `T`\"] # [doc = \" has no monomorphic L0 form until `derive Name for <concrete>` *instantiates* `T` — and **how a\"] # [doc = \" `derive` site's instantiated L0 attaches to / is referenced by the surrounding program is\"] # [doc = \" underdetermined by DN-54** (the note's worked example RHS is an `impl` block, which is an *item*,\"] # [doc = \" not an [`Expr`], so it is not even expressible as a v0 rule RHS). Rather than invent that\"] # [doc = \" consumption semantics (G2/VR-5: a correct partial landing beats a guessed elaboration), this\"] # [doc = \" elaborates the rule's RHS *as written* (the param-instantiation of a non-nullary RHS surfaces as\"] # [doc = \" the ordinary generic [`ElabError::Residual`] from [`elaborate`]) and leaves the attachment model\"] # [doc = \" for the maintainer to ratify. See DN-54 §3.2/§5 and the M-812-cont FLAG.\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" [`ElabError::UnknownFn`] if `rule_name` is not a registered `lower` rule; [`ElabError::Residual`]\"] # [doc = \" if the RHS is outside the evaluation-complete fragment (e.g. it mentions an un-instantiated type\"] # [doc = \" parameter, or a `wild`/`spore`/`Substrate` site — never a fabricated artifact, G2).\"] pub fn elaborate_lower_rule (env : & Env , rule_name : & str) -> Result < Node , ElabError > { let Some (rule) = env . lower_rules . get (rule_name) else { return Err (ElabError :: UnknownFn (rule_name . to_owned ())) ; } ; let rule = match & rule . rhs { crate :: ast :: LowerRhs :: Expr (_) => rule , crate :: ast :: LowerRhs :: Impl (_) => { return residual (rule_name , \"this `lower` rule has an item-shaped (`impl … for …`) RHS — a DN-54 §10 Model A \\\n                 sibling-injection template (M-973). It has no stand-alone L0 term: its output is \\\n                 the concrete `impl` injected at each `derive Name for T` site (checked/lowered by \\\n                 the ordinary instance passes). `elaborate_lower_rule` lowers only expression-shaped \\\n                 rules; there is nothing to elaborate here (never silent — G2).\" ,) ; } } ; let rhs_expr = rule . expr_rhs () . expect (\"matched the Expr arm just above — item-shaped rules returned early\") ; let entry = format ! (\"%lower-rhs%{rule_name}\") ; let synth = crate :: ast :: FnDecl { vis : crate :: ast :: Vis :: Private , thaw : false , tier : None , sig : crate :: ast :: FnSig { name : entry . clone () , params : vec ! [] , value_params : vec ! [] , ret : crate :: ast :: TypeRef :: unguaranteed (BaseType :: Binary (WidthRef :: Lit (0))) , effects : vec ! [] , effect_budgets : std :: collections :: BTreeMap :: new () , } , body : rhs_expr . clone () , } ; let mut env2 = env . clone () ; env2 . fns . insert (entry . clone () , synth) ; elaborate (& env2 , & entry) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "elaborate_lower_rule"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 619,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" Shared front-end of [`elaborate`]/[`elaborate_colony`]: validate the entry is a closed (nullary,\"] # [doc = \" no dynamic guarantee) definition, build the data registry, decompose the reachable call graph into\"] # [doc = \" callee-first recursive SCCs (Tarjan), and elaborate each SCC's recursive binder. Returns the\"] # [doc = \" primed [`Elab`], the callee-first binder list, and the entry's [`FnDecl`]. DRY: the recursion\"] # [doc = \" machinery is identical whether the entry body is sequentialized to one `Node` or split per-hypha.\"] fn elab_prelude < 'e > (env : & 'e Env , entry : & str ,) -> Result < (Elab < 'e > , Vec < RecBinding > , & 'e crate :: ast :: FnDecl) , ElabError > { let Some (fd) = env . fns . get (entry) else { return Err (ElabError :: UnknownFn (entry . to_owned ())) ; } ; if ! fd . sig . value_params . is_empty () { return residual (entry , \"the entry has value parameters — v0 elaborates closed (nullary) entries; \\\n             apply it from a nullary definition\" ,) ; } if ! fd . sig . params . is_empty () { return residual (entry , \"the entry is generic — a generic definition's L0 lowering is staged (monomorphization; \\\n             RFC-0007 §11.3, the M-657 follow-up). Elaborate a concrete (monomorphic) entry.\" ,) ; } let registry = build_registry (env) ? ; let sccs = recursive_sccs (env , entry) ? ; let mut el = Elab { env , registry , fresh : 0 , rec : BTreeMap :: new () , depth : 0 , } ; for scc in & sccs { for f in scc { let kf = el . fresh (f) ; el . rec . insert (f . clone () , kf) ; } } let mut binders : Vec < RecBinding > = Vec :: with_capacity (sccs . len ()) ; for scc in & sccs { if scc . len () == 1 { let f = & scc [0] ; let kf = el . rec [f] . clone () ; let fix = Box :: new (el . elab_recursive_fn (f , & kf) ?) ; binders . push (RecBinding :: Single { var : kf , fix }) ; } else { let mut defs : Vec < (String , Box < Node >) > = Vec :: with_capacity (scc . len ()) ; for f in scc { let kf = el . rec [f] . clone () ; defs . push ((kf , Box :: new (el . elab_fn_lam (f) ?))) ; } binders . push (RecBinding :: Group (defs)) ; } } Ok ((el , binders , fd)) }",
      "reason": "lifetime parameter `'e` has no grammar surface",
      "item_name": "elab_prelude"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 690,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Wrap an elaborated body in a callee-first binder prelude. `binders` is callee-first; fold in\"] # [doc = \" reverse so the first (callee) binding ends up outermost (in scope for every later binding and the\"] # [doc = \" body). Shared by [`elaborate`] (one body) and [`elaborate_colony`] (each hypha body).\"] fn wrap_in_binders (binders : Vec < RecBinding > , body : Node) -> Node { binders . into_iter () . rev () . fold (body , | acc , b | match b { RecBinding :: Single { var , fix } => Node :: Let { id : var , bound : fix , body : Box :: new (acc) , } , RecBinding :: Group (defs) => Node :: FixGroup { defs , body : Box :: new (acc) , } , }) }",
      "reason": "unsupported expression form `| acc , b | match b { RecBinding :: Single { var , fix } => Node :: Let { id : var , bound : fix , body : Box :: new (acc) , } , RecBinding :: Group (defs) => Node :: FixGroup { defs , body : Box :: new (acc) , } , }`",
      "item_name": "wrap_in_binders"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 707,
      "col": 1,
      "category": "PayloadVariant",
      "rust_construct": "PayloadVariant",
      "snippet": "# [doc = \" One recursive binding the entry body is wrapped in: a self-recursive singleton (`Fix`, bound via\"] # [doc = \" `Let`) or a mutually-recursive group (`FixGroup`). Built callee-first; see [`elaborate`].\"] # [derive (Clone)] enum RecBinding { # [doc = \" A self-recursive function: its kernel variable and the `Fix` node bound to it (boxed — the\"] # [doc = \" `Group` variant is pointer-sized, so an unboxed `Node` here would unbalance the enum).\"] Single { var : String , fix : Box < Node > } , # [doc = \" A mutually-recursive group: `(member variable, curried lambda)` pairs, all mutually in scope.\"] Group (Vec < (String , Box < Node >) >) , }",
      "reason": "enum `RecBinding` variant `Single` uses named fields — `constructor ::= Ident ('(' type_ref (',' type_ref)* ')')?` has no named-field/record form",
      "item_name": "RecBinding"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 720,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The **recursive** strongly-connected components of the reachable call graph, **callee-first**\"] # [doc = \" (a callee SCC is bound *outside* its callers). A self-recursive singleton (`{f}` with a self-call)\"] # [doc = \" and a mutual group (≥2 functions in a cycle) are both recursive SCCs; a function in no cycle\"] # [doc = \" inlines and is **not** returned. Computed with Tarjan's algorithm — which finalises each SCC only\"] # [doc = \" after all its successor (callee) SCCs, i.e. in reverse-topological = callee-first order. Roots,\"] # [doc = \" successors, and each SCC's members are visited/sorted deterministically so the lowering (and thus\"] # [doc = \" the content hash) is reproducible. A function is \\\"reachable\\\" if the entry transitively calls it.\"] fn recursive_sccs (env : & Env , entry : & str) -> Result < Vec < Vec < String > > , ElabError > { let mut reachable : BTreeSet < String > = BTreeSet :: new () ; let mut frontier = vec ! [entry . to_owned ()] ; while let Some (f) = frontier . pop () { if ! reachable . insert (f . clone ()) { continue ; } if let Some (fd) = env . fns . get (& f) { for callee in calls_in_fn (& f , & fd . body) ? { if env . fns . contains_key (& callee) { frontier . push (callee) ; } } } } struct Tarjan < 'e > { env : & 'e Env , reachable : & 'e BTreeSet < String > , index : usize , idx : BTreeMap < String , usize > , low : BTreeMap < String , usize > , on_stack : BTreeSet < String > , stack : Vec < String > , out : Vec < Vec < String > > , } fn successors (env : & Env , reachable : & BTreeSet < String > , f : & str ,) -> Result < BTreeSet < String > , ElabError > { Ok (calls_in_fn (f , & env . fns [f] . body) ? . into_iter () . filter (| c | reachable . contains (c) && env . fns . contains_key (c)) . collect ()) } fn strongconnect (t : & mut Tarjan , v : & str) -> Result < () , ElabError > { t . idx . insert (v . to_owned () , t . index) ; t . low . insert (v . to_owned () , t . index) ; t . index += 1 ; t . stack . push (v . to_owned ()) ; t . on_stack . insert (v . to_owned ()) ; for w in successors (t . env , t . reachable , v) ? { if ! t . idx . contains_key (& w) { strongconnect (t , & w) ? ; let lw = t . low [& w] ; let lv = t . low . get_mut (v) . expect (\"v indexed\") ; * lv = (* lv) . min (lw) ; } else if t . on_stack . contains (& w) { let iw = t . idx [& w] ; let lv = t . low . get_mut (v) . expect (\"v indexed\") ; * lv = (* lv) . min (iw) ; } } if t . low [v] == t . idx [v] { let mut scc : Vec < String > = Vec :: new () ; loop { let w = t . stack . pop () . expect (\"stack non-empty while popping an SCC\") ; t . on_stack . remove (& w) ; let is_root = w == v ; scc . push (w) ; if is_root { break ; } } scc . sort () ; t . out . push (scc) ; } Ok (()) } let mut t = Tarjan { env , reachable : & reachable , index : 0 , idx : BTreeMap :: new () , low : BTreeMap :: new () , on_stack : BTreeSet :: new () , stack : Vec :: new () , out : Vec :: new () , } ; for f in & reachable { if ! t . idx . contains_key (f) { strongconnect (& mut t , f) ? ; } } let mut sccs = Vec :: with_capacity (t . out . len ()) ; for scc in t . out { let recursive = scc . len () > 1 || calls_in_fn (& scc [0] , & env . fns [& scc [0]] . body) ? . contains (& scc [0]) ; if recursive { sccs . push (scc) ; } } Ok (sccs) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "recursive_sccs"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 831,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The set of function/constructor/prim names a body calls (single-segment heads + bare paths). A\"] # [doc = \" superset filter — the caller intersects with `env.fns` to get function calls.\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" [`ElabError::DepthExceeded`] once the shared [`crate::totality::walk_expr`] traversal's own\"] # [doc = \" recursion exceeds its explicit budget (M-674) on a pathologically-nested `body` — a clean,\"] # [doc = \" explicit refusal rather than a host-stack overflow.\"] fn calls_in_fn (site : & str , body : & Expr) -> Result < BTreeSet < String > , ElabError > { let mut out = BTreeSet :: new () ; collect_calls (site , body , & mut out) ? ; Ok (out) }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "calls_in_fn"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 844,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn collect_calls (site : & str , e : & Expr , out : & mut BTreeSet < String >) -> Result < () , ElabError > { crate :: totality :: walk_expr (e , & mut | x | { if let Expr :: Path (p) = x { if p . 0 . len () == 1 { out . insert (p . 0 [0] . clone ()) ; } } }) . map_err (| e | ElabError :: DepthExceeded { site : site . to_owned () , limit : e . limit , }) }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "collect_calls"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 863,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Build the content-addressed data registry `Σ` (RFC-0001 §4.3 r3) from the checked environment's\"] # [doc = \" type declarations, so the elaborator can resolve constructor names to `#T#i` [`CtorRef`]s. A type\"] # [doc = \" carrying a field outside the r3 data fragment (e.g. a `Substrate` field) is skipped; if a\"] # [doc = \" *reachable* type references it, the registry build fails and the program is honestly `Residual`.\"] # [doc = \"\"] # [doc = \" Public so a differential / a consumer can rebuild the **same** registry the elaborator used\"] # [doc = \" (it is a pure, content-addressed function of `env.types`) — e.g. to map an L1 evaluator's\"] # [doc = \" name-keyed data value onto the elaborated L0 value's `#T#i` identity (NFR-7).\"] pub fn build_registry (env : & Env) -> Result < DataRegistry , ElabError > { let mut specs : BTreeMap < String , DeclSpec > = BTreeMap :: new () ; 'types : for (name , d) in & env . types { let mut ctors = Vec :: with_capacity (d . ctors . len ()) ; for c in & d . ctors { let mut fields = Vec :: with_capacity (c . fields . len ()) ; for f in & c . fields { match field_spec (f) { Some (fs) => fields . push (fs) , None => continue 'types , } } ctors . push (CtorSpec { fields }) ; } specs . insert (name . clone () , DeclSpec { ctors }) ; } DataRegistry :: build (& specs) . map_err (| e | ElabError :: Residual { site : \"<data registry>\" . to_owned () , what : format ! (\"a reachable data type is outside the r3 fragment: {e}\") , }) }",
      "reason": "unsupported Rust type form `& Env`",
      "item_name": "build_registry"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 893,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Convert a v0 field type to a registry [`FieldSpec`]; `None` for a type with no monomorphic r3\"] # [doc = \" value form. **Stage-1 (RFC-0007 §11.3):** a **generic instantiation** (`Data` with type arguments)\"] # [doc = \" or an **abstract type parameter** ([`Ty::Var`]) has no monomorphic registry form — its elaboration\"] # [doc = \" is *staged* (monomorphization is the M-657 follow-up). Returning `None` makes [`build_registry`]\"] # [doc = \" skip the owning declaration, so any *use* of a generic value surfaces as an explicit `Residual`\"] # [doc = \" (never a silent, half-monomorphized artifact — G2/VR-5).\"] # [doc = \"\"] # [doc = \" **`Ty::Fn` (ADR-033/DN-74, M-923).** A function-typed field lowers to the kernel\"] # [doc = \" [`FieldSpec::Fn`] primitive — Path A, the type-carrying full-signature encoding DN-74 ratified\"] # [doc = \" as the final FLAG-1 disposition (2026-07-02): the parameter type and the return type are each\"] # [doc = \" resolved to a [`FieldTyRef`] and folded into an [`FnSig`], so two `Fn`-typed fields with\"] # [doc = \" different signatures hash to **distinct** content addresses (the ADR-033 §10.1 same-arity\"] # [doc = \" collision is closed at the kernel level; `crates/mycelium-core/src/data.rs` §\\\"ADR-033 FLAG-1\"] # [doc = \" Path A\\\"). Curried multi-parameter arrows (`A => B => C`, M-822) compose naturally: each `Ty::Fn`\"] # [doc = \" contributes one parameter, with the return position recursing into a nested [`FieldTyRef::Fn`]\"] # [doc = \" for the remaining arrow — no separate multi-arity encoding needed. This replaces the former\"] # [doc = \" blanket `None` (staged `Residual`) for **concrete** signatures; a signature whose param/return\"] # [doc = \" resolves through a still-open type variable, an unresolved width, or a generic `Data`\"] # [doc = \" instantiation stays honestly staged (`ty_to_field_ty_ref` returns `None` for those leaves,\"] # [doc = \" narrowing — never eliminating — the residual, G2/VR-5).\"] # [doc = \"\"] # [doc = \" **Reachability note (honest scope, DN-74 new evidence).** `crate::mono`'s closure\"] # [doc = \" defunctionalization (RFC-0024 §4A/M-704) unconditionally rewrites every reachable `Ty::Fn` field\"] # [doc = \" into a closure tag-sum `Ty::Data` reference *before* [`elaborate`] ever calls [`build_registry`]\"] # [doc = \" — so through the standard `elaborate` entry point this arm is not reached (defunctionalization\"] # [doc = \" already produces a closed, executing L0 term for every such field, verified by the existing\"] # [doc = \" closures three-way differential). This `FieldSpec::Fn` lowering is exercised through\"] # [doc = \" [`build_registry`] called directly, and through [`elaborate_direct`] (below) — the narrow,\"] # [doc = \" additive entry point that targets the kernel primitive `elaborate` cannot reach without also\"] # [doc = \" changing `mono.rs`'s defunctionalization scope (out of this leaf's owned files).\"] fn field_spec (ty : & Ty) -> Option < FieldSpec > { Some (match ty { Ty :: Binary (crate :: checkty :: Width :: Lit (n)) => FieldSpec :: Repr (Repr :: Binary { width : * n }) , Ty :: Binary (crate :: checkty :: Width :: Var (_)) => return None , Ty :: Ternary (crate :: checkty :: Width :: Lit (m)) => FieldSpec :: Repr (Repr :: Ternary { trits : * m }) , Ty :: Ternary (crate :: checkty :: Width :: Var (_)) => return None , Ty :: Dense (d , s) => FieldSpec :: Repr (Repr :: Dense { dim : * d , dtype : scalar_kind (* s) , }) , Ty :: Vsa { model , dim , sparsity , } => FieldSpec :: Repr (Repr :: Vsa { model : model . clone () , dim : * dim , sparsity : sparsity_class (sparsity) , }) , Ty :: Seq (elem , n) => FieldSpec :: Repr (Repr :: Seq { elem : Box :: new (ty_to_repr (elem) ?) , len : * n , }) , Ty :: Bytes => FieldSpec :: Repr (Repr :: Bytes) , Ty :: Float => FieldSpec :: Repr (Repr :: Float { width : FloatWidth :: F64 , }) , Ty :: Data (n , args) if args . is_empty () => FieldSpec :: Data (n . clone ()) , Ty :: Data (_ , _) | Ty :: Var (_) => return None , Ty :: Substrate (_) => return None , Ty :: Fn (param , ret) => { let param = ty_to_field_ty_ref (param) ? ; let ret = ty_to_field_ty_ref (ret) ? ; FieldSpec :: Fn { arity : 1 , sig : FnSig { arity : 1 , params : vec ! [param] , ret : Box :: new (ret) , } , } } }) }",
      "reason": "unsupported Rust type form `& Ty`",
      "item_name": "field_spec"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 977,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Convert a **representation** [`Ty`] to its kernel [`Repr`] — the element-type resolver for a\"] # [doc = \" `Seq{T, N}` field (RFC-0032 D3). Returns `None` for any non-representation type (a `Data`/`Var`/\"] # [doc = \" `Fn` element has no monomorphic kernel repr in stage-1), so a `Seq` of such an element is itself\"] # [doc = \" staged (`field_spec` returns `None`) rather than half-elaborated (never a silent artifact —\"] # [doc = \" G2/VR-5).\"] fn ty_to_repr (ty : & Ty) -> Option < Repr > { Some (match ty { Ty :: Binary (crate :: checkty :: Width :: Lit (n)) => Repr :: Binary { width : * n } , Ty :: Binary (crate :: checkty :: Width :: Var (_)) => return None , Ty :: Ternary (crate :: checkty :: Width :: Lit (m)) => Repr :: Ternary { trits : * m } , Ty :: Ternary (crate :: checkty :: Width :: Var (_)) => return None , Ty :: Dense (d , s) => Repr :: Dense { dim : * d , dtype : scalar_kind (* s) , } , Ty :: Vsa { model , dim , sparsity , } => Repr :: Vsa { model : model . clone () , dim : * dim , sparsity : sparsity_class (sparsity) , } , Ty :: Seq (elem , n) => Repr :: Seq { elem : Box :: new (ty_to_repr (elem) ?) , len : * n , } , Ty :: Bytes => Repr :: Bytes , Ty :: Float => Repr :: Float { width : FloatWidth :: F64 , } , Ty :: Data (_ , _) | Ty :: Var (_) | Ty :: Substrate (_) | Ty :: Fn (_ , _) => return None , }) }",
      "reason": "unsupported Rust type form `& Ty`",
      "item_name": "ty_to_repr"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1015,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Convert a v0 type to a [`FieldTyRef`] — the leaf type a `Fn`-typed field's signature can hold\"] # [doc = \" (ADR-033 §10.2 Path A): a `Repr` leaf (via [`ty_to_repr`]), a *monomorphic* `Data` reference (by\"] # [doc = \" build-time name — resolved to a `ContentHash` at [`DataRegistry::build`] time, exactly like a\"] # [doc = \" top-level [`FieldSpec::Data`]), or a nested [`FieldTyRef::Fn`] for a higher-order/curried\"] # [doc = \" parameter or return. `None` for anything with no monomorphic form here — a generic `Data`\"] # [doc = \" instantiation, an unresolved [`Ty::Var`]/width, or [`Ty::Substrate`] (M-923; mirrors\"] # [doc = \" [`field_spec`]'s own staging: a signature leaf that cannot resolve keeps the *owning* `Fn`\"] # [doc = \" field staged, never a half-encoded signature — G2/VR-5).\"] fn ty_to_field_ty_ref (ty : & Ty) -> Option < FieldTyRef > { Some (match ty { Ty :: Data (n , args) if args . is_empty () => FieldTyRef :: Data (n . clone ()) , Ty :: Data (_ , _) | Ty :: Var (_) | Ty :: Substrate (_) => return None , Ty :: Fn (param , ret) => { let param = ty_to_field_ty_ref (param) ? ; let ret = ty_to_field_ty_ref (ret) ? ; FieldTyRef :: Fn (Box :: new (FnSig { arity : 1 , params : vec ! [param] , ret : Box :: new (ret) , })) } _ => FieldTyRef :: Repr (ty_to_repr (ty) ?) , }) }",
      "reason": "unsupported Rust type form `& Ty`",
      "item_name": "ty_to_field_ty_ref"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1042,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The `Scalar` → kernel `ScalarKind` mapping (shared with [`type_repr`]).\"] fn scalar_kind (s : Scalar) -> ScalarKind { match s { Scalar :: F16 => ScalarKind :: F16 , Scalar :: Bf16 => ScalarKind :: Bf16 , Scalar :: F32 => ScalarKind :: F32 , Scalar :: F64 => ScalarKind :: F64 , } }",
      "reason": "match pattern constructor `F16` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "scalar_kind"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1052,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The surface `Sparsity` → kernel `SparsityClass` mapping (M-892; shared with [`type_repr`]).\"] fn sparsity_class (sp : & Sparsity) -> SparsityClass { match sp { Sparsity :: Dense => SparsityClass :: Dense , Sparsity :: Sparse (k) => SparsityClass :: Sparse { max_active : * k } , } }",
      "reason": "unsupported Rust type form `& Sparsity`",
      "item_name": "sparsity_class"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1060,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The elaboration context: the checked environment, the data registry `Σ`, a fresh-name counter\"] # [doc = \" (for inlining + match/lambda binders), and the **recursion scope** — the reachable self-recursive\"] # [doc = \" functions mapped to their kernel `Fix` variables (RFC-0001 r4). A call to a name in `rec`\"] # [doc = \" elaborates to an `App` chain on its `Fix` var; every other function call still **inlines**.\"] # [doc = \" The elaborator's **explicit expression-nesting budget** — the elaborator's twin of\"] # [doc = \" `checkty::MAX_CHECK_DEPTH` and `parse::MAX_EXPR_DEPTH` (banked guard 4; A4-02). Elaboration runs on\"] # [doc = \" the deep worker stack ([`mycelium_stack`]) and only ever sees a *checked* program (so its depth is\"] # [doc = \" already ≤ the checker's budget), but it carries its own reified budget so it is **self-sufficient**:\"] # [doc = \" fed a hand-built `Env` straight through the API, it refuses past this with a clean [`ElabError`],\"] # [doc = \" never a host-stack overflow. Same value as the checker — the deep worker stack accommodates it with\"] # [doc = \" the same ~6× margin (measured ~24,600-level physical ceiling).\"] const MAX_ELAB_DEPTH : u32 = 4096 ;",
      "reason": "top-level `const MAX_ELAB_DEPTH` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "MAX_ELAB_DEPTH"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1073,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "pub (crate) struct Elab < 'e > { pub (crate) env : & 'e Env , pub (crate) registry : DataRegistry , pub (crate) fresh : u32 , pub (crate) rec : BTreeMap < String , String > , # [doc = \" Live expression-nesting depth for the explicit [`MAX_ELAB_DEPTH`] budget.\"] pub (crate) depth : u32 , }",
      "reason": "lifetime parameter `'e` has no grammar surface",
      "item_name": "Elab"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 1082,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Elab < '_ > { # [doc = \" A fresh kernel variable for surface name `base`. `%` is not an identifier character in the\"] # [doc = \" surface lexer, so fresh names can never capture or collide with surface binders.\"] fn fresh (& mut self , base : & str) -> String { let n = self . fresh ; self . fresh += 1 ; format ! (\"{base}%{n}\") } # [doc = \" The `#T#i` [`CtorRef`] for constructor `name`, resolved through the same `Env::ctor` lookup\"] # [doc = \" the checker uses (so the elaborator and the L1 evaluator agree on constructor identity).\"] fn ctor_ref (& self , name : & str) -> Option < CtorRef > { let (d , i) = self . env . ctor (name) ? ; self . registry . ctor_ref (& d . name , u32 :: try_from (i) . ok () ?) } # [doc = \" The surface→type view of `scope`, for re-inferring a scrutinee/bound type.\"] fn ty_scope (scope : & [Binding]) -> Vec < (String , Ty) > { scope . iter () . map (| (s , _ , t) | (s . clone () , t . clone ())) . collect () } # [doc = \" Depth-guarded entry to elaboration (banked guard 4): charge one nesting level against the\"] # [doc = \" explicit [`MAX_ELAB_DEPTH`] budget, refuse past it with a clean [`ElabError`] (never a\"] # [doc = \" host-stack overflow), and release the level on every exit path. Mirrors the parser's\"] # [doc = \" `parse_expr` guard and the checker's `Cx::enter`. All recursive elaboration goes through here.\"] fn expr (& mut self , stack : & mut Vec < String > , scope : & [Binding] , e : & Expr ,) -> Result < Node , ElabError > { self . depth += 1 ; if self . depth > MAX_ELAB_DEPTH { self . depth -= 1 ; let site = stack . last () . map_or (\"<elaborate>\" , String :: as_str) ; return residual (site , format ! (\"expression nesting exceeds the elaborator depth budget ({MAX_ELAB_DEPTH}) — an \\\n                     explicit budget (banked guard 4), refused cleanly rather than overflowing the \\\n                     host stack (RFC-0007 §4.6 clocked-recursion discipline)\") ,) ; } let r = self . expr_inner (stack , scope , e) ; self . depth -= 1 ; r } # [doc = \" Elaborate `e` under `scope` (surface name → kernel variable + type). `stack` is the call\"] # [doc = \" path — the cycle (recursion) detector and the error site. Always entered via [`Self::expr`]\"] # [doc = \" (the depth-budget guard), so it — and every `self.expr(…)` it makes — is depth-bounded.\"] fn expr_inner (& mut self , stack : & mut Vec < String > , scope : & [Binding] , e : & Expr ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"stack starts with the entry\") . clone () ; let site = site . as_str () ; match e { Expr :: Lit (Literal :: List (elems)) => { let mut vals = Vec :: with_capacity (elems . len ()) ; for el in elems { let reduced = self . expr (stack , scope , el) ? ; match & reduced { Node :: Const (v) => vals . push (v . clone ()) , _ => return residual (site , \"a list literal element did not reduce to a constant — the v0 `Seq` \\\n                                 surface constructs from constant elements only (RFC-0032 D3)\" ,) , } } let Some (first) = vals . first () else { return residual (site , \"an empty list literal `[]` has no element repr to anchor the `Seq` \\\n                         descriptor at elaboration (RFC-0032 D3); the empty-seq surface is staged\" ,) ; } ; let elem = first . repr () . clone () ; let len = u32 :: try_from (vals . len ()) . map_or (u32 :: MAX , | n | n) ; Value :: new (Repr :: Seq { elem : Box :: new (elem) , len , } , Payload :: Seq (vals) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed sequence literal: {e}\")) , | v | Ok (Node :: Const (v)) ,) } Expr :: Lit (l) => Ok (Node :: Const (lit_value (site , l) ?)) , Expr :: Path (p) => { if p . 0 . len () == 1 { let name = & p . 0 [0] ; if let Some ((_ , kvar , _)) = scope . iter () . rev () . find (| (s , _ , _) | s == name) { return Ok (Node :: Var (kvar . clone ())) ; } if let Some (kf) = self . rec . get (name) { return Ok (Node :: Var (kf . clone ())) ; } if self . env . ctor (name) . is_some () { let ctor = self . ctor_ref (name) . ok_or_else (| | ElabError :: Residual { site : site . to_owned () , what : format ! (\"`{name}` is outside the r3 data registry\") , }) ? ; return Ok (Node :: Construct { ctor , args : vec ! [] }) ; } if let Some (fd) = self . env . fns . get (name) { if ! fd . sig . params . is_empty () { return residual (site , format ! (\"generic function `{name}<…>` used as a value has no L0 form \\\n                                     yet (monomorphization staged — RFC-0007 §11.3, the M-657 \\\n                                     follow-up)\") ,) ; } return self . elab_fn_lam (name) ; } } residual (site , format ! (\"unresolved name `{}`\" , p . 0 . join (\".\"))) } Expr :: Let { name , ty : _ , bound , body , } => { let kbound = self . expr (stack , scope , bound) ? ; let bty = infer_type (self . env , & mut Self :: ty_scope (scope) , bound) . map_err (| e | { ElabError :: Residual { site : site . to_owned () , what : format ! (\"could not re-infer `let {name}`'s type: {e}\") , } }) ? ; let kvar = self . fresh (name) ; let mut inner = scope . to_vec () ; inner . push ((name . clone () , kvar . clone () , bty)) ; let kbody = self . expr (stack , & inner , body) ? ; Ok (Node :: Let { id : kvar , bound : Box :: new (kbound) , body : Box :: new (kbody) , }) } Expr :: If { cond , conseq , alt } => self . elab_if (stack , scope , cond , conseq , alt) , Expr :: Match { scrutinee , arms } => self . elab_match (stack , scope , scrutinee , arms) , Expr :: For { x , xs , acc , init , body , } => self . elab_for (stack , scope , x , xs , acc , init , body) , Expr :: Swap { value , target , policy , } => { let src = self . expr (stack , scope , value) ? ; let target_repr = type_repr (site , target) ? ; if matches ! (target_repr , Repr :: Dense { .. }) { return residual (site , \"Dense swap targets are staged — the standard three-way harness \\\n                         (BinaryTernarySwapEngine) does not cover Dense conversions; a Dense swap \\\n                         engine lands with E2-1/ADR-033 (DN-52 FLAG-1 → Explicit-Residual; \\\n                         freeze-ledger W5)\" ,) ; } Ok (Node :: Swap { src : Box :: new (src) , target : target_repr , policy : policy_name_ref (policy) , }) } Expr :: WithParadigm { .. } => residual (site , \"internal: a `with paradigm` block reached elaboration — the ambient resolution \\\n                 pass strips it (RFC-0012 §4.4)\" ,) , Expr :: Wild (body) => self . elab_wild (stack , scope , body) , Expr :: Spore (_) => residual (site , \"`spore` is deferred (E2-5/M-260)\") , Expr :: Consume (operand) => self . expr (stack , scope , operand) , Expr :: Colony (hyphae) => self . elab_colony (stack , scope , hyphae) , Expr :: Lambda { .. } => residual (site , \"internal: an `Expr::Lambda` reached elaboration — closures are lowered by \\\n                 monomorphization before elaborate (RFC-0024 §4A / M-704); this is a staging \\\n                 invariant break, never a silent accept (G2)\" ,) , Expr :: Ascribe (inner , _t) => { self . expr (stack , scope , inner) } Expr :: App { head , args } => self . app (stack , scope , head , args) , Expr :: Fuse { left , right } => { let lty = infer_type (self . env , & mut Self :: ty_scope (scope) , left) . map_err (| e | { ElabError :: Residual { site : site . to_owned () , what : format ! (\"could not re-infer `fuse` left-operand type: {e} (DN-58 §A.5)\") , } }) ? ; match & lty { Ty :: Binary (_) => { let la = self . expr (stack , scope , left) ? ; let ra = self . expr (stack , scope , right) ? ; Ok (Node :: Op { prim : \"fuse_join:binary\" . to_owned () , args : vec ! [la , ra] , }) } Ty :: Ternary (_) | Ty :: Dense (_ , _) | Ty :: Bytes | Ty :: Seq (_ , _) => residual (site , format ! (\"`fuse` over `{lty}` has no committed semilattice-meet prim in v0 — only \\\n                             the `Binary` repr meet (`fuse_join:binary`, bitwise-AND) and a user \\\n                             `Data` type with a `Fuse` instance execute (DN-58 §A.6 F-A3 defers the \\\n                             other paradigm meets); declare an `impl Fuse` or fuse `Binary` values \\\n                             (never a fabricated meet — G2)\") ,) , _ => residual (site , format ! (\"internal: a Data-type `fuse` over `{lty}` reached elaboration — \\\n                             monomorphization desugars a Data `fuse` to the resolved `Fuse::join` \\\n                             call (DN-58 §A.5); a `Fuse` node here means the instance was not resolved \\\n                             (never a fabricated lowering — G2)\") ,) , } } Expr :: Reclaim { policy , body } => { let policy_node = self . expr (stack , scope , policy) ? ; let body_node = self . expr (stack , scope , body) ? ; Ok (Node :: Let { id : self . fresh (\"reclaim_policy\") , bound : Box :: new (policy_node) , body : Box :: new (body_node) , }) } Expr :: TupleLit (_) => residual (site , \"internal: TupleLit survived to elaboration — the checker should have rewritten \\\n                 it to a constructor App (M-826; never silent, G2)\" ,) , } } # [doc = \" Lower a `wild { name(a₁,…,aₙ) }` block to a host-dispatch [`Node::Op`] in the reserved\"] # [doc = \" `wild:` prim namespace (RFC-0028 §4.2/§4.3; M-720). The body is the trusted/opaque FFI escape\"] # [doc = \" (M-661): it is **not** type-checked, so only its *shape* is interpreted here — a single\"] # [doc = \" host-call form `name(args…)` or a bare `name` with an undotted host-operation name. The\"] # [doc = \" arguments are ordinary in-scope expressions, elaborated through the normal path. Any other\"] # [doc = \" body shape is an explicit [`ElabError::Residual`] (never a fabricated lowering — G2).\"] # [doc = \"\"] # [doc = \" The resulting `Op { prim: \\\"wild:name\\\", … }` is resolved at runtime through the interpreter's\"] # [doc = \" prim registry — the capability handle (RFC-0028 §4.3) — which registers **no** `wild:` op by\"] # [doc = \" default, so an ungranted host op is a never-silent `UnknownPrim` (G2). The `wild:` prefix is\"] # [doc = \" reserved: no built-in paradigm primitive uses it, so a `wild:`-prefixed `Op` is unambiguously\"] # [doc = \" a host call. **No new Core-IR node** (KC-3).\"] fn elab_wild (& mut self , stack : & mut Vec < String > , scope : & [Binding] , body : & Expr ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let (name , args) : (& str , & [Expr]) = match body { Expr :: App { head , args } => match head . as_ref () { Expr :: Path (p) if p . 0 . len () == 1 => (p . 0 [0] . as_str () , args . as_slice ()) , _ => { return residual (& site , \"a v0 `wild` block body must be a host-call form `name(args…)` with a \\\n                         single, undotted host-operation name (RFC-0028 §4.2) — never a guess (G2)\" ,) } } , Expr :: Path (p) if p . 0 . len () == 1 => (p . 0 [0] . as_str () , & []) , _ => { return residual (& site , \"a v0 `wild` block body must be a host-call form `name(args…)` or a bare \\\n                     `name` (RFC-0028 §4.2); other body shapes are a future append-only extension \\\n                     — never a fabricated lowering (G2)\" ,) } } ; let prim = format ! (\"wild:{name}\") ; let mut elab_args = Vec :: with_capacity (args . len ()) ; for a in args { elab_args . push (self . expr (stack , scope , a) ?) ; } Ok (Node :: Op { prim , args : elab_args , }) } # [doc = \" `if c then t else e` desugars to a flat `Match` on the prelude `Bool` (RFC-0007 §4.4; the\"] # [doc = \" constructors `True`/`False` come from the same registry the surface checks against).\"] fn elab_if (& mut self , stack : & mut Vec < String > , scope : & [Binding] , cond : & Expr , conseq : & Expr , alt : & Expr ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let cond_node = self . expr (stack , scope , cond) ? ; let true_ref = self . bool_ctor (& site , \"True\") ? ; let false_ref = self . bool_ctor (& site , \"False\") ? ; let conseq_node = self . expr (stack , scope , conseq) ? ; let alt_node = self . expr (stack , scope , alt) ? ; let cond_var = self . fresh (\"cond\") ; let m = Node :: Match { scrutinee : Box :: new (Node :: Var (cond_var . clone ())) , alts : vec ! [Alt :: Ctor { ctor : true_ref , binders : vec ! [] , body : conseq_node , } , Alt :: Ctor { ctor : false_ref , binders : vec ! [] , body : alt_node , } ,] , default : None , } ; Ok (Node :: Let { id : cond_var , bound : Box :: new (cond_node) , body : Box :: new (m) , }) } fn bool_ctor (& self , site : & str , name : & str) -> Result < CtorRef , ElabError > { self . ctor_ref (name) . ok_or_else (| | ElabError :: Residual { site : site . to_owned () , what : format ! (\"the prelude `Bool` constructor `{name}` is missing from the registry\") , }) } # [doc = \" Lower a surface `match` to the flat L0 `Match` via the M-320 Maranget decision tree\"] # [doc = \" (RFC-0011 §4.4). Re-infers the scrutinee type, normalises each arm pattern (collecting binder\"] # [doc = \" occurrences), compiles the verified-`Fail`-free decision tree, and threads it into nested L0\"] # [doc = \" `Match` nodes — binding the scrutinee once in an enclosing `Let`.\"] fn elab_match (& mut self , stack : & mut Vec < String > , scope : & [Binding] , scrutinee : & Expr , arms : & [Arm] ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let sty = infer_type (self . env , & mut Self :: ty_scope (scope) , scrutinee) . map_err (| e | { ElabError :: Residual { site : site . clone () , what : format ! (\"could not re-infer the match scrutinee's type: {e}\") , } }) ? ; let scrut_node = self . expr (stack , scope , scrutinee) ? ; let scrut_var = self . fresh (\"scrut\") ; let mut matrix : Vec < Vec < crate :: usefulness :: Pat > > = Vec :: with_capacity (arms . len ()) ; let mut arm_binders : Vec < Vec < (String , Ty , Vec < usize >) > > = Vec :: with_capacity (arms . len ()) ; for arm in arms { let mut binds = Vec :: new () ; let pat = normalize_pattern (& self . env . types , & site , & arm . pattern , & sty , & [] , & mut binds) . map_err (| e | ElabError :: Residual { site : site . clone () , what : format ! (\"could not normalise a match pattern: {e}\") , }) ? ; matrix . push (vec ! [pat]) ; arm_binders . push (binds) ; } let arm_ix : Vec < usize > = (0 .. arms . len ()) . collect () ; let occ_root = [Vec :: < usize > :: new ()] ; let tree = decision :: compile (& self . env . types , & matrix , & arm_ix , & occ_root , & [sty]) . map_err (| e | ElabError :: Residual { site : site . clone () , what : format ! (\"match compilation exceeded the recursion budget: {e} (RFC-0041 §4.7)\") , }) ? ; if decision :: has_reachable_fail (& tree) { return residual (& site , \"the match compiled to a decision tree with a reachable Fail (usefulness and the \\\n                 Maranget compiler disagree) — refusing to emit an unsound L0 Match\" ,) ; } let mut occ_map : BTreeMap < Vec < usize > , String > = BTreeMap :: new () ; occ_map . insert (Vec :: new () , scrut_var . clone ()) ; let body = self . lower_tree (stack , scope , & tree , & occ_map , arms , & arm_binders) ? ; Ok (Node :: Let { id : scrut_var , bound : Box :: new (scrut_node) , body : Box :: new (body) , }) } # [doc = \" Lower a Maranget [`Tree`] into nested L0 `Match` nodes. `occ_map` maps each already-bound\"] # [doc = \" occurrence (a path into the scrutinee) to its kernel variable; a `Switch` matches on the\"] # [doc = \" occurrence's variable, a constructor case binds *all* its fields (extending `occ_map`), and a\"] # [doc = \" leaf elaborates the surface arm body with its binders resolved through `occ_map`.\"] fn lower_tree (& mut self , stack : & mut Vec < String > , scope : & [Binding] , tree : & Tree , occ_map : & BTreeMap < Vec < usize > , String > , arms : & [Arm] , arm_binders : & [Vec < (String , Ty , Vec < usize >) >] ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; match tree { Tree :: Leaf (i) => { let mut arm_scope = scope . to_vec () ; for (name , ty , occ) in & arm_binders [* i] { let kvar = occ_map . get (occ) . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"internal: binder `{name}` at occurrence {occ:?} was not bound by the \\\n                             decision tree\") , }) ? ; arm_scope . push ((name . clone () , kvar . clone () , ty . clone ())) ; } self . expr (stack , & arm_scope , & arms [* i] . body) } Tree :: Fail => residual (& site , \"internal: the decision tree reached a Fail (a checked-exhaustive match must not)\" ,) , Tree :: Switch { occurrence , cases , default , } => { let scrut_kvar = occ_map . get (occurrence) . cloned () . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"internal: switch occurrence {occurrence:?} is not bound\") , }) ? ; let mut alts = Vec :: with_capacity (cases . len ()) ; for (head , subtree) in cases { match head { Head :: Ctor (name , arity) => { let ctor = self . ctor_ref (name) . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"`{name}` is outside the r3 data registry\") , }) ? ; let binders : Vec < String > = (0 .. * arity) . map (| _ | self . fresh (name)) . collect () ; let mut child_map = occ_map . clone () ; for (j , b) in binders . iter () . enumerate () { let mut child = occurrence . clone () ; child . push (j) ; child_map . insert (child , b . clone ()) ; } let body = self . lower_tree (stack , scope , subtree , & child_map , arms , arm_binders ,) ? ; alts . push (Alt :: Ctor { ctor , binders , body , }) ; } Head :: Lit (key) => { let value = lit_key_to_value (& site , key) ? ; let body = self . lower_tree (stack , scope , subtree , occ_map , arms , arm_binders) ? ; alts . push (Alt :: Lit { value , body }) ; } } } let default_node = match default { Some (d) => Some (Box :: new (self . lower_tree (stack , scope , d , occ_map , arms , arm_binders ,) ?)) , None => None , } ; Ok (Node :: Match { scrutinee : Box :: new (Node :: Var (scrut_kvar)) , alts , default : default_node , }) } } } # [doc = \" Elaborate an application: prims become `Op` nodes; saturated constructors become `Construct`\"] # [doc = \" nodes; a call to a recursive function (in `self.rec`) becomes a curried `App` on its recursion\"] # [doc = \" variable (`Fix`/`FixGroup`), and every **other** user-function call **inlines** (the residual\"] # [doc = \" non-recursive call graph is acyclic, so inlining terminates).\"] fn app (& mut self , stack : & mut Vec < String > , scope : & [Binding] , head : & Expr , args : & [Expr] ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let site = site . as_str () ; let Expr :: Path (p) = head else { return residual (site , \"v0 application head must be a name (first-order)\") ; } ; if p . 0 . len () != 1 { return residual (site , format ! (\"dotted call `{}`\" , p . 0 . join (\".\"))) ; } let name = & p . 0 [0] ; if let Some ((_ , kvar , sty)) = scope . iter () . rev () . find (| (s , _ , _) | s == name) { if matches ! (sty , Ty :: Fn (_ , _)) { let mut node = Node :: Var (kvar . clone ()) ; for a in args { let karg = self . expr (stack , scope , a) ? ; node = Node :: App { func : Box :: new (node) , arg : Box :: new (karg) , } ; } return Ok (node) ; } } if let Some (kf) = self . rec . get (name) . cloned () { let mut node = Node :: Var (kf) ; for a in args { let karg = self . expr (stack , scope , a) ? ; node = Node :: App { func : Box :: new (node) , arg : Box :: new (karg) , } ; } return Ok (node) ; } if let Some (fd) = self . env . fns . get (name) { if ! fd . sig . params . is_empty () { return residual (site , format ! (\"generic function `{name}<…>` type-checks, but lowering a generic \\\n                         instantiation to L0 is staged (monomorphization — RFC-0007 §11.3; the \\\n                         M-657 follow-up). This call has no L0 form yet.\") ,) ; } if stack . iter () . any (| f | f == name) { return residual (site , format ! (\"`{name}` is in a call cycle that was not registered as recursive — internal \\\n                         elaboration invariant (every cycle should lower to `Fix`/`FixGroup`)\") ,) ; } let mut bindings = Vec :: new () ; for (param , arg) in fd . sig . value_params . iter () . zip (args) { let karg = self . expr (stack , scope , arg) ? ; let pty = resolve_ty (site , & self . env . types , & [] , & param . ty) . map (| (t , _) | t) . map_err (| e | ElabError :: Residual { site : site . to_owned () , what : format ! (\"could not resolve `{name}`'s parameter type: {e}\") , }) ? ; bindings . push ((param . name . clone () , self . fresh (& param . name) , karg , pty)) ; } let callee_scope : Vec < Binding > = bindings . iter () . map (| (s , k , _ , t) | (s . clone () , k . clone () , t . clone ())) . collect () ; stack . push (name . clone ()) ; let body = self . expr (stack , & callee_scope , & fd . body) ? ; stack . pop () ; let node = bindings . into_iter () . rev () . fold (body , | acc , (_ , kvar , karg , _) | Node :: Let { id : kvar , bound : Box :: new (karg) , body : Box :: new (acc) , }) ; return Ok (node) ; } if self . env . ctor (name) . is_some () { let ctor = self . ctor_ref (name) . ok_or_else (| | ElabError :: Residual { site : site . to_owned () , what : format ! (\"`{name}` is outside the r3 data registry\") , }) ? ; let mut kargs = Vec :: with_capacity (args . len ()) ; for a in args { kargs . push (self . expr (stack , scope , a) ?) ; } return Ok (Node :: Construct { ctor , args : kargs }) ; } if let Some (kernel) = prim_kernel_name (name) { let mut kargs = Vec :: new () ; for a in args { kargs . push (self . expr (stack , scope , a) ?) ; } return Ok (Node :: Op { prim : kernel . to_owned () , args : kargs , }) ; } if self . env . traits . values () . any (| tr | tr . sigs . iter () . any (| s | s . name == * name)) { return residual (site , format ! (\"trait-method call `{name}` type-checks, but dictionary-passing lowering to L0 \\\n                     is staged (RFC-0019 §4.4 / RFC-0007 §12.3; the M-673 follow-up). No L0 form yet.\") ,) ; } residual (site , format ! (\"unknown function/constructor/prim `{name}`\")) } # [doc = \" Elaborate a reachable **self-recursive** function `fname` to `Fix(kf, λparams. body)` — the\"] # [doc = \" closed form r4 uses for direct recursion (RFC-0007 §4.1; the v0 surface is first-order, so the\"] # [doc = \" body is closed except for its params, `kf`, and the other recursive functions in scope).\"] fn elab_recursive_fn (& mut self , fname : & str , kf : & str) -> Result < Node , ElabError > { Ok (Node :: Fix { name : kf . to_owned () , body : Box :: new (self . elab_fn_lam (fname) ?) , }) } # [doc = \" Elaborate `fname` to its curried lambda `λp1. … λpn. body` (params `p1` outermost), with the\"] # [doc = \" body in scope of the params and **every** recursion variable in `self.rec` (its own name plus\"] # [doc = \" any sibling in its group). This is the recursion-variable-agnostic core shared by\"] # [doc = \" [`Self::elab_recursive_fn`] (which wraps it in a `Fix`) and the `FixGroup` group lowering\"] # [doc = \" (which binds the lambdas of a mutually-recursive SCC together — RFC-0001 r5).\"] pub (crate) fn elab_fn_lam (& mut self , fname : & str) -> Result < Node , ElabError > { let fd = self . env . fns [fname] . clone () ; if ! fd . sig . params . is_empty () { return residual (fname , format ! (\"generic function `{fname}<…>` type-checks, but its L0 lowering is staged \\\n                     (monomorphization — RFC-0007 §11.3; the M-657 follow-up)\") ,) ; } let mut scope : Vec < Binding > = Vec :: new () ; let mut param_kvars : Vec < String > = Vec :: new () ; for p in & fd . sig . value_params { let kp = self . fresh (& p . name) ; let pty = resolve_ty (fname , & self . env . types , & [] , & p . ty) . map (| (t , _) | t) . map_err (| e | ElabError :: Residual { site : fname . to_owned () , what : format ! (\"could not resolve `{fname}`'s parameter type: {e}\") , }) ? ; scope . push ((p . name . clone () , kp . clone () , pty)) ; param_kvars . push (kp) ; } let mut stack = vec ! [fname . to_owned ()] ; let body = self . expr (& mut stack , & scope , & fd . body) ? ; Ok (param_kvars . into_iter () . rev () . fold (body , | acc , kp | Node :: Lam { param : kp , body : Box :: new (acc) , })) } # [doc = \" Elaborate `for x in xs, acc = init => body` to its synthesized self-recursive fold (RFC-0007\"] # [doc = \" §4.8), as an inline `Fix` over the linearly-recursive spine type:\"] # [doc = \"\"] # [doc = \" ```text\"] # [doc = \" App(App(Fix(fold, λs. λa. Match s {\"] # [doc = \"            Nil          => a,\"] # [doc = \"            Cons(x,rest) => App(App(fold, rest), body[acc↦a]) }),\"] # [doc = \"         xs), init)\"] # [doc = \" ```\"] # [doc = \"\"] # [doc = \" The nil/cons shape was already validated by the checker (`linear_elem_ty`); here we just read\"] # [doc = \" off the element/spine field positions from the registry.\"] # [allow (clippy :: too_many_arguments)] fn elab_for (& mut self , stack : & mut Vec < String > , scope : & [Binding] , x : & str , xs : & Expr , acc : & str , init : & Expr , body : & Expr ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let sty = infer_type (self . env , & mut Self :: ty_scope (scope) , xs) . map_err (| e | { ElabError :: Residual { site : site . clone () , what : format ! (\"could not infer the `for` spine type: {e}\") , } }) ? ; let Ty :: Data (tname , _) = & sty else { return residual (& site , format ! (\"`for` spine is not a data type: {sty}\")) ; } ; let d = self . env . types . get (tname) . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"unknown type `{tname}`\") , }) ? . clone () ; let mut nil_name : Option < String > = None ; let mut cons : Option < (String , usize , usize , Ty) > = None ; for c in & d . ctors { if c . fields . is_empty () { nil_name = Some (c . name . clone ()) ; continue ; } let Some (spine_idx) = c . fields . iter () . position (| f | matches ! (f , Ty :: Data (n , _) if n == tname)) else { return residual (& site , format ! (\"`for` constructor `{}` has no spine field\" , c . name) ,) ; } ; let Some (elem_idx) = (0 .. c . fields . len ()) . find (| & i | i != spine_idx) else { return residual (& site , format ! (\"`for` constructor `{}` has no element field\" , c . name) ,) ; } ; cons = Some ((c . name . clone () , elem_idx , spine_idx , c . fields [elem_idx] . clone () ,)) ; } let (Some (nil_name) , Some ((cons_name , _elem_idx , spine_idx , elem_ty))) = (nil_name , cons) else { return residual (& site , format ! (\"`for` needs a nil + cons shape on `{tname}`\") ,) ; } ; let aty = infer_type (self . env , & mut Self :: ty_scope (scope) , init) . map_err (| e | { ElabError :: Residual { site : site . clone () , what : format ! (\"could not infer the `for` accumulator type: {e}\") , } }) ? ; let xs_node = self . expr (stack , scope , xs) ? ; let init_node = self . expr (stack , scope , init) ? ; let fold = self . fresh (\"fold\") ; let s_kv = self . fresh (\"s\") ; let a_kv = self . fresh (\"acc\") ; let elem_kv = self . fresh (x) ; let spine_kv = self . fresh (\"rest\") ; let cons_arity = d . ctors . iter () . find (| c | c . name == cons_name) . expect (\"cons ctor present\") . fields . len () ; let binders : Vec < String > = (0 .. cons_arity) . map (| i | { if i == spine_idx { spine_kv . clone () } else { elem_kv . clone () } }) . collect () ; let mut body_scope = scope . to_vec () ; body_scope . push ((x . to_owned () , elem_kv . clone () , elem_ty)) ; body_scope . push ((acc . to_owned () , a_kv . clone () , aty)) ; let body_node = self . expr (stack , & body_scope , body) ? ; let nil_ref = self . ctor_ref (& nil_name) . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"`{nil_name}` is outside the r3 data registry\") , }) ? ; let cons_ref = self . ctor_ref (& cons_name) . ok_or_else (| | ElabError :: Residual { site : site . clone () , what : format ! (\"`{cons_name}` is outside the r3 data registry\") , }) ? ; let rec_call = Node :: App { func : Box :: new (Node :: App { func : Box :: new (Node :: Var (fold . clone ())) , arg : Box :: new (Node :: Var (spine_kv)) , }) , arg : Box :: new (body_node) , } ; let match_node = Node :: Match { scrutinee : Box :: new (Node :: Var (s_kv . clone ())) , alts : vec ! [Alt :: Ctor { ctor : nil_ref , binders : vec ! [] , body : Node :: Var (a_kv . clone ()) , } , Alt :: Ctor { ctor : cons_ref , binders , body : rec_call , } ,] , default : None , } ; let fix = Node :: Fix { name : fold , body : Box :: new (Node :: Lam { param : s_kv , body : Box :: new (Node :: Lam { param : a_kv , body : Box :: new (match_node) , }) , }) , } ; Ok (Node :: App { func : Box :: new (Node :: App { func : Box :: new (fix) , arg : Box :: new (xs_node) , }) , arg : Box :: new (init_node) , }) } # [doc = \" Elaborate `colony { hypha e1, …, hypha eN }` to its **RT2 spawn-order sequentialization**\"] # [doc = \" (RFC-0008 §4.2/RT2; M-666). RFC-0008 makes the *reference semantics* of a deterministic\"] # [doc = \" concurrent program its deterministic sequentialization, and content-addressing/the NFR-7\"] # [doc = \" differential are over that reference — so the honest L0 form is the sequentialization, **not**\"] # [doc = \" a concurrency node (the L0 Core IR has none; the trusted base stays sequential — KC-3). It\"] # [doc = \" lowers to a chain of `Let`s that evaluates each leading hypha for its (sequentialized) effect,\"] # [doc = \" in order, and yields the **last** hypha's value:\"] # [doc = \"\"] # [doc = \" ```text\"] # [doc = \" Let(_1, ⟦e1⟧, Let(_2, ⟦e2⟧, … ⟦eN⟧))      (each _i a fresh, `%`-named unused binder)\"] # [doc = \" ```\"] # [doc = \"\"] # [doc = \" Nothing is dropped silently (G2): every hypha body is elaborated and bound, so a leading\"] # [doc = \" hypha's refusal/divergence is preserved under CBV (RT4/I1). This sequentialization is the\"] # [doc = \" **RT2 reference ORACLE**: the real concurrent executor (`mycelium-mlir::runtime` —\"] # [doc = \" `Scope`/`Colony`/`Task`, structured fork/join, M-357) runs the colony's per-hypha L0 programs\"] # [doc = \" ([`elaborate_colony`]) as concurrent tasks and is **validated equal to this reference** (the\"] # [doc = \" RT2 differential, `mycelium_mlir::run_colony`); a divergence is an explicit error, never a\"] # [doc = \" silent race (G2/RT4). The concurrent run adds **no L0 kernel node** — the trusted base stays\"] # [doc = \" sequential (RFC-0008 §4.2; KC-3).\"] # [doc = \"\"] # [doc = \" Honesty (Declared at the surface; the lowering itself adds no guarantee): this realizes the\"] # [doc = \" *deterministic* R1 fragment (RFC-0008 §4.6 R1) only. With no v0 product type the colony's\"] # [doc = \" observable is the last hypha's value (the sequential reference's final step), never a\"] # [doc = \" fabricated join-product.\"] fn elab_colony (& mut self , stack : & mut Vec < String > , scope : & [Binding] , hyphae : & [crate :: ast :: Hypha] ,) -> Result < Node , ElabError > { let site = stack . last () . expect (\"non-empty\") . clone () ; let Some ((last , leading)) = hyphae . split_last () else { return residual (& site , \"internal: an empty `colony` reached elaboration — the parser requires ≥ 1 hypha \\\n                 (RFC-0008 §4.7)\" ,) ; } ; let mut node = self . elab_hypha_node (stack , scope , & site , last) ? ; for h in leading . iter () . rev () { let bound = self . elab_hypha_node (stack , scope , & site , h) ? ; let kvar = self . fresh (\"hypha\") ; node = Node :: Let { id : kvar , bound : Box :: new (bound) , body : Box :: new (node) , } ; } Ok (node) } # [doc = \" Elaborate one hypha's own contribution node, prefixed with its `@forage(policy)` policy\"] # [doc = \" evaluation if present (RFC-0008 RT3; DN-63 §3.5; M-906/DN-70 D1) —\"] # [doc = \" `Let{_=policy_node, node}`, mirroring `reclaim`'s `Let{_=policy, body}` sequential-reference\"] # [doc = \" shape (DN-58 §B) exactly: the policy is evaluated for its effect (semantics-free placement,\"] # [doc = \" RT3 — it never changes `node`'s value) after the static empty-candidate-set check\"] # [doc = \" ([`forage_reject_if_empty`]) has already refused an all-zero bitmask. No new L0 node (KC-3).\"] fn elab_hypha_node (& mut self , stack : & mut Vec < String > , scope : & [Binding] , site : & str , h : & crate :: ast :: Hypha ,) -> Result < Node , ElabError > { forage_reject_if_empty (site , h) ? ; let node = self . expr (stack , scope , & h . body) ? ; let Some (policy) = & h . forage else { return Ok (node) ; } ; let policy_node = self . expr (stack , scope , policy) ? ; Ok (Node :: Let { id : self . fresh (\"forage_policy\") , bound : Box :: new (policy_node) , body : Box :: new (node) , }) } }",
      "reason": "impl target type `Elab < '_ >` has no confirmed mapping (generic type path `Elab < '_ >` — type argument `'_` is not a type (lifetime / const-generic / associated-type binding-or-constraint); `type_args` admits only type_refs, so left an explicit gap (VR-5))",
      "item_name": "Elab < '_ >"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 2205,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Statically validate a hypha's `@forage(policy)` D-lite bitmask (RFC-0008 RT3; DN-63 §3.5\"] # [doc = \" FLAG-14; M-906/DN-70 D1). The checker guarantees `policy` is `Expr::Lit(Literal::Bin(_))` when\"] # [doc = \" present ([`crate::checkty::Cx::check_forage_policy`]); a defensive [`ElabError::Residual`]\"] # [doc = \" covers the (unreachable-on-a-checked-env) alternative — never a fabricated lowering (G2). An\"] # [doc = \" **all-zero mask** is the DN-63 FLAG-14 empty-candidate-set case: refused here, explicitly, as\"] # [doc = \" an [`ElabError::Residual`] (so neither elaborated path — L0-interp nor AOT — ever silently\"] # [doc = \" accepts a no-candidate forage) — the L1 evaluator refuses the *identical* source with a typed\"] # [doc = \" [`crate::eval::L1Error::Forage`] (`ForageError::NoCandidates`); see `differential.rs`'s\"] # [doc = \" `forage_no_candidates_is_an_explicit_refusal_on_every_path` for the three-way consistency\"] # [doc = \" check.\"] fn forage_reject_if_empty (site : & str , h : & crate :: ast :: Hypha) -> Result < () , ElabError > { let Some (policy) = & h . forage else { return Ok (()) ; } ; let Expr :: Lit (Literal :: Bin (s)) = policy . as_ref () else { return residual (site , \"internal: `@forage(policy)` reached elaboration with a non-literal policy — the \\\n             checker requires a literal binary bitmask (M-906/DN-70 D1); never a fabricated \\\n             lowering (G2)\" ,) ; } ; let popcount = s . chars () . filter (| c | * c == '1') . count () ; if popcount == 0 { return residual (site , \"`@forage(policy)` has an all-zero worker-availability bitmask — the D-lite \\\n             single-node candidate set is empty (DN-63 §3.5 FLAG-14); the L1 evaluator refuses \\\n             this identically with an explicit `ForageError::NoCandidates` (never a silent \\\n             placement — G2/RT4)\" ,) ; } Ok (()) }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "forage_reject_if_empty"
    },
    {
      "file": "crates/mycelium-l1/src/elab.rs",
      "line": 2240,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Reconstruct the L0 [`Value`] of a literal-pattern key (`b:1010` / `t:+0-`) produced by the\"] # [doc = \" checker's `literal_key` (the `_` separators already normalised away). The width is the digit\"] # [doc = \" count (Q6: a literal *is* its representation).\"] fn lit_key_to_value (site : & str , key : & str) -> Result < Value , ElabError > { if let Some (bits) = key . strip_prefix (\"b:\") { let bits : Vec < bool > = bits . chars () . map (| c | c == '1') . collect () ; let width = u32 :: try_from (bits . len ()) . expect (\"digit count fits u32\") ; Value :: new (Repr :: Binary { width } , Payload :: Bits (bits) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed binary literal key: {e}\")) , Ok ,) } else if let Some (trits) = key . strip_prefix (\"t:\") { let trits : Vec < Trit > = trits . chars () . map (| c | match c { '+' => Ok (Trit :: Pos) , '0' => Ok (Trit :: Zero) , '-' => Ok (Trit :: Neg) , other => Err (other) , }) . collect :: < Result < _ , _ > > () . map_or_else (| c | residual (site , format ! (\"non-trit char {c:?} in ternary literal key\")) , Ok ,) ? ; let width = u32 :: try_from (trits . len ()) . expect (\"trit count fits u32\") ; Value :: new (Repr :: Ternary { trits : width } , Payload :: Trits (trits) , Meta :: exact (Provenance :: Root) ,) . map_or_else (| e | residual (site , format ! (\"malformed ternary literal key: {e}\")) , Ok ,) } else if let Some (text) = key . strip_prefix (\"f:\") { residual (site , format ! (\"internal: a float literal-pattern key `f:{text}` reached elaboration — the \\\n                 checker refuses float patterns (ADR-040 FLAG-4)\") ,) } else { residual (site , format ! (\"unrecognised literal key `{key}`\")) } }",
      "reason": "unsupported Rust type form `& str`",
      "item_name": "lit_key_to_value"
    }
  ],
  "total_top_level_items": 37
}