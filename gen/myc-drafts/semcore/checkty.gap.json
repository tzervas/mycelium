{
  "source": "crates/mycelium-l1/src/checkty.rs",
  "emitted_items": [
    "Width",
    "CheckError",
    "EffectSource",
    "Stage3Fresh",
    "infer_type_with_active_affine",
    "infer_type_with_active_affine_sabotaged",
    "prim_name_is_recognized"
  ],
  "gaps": [
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use std :: cell :: Cell ;",
      "reason": "`use std.cell::Cell` — `std.cell` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 11,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use std :: collections :: { BTreeMap , BTreeSet } ;",
      "reason": "`use std.collections::BTreeMap` — `std.collections` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use std.collections::BTreeSet` — `std.collections` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 13,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: affine :: { Tracker , UseOutcome } ;",
      "reason": "`use affine::Tracker` — `affine` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use affine::UseOutcome` — `affine` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 14,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: ambient :: AmbientError ;",
      "reason": "`use ambient::AmbientError` — `ambient` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 15,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use crate :: ast :: { Arm , BaseType , DeriveDecl , Expr , FnDecl , FnSig , Hypha , ImplDecl , Item , Literal , LowerDecl , Nodule , ObjectDecl , Paradigm , Param , ParamKind , Path , Pattern , Phylum , Scalar , Sparsity , Strength , TraitRef , TypeDecl , TypeParam , TypeRef , UsePath , WidthRef , } ;",
      "reason": "`use ast::Arm` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::BaseType` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::DeriveDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Expr` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::FnDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::FnSig` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Hypha` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::ImplDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Item` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Literal` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::LowerDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Nodule` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::ObjectDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Paradigm` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Param` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::ParamKind` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Path` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Pattern` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Phylum` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Scalar` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Sparsity` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::Strength` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::TraitRef` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::TypeDecl` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::TypeParam` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::TypeRef` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::UsePath` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use ast::WidthRef` — `ast` is not a sibling module transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 21,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The checker's **explicit expression-nesting budget** (the \\\"banked guard 4\\\" discipline; A4-02).\"] # [doc = \" Type-checking recurses on the expression AST; rather than rely on the host call stack to bound\"] # [doc = \" that recursion (a resource that varies by thread and by IR frame size — never a semantic limit),\"] # [doc = \" the checker carries this reified budget and refuses past it with a clean [`CheckError`], exactly as\"] # [doc = \" the parser ([`crate::parse::MAX_EXPR_DEPTH`]) and the evaluator (`eval::DEFAULT_DEPTH`) do for their\"] # [doc = \" recursions. It is set comfortably **above** the parser's surface-nesting cap (so it never trips for\"] # [doc = \" parser-produced ASTs — it is the defense-in-depth ceiling for an AST handed straight to the checker\"] # [doc = \" via the API), and the recursion runs on a deep worker stack ([`mycelium_stack`]) so this budget — not a\"] # [doc = \" host-stack overflow — is always what bounds a pathological input.\"] # [doc = \"\"] # [doc = \" **Grounding (measured, not guessed).** The 256 MiB worker stack physically supports **~24,600**\"] # [doc = \" levels of `check` recursion in a debug build (empirically: 24,589 survives, 24,765 aborts;\"] # [doc = \" ~10.9 KiB/frame — release frames are smaller, so the ceiling is *higher* there). This budget\"] # [doc = \" (`4096`) is therefore a **~6× safety margin** below the measured physical ceiling, and **16×**\"] # [doc = \" above the parser's 256-deep surface cap — so a real (parsed) program is never within ~16× of it,\"] # [doc = \" and even a synthetic AST refuses cleanly with ~6× stack headroom to spare. Raising it is safe up to\"] # [doc = \" roughly a third of the physical ceiling; widen the worker stack first if more is ever wanted.\"] # [doc = \"\"] # [doc = \" **Self-hosting:** this explicit budget is the portable primitive (it carries over to the\"] # [doc = \" Mycelium-native frontend's clocked bounded-computation model); the worker stack is the transitional\"] # [doc = \" Rust-only adapter (`mycelium_stack`).\"] pub const MAX_CHECK_DEPTH : u32 = 4096 ;",
      "reason": "top-level `const MAX_CHECK_DEPTH` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "MAX_CHECK_DEPTH"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 44,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" RAII depth accounting for the checker's recursive [`Cx::check`] (paired with [`MAX_CHECK_DEPTH`]):\"] # [doc = \" increments the live nesting depth on entry and decrements it on drop, so the budget is honoured on\"] # [doc = \" **every** exit path (early `return`, `?`, or fall-through) — never a counter that leaks on error.\"] struct DepthGuard < 'a > (& 'a Cell < u32 >) ;",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "DepthGuard"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 49,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Drop for DepthGuard < '_ > { fn drop (& mut self) { self . 0 . set (self . 0 . get () . saturating_sub (1)) ; } }",
      "reason": "impl target type `DepthGuard < '_ >` has no confirmed mapping (generic type path `DepthGuard < '_ >` — type argument `'_` is not a type (lifetime / const-generic / associated-type binding-or-constraint); `type_args` admits only type_refs, so left an explicit gap (VR-5))",
      "item_name": "DepthGuard < '_ >"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 55,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" A width argument in a [`Ty::Binary`] or [`Ty::Ternary`] — either a concrete literal or an\"] # [doc = \" abstract width parameter (DN-42 / M-753). Width variables (`Var`) are introduced in step (b)\"] # [doc = \" and survive only during generic-fn checking; they must not reach elaboration.\"] # [derive (Debug , Clone , PartialEq , Eq , Hash)] pub enum Width { # [doc = \" A concrete width literal (e.g. `8` in `Binary{8}`).\"] Lit (u32) , # [doc = \" An abstract width parameter (e.g. `N` in `fn f<N>(x: Binary{N})`; DN-42 / M-753 v1).\"] Var (String) , }",
      "reason": "dropped non-doc attribute(s) on enum `Width`: # [derive (Debug , Clone , PartialEq , Eq , Hash)]",
      "item_name": "Width"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 66,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for Width { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { Width :: Lit (n) => write ! (f , \"{n}\") , Width :: Var (v) => write ! (f , \"{v}\") , } } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `fmt` signature: qualified type path `core :: fmt :: Formatter < '_ >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "Width"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 75,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" A checked type. Stage-0 is monomorphic; stage-1 (RFC-0007 §11) adds **type parameters as\"] # [doc = \" abstract variables** ([`Ty::Var`]) and **applied data types** ([`Ty::Data`] with arguments).\"] # [derive (Debug , Clone , PartialEq , Eq)] pub enum Ty { # [doc = \" `Binary{n}`.\"] Binary (Width) , # [doc = \" `Ternary{m}`.\"] Ternary (Width) , # [doc = \" `Dense{d, s}`.\"] Dense (u32 , Scalar) , # [doc = \" `VSA{model, dim, sparsity}` (RFC-0003 §3; M-892 — the type-level lift of the former\"] # [doc = \" blanket \\\"VSA types are deferred\\\" refusal). The `model` is the **kernel model id**\"] # [doc = \" (`\\\"MAP-I\\\"`, `\\\"FHRR\\\"`, `\\\"BSC\\\"`, …) — the surface ident is canonicalized by\"] # [doc = \" [`vsa_kernel_model_id`] at resolution (an ident cannot carry the kernel ids' `-`).\"] # [doc = \" Any model id is admissible as a type *mention* (exactly as `mycelium-core` type-checks\"] # [doc = \" hypervector mentions without the algebra — ADR-008); the `vsa_*` prims are additionally\"] # [doc = \" gated to their dispatch set by [`Checker::try_check_vsa_prim`]. Value construction is\"] # [doc = \" injection-only until a surface hypervector form lands (the M-890 Dense posture).\"] Vsa { # [doc = \" The kernel model id (canonicalized — ADR-003: the name here is metadata, matching\"] # [doc = \" `Repr::Vsa { model }`).\"] model : String , # [doc = \" Hypervector dimensionality.\"] dim : u32 , # [doc = \" Declared sparsity (the `vsa_*` prims require `Dense` at introduction — the kernel's\"] # [doc = \" Value-level ops construct dense-class results).\"] sparsity : Sparsity , } , # [doc = \" A registered data type applied to type arguments — `Data(\\\"List\\\", [Binary(8)])` is\"] # [doc = \" `List<Binary{8}>`; an empty argument vector is a monomorphic/nullary type (`Data(\\\"Bool\\\", [])`).\"] # [doc = \" Content addressing of declarations: RFC-0007 §4.2 (parameterized declarations are one registry\"] # [doc = \" entry); the prototype keys by name since v0 is single-nodule.\"] Data (String , Vec < Ty >) , # [doc = \" `Substrate{tag}` — the affine external-resource kind (LR-8). No value forms exist in v0.\"] Substrate (String) , # [doc = \" `Seq{T, N}` — a first-class indexed homogeneous sequence (RFC-0032 D3; M-749): `N` elements\"] # [doc = \" each of element type `T`. Constructed by the `[e1, …]` list literal; the surface checks\"] # [doc = \" elements are homogeneous and the count matches `N` (never-silent — G2). Guarantee: `Exact`\"] # [doc = \" (a literal *is* its representation).\"] Seq (Box < Ty > , u32) , # [doc = \" `Bytes` — a first-class byte string (RFC-0032 D4; M-750). Constructed by the `0x…` literal.\"] # [doc = \" Guarantee: `Exact`.\"] Bytes , # [doc = \" `Float` — the first-class scalar float, IEEE-754 binary64 (ADR-040; M-897). A nullary repr\"] # [doc = \" type like [`Ty::Bytes`] (the width set is F64-only at introduction — ADR-040 FLAG-1).\"] # [doc = \" Constructed by the decimal float literal (`1.5`), which denotes the **correctly-rounded**\"] # [doc = \" (RNE) binary64 of its decimal text (FLAG-3). Guarantee: `Exact` as a *definition* (the\"] # [doc = \" literal denotes exactly that binary64 value); the host-conversion claim is pinned\"] # [doc = \" `Empirical` by the round-trip conformance corpus (ADR-040 §2.6 — VR-5, no silent upgrade).\"] Float , # [doc = \" An **abstract type parameter** (a skolem variable) — in scope only while checking a generic\"] # [doc = \" declaration's constructors or a generic function's body (RFC-0007 §11.2). Two `Var`s are equal\"] # [doc = \" iff their names match; that structural equality is the engine of parametric checking. A\"] # [doc = \" `Var`-typed value is **representation-opaque**: no representation-specific `Op` may apply to it\"] # [doc = \" (this is the unbounded-case form of RFC-0019 §4.6's Repr-polymorphism restriction — it falls\"] # [doc = \" out of the abstract-variable discipline, restating S1 at the polymorphic level).\"] Var (String) , # [doc = \" A **function type** `A -> B` (RFC-0024 §3, M-686): the type of a named top-level function\"] # [doc = \" used as a first-class value. Stage-1 supports single-argument arrows only; the parameter\"] # [doc = \" type and return type may themselves be abstract (`Ty::Var`). A `Fn`-typed value is not a\"] # [doc = \" legal instance head (coherence is over concrete types — same posture as `Ty::Var`).\"] # [doc = \"\"] # [doc = \" Guarantee: `Declared` (a type-level contract; no theorem — VR-5).\"] Fn (Box < Ty > , Box < Ty >) , }",
      "reason": "enum variant/constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "Ty"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 141,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for Ty { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { Ty :: Binary (n) => write ! (f , \"Binary{{{n}}}\") , Ty :: Ternary (m) => write ! (f , \"Ternary{{{m}}}\") , Ty :: Dense (d , s) => write ! (f , \"Dense{{{d}, {s:?}}}\") , Ty :: Data (n , args) if args . is_empty () => write ! (f , \"{n}\") , Ty :: Data (n , args) => { write ! (f , \"{n}<\") ? ; for (i , a) in args . iter () . enumerate () { if i > 0 { write ! (f , \", \") ? ; } write ! (f , \"{a}\") ? ; } write ! (f , \">\") } Ty :: Substrate (t) => write ! (f , \"Substrate{{{t}}}\") , Ty :: Vsa { model , dim , sparsity , } => match sparsity { Sparsity :: Dense => write ! (f , \"VSA{{{model}, {dim}, Dense}}\") , Sparsity :: Sparse (k) => write ! (f , \"VSA{{{model}, {dim}, Sparse{{{k}}}}}\") , } , Ty :: Seq (elem , n) => write ! (f , \"Seq{{{elem}, {n}}}\") , Ty :: Bytes => write ! (f , \"Bytes\") , Ty :: Float => write ! (f , \"Float\") , Ty :: Var (v) => write ! (f , \"{v}\") , Ty :: Fn (a , r) if matches ! (a . as_ref () , Ty :: Fn (_ , _)) => write ! (f , \"({a}) => {r}\") , Ty :: Fn (a , r) => write ! (f , \"{a} => {r}\") , } } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `fmt` signature: qualified type path `core :: fmt :: Formatter < '_ >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "Ty"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 179,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" An explicit check failure (never a silent pass or a guess — S5/G2).\"] # [derive (Debug , Clone , PartialEq , Eq)] pub struct CheckError { # [doc = \" Which function (or item) the error is in, when known.\"] pub site : String , # [doc = \" What went wrong.\"] pub message : String , }",
      "reason": "dropped non-doc attribute(s) on struct `CheckError`: # [derive (Debug , Clone , PartialEq , Eq)]",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 179,
      "col": 1,
      "category": "NamedFieldDrop",
      "rust_construct": "NamedFieldDrop",
      "snippet": "# [doc = \" An explicit check failure (never a silent pass or a guess — S5/G2).\"] # [derive (Debug , Clone , PartialEq , Eq)] pub struct CheckError { # [doc = \" Which function (or item) the error is in, when known.\"] pub site : String , # [doc = \" What went wrong.\"] pub message : String , }",
      "reason": "struct `CheckError` named field(s) `site, message` emitted positionally as `CheckError(Bytes, Bytes)` — Mycelium's `constructor` is positional-only (no record surface); product structure preserved, field names dropped (matches `lib/std/*.myc` hand-ports)",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 188,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl CheckError { pub (crate) fn new (site : & str , message : impl Into < String >) -> Self { CheckError { site : site . to_owned () , message : message . into () , } } # [doc = \" Public, ergonomic constructor: a check failure at `site` with `message`. Mirrors the\"] # [doc = \" crate-internal `CheckError::new` (which stays private) so external callers — the\"] # [doc = \" toolchain crates that surface L1 diagnostics — can build a [`CheckError`] without reaching\"] # [doc = \" through the struct fields (Law of Demeter). Additive; no existing signature changes.\"] # [must_use] pub fn at (site : impl Into < String > , message : impl Into < String >) -> Self { CheckError { site : site . into () , message : message . into () , } } }",
      "reason": "no member of this impl block could be transpiled (2 sub-issue(s)): impl method `new` signature: unsupported Rust type form `impl Into < String >`; impl method `at` signature: unsupported Rust type form `impl Into < String >`",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 209,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for CheckError { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { write ! (f , \"check error in `{}`: {}\" , self . site , self . message) } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `fmt` signature: qualified type path `core :: fmt :: Formatter < '_ >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 215,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl std :: error :: Error for CheckError { }",
      "reason": "impl block has no items",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 217,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "# [doc = \" A resolution-pass refusal flows through the unified [`CheckError`] (the toolchain's one\"] # [doc = \" surface→checked-program error), preserving the never-silent site + message (RFC-0012 §4.3/§4.4).\"] impl From < AmbientError > for CheckError { fn from (e : AmbientError) -> Self { let site = match & e { AmbientError :: UnresolvedAmbient { site } | AmbientError :: ParadigmShapeMismatch { site , .. } | AmbientError :: BareDecimalNoEncoding { site , .. } | AmbientError :: DepthExceeded { site , .. } => site . clone () , AmbientError :: MultipleDefaults { .. } => \"<nodule>\" . to_owned () , } ; CheckError { site , message : e . to_string () , } } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `from` body: unsupported match pattern form `AmbientError :: UnresolvedAmbient { site }`",
      "item_name": "CheckError"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 235,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" One constructor of a registered data type.\"] # [derive (Debug , Clone , PartialEq)] pub struct CtorInfo { # [doc = \" Constructor name.\"] pub name : String , # [doc = \" Field types.\"] pub fields : Vec < Ty > , }",
      "reason": "struct `CtorInfo` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "CtorInfo"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 244,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A registered data type. **Stage-1 (RFC-0007 §11):** `params` are the type parameters (empty for a\"] # [doc = \" monomorphic type); a constructor's [`CtorInfo::fields`] may reference them as [`Ty::Var`]. The\"] # [doc = \" fields are stored *abstractly* (over the parameters) — [`subst_ty`] instantiates them at concrete\"] # [doc = \" type arguments when a value is constructed or matched.\"] # [derive (Debug , Clone , PartialEq)] pub struct DataInfo { # [doc = \" Type name — the **bare/local** (simple) name, unchanged from v0 (the registry-lookup key\"] # [doc = \" this crate has always used; DN-112 Rank 1 / M-1036 does not qualify this field, only the\"] # [doc = \" [`Ty::Data`] identity string derived from it via [`qualify_type_name`]).\"] pub name : String , # [doc = \" **Nodule-qualified type identity — the home** (DN-112 Rank 1 / M-1036): the declaring\"] # [doc = \" nodule's home string (`nodule.path` dot-joined, e.g. `\\\"a\\\"`, `\\\"a.b\\\"`), or [`PRELUDE_HOME`]\"] # [doc = \" for a prelude/synthetic type (`Bool`, `Tuple$N`) that must resolve identically under every\"] # [doc = \" nodule (the §9 uniform-home invariant). Stamped **once**, at first registration\"] # [doc = \" ([`register_types`]), from the **declaring** nodule — never the use-site (§9 invariant ii):\"] # [doc = \" an imported [`DataInfo`] clone carries its original declaring nodule's home unchanged\"] # [doc = \" through the export/import pipeline. [`qualify_type_name`] combines this with [`Self::name`]\"] # [doc = \" to produce the identity string embedded in a fresh [`Ty::Data`] (e.g. `\\\"a::T\\\"`); the\"] # [doc = \" registry KEY (this map's `String` key) stays bare/simple throughout — only the `Ty::Data`\"] # [doc = \" identity is qualified (KC-3 — the ~79 destructuring arms DN-112 §3 counts need no change).\"] pub home : String , # [doc = \" Type parameters, in declaration order (empty for a monomorphic type). `List<A>` ⇒ `[\\\"A\\\"]`.\"] pub params : Vec < String > , # [doc = \" Constructors, in declaration order (the index is the `#type#i` of RFC-0007 §4.2). Field types\"] # [doc = \" are abstract over `params` (may contain [`Ty::Var`]).\"] pub ctors : Vec < CtorInfo > , }",
      "reason": "struct `DataInfo` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "DataInfo"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 272,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A registered **trait** (RFC-0019 §4.2; LR-2). Stage-1 is single-parameter, but the structure\"] # [doc = \" carries `params: Vec<String>` uniformly (the trait's type-variable names, in scope as `Ty::Var`\"] # [doc = \" while its method signatures are checked). The method `sigs` are stored as their surface\"] # [doc = \" [`FnSig`]s; an `impl`'s methods are checked against the trait's sigs with `params ↦ trait_args`.\"] # [derive (Debug , Clone , PartialEq)] pub struct TraitInfo { # [doc = \" Trait name.\"] pub name : String , # [doc = \" Type-parameter names (single-parameter in stage-1; abstract over the method sigs).\"] pub params : Vec < String > , # [doc = \" The required method signatures, in declaration order.\"] pub sigs : Vec < FnSig > , }",
      "reason": "struct `TraitInfo` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "TraitInfo"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 286,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A registered **instance** `impl Trait<args> for T` (RFC-0019 §4.5). Keyed in [`Env::instances`]\"] # [doc = \" by `(trait_name, type_head(for_ty))`; the full `for_ty`/`trait_args` are kept for `EXPLAIN` and\"] # [doc = \" the staged dictionary lowering (M-673).\"] # [derive (Debug , Clone , PartialEq)] pub struct InstanceInfo { # [doc = \" The trait this instances.\"] pub trait_name : String , # [doc = \" The (concrete) trait arguments.\"] pub trait_args : Vec < Ty > , # [doc = \" The (concrete) type the instance is for.\"] pub for_ty : Ty , # [doc = \" The provided method names (a non-silent record of what the dictionary supplies).\"] pub methods : Vec < String > , }",
      "reason": "struct `InstanceInfo` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "InstanceInfo"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 301,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The **coherence key** of a type (RFC-0019 §4.5): the head a `(trait, type-head)` instance key is\"] # [doc = \" computed from. Width/shape is intentionally **erased** — stage-1 keys per *head*, conservatively\"] # [doc = \" rejecting two instances on the same head even at different widths (a documented, deferrable\"] # [doc = \" refinement; the alternative, width-granular keying, needs the role/variance machinery deferred to\"] # [doc = \" v2). A bare type **variable** is not a legal instance head in stage-1 (an `impl … for T` over an\"] # [doc = \" abstract `T` would be a blanket instance — refused explicitly, never silently), so `Ty::Var`\"] # [doc = \" yields `None`.\"] # [must_use] pub fn type_head (ty : & Ty) -> Option < String > { Some (match ty { Ty :: Binary (_) => \"Binary\" . to_owned () , Ty :: Ternary (_) => \"Ternary\" . to_owned () , Ty :: Dense (_ , _) => \"Dense\" . to_owned () , Ty :: Vsa { .. } => \"Vsa\" . to_owned () , Ty :: Substrate (t) => format ! (\"Substrate:{t}\") , Ty :: Seq (_ , _) => \"Seq\" . to_owned () , Ty :: Bytes => \"Bytes\" . to_owned () , Ty :: Float => \"Float\" . to_owned () , Ty :: Data (n , _) => format ! (\"Data:{n}\") , Ty :: Var (_) | Ty :: Fn (_ , _) => return None , }) }",
      "reason": "match pattern constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "type_head"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 344,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The single reserved home for prelude/synthetic types (`Bool`, `Tuple$N`) that must resolve\"] # [doc = \" **identically** under every nodule (DN-112 §9 invariant i). A type registered under this home is\"] # [doc = \" never qualified by [`qualify_type_name`] — its `Ty::Data` identity stays the bare name, exactly\"] # [doc = \" as before this fix, so every nodule sees the same builtin regardless of which nodule's\"] # [doc = \" registration pass happened to seed it (`Bool` is re-seeded per nodule via [`prelude`]; `Tuple$N`\"] # [doc = \" is synthesized on demand per nodule via [`synthetic_tuple_data`]).\"] pub (crate) const PRELUDE_HOME : & str = \"<prelude>\" ;",
      "reason": "top-level `const PRELUDE_HOME` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "PRELUDE_HOME"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 352,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" A nodule's **home** string for type-identity qualification (DN-112 Rank 1): its dot-joined path\"] # [doc = \" (`\\\"a\\\"`, `\\\"a.b\\\"`), or `\\\"\\\"` for a path-less (anonymous, header-less) nodule. An empty home is\"] # [doc = \" treated as bare/unqualified by [`qualify_type_name`] (the same \\\"stay bare\\\" treatment as\"] # [doc = \" [`PRELUDE_HOME`]) — a documented, narrow residual: two distinct anonymous nodules in one phylum\"] # [doc = \" would share the empty home, but the pre-existing [`PhylumEnv::link`] cross-nodule bare-name\"] # [doc = \" collision refusal already rejects that shape independent of this fix, and every multi-nodule\"] # [doc = \" fixture in this crate's test corpus uses an explicit `nodule NAME;` header.\"] # [must_use] pub (crate) fn nodule_home (path : & Path) -> String { path . 0 . join (\".\") }",
      "reason": "field access on a non-`self` base — the transpiler tracks no local types, so the projection cannot be resolved to a constructor position (only `self.<field>` desugars to a `match`)",
      "item_name": "nodule_home"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 364,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Qualify a type's bare/local name with its declaring nodule's `home` (DN-112 Rank 1 / M-1036) —\"] # [doc = \" the identity embedded in a freshly-constructed [`Ty::Data`]. A [`PRELUDE_HOME`] (or empty) home\"] # [doc = \" stays **bare** (the single-reserved-home exemption, §9 invariant i) — so `Bool`/`Tuple$N` mangle\"] # [doc = \" and compare exactly as they did before this fix, and every existing bare-name literal comparison\"] # [doc = \" against them (`n == \\\"Bool\\\"`) is unaffected.\"] # [must_use] pub (crate) fn qualify_type_name (home : & str , bare : & str) -> String { if home . is_empty () || home == PRELUDE_HOME { bare . to_owned () } else { format ! (\"{home}::{bare}\") } }",
      "reason": "Rust ownership/identity-conversion no-op method `.to_owned()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `to_owned(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `to_owned`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5)",
      "item_name": "qualify_type_name"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 378,
      "col": 1,
      "category": "Closure",
      "rust_construct": "Closure",
      "snippet": "# [doc = \" The **local/bare** segment of a (possibly nodule-qualified) `Ty::Data` identity — the part after\"] # [doc = \" the last `::` separator, or the whole string if unqualified (DN-112 Rank 1). `::` is not a legal\"] # [doc = \" Mycelium surface-identifier character, so this split is unambiguous and never mistakes a real\"] # [doc = \" identifier for a qualified one. Used wherever a heuristic or lookup needs the type's simple name\"] # [doc = \" regardless of which nodule declared it — the `$try` postfix's `Result`/`Option` naming\"] # [doc = \" convention (any nodule may declare its own type literally named `Result`), or the L0 bridge in\"] # [doc = \" `crate::elab` (which stays bare/nodule-agnostic by design — DN-112 §3, no runtime change).\"] # [must_use] pub (crate) fn ty_local_name (qualified_or_bare : & str) -> & str { qualified_or_bare . rsplit_once (\"::\") . map_or (qualified_or_bare , | (_ , local) | local) }",
      "reason": "closure parameter `(_ , local)` has no explicit type annotation — Mycelium's `lambda` parameters are always `name: Type` (grammar `lambda_expr` / `param`) and this transpiler has no type-inference pass to recover an omitted Rust closure-param type",
      "item_name": "ty_local_name"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 392,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" Look up a data type by its `Ty::Data` identity string, which may be **qualified** (any consumer\"] # [doc = \" downstream of the checker — `crate::mono`/`crate::elab`/`crate::decision`/`crate::usefulness`/\"] # [doc = \" `crate::fuse` — sees the checked, home-stamped name) or **bare** (the checking-time surface\"] # [doc = \" registry `resolve_ty` itself consults, always keyed by simple name; DN-112 Rank 1 / M-1036).\"] # [doc = \" Tries the exact key first — the common case, and the *only* case for a genuinely bare surface\"] # [doc = \" name, so this is a pure passthrough for every pre-existing (single-nodule) call site — then\"] # [doc = \" falls back to the local/bare segment, which is what the (unchanged, simple-name-keyed) registry\"] # [doc = \" `BTreeMap` is keyed by. Never *wrongly* matches within a single-registry lookup: the registry\"] # [doc = \" keys are bare, so the fallback only ever succeeds when the qualified input's local part is\"] # [doc = \" genuinely registered *in that registry*.\"] # [doc = \"\"] # [doc = \" **CORRECTED (CRITICAL #2, post-landing — the claim below was FALSE; VR-5 downgrade).** The\"] # [doc = \" original doc text here claimed *\\\"the static check stays sound... this cannot admit an unsound\"] # [doc = \" accept... an unwitnessed elaboration-time misresolution risk... out of DN-112 Rank 1's stated\"] # [doc = \" check-time scope.\\\"* That is **wrong**: this exact fallback is also reached from **inside the\"] # [doc = \" checker itself** — [`normalize_pattern`] is called from `Cx::check_pattern` (check-time, not\"] # [doc = \" merely elaboration), and a same-nodule-shadow-plus-legitimate-cross-nodule-reach program was\"] # [doc = \" reproduced type-checking a pattern binder to the SHADOW's `DataInfo` (a `Binary{4}` local decoy)\"] # [doc = \" while the scrutinee's real runtime value was the FOREIGN type's shape (`Binary{8}`) — a\"] # [doc = \" checker-accepted static/runtime type mismatch, unsealed, no `priv` needed. Closed at the\"] # [doc = \" pattern-normalization call sites via [`lookup_data_home_checked`] (conservative refuse — see its\"] # [doc = \" own doc comment for why the full correct-home resolution is not reachable from a single\"] # [doc = \" bare-keyed registry). **Every other consumer of this plain `lookup_data`** (`crate::mono`,\"] # [doc = \" `crate::elab`'s non-pattern paths, `crate::decision`, `crate::usefulness`, `crate::fuse`) is\"] # [doc = \" **unaudited by this fix** — the same latent risk this doc originally (mis)claimed was merely an\"] # [doc = \" elaboration-time curiosity may be reachable through them too; treat as an **open, disclosed\"] # [doc = \" residual**, not re-asserted-safe (G2/VR-5 — downgrade honestly, never claim more than checked).\"] # [doc = \"\"] # [doc = \" **Original (uncorrected) framing, kept for the historical record — DO NOT treat as current:**\"] # [doc = \" *\\\"the consulted registry is a single nodule's own `Env.types`... if that nodule (a) declares its\"] # [doc = \" own local type of the same bare name as a foreign type and (b) legitimately reaches a value of\"] # [doc = \" the foreign type... this fallback resolves the foreign qualified name to the local shadow's\"] # [doc = \" `DataInfo`... The static check stays sound... this cannot admit an unsound accept.\\\"* This framing\"] # [doc = \" is superseded by the correction above.\"] # [must_use] pub (crate) fn lookup_data < 'a > (types : & 'a BTreeMap < String , DataInfo > , name : & str ,) -> Option < & 'a DataInfo > { types . get (name) . or_else (| | types . get (ty_local_name (name))) }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "lookup_data"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 434,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" **Home-checked** variant of [`lookup_data`] (CRITICAL #2 fix, DN-112 Rank 1 / M-1036 residual\"] # [doc = \" close). Used at the pattern-normalization call sites ([`normalize_pattern`], shared by\"] # [doc = \" `Cx::check_pattern` at check-time and the elaborator) — the sites where [`lookup_data`]'s own\"] # [doc = \" bare-keyed fallback was found to be reachable from **inside the checker itself**, not merely\"] # [doc = \" elaboration, making the type/runtime mismatch **checker-accepted** rather than an elaboration-only\"] # [doc = \" curiosity (see the corrected doc comment on [`lookup_data`] above).\"] # [doc = \"\"] # [doc = \" When the exact-key lookup misses and the bare-local fallback would resolve a **qualified** `name`\"] # [doc = \" (one with a `home::` prefix) against a `DataInfo` whose own [`DataInfo::home`] is **different**\"] # [doc = \" from that prefix — the exact same-nodule-shadow-plus-legitimate-cross-nodule-reach shape — this\"] # [doc = \" **refuses explicitly** (G2) rather than silently proceeding with the wrong declaration's\"] # [doc = \" ctors/field-shape. This is the **conservative (b) closure**, not the full (a) correct-home\"] # [doc = \" resolution: a single bare-keyed registry (own decls shadow imports, RFC-0006 §4.3) holds at most\"] # [doc = \" ONE `DataInfo` per bare name, so once a nodule locally shadows a foreign type's bare name, the\"] # [doc = \" foreign type's own `DataInfo` is not reachable from `types` at all — there is nothing correct to\"] # [doc = \" resolve against here. **The full (a) fix — resolving against the foreign type's own `DataInfo`\"] # [doc = \" via per-import-provenance-scoped resolution — needs a materially larger change in `crate::mono`\"] # [doc = \" (per-import registries, not one merged bare-keyed map) and is an explicit, disclosed residual, not\"] # [doc = \" silently deferred (G2/VR-5).** A false-refuse of the legitimate shadow+cross-nodule-pattern shape\"] # [doc = \" is sound (conservative); the prior false-accept was not.\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" A [`CheckError`] iff `name` is not registered at all under its own bare key (an internal\"] # [doc = \" consistency violation — the same condition [`lookup_data`]'s callers previously `.expect()`ed,\"] # [doc = \" now surfaced never-silently instead of panicking), or iff `name` is nodule-qualified and its home\"] # [doc = \" prefix does not match the resolved [`DataInfo::home`] (the shadow/home-mismatch case above).\"] pub (crate) fn lookup_data_home_checked < 'a > (types : & 'a BTreeMap < String , DataInfo > , name : & str , site : & str ,) -> Result < & 'a DataInfo , CheckError > { if let Some (info) = types . get (name) { return Ok (info) ; } let local = ty_local_name (name) ; let Some (info) = types . get (local) else { return Err (CheckError :: new (site , format ! (\"internal: data type `{name}` is not registered in this nodule's own type \\\n                 registry — a checked type escaped registration (report this; never a silent \\\n                 default, G2)\") ,)) ; } ; if let Some (home) = data_home_mismatch (name , info) { return Err (CheckError :: new (site , data_home_mismatch_message (name , home , info) ,)) ; } Ok (info) }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "lookup_data_home_checked"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 488,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" The qualifier-home mismatch, if any: `Some(home)` iff `name` is nodule-qualified (has a `::`\"] # [doc = \" separator) and that qualifier `home` differs from `info`'s own declared [`DataInfo::home`] — the\"] # [doc = \" same-nodule-shadow-plus-legitimate-cross-nodule-reach shape [`lookup_data_home_checked`] refuses\"] # [doc = \" (DN-112 Rank 1 / M-1036). `name`'s qualifier prefix is everything before the LAST `::` (home\"] # [doc = \" strings use `.` as their OWN internal separator — `nodule_home` — so a single `::` unambiguously\"] # [doc = \" splits home from bare name; see [`ty_local_name`]'s doc comment). An unqualified `name` (no `::`)\"] # [doc = \" never mismatches — the common own-type / single-nodule surface-syntax case, structurally\"] # [doc = \" unaffected. Shared by [`lookup_data_home_checked`] (the checker-side pattern-normalization sites)\"] # [doc = \" and [`resolve_ty`]'s own `BaseType::Named` arm (the re-resolution round-trip a *qualified* name\"] # [doc = \" can reach via `crate::mono`'s `ty_to_source_ref`, DN-112 Rank 1 residual close, KC-3/DRY: one\"] # [doc = \" mismatch predicate, not two divergent copies).\"] fn data_home_mismatch < 'a > (name : & 'a str , info : & DataInfo) -> Option < & 'a str > { let (home , _) = name . rsplit_once (\"::\") ? ; (home != info . home) . then_some (home) }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "data_home_mismatch"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 504,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The shared home-mismatch [`CheckError`] message — identical wording regardless of which caller\"] # [doc = \" ([`lookup_data_home_checked`] or [`resolve_ty`]) detected the mismatch, so an `EXPLAIN`/audit\"] # [doc = \" reader sees one consistent diagnostic for this exploit class (G2 — never silent, and never two\"] # [doc = \" subtly-different refusal texts for the same underlying shape).\"] fn data_home_mismatch_message (name : & str , home : & str , info : & DataInfo) -> String { format ! (\"type-identity mismatch: `{name}` resolves, in this nodule's own type registry, to a \\\n         DIFFERENT declaration of the same bare name — `{}` is declared in `{}`, not the expected \\\n         `{home}` (a local type shadows a foreign-home type of the same name, and this position \\\n         legitimately reaches a value of the foreign type — CRITICAL #2 / DN-112 Rank 1 residual \\\n         close; refused rather than silently checked against the wrong declaration's shape, G2/VR-5)\" , info . name , info . home) }",
      "reason": "unsupported expression form `format ! (\"type-identity mismatch: `{name}` resolves, in this nodule's own type registry, to a \\\n         DIFFERENT declaration of the same bare name — `{}` is declared in `{}`, not the expected \\\n         `{home}` (a local type shadows a foreign-home type of the same name, and this position \\\n         legitimately reaches a value of the foreign type — CRITICAL #2 / DN-112 Rank 1 residual \\\n         close; refused rather than silently checked against the wrong declaration's shape, G2/VR-5)\" , info . name , info . home)`",
      "item_name": "data_home_mismatch_message"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 519,
      "col": 1,
      "category": "Closure",
      "rust_construct": "Closure",
      "snippet": "# [doc = \" Substitute type arguments for the abstract parameters in a stage-1 type (RFC-0007 §11.2): replace\"] # [doc = \" each [`Ty::Var`] by its binding in `s`, recursing into [`Ty::Data`] arguments. A `Var` with no\"] # [doc = \" binding is left as-is (it is a parameter still in scope — e.g. while checking a generic body). This\"] # [doc = \" is total and never inserts a `Swap` (S1): it only renames/instantiates type structure.\"] pub (crate) fn subst_ty (ty : & Ty , s : & BTreeMap < String , Ty >) -> Ty { match ty { Ty :: Var (v) => s . get (v) . cloned () . unwrap_or_else (| | ty . clone ()) , Ty :: Data (n , args) => Ty :: Data (n . clone () , args . iter () . map (| a | subst_ty (a , s)) . collect ()) , Ty :: Fn (a , r) => Ty :: Fn (Box :: new (subst_ty (a , s)) , Box :: new (subst_ty (r , s))) , Ty :: Seq (elem , n) => Ty :: Seq (Box :: new (subst_ty (elem , s)) , * n) , Ty :: Binary (Width :: Var (v)) => s . get (v) . map (| carrier | match carrier { Ty :: Binary (Width :: Lit (n)) => Ty :: Binary (Width :: Lit (* n)) , _ => ty . clone () , }) . unwrap_or_else (| | ty . clone ()) , Ty :: Ternary (Width :: Var (v)) => s . get (v) . map (| carrier | match carrier { Ty :: Binary (Width :: Lit (n)) => Ty :: Ternary (Width :: Lit (* n)) , _ => ty . clone () , }) . unwrap_or_else (| | ty . clone ()) , Ty :: Binary (Width :: Lit (_)) | Ty :: Ternary (Width :: Lit (_)) | Ty :: Dense (_ , _) | Ty :: Vsa { .. } | Ty :: Substrate (_) | Ty :: Bytes | Ty :: Float => ty . clone () , } }",
      "reason": "a zero-parameter closure has no v0 `lambda` form (grammar note on `lambda_expr` — a never-silent refusal, G2)",
      "item_name": "subst_ty"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 564,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Build the parameter→argument substitution for a data type's constructor fields (RFC-0007 §11.2):\"] # [doc = \" pairs each declared parameter name with the corresponding concrete type argument. A mismatched\"] # [doc = \" length yields a partial map (the caller has already arity-checked, or is in a position where the\"] # [doc = \" extra/missing entries simply do not substitute — never a panic).\"] pub (crate) fn param_subst (params : & [String] , args : & [Ty]) -> BTreeMap < String , Ty > { params . iter () . cloned () . zip (args . iter () . cloned ()) . collect () }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "param_subst"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 572,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Does `ty` mention any abstract type parameter ([`Ty::Var`])? Used to decide whether a (partially\"] # [doc = \" substituted) declared type is concrete enough to drive a bidirectional check, or must let the\"] # [doc = \" argument synthesize its own type so the parameter can be inferred (RFC-0007 §11.3).\"] pub (crate) fn has_var (ty : & Ty) -> bool { match ty { Ty :: Var (_) => true , Ty :: Data (_ , args) => args . iter () . any (has_var) , Ty :: Fn (a , r) => has_var (a) || has_var (r) , Ty :: Seq (elem , _) => has_var (elem) , Ty :: Binary (Width :: Var (_)) | Ty :: Ternary (Width :: Var (_)) => true , Ty :: Binary (Width :: Lit (_)) | Ty :: Ternary (Width :: Lit (_)) | Ty :: Dense (_ , _) | Ty :: Vsa { .. } | Ty :: Substrate (_) | Ty :: Bytes | Ty :: Float => false , } }",
      "reason": "match pattern constructor `Seq` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "has_var"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 597,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" One-sided **unification** of a declared type (which may contain [`Ty::Var`] parameters) against a\"] # [doc = \" concrete `actual` type, accumulating the parameter substitution `s` (RFC-0007 §11.3). Used to\"] # [doc = \" **infer** a generic function's type arguments from its call-site argument types, and a constructor's\"] # [doc = \" from its field arguments. A parameter is bound at most once; a second, conflicting binding is an\"] # [doc = \" explicit mismatch (never a silent re-coercion — G2/VR-5). No `Swap` is ever inserted: a\"] # [doc = \" representation-level disagreement (`Binary{8}` vs `Binary{16}`) is an explicit error, not a\"] # [doc = \" conversion (S1).\"] pub (crate) fn unify (site : & str , decl : & Ty , actual : & Ty , s : & mut BTreeMap < String , Ty > ,) -> Result < () , CheckError > { match (decl , actual) { (Ty :: Var (v) , _) => match s . get (v) { Some (bound) if bound != actual => Err (CheckError :: new (site , format ! (\"type parameter `{v}` would have to be both {bound} and {actual} — \\\n                     an ambiguous instantiation, not a guess (RFC-0007 §11.3)\") ,)) , _ => { s . insert (v . clone () , actual . clone ()) ; Ok (()) } } , (_ , Ty :: Var (_)) if decl == actual => Ok (()) , (Ty :: Data (n1 , a1) , Ty :: Data (n2 , a2)) if n1 == n2 && a1 . len () == a2 . len () => { for (d , a) in a1 . iter () . zip (a2) { unify (site , d , a , s) ? ; } Ok (()) } (Ty :: Fn (a1 , r1) , Ty :: Fn (a2 , r2)) => { unify (site , a1 , a2 , s) ? ; unify (site , r1 , r2 , s) } (Ty :: Seq (e1 , n1) , Ty :: Seq (e2 , n2)) if n1 == n2 => unify (site , e1 , e2 , s) , (Ty :: Binary (Width :: Var (v)) , Ty :: Binary (Width :: Lit (n))) | (Ty :: Ternary (Width :: Var (v)) , Ty :: Ternary (Width :: Lit (n))) => { let carrier = Ty :: Binary (Width :: Lit (* n)) ; match s . get (v) { Some (bound) if * bound != carrier => { let prior = match bound { Ty :: Binary (w) => w . to_string () , other => other . to_string () , } ; Err (CheckError :: new (site , format ! (\"width parameter `{v}` would have to be both {prior} and {n} — a width mismatch, not a coercion (DN-42 §4 / VR-5)\") ,)) } _ => { s . insert (v . clone () , carrier) ; Ok (()) } } } (Ty :: Binary (Width :: Var (v1)) , Ty :: Binary (Width :: Var (v2))) | (Ty :: Ternary (Width :: Var (v1)) , Ty :: Ternary (Width :: Var (v2))) => { let carrier = Ty :: Binary (Width :: Var (v2 . clone ())) ; match s . get (v1) { Some (bound) if * bound != carrier => Err (CheckError :: new (site , format ! (\"width parameter `{v1}` would have to be both {bound} and the abstract width \\\n                         `{v2}` — an undetermined width, not a coercion (DN-42 §4 / VR-5)\") ,)) , _ => { s . insert (v1 . clone () , carrier) ; Ok (()) } } } _ if decl == actual => Ok (()) , _ => Err (CheckError :: new (site , format ! (\"cannot match {decl} against {actual} (RFC-0007 §11.3 — never a silent swap)\") ,)) , } }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "unify"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 716,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The checked program environment: registry + function table. Built by [`check_nodule`]; the\"] # [doc = \" evaluator and elaborator consume it (so nothing runs unchecked).\"] # [derive (Debug , Clone)] pub struct Env { # [doc = \" Data registry, keyed by type name.\"] pub types : BTreeMap < String , DataInfo > , # [doc = \" Function table, keyed by name.\"] pub fns : BTreeMap < String , FnDecl > , # [doc = \" Per-function totality classification (RFC-0007 §4.5), filled by the totality checker.\"] pub totality : BTreeMap < String , crate :: totality :: Totality > , # [doc = \" Trait registry (RFC-0019 §4.2; LR-2), keyed by trait name. A trait is a **registry entry**,\"] # [doc = \" never a kernel node (KC-3). Stored for `EXPLAIN` and for the staged dictionary lowering\"] # [doc = \" (M-673).\"] pub traits : BTreeMap < String , TraitInfo > , # [doc = \" Instance registry (RFC-0019 §4.5; the **coherence** key), keyed by `(trait_name, type_head)`.\"] # [doc = \" Head-granular keying is the stage-1 coherence discipline — at most one instance per\"] # [doc = \" `(trait, type-head)` (global uniqueness). Stored for instance resolution + the staged\"] # [doc = \" dictionary lowering (M-673).\"] pub instances : BTreeMap < (String , String) , InstanceInfo > , # [doc = \" **Retained impl-method bodies** (M-673), keyed `(trait_name, type_head(for_ty))` — parallel to\"] # [doc = \" [`Self::instances`] (head-granular: at most one instance per key by coherence). Each value is\"] # [doc = \" the instance's **resolved** method `FnDecl`s (ambient literals + ctor/binder patterns\"] # [doc = \" normalized — the same canonical form a top-level fn carries). [`InstanceInfo::methods`] keeps\"] # [doc = \" only the method *names* (a non-silent record of the dictionary slots); to **monomorphize** a\"] # [doc = \" trait-method call to a direct call (`crate::mono`) the elaborator needs the method *bodies*, so\"] # [doc = \" they are retained here additively (rather than mutating [`InstanceInfo`], whose `PartialEq` /\"] # [doc = \" equality sites stay untouched). Empty when a nodule declares no `impl`s — a non-trait program is\"] # [doc = \" byte-identical to the pre-M-673 `Env`.\"] pub impls : BTreeMap < (String , String) , Vec < FnDecl > > , # [doc = \" **User-defined generative-lowering rules** (DN-54 / M-812), keyed by rule name.\"] # [doc = \" Populated by `check_nodule_with` from every `lower Name[params] = <rhs>` declaration in the\"] # [doc = \" nodule; looked up by `derive Name for T` application during elaboration ([`crate::elab`]).\"] # [doc = \" Empty in any nodule that declares no `lower` rules. Acyclicity (DN-54 §4.2) and KC-3 (RHS\"] # [doc = \" must lower to existing L0 nodes, never grow the kernel) are both enforced at check time —\"] # [doc = \" never-silent (G2).\"] pub lower_rules : BTreeMap < String , LowerDecl > , # [doc = \" **Provenance of `derive`-generated sibling impls** (DN-54 §10.6 OQ-A / DN-81 §6.5 / RFC-0001\"] # [doc = \" §4.3; M-973). Keyed by the same `(trait_name, type_head)` coherence key as [`Self::instances`],\"] # [doc = \" each value records the `(rule_name, for_ty_render)` a Model-A sibling injection came from — so\"] # [doc = \" a derived impl is distinguishable from a hand-written one (`reveal`/EXPLAIN provenance), and\"] # [doc = \" Model A retains Model B's one intrinsic edge (DN-81 §5.3 Objection 2). Empty in any nodule with\"] # [doc = \" no `derive` sites. **Guarantee: `Declared`** — this is recorded metadata, not a checked\"] # [doc = \" property; ADR-003 content-addressing is the substantive identity, this map is the human-facing\"] # [doc = \" origin label (VR-5 — carried honestly, never upgraded).\"] pub derived_provenance : BTreeMap < (String , String) , (String , String) > , # [doc = \" **Provenance of `via`-generated forwarding impls** (DN-53 §A.3.2/§A.3.3; M-966), the `via`\"] # [doc = \" analogue of [`Self::derived_provenance`]. Keyed by the same `(trait_name, type_head)`\"] # [doc = \" coherence key as [`Self::instances`]; each value records `(field_idx, object_name)` — which\"] # [doc = \" positional field the forwarding impl delegates through, and which `object` declared it — so\"] # [doc = \" `reveal`/EXPLAIN can label the generated impl `[generated: via <field_idx> : <TraitName>]`\"] # [doc = \" per DN-53 §A.3.3 (never a silent/opaque dispatch, G2). Resolution itself is deterministic by\"] # [doc = \" construction: `via_decls` is an ordered `Vec` (declaration order), never a `HashMap`, and two\"] # [doc = \" `via` clauses on the same object claiming the same trait are refused up front\"] # [doc = \" ([`expand_object_via_decls`]) rather than silently choosing one. Empty in any nodule with no\"] # [doc = \" `via` clauses. **Guarantee: `Declared`** — recorded metadata, not a checked property (VR-5 —\"] # [doc = \" carried honestly, never upgraded).\"] pub via_provenance : BTreeMap < (String , String) , (u32 , String) > , }",
      "reason": "struct `Env` has a field type with no confirmed mapping (qualified type path `crate :: totality :: Totality` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5))",
      "item_name": "Env"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 775,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Env { # [doc = \" Find the data type owning constructor `ctor`, with its index — `None` if no type has it.\"] # [must_use] pub fn ctor (& self , ctor : & str) -> Option < (& DataInfo , usize) > { self . types . values () . find_map (| d | d . ctors . iter () . position (| c | c . name == ctor) . map (| i | (d , i))) } # [doc = \" The registered data type named `name`, if any. Additive read-only accessor over the public\"] # [doc = \" [`types`](Env::types) map — a Law-of-Demeter-friendly alternative to `env.types.get(name)`.\"] # [must_use] pub fn type_info (& self , name : & str) -> Option < & DataInfo > { self . types . get (name) } # [doc = \" The function declaration named `name`, if any. Additive read-only accessor over the public\"] # [doc = \" [`fns`](Env::fns) map.\"] # [must_use] pub fn fn_decl (& self , name : & str) -> Option < & FnDecl > { self . fns . get (name) } # [doc = \" The totality verdict for function `name`, if it has been classified. Additive read-only\"] # [doc = \" accessor over the public [`totality`](Env::totality) map. `Totality` is `Copy`, so this\"] # [doc = \" returns it by value (never silently fabricating a verdict for an unclassified name — `None`).\"] # [must_use] pub fn fn_totality (& self , name : & str) -> Option < crate :: totality :: Totality > { self . totality . get (name) . copied () } # [doc = \" The registered trait named `name`, if any (RFC-0019 §4.2). Additive read-only accessor over\"] # [doc = \" the public [`traits`](Env::traits) map.\"] # [must_use] pub fn trait_info (& self , name : & str) -> Option < & TraitInfo > { self . traits . get (name) } # [doc = \" The registered instance for `(trait_name, head)`, if any (RFC-0019 §4.5). `head` is a\"] # [doc = \" [`type_head`]. Additive read-only accessor over the public [`instances`](Env::instances) map.\"] # [must_use] pub fn instance (& self , trait_name : & str , head : & str) -> Option < & InstanceInfo > { self . instances . get (& (trait_name . to_owned () , head . to_owned ())) } }",
      "reason": "no member of this impl block could be transpiled (6 sub-issue(s)): impl method `ctor` body: field projection `self.types` on `Env` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `type_info` body: field projection `self.types` on `Env` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `fn_decl` body: field projection `self.fns` on `Env` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `fn_totality` signature: qualified type path `crate :: totality :: Totality` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5); impl method `trait_info` body: field projection `self.traits` on `Env` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `instance` body: field projection `self.instances` on `Env` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against)",
      "item_name": "Env"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 822,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The builtin prelude: `type Bool = False | True` (`if` scrutinizes it; RFC-0007 keeps `if` as\"] # [doc = \" elaboration-level sugar over `Match` on this registry entry).\"] pub (crate) fn prelude () -> DataInfo { DataInfo { name : \"Bool\" . to_owned () , home : PRELUDE_HOME . to_owned () , params : vec ! [] , ctors : vec ! [CtorInfo { name : \"False\" . to_owned () , fields : vec ! [] , } , CtorInfo { name : \"True\" . to_owned () , fields : vec ! [] , } ,] , } }",
      "reason": "struct literal `DataInfo { .. }` — not an in-file single-ctor struct that emits (no constructor to build)",
      "item_name": "prelude"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 844,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Resolve a surface [`TypeRef`] to a checked [`Ty`], with the type parameters `tyvars` in scope\"] # [doc = \" (RFC-0007 §11.2): a `Named(name, [])` whose `name` is a type parameter resolves to [`Ty::Var`];\"] # [doc = \" any other `Named` is a data type whose **arity is checked** against its declaration (`List<A>`\"] # [doc = \" applied to the wrong number of arguments is an explicit error, never a guess). VSA types resolve\"] # [doc = \" concretely as of M-892 (their surface model ident canonicalized to the kernel model id). The\"] # [doc = \" guarantee index is *allowed* and returned alongside (checked dynamically at\"] # [doc = \" stage 0 — RFC-0007 §4.3). `tyvars` is `&[]` in a monomorphic context.\"] pub (crate) fn resolve_ty (site : & str , types : & BTreeMap < String , DataInfo > , tyvars : & [String] , t : & TypeRef ,) -> Result < (Ty , Option < Strength >) , CheckError > { let base = match & t . base { BaseType :: Binary (WidthRef :: Lit (n)) => Ty :: Binary (Width :: Lit (* n)) , BaseType :: Binary (WidthRef :: Name (v)) => Ty :: Binary (Width :: Var (v . clone ())) , BaseType :: Ternary (WidthRef :: Lit (m)) => Ty :: Ternary (Width :: Lit (* m)) , BaseType :: Ternary (WidthRef :: Name (v)) => Ty :: Ternary (Width :: Var (v . clone ())) , BaseType :: Dense (d , s) => Ty :: Dense (* d , * s) , BaseType :: Substrate (tag) => Ty :: Substrate (tag . clone ()) , BaseType :: Seq { elem , len } => { let (elem_ty , _) = resolve_ty (site , types , tyvars , elem) ? ; Ty :: Seq (Box :: new (elem_ty) , * len) } BaseType :: Bytes => Ty :: Bytes , BaseType :: Float => Ty :: Float , BaseType :: Vsa { model , dim , sparsity , } => Ty :: Vsa { model : vsa_kernel_model_id (model) , dim : * dim , sparsity : sparsity . clone () , } , BaseType :: Named (name , args) => { if args . is_empty () && tyvars . iter () . any (| v | v == name) { Ty :: Var (name . clone ()) } else { let Some (decl) = lookup_data (types , name) else { return Err (CheckError :: new (site , format ! (\"unknown type `{name}`\"))) ; } ; if let Some (home) = data_home_mismatch (name , decl) { return Err (CheckError :: new (site , data_home_mismatch_message (name , home , decl) ,)) ; } if args . len () != decl . params . len () { return Err (CheckError :: new (site , format ! (\"`{name}` takes {} type argument(s), got {} (RFC-0007 §11.3)\" , decl . params . len () , args . len ()) ,)) ; } let qname = qualify_type_name (& decl . home , ty_local_name (name)) ; let mut resolved = Vec :: with_capacity (args . len ()) ; for a in args { resolved . push (resolve_ty (site , types , tyvars , a) ? . 0) ; } Ty :: Data (qname , resolved) } } BaseType :: Ambient (_) => { return Err (CheckError :: new (site , \"internal: an unresolved paradigm-less repr `{…}` reached the checker — the \\\n                 ambient resolution pass should have filled it (RFC-0012 §4.3)\" ,)) ; } BaseType :: Fn (param , ret) => { let (param_ty , _) = resolve_ty (site , types , tyvars , param) ? ; let (ret_ty , _) = resolve_ty (site , types , tyvars , ret) ? ; Ty :: Fn (Box :: new (param_ty) , Box :: new (ret_ty)) } BaseType :: Tuple (elems) => { let n = elems . len () ; let tname = tuple_type_name (n) ; if ! types . contains_key (& tname) { return Err (CheckError :: new (site , format ! (\"internal: synthetic tuple type `{tname}` not registered — \\\n                         `register_tuple_types` must run before the checker (M-826; never silent)\") ,)) ; } let mut resolved = Vec :: with_capacity (n) ; for e in elems { resolved . push (resolve_ty (site , types , tyvars , e) ? . 0) ; } Ty :: Data (tname , resolved) } } ; Ok ((base , t . guarantee)) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "resolve_ty"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 985,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **DN-112 Rank 1 / M-1036 (a required piece of the mechanism, not an optional hardening).**\"] # [doc = \" Pre-resolve a `pub fn`'s value-parameter and return types against its **own declaring nodule's**\"] # [doc = \" registry, exactly once, at export time — the function-signature analogue of how a constructor's\"] # [doc = \" field types are already baked once at `register_types`/`resolve_ctors` time.\"] # [doc = \"\"] # [doc = \" **Why this is load-bearing, not cosmetic.** `FnDecl.sig` stores *surface* [`TypeRef`]s\"] # [doc = \" (never a resolved [`Ty`]); every call site re-resolves a callee's parameter/return types fresh\"] # [doc = \" via `resolve_ty(caller_site, caller_types, …, &pm.ty)` — using the **caller's own** merged\"] # [doc = \" registry (own decls shadow imports, RFC-0006 §4.3). Without baking, a foreign caller that\"] # [doc = \" shadows a name locally would re-resolve the callee's signature against *its own* (wrong-home)\"] # [doc = \" declaration — reintroducing, one level up, exactly the bare-name conflation DN-104's CRITICAL\"] # [doc = \" callout named as M-1036's root cause. Baking here closes it for the DN-112 §10 item 2 exploit\"] # [doc = \" shape (`use a.use_t; type T = Mk(..); use_t(forge())`): `use_t`'s parameter type is permanently\"] # [doc = \" `a::T`, never re-derived from whichever nodule happens to be checking the call.\"] # [doc = \"\"] # [doc = \" **Scoped, honest best-effort (VR-5) — never a spurious refusal.** Resolved against the\"] # [doc = \" declaring nodule's **own** registered types only (no cross-nodule imports it itself might use —\"] # [doc = \" resolving those would need the whole phylum's imports settled first, a larger change). A\"] # [doc = \" signature that itself references a type the declaring nodule imports from elsewhere fails to\"] # [doc = \" resolve here; the caller (`Exports`-building loop) treats that as \\\"not bakeable\\\" and leaves the\"] # [doc = \" entry absent — `check_app` then falls back to the pre-existing (unbaked) re-resolution path for\"] # [doc = \" that one function, unchanged from before this fix (never a hard error from this best-effort\"] # [doc = \" step). This is a disclosed, narrower residual, not a silent gap.\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" A [`CheckError`] iff a value-parameter or return `TypeRef` does not resolve against `types` —\"] # [doc = \" the caller treats this as \\\"not bakeable\\\" rather than propagating it.\"] pub (crate) fn resolve_fn_sig (types : & BTreeMap < String , DataInfo > , sig : & FnSig ,) -> Result < (Vec < Ty > , Ty) , CheckError > { let tyvars = sig . param_names () ; let mut params = Vec :: with_capacity (sig . value_params . len ()) ; for p in & sig . value_params { let (ty , _) = resolve_ty (& sig . name , types , & tyvars , & p . ty) ? ; params . push (ty) ; } let (ret , _) = resolve_ty (& sig . name , types , & tyvars , & sig . ret) ? ; Ok ((params , ret)) }",
      "reason": "qualified/associated-function call `Vec :: with_capacity` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin)",
      "item_name": "resolve_fn_sig"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1026,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The synthetic name for a tuple type of arity `n` (M-826, KC-3). Injective over n; `$` is not\"] # [doc = \" a surface-identifier character, so this name cannot collide with user-defined types.\"] # [doc = \" `Tuple$2` = the 2-tuple (pair), `Tuple$3` = the 3-tuple, etc.\"] # [must_use] pub (crate) fn tuple_type_name (n : usize) -> String { format ! (\"Tuple${n}\") }",
      "reason": "unsupported expression form `format ! (\"Tuple${n}\")`",
      "item_name": "tuple_type_name"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1034,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The synthetic constructor name for a tuple of arity `n` (M-826). Same injective scheme.\"] # [must_use] pub (crate) fn tuple_ctor_name (n : usize) -> String { format ! (\"MkTuple${n}\") }",
      "reason": "unsupported expression form `format ! (\"MkTuple${n}\")`",
      "item_name": "tuple_ctor_name"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1040,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Synthesize the [`DataInfo`] for a tuple of arity `n` (n ≥ 2; M-826, KC-3). The type has `n`\"] # [doc = \" type parameters `T0 … T{n-1}` and a single constructor `MkTuple$N(T0, T1, …)`.\"] # [doc = \" This is a pure data declaration (no kernel node — KC-3); the checker registers it on demand.\"] # [must_use] pub (crate) fn synthetic_tuple_data (n : usize) -> DataInfo { let params : Vec < String > = (0 .. n) . map (| i | format ! (\"T{i}\")) . collect () ; let fields : Vec < Ty > = params . iter () . map (| p | Ty :: Var (p . clone ())) . collect () ; DataInfo { name : tuple_type_name (n) , home : PRELUDE_HOME . to_owned () , params : params . clone () , ctors : vec ! [CtorInfo { name : tuple_ctor_name (n) , fields , }] , } }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "synthetic_tuple_data"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1060,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Walk a nodule and collect all tuple arities referenced (in `BaseType::Tuple` and\"] # [doc = \" `Expr::TupleLit`) so `register_types` can pre-register the synthetic `Tuple$N` data types\"] # [doc = \" before checking. Uses a recursive AST scan (pre-order, no depth guard needed — it's a scan,\"] # [doc = \" not evaluation).\"] pub (crate) fn collect_tuple_arities (nodule : & Nodule) -> std :: collections :: BTreeSet < usize > { let mut arities = std :: collections :: BTreeSet :: new () ; for item in & nodule . items { collect_tuple_arities_item (item , & mut arities) ; } arities }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "collect_tuple_arities"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1072,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn collect_tuple_arities_item (item : & crate :: ast :: Item , out : & mut std :: collections :: BTreeSet < usize > ,) { use crate :: ast :: Item ; match item { Item :: Fn (fd) => { collect_tuple_arities_sig (& fd . sig , out) ; collect_tuple_arities_expr (& fd . body , out) ; } Item :: Type (td) => { for ctor in & td . ctors { for f in & ctor . fields { collect_tuple_arities_typeref (f , out) ; } } } Item :: Trait (tr) => { for sig in & tr . sigs { collect_tuple_arities_sig (sig , out) ; } } Item :: Impl (id) => { for t in & id . trait_args { collect_tuple_arities_typeref (t , out) ; } collect_tuple_arities_typeref (& id . for_ty , out) ; for m in & id . methods { collect_tuple_arities_sig (& m . sig , out) ; collect_tuple_arities_expr (& m . body , out) ; } } Item :: Object (od) => { for f in & od . ctor . fields { collect_tuple_arities_typeref (f , out) ; } for impl_d in & od . impls { for t in & impl_d . trait_args { collect_tuple_arities_typeref (t , out) ; } collect_tuple_arities_typeref (& impl_d . for_ty , out) ; for m in & impl_d . methods { collect_tuple_arities_sig (& m . sig , out) ; collect_tuple_arities_expr (& m . body , out) ; } } for f in & od . fns { collect_tuple_arities_sig (& f . sig , out) ; collect_tuple_arities_expr (& f . body , out) ; } } Item :: Lower (ld) => match & ld . rhs { crate :: ast :: LowerRhs :: Expr (e) => collect_tuple_arities_expr (e , out) , crate :: ast :: LowerRhs :: Impl (id) => { for t in & id . trait_args { collect_tuple_arities_typeref (t , out) ; } collect_tuple_arities_typeref (& id . for_ty , out) ; for m in & id . methods { collect_tuple_arities_sig (& m . sig , out) ; collect_tuple_arities_expr (& m . body , out) ; } } } , Item :: InherentImpl (iid) => { collect_tuple_arities_typeref (& iid . for_ty , out) ; for m in & iid . methods { collect_tuple_arities_sig (& m . sig , out) ; collect_tuple_arities_expr (& m . body , out) ; } } Item :: Use (_) | Item :: Default (_) | Item :: Derive (_) => { } } }",
      "reason": "qualified type path `crate :: ast :: Item` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "collect_tuple_arities_item"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1149,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn collect_tuple_arities_typeref (tr : & TypeRef , out : & mut std :: collections :: BTreeSet < usize >) { use crate :: ast :: BaseType ; match & tr . base { BaseType :: Tuple (elems) => { out . insert (elems . len ()) ; for e in elems { collect_tuple_arities_typeref (e , out) ; } } BaseType :: Seq { elem , .. } => collect_tuple_arities_typeref (elem , out) , BaseType :: Named (_ , args) => { for a in args { collect_tuple_arities_typeref (a , out) ; } } BaseType :: Fn (a , b) => { collect_tuple_arities_typeref (a , out) ; collect_tuple_arities_typeref (b , out) ; } _ => { } } }",
      "reason": "qualified type path `std :: collections :: BTreeSet < usize >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "collect_tuple_arities_typeref"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1172,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn collect_tuple_arities_sig (sig : & crate :: ast :: FnSig , out : & mut std :: collections :: BTreeSet < usize >) { for p in & sig . value_params { collect_tuple_arities_typeref (& p . ty , out) ; } collect_tuple_arities_typeref (& sig . ret , out) ; }",
      "reason": "qualified type path `crate :: ast :: FnSig` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "collect_tuple_arities_sig"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1179,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Scan a pattern for tuple arities (M-826) — a `Pattern::Tuple` of arity N requires the synthetic\"] # [doc = \" `Tuple$N` data type to be registered before checking, even when no `TupleLit` of that arity is\"] # [doc = \" constructed in the nodule. Recurses through constructor sub-patterns and or-pattern alternatives.\"] fn collect_tuple_arities_pattern (p : & Pattern , out : & mut std :: collections :: BTreeSet < usize >) { match p { Pattern :: Tuple (subs) => { out . insert (subs . len ()) ; for sub in subs { collect_tuple_arities_pattern (sub , out) ; } } Pattern :: Ctor (_ , subs) => { for sub in subs { collect_tuple_arities_pattern (sub , out) ; } } Pattern :: Or (alts) => { for alt in alts { collect_tuple_arities_pattern (alt , out) ; } } Pattern :: Wildcard | Pattern :: Lit (_) | Pattern :: Ident (_) => { } } }",
      "reason": "qualified type path `std :: collections :: BTreeSet < usize >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "collect_tuple_arities_pattern"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1204,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "fn collect_tuple_arities_expr (e : & crate :: ast :: Expr , out : & mut std :: collections :: BTreeSet < usize >) { use crate :: ast :: Expr ; match e { Expr :: TupleLit (elems) => { out . insert (elems . len ()) ; for el in elems { collect_tuple_arities_expr (el , out) ; } } Expr :: Let { bound , body , ty , .. } => { if let Some (t) = ty { collect_tuple_arities_typeref (t , out) ; } collect_tuple_arities_expr (bound , out) ; collect_tuple_arities_expr (body , out) ; } Expr :: If { cond , conseq , alt } => { collect_tuple_arities_expr (cond , out) ; collect_tuple_arities_expr (conseq , out) ; collect_tuple_arities_expr (alt , out) ; } Expr :: Match { scrutinee , arms } => { collect_tuple_arities_expr (scrutinee , out) ; for arm in arms { collect_tuple_arities_pattern (& arm . pattern , out) ; collect_tuple_arities_expr (& arm . body , out) ; } } Expr :: For { xs , init , body , .. } => { collect_tuple_arities_expr (xs , out) ; collect_tuple_arities_expr (init , out) ; collect_tuple_arities_expr (body , out) ; } Expr :: Swap { value , .. } => collect_tuple_arities_expr (value , out) , Expr :: Wild (b) | Expr :: Spore (b) | Expr :: Consume (b) | Expr :: Wrapping (b) | Expr :: Try (b) => { collect_tuple_arities_expr (b , out) ; } Expr :: Colony (hyphae) => { for h in hyphae { collect_tuple_arities_expr (& h . body , out) ; } } Expr :: Lambda { params , body } => { for p in params { collect_tuple_arities_typeref (& p . ty , out) ; } collect_tuple_arities_expr (body , out) ; } Expr :: App { head , args } => { collect_tuple_arities_expr (head , out) ; for a in args { collect_tuple_arities_expr (a , out) ; } } Expr :: Fuse { left , right } => { collect_tuple_arities_expr (left , out) ; collect_tuple_arities_expr (right , out) ; } Expr :: Reclaim { policy , body } => { collect_tuple_arities_expr (policy , out) ; collect_tuple_arities_expr (body , out) ; } Expr :: Ascribe (inner , t) => { collect_tuple_arities_expr (inner , out) ; collect_tuple_arities_typeref (t , out) ; } Expr :: WithParadigm { body , .. } => collect_tuple_arities_expr (body , out) , Expr :: Path (_) | Expr :: Lit (_) => { } } }",
      "reason": "qualified type path `crate :: ast :: Expr` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "collect_tuple_arities_expr"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1282,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The checked environments of a whole **phylum** (M-662): one [`Env`] per nodule, paired with the\"] # [doc = \" nodule's path. The product of [`check_phylum`]. For a phylum-of-one this holds the single nodule's\"] # [doc = \" `Env` (so [`check_nodule`] just unwraps it) — additive, backward-compatible.\"] # [derive (Debug , Clone)] pub struct PhylumEnv { # [doc = \" One `(nodule path, checked Env)` per nodule, in source order.\"] pub nodules : Vec < (Path , Env) > , # [doc = \" Per-nodule record of the names each nodule **declares itself** — the runtime-link owner map\"] # [doc = \" (M-1024). Parallel to [`Self::nodules`]. Consulted only by [`Self::link`], which merges each\"] # [doc = \" name from its **home** nodule's *checked* [`Env`] (never an imported, less-resolved copy) and\"] # [doc = \" refuses a cross-nodule name collision never-silently. Kept private (an internal owner record,\"] # [doc = \" not part of the import/coherence views).\"] own : Vec < OwnDecls > , }",
      "reason": "struct `PhylumEnv` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "PhylumEnv"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1297,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The names one nodule **declares itself** (M-1024): its own data types, functions, and traits, with\"] # [doc = \" the injected prelude `Bool` and the conditionally-seeded built-in `Fuse` trait excluded (they are\"] # [doc = \" identical in every nodule, so they are seeded once by [`PhylumEnv::link`] rather than treated as a\"] # [doc = \" per-nodule declaration that could \\\"collide\\\"). This is the authoritative-owner record the flat\"] # [doc = \" phylum link merges from.\"] # [derive (Debug , Default , Clone)] struct OwnDecls { types : BTreeSet < String > , fns : BTreeSet < String > , traits : BTreeSet < String > , }",
      "reason": "struct `OwnDecls` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "OwnDecls"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1309,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl PhylumEnv { # [doc = \" The single nodule's [`Env`] when this is a phylum-of-one, else `None`. The bridge\"] # [doc = \" [`check_nodule`] uses to keep its single-`Env` return type (M-662).\"] # [must_use] pub fn single (& self) -> Option < & Env > { match self . nodules . as_slice () { [(_ , env)] => Some (env) , _ => None , } } # [doc = \" The checked [`Env`] of the nodule whose path equals `path`, if present.\"] # [must_use] pub fn nodule (& self , path : & Path) -> Option < & Env > { self . nodules . iter () . find (| (p , _) | p == path) . map (| (_ , e) | e) } # [doc = \" **The phylum-wide runtime link (M-1024; ENB-1).** Fold every nodule's *checked* declarations\"] # [doc = \" into **one** [`Env`] so the downstream pipeline ([`crate::elab`] / [`crate::mono`] /\"] # [doc = \" [`crate::eval`], all of which consume a single `&Env`) can **execute a `use`d symbol across\"] # [doc = \" nodules** — the runtime dual of the check-time cross-nodule resolution (`resolve_imports`,\"] # [doc = \" M-662). This is the \\\"give the evaluator a phylum-wide view\\\" close that retires the\"] # [doc = \" local-mirror sidestep (DN-99 register row #41; ADR-045 unfrozen posture).\"] # [doc = \"\"] # [doc = \" **What it reuses (DRY, KC-3 — no new L0 node):** the check pass already resolved every `use`\"] # [doc = \" against the phylum-wide export table and refused unknown/private/ambiguous imports; those\"] # [doc = \" checks stand. `link` reuses that verified structure by merging **each name from its home\"] # [doc = \" nodule's checked `Env`** — the authoritative, ambient-resolved declaration — keyed by simple\"] # [doc = \" name. (An *imported* copy in a consumer nodule's `Env` is a less-resolved clone of the home\"] # [doc = \" decl; `link` never merges those, so the linked env is *strictly more correct* than running a\"] # [doc = \" consumer nodule's per-nodule `Env` directly — which is why a phylum, including a phylum-of-one,\"] # [doc = \" should be run through `link`.)\"] # [doc = \"\"] # [doc = \" **Flat-namespace v0 (never-silent boundary, G2/VR-5).** The merge is a **flat phylum\"] # [doc = \" namespace**: exactly one declaration per simple name across the whole phylum. This runs the\"] # [doc = \" common shape — a shared-type/helper nodule plus consumers with distinct names (the stdlib\"] # [doc = \" porter's user story) — and the *transitive* case a per-nodule `Env` cannot (a `pub` fn whose\"] # [doc = \" body calls its home nodule's **private** helper now resolves, because the helper is linked in\"] # [doc = \" from its home nodule). A **cross-nodule simple-name collision** (two nodules each declaring the\"] # [doc = \" same fn/type/trait name) is an **explicit [`CheckError`]**, never a silent winner — the\"] # [doc = \" qualified-name / per-frame home-nodule scoping that would *disambiguate* a collision (rather\"] # [doc = \" than refuse it) is the flagged design residual (M-982 follow-up; needs the ratifying DN).\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" A never-silent [`CheckError`] on any cross-nodule collision the flat namespace cannot represent\"] # [doc = \" (a duplicate simple name across nodules, or — defensively — a duplicate coherence-keyed\"] # [doc = \" instance/impl/`lower` rule, which the check pass's phylum-wide coherence should already have\"] # [doc = \" refused).\"] pub fn link (& self) -> Result < Env , CheckError > { let mut types : BTreeMap < String , DataInfo > = BTreeMap :: new () ; let p = prelude () ; types . insert (p . name . clone () , p) ; let mut traits : BTreeMap < String , TraitInfo > = BTreeMap :: new () ; if self . nodules . iter () . any (| (_ , e) | e . traits . contains_key (crate :: fuse :: TRAIT_NAME)) { traits . insert (crate :: fuse :: TRAIT_NAME . to_owned () , crate :: fuse :: prelude ()) ; } if self . nodules . iter () . any (| (_ , e) | e . traits . contains_key (crate :: ord3 :: TRAIT_NAME)) { traits . insert (crate :: ord3 :: TRAIT_NAME . to_owned () , crate :: ord3 :: prelude ()) ; } let mut fns : BTreeMap < String , FnDecl > = BTreeMap :: new () ; let mut totality : BTreeMap < String , crate :: totality :: Totality > = BTreeMap :: new () ; let mut instances : BTreeMap < (String , String) , InstanceInfo > = BTreeMap :: new () ; let mut impls : BTreeMap < (String , String) , Vec < FnDecl > > = BTreeMap :: new () ; let mut lower_rules : BTreeMap < String , LowerDecl > = BTreeMap :: new () ; let mut derived_provenance : BTreeMap < (String , String) , (String , String) > = BTreeMap :: new () ; let mut via_provenance : BTreeMap < (String , String) , (u32 , String) > = BTreeMap :: new () ; let collide = | kind : & str , name : & str | { CheckError :: new (name , format ! (\"cross-nodule name collision: `{name}` ({kind}) is declared by more than one \\\n                     nodule of this phylum — the v0 runtime link is a flat phylum namespace (one \\\n                     declaration per simple name; M-1024). Rename one, or await the qualified \\\n                     per-nodule scoping that disambiguates a collision (M-982 — never a silent \\\n                     winner, G2)\") ,) } ; for ((_path , env) , own) in self . nodules . iter () . zip (self . own . iter ()) { for name in & own . types { if types . contains_key (name) { return Err (collide (\"type\" , name)) ; } if let Some (info) = env . types . get (name) { types . insert (name . clone () , info . clone ()) ; } } for name in & own . traits { if traits . contains_key (name) { return Err (collide (\"trait\" , name)) ; } if let Some (info) = env . traits . get (name) { traits . insert (name . clone () , info . clone ()) ; } } for name in & own . fns { if fns . contains_key (name) { return Err (collide (\"fn\" , name)) ; } if let Some (fd) = env . fns . get (name) { fns . insert (name . clone () , fd . clone ()) ; } if let Some (t) = env . totality . get (name) { totality . insert (name . clone () , * t) ; } } for (k , v) in & env . instances { if instances . insert (k . clone () , v . clone ()) . is_some () { return Err (collide (\"instance\" , & format ! (\"{}:{}\" , k . 0 , k . 1))) ; } } for (k , v) in & env . impls { if impls . insert (k . clone () , v . clone ()) . is_some () { return Err (collide (\"impl\" , & format ! (\"{}:{}\" , k . 0 , k . 1))) ; } } for (name , v) in & env . lower_rules { if lower_rules . insert (name . clone () , v . clone ()) . is_some () { return Err (collide (\"lower rule\" , name)) ; } } for (k , v) in & env . derived_provenance { if derived_provenance . insert (k . clone () , v . clone ()) . is_some () { return Err (collide (\"derived impl\" , & format ! (\"{}:{}\" , k . 0 , k . 1))) ; } } for (k , v) in & env . via_provenance { if via_provenance . insert (k . clone () , v . clone ()) . is_some () { return Err (collide (\"via impl\" , & format ! (\"{}:{}\" , k . 0 , k . 1))) ; } } } Ok (Env { types , fns , totality , traits , instances , impls , lower_rules , derived_provenance , via_provenance , }) } }",
      "reason": "no member of this impl block could be transpiled (3 sub-issue(s)): impl method `single` body: Rust ownership/identity-conversion no-op method `.as_slice()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `as_slice(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `as_slice`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5); impl method `nodule`: impl method name `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5); impl method `link` body: `let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "PhylumEnv"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1481,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The **phylum-wide export table** (M-662): the `pub` items of every nodule, keyed by **qualified\"] # [doc = \" name** (`nodule.path` + `.` + item name, e.g. `\\\"std.collections.List\\\"`). This is the *import\"] # [doc = \" registry* — the **only-`pub`** view a `use` resolves against (RFC-0006 §4.3). It is kept strictly\"] # [doc = \" separate from the pub-blind coherence view (below): conflating the two would let a `use` import a\"] # [doc = \" private name or let the orphan rule miss a private declaration (a bug — the two views answer\"] # [doc = \" different questions).\"] # [derive (Debug , Default , Clone)] pub (crate) struct Exports { # [doc = \" Exported data types, by qualified name.\"] pub (crate) types : BTreeMap < String , DataInfo > , # [doc = \" Exported functions, by qualified name.\"] pub (crate) fns : BTreeMap < String , FnDecl > , # [doc = \" Exported traits, by qualified name.\"] pub (crate) traits : BTreeMap < String , TraitInfo > , # [doc = \" **All** declared simple names per nodule-prefix, with their `pub`-ness — used to distinguish\"] # [doc = \" \\\"no such name\\\" from \\\"exists but private\\\" in a `use` refusal (G2 — an honest, helpful\"] # [doc = \" diagnostic). Keyed by qualified name → `is_pub`.\"] pub (crate) declared : BTreeMap < String , bool > , # [doc = \" **Sealed constructors** per `pub` type (M-1027 / ENB-4; DN-104 §4). Keyed by the type's\"] # [doc = \" **qualified name** → the set of its **`priv`-sealed** constructor names. A `pub type` exports its\"] # [doc = \" type *name*, but a sealed constructor is **withheld from cross-nodule construction**: when a\"] # [doc = \" nodule imports the type, these names are folded into its per-nodule withheld set\"] # [doc = \" ([`NoduleImports::sealed`]). Only `pub` types appear here (a private type is unimportable, so its\"] # [doc = \" seal is moot). Empty for a phylum with no sealed constructors (backward-compatible).\"] pub (crate) sealed_ctors : BTreeMap < String , BTreeSet < String > > , # [doc = \" **DN-112 Rank 1 / M-1036:** each `pub` fn's [`resolve_fn_sig`]-baked `(param types, return\"] # [doc = \" type)`, keyed by qualified name — best-effort (absent if the signature could not resolve\"] # [doc = \" against the declaring nodule's own registry; see [`resolve_fn_sig`]'s doc comment).\"] pub (crate) resolved_fn_sigs : BTreeMap < String , (Vec < Ty > , Ty) > , }",
      "reason": "struct `Exports` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "Exports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1520,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The resolved imports available to **one** nodule while its bodies are checked (M-662): the\"] # [doc = \" imported `pub` declarations, merged by **simple name** at the documented precedence (own decls\"] # [doc = \" shadow explicit `use` shadow glob), plus the set of names that a **glob-vs-glob collision** left\"] # [doc = \" **ambiguous** (importable only by an explicit `use`). The ambiguous set is consulted at every\"] # [doc = \" unresolved-name site so a *reference* to an ambiguous glob name is a never-silent `CheckError`,\"] # [doc = \" never a silent winner (G2).\"] # [derive (Debug , Default , Clone , PartialEq)] pub (crate) struct NoduleImports { # [doc = \" Imported data types, by simple name.\"] pub (crate) types : BTreeMap < String , DataInfo > , # [doc = \" Imported functions, by simple name.\"] pub (crate) fns : BTreeMap < String , FnDecl > , # [doc = \" Imported traits, by simple name.\"] pub (crate) traits : BTreeMap < String , TraitInfo > , # [doc = \" Names brought in by **two or more** globs (and not resolved by an explicit `use` or a local\"] # [doc = \" decl): a *reference* to one of these is the never-silent glob-vs-glob ambiguity error.\"] pub (crate) ambiguous : BTreeSet < String > , # [doc = \" **Withheld (sealed) constructor names** (M-1027 / ENB-4; DN-104 §4). The imported constructor\"] # [doc = \" simple-names that are `priv`-sealed in their home nodule — **constructing** one from *this*\"] # [doc = \" nodule via an imported (`use`-resolved) name is a never-silent [`CheckError`]. The exact twin\"] # [doc = \" of [`Self::ambiguous`]: `ambiguous` answers \\\"which imported names may not be *referenced*\\\",\"] # [doc = \" `sealed` answers \\\"which imported constructor names may not be *constructed*\\\". This nodule's\"] # [doc = \" **own** constructor names are subtracted (an own constructor is always constructible in its\"] # [doc = \" home; own decls shadow imports). Consulted only at the two constructor-application sites.\"] # [doc = \"\"] # [doc = \" **Known gap (M-1036, not yet fixed — see `tests/ctor_seal.rs`'s\"] # [doc = \" `known_gap_a_same_named_local_shadow_type_bypasses_the_seal`):** this withheld set is keyed\"] # [doc = \" by **bare constructor name**, and Mycelium resolves types/ctors by bare name in the\"] # [doc = \" *caller's own scope* (own decls shadow imports — the pre-existing RFC-0006 §4.3 precedence\"] # [doc = \" rule), not by nodule-qualified nominal identity. So a foreign nodule that declares its own\"] # [doc = \" unsealed type/ctor of the **same name** — without ever importing the sealed original — is\"] # [doc = \" never populated into *its* withheld set and bypasses the seal entirely. This is **not** an\"] # [doc = \" enforced capability/security boundary against an adversarial or accidentally-colliding\"] # [doc = \" same-named local declaration; it only ever refuses a well-behaved caller that goes through\"] # [doc = \" `use home.Ctor`. A real fix needs nodule-qualified type identity (tracked as M-1036).\"] # [doc = \"\"] # [doc = \" **DN-112 Rank 1 / M-1036 — CLOSED.** Type/ctor identity is now nodule-qualified\"] # [doc = \" ([`qualify_type_name`]), and each imported `pub` fn's signature is pre-resolved against its\"] # [doc = \" **declaring** nodule ([`Exports::resolved_fn_sigs`] / [`Self::resolved_fn_sigs`]) rather than\"] # [doc = \" re-resolved fresh against the caller's own (possibly shadowing) registry — so the exploit\"] # [doc = \" this comment described is a never-silent type mismatch, not a values-forged pass. Pinned by\"] # [doc = \" `tests/ctor_seal.rs`.\"] pub (crate) sealed : BTreeSet < String > , # [doc = \" **DN-112 Rank 1 / M-1036:** this nodule's imported `pub` fns' [`resolve_fn_sig`]-baked\"] # [doc = \" `(param types, return type)`, keyed by **simple** name (mirroring [`Self::fns`]) — the\"] # [doc = \" mechanism that closes the M-1036 exploit for plain function calls (see [`resolve_fn_sig`]'s\"] # [doc = \" doc comment). This nodule's **own** fn names are subtracted (own decls shadow imports,\"] # [doc = \" mirroring [`Self::sealed`]'s own-ctor subtraction) so a local fn of the same name is never\"] # [doc = \" wrongly resolved through a foreign baked signature.\"] pub (crate) resolved_fn_sigs : BTreeMap < String , (Vec < Ty > , Ty) > , # [doc = \" **M-1060 fix-cycle-3 (2026-07-11, adversarial-verification HOLE A/A2/B closure):** the\"] # [doc = \" imported trait/fn simple-names, respectively, whose CURRENT (highest-precedence) binding\"] # [doc = \" entered via a **cross-phylum** `use dep::…` (as opposed to an ordinary intra-phylum\"] # [doc = \" `use sibling.Item` — same phylum, different nodule). Detected from the resolved import key\"] # [doc = \" (`\\\"{dep}::…\\\"` — `::` is not a legal Mycelium identifier character, so it can never collide\"] # [doc = \" with an intra-phylum `.`-joined qualified name; see the DN-113 module doc comment above).\"] # [doc = \" Re-inserted/removed in lockstep with [`Self::traits`]/[`Self::fns`] at every `insert_export`\"] # [doc = \" call so a later (higher-precedence) intra-phylum shadow of an earlier cross-phylum glob\"] # [doc = \" import correctly clears the marker, and vice versa. This nodule's **own** trait/fn names are\"] # [doc = \" subtracted at the end of [`resolve_imports`] (own decls always shadow imports — RFC-0006\"] # [doc = \" §4.3), mirroring [`Self::resolved_fn_sigs`]'s own-fn subtraction, so a local declaration that\"] # [doc = \" happens to share a cross-phylum import's simple name is never wrongly treated as foreign.\"] # [doc = \"\"] # [doc = \" A cross-phylum trait's method signatures / a cross-phylum fn's signature (when\"] # [doc = \" [`Self::resolved_fn_sigs`] has no baked entry — best-effort, see [`resolve_fn_sig`]'s doc\"] # [doc = \" comment) are not yet re-homed against their declaring phylum's own type registry the way a\"] # [doc = \" ctor field / a bakeable fn signature now is (`merge_phyla_exports`, `qualify_ty_cross_phylum`)\"] # [doc = \" — the DN-113 §7 / DN-122 residual. `check_trait_method_call` / `check_app` /\"] # [doc = \" `check_app_generic_fn` consult these sets (never `traits`/`fns` wholesale) so their narrow\"] # [doc = \" never-silent refusal fires ONLY for the true cross-phylum case — never for a same-phylum\"] # [doc = \" sibling trait/fn, whose signature is safe to resolve against `self.types` (M-1036 already\"] # [doc = \" gives every intra-phylum type a qualified, unambiguous identity in that shared registry).\"] pub (crate) cross_phylum_traits : BTreeSet < String > , pub (crate) cross_phylum_fns : BTreeSet < String > , }",
      "reason": "struct `NoduleImports` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "NoduleImports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1599,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl NoduleImports { # [doc = \" The never-silent glob-vs-glob ambiguity refusal for `name`, if it is ambiguous here (G2).\"] # [doc = \" Returns `None` when the name is unambiguous (so the caller falls through to its own\"] # [doc = \" unknown-name diagnostic).\"] fn ambiguity_error (& self , site : & str , name : & str) -> Option < CheckError > { self . ambiguous . contains (name) . then (| | { CheckError :: new (site , format ! (\"`{name}` is ambiguous — imported by more than one glob `use … .*` in this \\\n                     nodule; import it explicitly (`use <path>.{name}`) to disambiguate (M-662; \\\n                     never a silent winner — G2)\") ,) }) } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `ambiguity_error` body: method call `then` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "NoduleImports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1646,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A dependency phylum, already **checked and linked** (DN-113 §5.1) — the unit [`Phyla`] holds one\"] # [doc = \" of, keyed by the consumer's `[dependencies]`-local name. **v1 (DN-113 §5.2/§8): whole-graph\"] # [doc = \" compilation with content-pinned inputs, not separate compilation** — this crate does not itself\"] # [doc = \" load/verify a dependency's source tree against `phylum_hash` (that is the manifest/loader's job,\"] # [doc = \" `mycelium-proj`/`mycelium-cli` territory, out of this crate's scope); a `ResolvedPhylum` is built\"] # [doc = \" from an already-resolved `(hash, checked Phylum)` pair the caller supplies (or via\"] # [doc = \" [`crate::phyla::build_phyla_graph`] for a whole named dependency graph).\"] # [derive (Debug , Clone)] pub struct ResolvedPhylum { # [doc = \" The content-addressed pin (ADR-003) this phylum resolved to — the authoritative half of the\"] # [doc = \" DN-113 §6 def-site ref `(phylum_hash, qualified_name)`. **`Declared`** here: this crate\"] # [doc = \" verifies nothing about the pin's provenance; it is carried through so a consumer (the future\"] # [doc = \" separate-compilation / diamond-policy work, or a facility's def-site capture, DN-113 §6 US-3)\"] # [doc = \" has it available, unforged, alongside the checked exports it was computed from.\"] pub phylum_hash : mycelium_core :: ContentHash , # [doc = \" This dependency's own `pub`-only import registry (its [`Exports`], unmodified) — merged,\"] # [doc = \" phylum-qualified, into a consumer's own `Exports` by [`merge_phyla_exports`].\"] pub (crate) exports : Exports , # [doc = \" This dependency's own whole-phylum linked [`Env`] (via the *unmodified* [`PhylumEnv::link`]) —\"] # [doc = \" retained for a future runtime dual (DN-113 §7 point 2; explicitly out of v1's check-time\"] # [doc = \" scope, DN-113 §8) and so a consumer can look up a foreign symbol's checked shape without\"] # [doc = \" re-deriving it.\"] pub env : Env , }",
      "reason": "struct `ResolvedPhylum` has a field type with no confirmed mapping (qualified type path `mycelium_core :: ContentHash` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5))",
      "item_name": "ResolvedPhylum"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1671,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl ResolvedPhylum { # [doc = \" **The v1 single-node resolution entry point (DN-113 §5.1) — for a caller resolving ONE\"] # [doc = \" dependency by hand** (a leaf, or a flat/manually-ordered dependency set). Check + link a\"] # [doc = \" dependency phylum — recursively against ITS OWN `deps` (a further-nested `Phyla`, empty for a\"] # [doc = \" leaf/no-dependency phylum) — into a `ResolvedPhylum` ready to insert into a consumer's own\"] # [doc = \" [`Phyla`]. [`crate::phyla::build_phyla_graph`] is the whole-graph, cycle-checked analogue for\"] # [doc = \" a *named* multi-phylum dependency set (it does its own single check-pass per node internally,\"] # [doc = \" rather than calling this method, so each node is checked exactly once even though it also\"] # [doc = \" needs the node's [`PhylumEnv`] — the artifact this method does not itself return).\"] # [doc = \"\"] # [doc = \" `phylum_hash` is carried through verbatim, never verified against the checked `phylum`'s\"] # [doc = \" content here (DN-113 §5.2: v1 is whole-graph compilation with content-pinned *inputs* — the\"] # [doc = \" pin's provenance is the loader's job, `Declared` from this crate's perspective).\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" Any never-silent refusal from checking `phylum` against `deps` (unknown/private/ambiguous\"] # [doc = \" import, an unknown cross-phylum dependency, a coherence/orphan violation, a per-nodule type\"] # [doc = \" error, or a cross-nodule/cross-phylum name collision at link time — all [`CheckError`]).\"] pub fn resolve (phylum_hash : mycelium_core :: ContentHash , phylum : & Phylum , deps : & Phyla ,) -> Result < Self , CheckError > { let (penv , exports) = check_phylum_matured_with_deps_and_exports (phylum , deps , false) ? ; let env = penv . link () ? ; Ok (Self { phylum_hash , exports , env , }) } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `resolve` signature: qualified type path `mycelium_core :: ContentHash` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "ResolvedPhylum"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1704,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The **resolved dependency set** of one phylum being checked (DN-113 §5.1) — additive over\"] # [doc = \" [`check_phylum`]/[`check_phylum_matured`]: a phylum with no declared dependencies checks against\"] # [doc = \" `Phyla::default()` (empty), which is **byte-identical** to the pre-M-1060 behavior (every\"] # [doc = \" cross-phylum `use` then refuses \\\"no such dependency\\\", exactly as an undeclared one should).\"] # [derive (Debug , Clone , Default)] pub struct Phyla { # [doc = \" `[dependencies]`-local name → its resolved, checked, linked phylum.\"] pub (crate) deps : BTreeMap < String , ResolvedPhylum > , }",
      "reason": "struct `Phyla` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "Phyla"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1714,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Phyla { # [doc = \" An empty dependency set (no `[dependencies]`) — the additive-identity `Phyla` every\"] # [doc = \" zero-dependency call site uses (DN-113 §5.1).\"] # [must_use] pub fn empty () -> Self { Self :: default () } # [doc = \" Build a `Phyla` directly from already-resolved dependencies, keyed by their consumer-local\"] # [doc = \" `[dependencies]` name. The v1 constructor for a **flat** (single-level) dependency set; a\"] # [doc = \" multi-phylum graph with its own acyclicity precondition (DN-113 §9.3) is\"] # [doc = \" [`crate::phyla::build_phyla_graph`]'s job, which calls this after resolving each node.\"] # [must_use] pub fn from_deps (deps : BTreeMap < String , ResolvedPhylum >) -> Self { Self { deps } } # [doc = \" Is `name` a declared dependency? Used by [`resolve_imports`] to distinguish \\\"no such\"] # [doc = \" dependency\\\" from \\\"no such name within a known dependency\\\" (DN-113 §9.5 — a more honest\"] # [doc = \" diagnostic than falling through to the generic unknown-name error).\"] # [must_use] pub (crate) fn has_dep (& self , name : & str) -> bool { self . deps . contains_key (name) } # [doc = \" Read-only access to the declared dependencies, by their local name — a Law-of-Demeter-friendly\"] # [doc = \" public accessor over the crate-private [`Self::deps`] field (mirrors [`Env::type_info`]'s\"] # [doc = \" pattern), so an external caller (a loader, a test) can inspect a `Phyla` it was handed without\"] # [doc = \" needing crate-internal visibility.\"] # [must_use] pub fn deps (& self) -> & BTreeMap < String , ResolvedPhylum > { & self . deps } }",
      "reason": "no member of this impl block could be transpiled (4 sub-issue(s)): impl method `empty` body: qualified/associated-function call `Self :: default` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin); impl method `from_deps` body: struct literal `Phyla { .. }` — not an in-file single-ctor struct that emits (no constructor to build); impl method `has_dep` body: field projection `self.deps` on `Phyla` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `deps` body: field projection `self.deps` on `Phyla` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against)",
      "item_name": "Phyla"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1749,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Re-home an identity string at the phylum boundary (DN-113 §7; DN-112 Rank 1 extension):\"] # [doc = \" unconditionally prepend the dependency's LOCAL name, UNLESS `existing` is the single reserved\"] # [doc = \" [`PRELUDE_HOME`] sentinel (DN-112 §9 invariant i — a builtin/synthetic type resolves identically\"] # [doc = \" everywhere, even across a phylum boundary; never re-qualified further). An empty `existing` (the\"] # [doc = \" documented anonymous/header-less-nodule residual, [`nodule_home`]'s doc comment) becomes just\"] # [doc = \" `dep_name` rather than the visually-ugly `\\\"dep_name::\\\"` — still a fully phylum-qualified, never\"] # [doc = \" bare, identity. Shared by [`DataInfo::home`] re-homing and [`qualify_ty_cross_phylum`] (one\"] # [doc = \" mismatch-free predicate, not two divergent copies — DN-112's own `data_home_mismatch` precedent).\"] # [doc = \"\"] # [doc = \" Widened to `pub(crate)` (zero logic change) so the in-crate white-box tests\"] # [doc = \" (`crates/mycelium-l1/src/tests/cross_phylum.rs`) can exercise this unit directly — the same\"] # [doc = \" widening precedent as `resolve_imports`/`resolve_ctors`/`CoherenceView` (M-1013 STEP 4).\"] pub (crate) fn qualify_cross_phylum (existing : & str , dep_name : & str) -> String { if existing == PRELUDE_HOME { existing . to_owned () } else if existing . is_empty () { dep_name . to_owned () } else { format ! (\"{dep_name}::{existing}\") } }",
      "reason": "Rust ownership/identity-conversion no-op method `.to_owned()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `to_owned(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `to_owned`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5)",
      "item_name": "qualify_cross_phylum"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1771,
      "col": 1,
      "category": "Closure",
      "rust_construct": "Closure",
      "snippet": "# [doc = \" Re-home every [`Ty::Data`] identity embedded in a dependency's already-**baked** [`resolve_fn_sig`]\"] # [doc = \" output (DN-112 Rank 1 / M-1036 extension across the phylum boundary — required for soundness, see\"] # [doc = \" this section's header comment). `dep_env_types` is the dependency's OWN linked [`Env::types`] (the\"] # [doc = \" oracle for \\\"is this bare name actually the single reserved prelude/synthetic home, or a dependency\"] # [doc = \" type whose own intra-phylum home happened to be bare?\\\" — `Ty::Data` names never embed\"] # [doc = \" [`PRELUDE_HOME`] literally, so a string-only rewrite cannot tell the two apart; the type registry\"] # [doc = \" can). `Var`/`Binary`/`Ternary`/`Dense`/`Vsa`/`Substrate`/`Bytes`/`Float` carry no `Data` identity to\"] # [doc = \" re-home and are returned unchanged.\"] # [doc = \"\"] # [doc = \" Widened to `pub(crate)` (zero logic change) for the in-crate white-box tests (see\"] # [doc = \" [`qualify_cross_phylum`]'s doc comment).\"] pub (crate) fn qualify_ty_cross_phylum (ty : & Ty , dep_name : & str , dep_env_types : & BTreeMap < String , DataInfo > ,) -> Ty { match ty { Ty :: Data (name , args) => { let is_prelude = dep_env_types . get (ty_local_name (name)) . is_some_and (| d | d . home == PRELUDE_HOME) ; let qname = if is_prelude { name . clone () } else { qualify_cross_phylum (name , dep_name) } ; Ty :: Data (qname , args . iter () . map (| a | qualify_ty_cross_phylum (a , dep_name , dep_env_types)) . collect () ,) } Ty :: Fn (a , r) => Ty :: Fn (Box :: new (qualify_ty_cross_phylum (a , dep_name , dep_env_types)) , Box :: new (qualify_ty_cross_phylum (r , dep_name , dep_env_types)) ,) , Ty :: Seq (elem , n) => Ty :: Seq (Box :: new (qualify_ty_cross_phylum (elem , dep_name , dep_env_types)) , * n ,) , other => other . clone () , } }",
      "reason": "closure parameter `d` has no explicit type annotation — Mycelium's `lambda` parameters are always `name: Type` (grammar `lambda_expr` / `param`) and this transpiler has no type-inference pass to recover an omitted Rust closure-param type",
      "item_name": "qualify_ty_cross_phylum"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1816,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Extend a phylum's own `pub` export table with every declared dependency's `pub` exports,\"] # [doc = \" phylum-qualified under `\\\"{dep_local_name}::\\\"` (DN-113 §7 point 1) — the \\\"one added qualifier\"] # [doc = \" dimension\\\" that lets [`resolve_imports`] resolve a cross-phylum `use` through the exact same\"] # [doc = \" merge/precedence/never-silent logic as an intra-phylum one (DRY; no second resolver — DN-113\"] # [doc = \" §9.6). A no-op when `phyla` is empty (`Phyla::default()`), so a zero-dependency phylum's `exports`\"] # [doc = \" is unchanged from the pre-M-1060 shape.\"] # [doc = \"\"] # [doc = \" Widened to `pub(crate)` (zero logic change) for the in-crate white-box tests (see\"] # [doc = \" [`qualify_cross_phylum`]'s doc comment).\"] pub (crate) fn merge_phyla_exports (mut local : Exports , phyla : & Phyla) -> Exports { for (dep_name , resolved) in & phyla . deps { for (qual , is_pub) in & resolved . exports . declared { local . declared . insert (format ! (\"{dep_name}::{qual}\") , * is_pub) ; } for (qual , info) in & resolved . exports . types { let mut info = info . clone () ; info . home = qualify_cross_phylum (& info . home , dep_name) ; let dep_types = & resolved . env . types ; for ctor in & mut info . ctors { for field in & mut ctor . fields { * field = qualify_ty_cross_phylum (field , dep_name , dep_types) ; } } local . types . insert (format ! (\"{dep_name}::{qual}\") , info) ; } for (qual , fd) in & resolved . exports . fns { local . fns . insert (format ! (\"{dep_name}::{qual}\") , fd . clone ()) ; } for (qual , info) in & resolved . exports . traits { local . traits . insert (format ! (\"{dep_name}::{qual}\") , info . clone ()) ; } for (qual , sealed) in & resolved . exports . sealed_ctors { local . sealed_ctors . insert (format ! (\"{dep_name}::{qual}\") , sealed . clone ()) ; } for (qual , (params , ret)) in & resolved . exports . resolved_fn_sigs { let dep_types = & resolved . env . types ; let params = params . iter () . map (| t | qualify_ty_cross_phylum (t , dep_name , dep_types)) . collect () ; let ret = qualify_ty_cross_phylum (ret , dep_name , dep_types) ; local . resolved_fn_sigs . insert (format ! (\"{dep_name}::{qual}\") , (params , ret)) ; } } local }",
      "reason": "function body has a semicolon-terminated (value-discarding) statement expression before the tail — a `let`-chain body maps only simple `let x = e;` bindings plus a single trailing expression",
      "item_name": "merge_phyla_exports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1886,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Check a whole nodule: build the registry (prelude + declarations), then type every function\"] # [doc = \" body against its signature, classify totality. No maturation gate is applied (the scope is\"] # [doc = \" treated as non-matured). Returns the checked [`Env`].\"] # [doc = \"\"] # [doc = \" As of M-344 (RFC-0012) the input is first run through the **ambient resolution pass**\"] # [doc = \" ([`crate::ambient::resolve`]) — paradigm-less reprs are filled, `with paradigm` blocks stripped,\"] # [doc = \" bare decimals tagged — so the checker only ever sees fully-explicit (longhand) forms. A program\"] # [doc = \" using no ambient is unchanged (resolution is identity).\"] # [doc = \"\"] # [doc = \" As of M-662 a bare nodule is checked as a **phylum-of-one** ([`check_phylum`]); this is a thin\"] # [doc = \" wrapper that unwraps the single [`Env`]. Behavior on every single-nodule program is unchanged\"] # [doc = \" (no imports, the orphan rule's locality set is exactly this one nodule).\"] pub fn check_nodule (nodule : & Nodule) -> Result < Env , CheckError > { check_and_resolve (nodule) . map (| (env , _) | env) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_nodule"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1902,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Check a whole **phylum** (M-662; RFC-0006 §4.3): build the phylum-wide `pub` **export table** and\"] # [doc = \" the pub-blind **coherence view**, then check each nodule's bodies with its resolved `use` imports\"] # [doc = \" available and the **phylum-wide orphan rule** enforced. Returns one [`Env`] per nodule.\"] # [doc = \"\"] # [doc = \" Two strictly-separate phylum-wide views (conflating them is a bug — they answer different\"] # [doc = \" questions): the **import registry** ([`Exports`]) is `pub`-only (what a `use` may bind); the\"] # [doc = \" **coherence view** is pub-blind (every nodule's trait/type declarations are visible to the orphan\"] # [doc = \" rule regardless of `pub`).\"] # [doc = \"\"] # [doc = \" Cross-nodule **execution** (M-1024; ENB-1): each per-nodule [`Env`] already carries its *directly*\"] # [doc = \" imported `pub` decls (they are seeded into its checking registry — [`check_nodule_with`] — and\"] # [doc = \" retained), so a `use`d `pub` fn with a self-contained body already runs when a consumer nodule's\"] # [doc = \" `Env` is evaluated. The **transitive** case — a `pub` fn whose body calls its home nodule's\"] # [doc = \" *private* helper — needs the whole phylum linked into one [`Env`]: use [`PhylumEnv::link`], the\"] # [doc = \" runtime dual of the import resolution above (a **flat phylum namespace**, never-silent on a\"] # [doc = \" cross-nodule name collision). Qualified per-nodule scoping that would *disambiguate* a collision\"] # [doc = \" (rather than refuse it) is the flagged residual (M-982).\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" Any never-silent refusal: an unknown/private/ambiguous import, a duplicate import, a coherence or\"] # [doc = \" phylum-wide orphan violation, or any per-nodule type error (all surfaced as [`CheckError`]).\"] pub fn check_phylum (phylum : & Phylum) -> Result < PhylumEnv , CheckError > { check_phylum_matured (phylum , false) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1927,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Like [`check_phylum`] but with the explicit `matured_scope` gate applied to **every** nodule\"] # [doc = \" (RFC-0017 §4.2; M-662). When `matured_scope` is `false` this is identical to [`check_phylum`].\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" See [`check_phylum`]; additionally a non-total non-`thaw` definition in any nodule under a matured\"] # [doc = \" scope is an explicit [`CheckError`].\"] pub fn check_phylum_matured (phylum : & Phylum , matured_scope : bool) -> Result < PhylumEnv , CheckError > { check_phylum_matured_with_deps (phylum , & Phyla :: default () , matured_scope) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum_matured"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1937,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Like [`check_phylum`], but resolving cross-phylum `use dep::a.b.Item` references against `deps`\"] # [doc = \" (DN-113 Rank 1 / M-1060) — **additive** over [`check_phylum`]: a phylum with no cross-phylum `use`\"] # [doc = \" checks identically whether `deps` is empty or non-empty (an unused dependency is simply never\"] # [doc = \" referenced), and `deps: &Phyla::default()` here is byte-identical to plain [`check_phylum`].\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" See [`check_phylum`]; additionally an unknown dependency, a v1-deferred cross-phylum glob, or a\"] # [doc = \" cross-phylum name that does not resolve in the named dependency's `pub` surface is an explicit\"] # [doc = \" [`CheckError`] (DN-113 §7–§9; never a silent skip — G2).\"] pub fn check_phylum_with_deps (phylum : & Phylum , deps : & Phyla) -> Result < PhylumEnv , CheckError > { check_phylum_matured_with_deps (phylum , deps , false) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum_with_deps"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1950,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" [`check_phylum_with_deps`] with the explicit `matured_scope` gate (see [`check_phylum_matured`]).\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" See [`check_phylum_with_deps`] and [`check_phylum_matured`].\"] pub fn check_phylum_matured_with_deps (phylum : & Phylum , deps : & Phyla , matured_scope : bool ,) -> Result < PhylumEnv , CheckError > { check_phylum_matured_with_deps_and_exports (phylum , deps , matured_scope) . map (| (penv , _) | penv) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum_matured_with_deps"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1962,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Like [`check_phylum_matured_with_deps`], but additionally returns the checked phylum's own\"] # [doc = \" `pub`-only [`Exports`] table (crate-internal — [`crate::phyla::build_phyla_graph`]'s use case: a\"] # [doc = \" resolved node in a multi-phylum graph needs to retain its `Exports` inside a [`ResolvedPhylum`]\"] # [doc = \" for a FURTHER consumer up the graph, not just its linked [`Env`]). One registration/check pass\"] # [doc = \" produces both artifacts (DRY — no second pass to keep them in sync).\"] # [doc = \"\"] # [doc = \" # Errors\"] # [doc = \" See [`check_phylum_matured_with_deps`].\"] pub (crate) fn check_phylum_matured_with_deps_and_exports (phylum : & Phylum , deps : & Phyla , matured_scope : bool ,) -> Result < (PhylumEnv , Exports) , CheckError > { mycelium_stack :: with_deep_stack (| | check_phylum_inner (phylum , deps , matured_scope)) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum_matured_with_deps_and_exports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 1978,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "fn check_phylum_inner (phylum : & Phylum , deps : & Phyla , matured_scope : bool ,) -> Result < (PhylumEnv , Exports) , CheckError > { let resolved : Vec < Nodule > = phylum . nodules . iter () . map (crate :: ambient :: resolve) . collect :: < Result < _ , _ > > () ? ; let mut via_objects_per_nodule : Vec < Vec < ObjectDecl > > = Vec :: with_capacity (resolved . len ()) ; let mut resolved_expanded : Vec < Nodule > = Vec :: with_capacity (resolved . len ()) ; for n in resolved { if ! n . items . iter () . any (| i | matches ! (i , Item :: Object (_) | Item :: InherentImpl (_))) { via_objects_per_nodule . push (vec ! []) ; resolved_expanded . push (n) ; continue ; } let mut items = Vec :: with_capacity (n . items . len ()) ; let mut via_objs : Vec < ObjectDecl > = Vec :: new () ; for item in n . items { match item { Item :: Object (od) => { if ! od . via_decls . is_empty () { via_objs . push (od . clone ()) ; } items . extend (desugar_object_structural (& od)) ; } Item :: InherentImpl (id) => { if let Some (dup) = first_duplicate (& id . params) { return Err (CheckError :: new (\"impl\" , format ! (\"duplicate type parameter `{dup}` in an impl-level slot \\\n                                 `impl[…]` (DN-103 / M-1026; never silent — G2)\") ,)) ; } let impl_tps : Vec < TypeParam > = id . params . iter () . map (| name | TypeParam { name : name . clone () , kind : ParamKind :: Type , bounds : Vec :: new () , }) . collect () ; for mut m in id . methods { if ! impl_tps . is_empty () { let mut params = impl_tps . clone () ; params . extend (m . sig . params) ; m . sig . params = params ; } items . push (Item :: Fn (m)) ; } } other => items . push (other) , } } via_objects_per_nodule . push (via_objs) ; resolved_expanded . push (Nodule { path : n . path , std_sys : n . std_sys , items , }) ; } let resolved = resolved_expanded ; let mut per_nodule_regs : Vec < NoduleRegs > = Vec :: with_capacity (resolved . len ()) ; let mut exports = Exports :: default () ; let mut coherence = CoherenceView :: default () ; for nodule in & resolved { let regs = register_nodule_decls (nodule) ? ; let qual = | name : & str | qualify (& nodule . path , name) ; for (name , info) in & regs . types { exports . declared . insert (qual (name) , info_is_pub_type (nodule , name)) ; if info_is_pub_type (nodule , name) { exports . types . insert (qual (name) , info . clone ()) ; let sealed : BTreeSet < String > = nodule . items . iter () . filter_map (| it | match it { Item :: Type (td) if & td . name == name => Some (td) , _ => None , }) . flat_map (| td | td . ctors . iter ()) . filter (| c | c . sealed) . map (| c | c . name . clone ()) . collect () ; if ! sealed . is_empty () { exports . sealed_ctors . insert (qual (name) , sealed) ; } } } for (name , fd) in & regs . fns { exports . declared . insert (qual (name) , fd . vis . is_pub ()) ; if fd . vis . is_pub () { exports . fns . insert (qual (name) , fd . clone ()) ; if let Ok (resolved) = resolve_fn_sig (& regs . types , & fd . sig) { exports . resolved_fn_sigs . insert (qual (name) , resolved) ; } } } for (name , info) in & regs . traits { exports . declared . insert (qual (name) , info_is_pub_trait (nodule , name)) ; if info_is_pub_trait (nodule , name) { exports . traits . insert (qual (name) , info . clone ()) ; } } for name in regs . traits . keys () { coherence . traits . insert (name . clone ()) ; } for name in regs . types . keys () { if name != \"Bool\" { coherence . types . insert (name . clone ()) ; } } per_nodule_regs . push (regs) ; } let exports = merge_phyla_exports (exports , deps) ; let mut out = Vec :: with_capacity (resolved . len ()) ; let mut own = Vec :: with_capacity (resolved . len ()) ; for (i , (nodule , regs)) in resolved . iter () . zip (per_nodule_regs) . enumerate () { own . push (OwnDecls { types : regs . types . keys () . filter (| n | n . as_str () != \"Bool\") . cloned () . collect () , fns : regs . fns . keys () . cloned () . collect () , traits : regs . traits . keys () . filter (| n | { n . as_str () != crate :: fuse :: TRAIT_NAME && n . as_str () != crate :: ord3 :: TRAIT_NAME }) . cloned () . collect () , }) ; let imports = resolve_imports (nodule , & exports , deps) ? ; let via_objects = & via_objects_per_nodule [i] ; let env = check_nodule_with (nodule , regs , & imports , & coherence , matured_scope , via_objects ,) ? ; out . push ((nodule . path . clone () , env)) ; } Ok ((PhylumEnv { nodules : out , own } , exports)) }",
      "reason": "fn parameter `phylum` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_phylum_inner"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2218,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" `nodule.path` + `.` + `name` — a top-level item's **qualified name** (the import-registry key;\"] # [doc = \" M-662). `nodule a.b` declaring `List` ⇒ `\\\"a.b.List\\\"`.\"] fn qualify (path : & Path , name : & str) -> String { if path . 0 . is_empty () { name . to_owned () } else { format ! (\"{}.{name}\" , path . 0 . join (\".\")) } }",
      "reason": "field access on a non-`self` base — the transpiler tracks no local types, so the projection cannot be resolved to a constructor position (only `self.<field>` desugars to a `match`)",
      "item_name": "qualify"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2228,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Is the named **type** declared `pub` in this (resolved) nodule? (The registry [`DataInfo`] does\"] # [doc = \" not carry `Vis` — it is a checked, post-resolution structure — so the surface `Vis` is read back\"] # [doc = \" from the nodule's items. The prelude `Bool` is never a surface item ⇒ not `pub`.)\"] fn info_is_pub_type (nodule : & Nodule , name : & str) -> bool { nodule . items . iter () . any (| i | matches ! (i , Item :: Type (td) if td . name == name && td . vis . is_pub ())) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "info_is_pub_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2238,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Is the named **trait** declared `pub` in this (resolved) nodule? (See [`info_is_pub_type`].)\"] fn info_is_pub_trait (nodule : & Nodule , name : & str) -> bool { nodule . items . iter () . any (| i | matches ! (i , Item :: Trait (td) if td . name == name && td . vis . is_pub ())) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "info_is_pub_trait"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2246,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The pub-blind **coherence view** for the phylum-wide orphan rule (M-662; RFC-0019 §4.5): the set\"] # [doc = \" of **all** trait names and **all** data-type names declared by **any** nodule of the phylum,\"] # [doc = \" regardless of `pub`. Distinct from the [`Exports`] (pub-only) import registry — an `impl` is legal\"] # [doc = \" iff its trait OR its `for`-type head is declared *somewhere* in the phylum, and that visibility is\"] # [doc = \" pub-blind (coherence is enforcement authority, not the `pub` namespace).\"] # [derive (Debug , Default)] pub (crate) struct CoherenceView { # [doc = \" Every trait name declared anywhere in the phylum (pub-blind).\"] pub (crate) traits : BTreeSet < String > , # [doc = \" Every data-type name declared anywhere in the phylum (pub-blind), excluding the prelude.\"] pub (crate) types : BTreeSet < String > , }",
      "reason": "struct `CoherenceView` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "CoherenceView"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2259,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" The **declaration-level** registries of one nodule (types/fns/traits), built before any body is\"] # [doc = \" checked (M-662). The phylum builds its two cross-nodule views from these, then re-uses them as the\"] # [doc = \" per-nodule base when checking that nodule's bodies (so registration runs once per nodule — DRY).\"] # [doc = \"\"] # [doc = \" Widened to `pub(crate)` (struct + all three fields) for the M-1013 STEP 5 `compiler.semcore`\"] # [doc = \" differential harness (`crates/mycelium-l1/src/tests/compiler_stage5_register.rs`), which needs to\"] # [doc = \" read the registries `register_nodule_decls` returns to compare them against the `.myc` port's\"] # [doc = \" mirror output — zero logic change, the same widening precedent as STEP 3's\"] # [doc = \" `resolve_ctors`/`first_duplicate` (commit 2bb06f88) and the `CoherenceView` widening\"] # [doc = \" (commit 65351071).\"] pub (crate) struct NoduleRegs { pub (crate) types : BTreeMap < String , DataInfo > , pub (crate) fns : BTreeMap < String , FnDecl > , pub (crate) traits : BTreeMap < String , TraitInfo > , }",
      "reason": "struct `NoduleRegs` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "NoduleRegs"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2275,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Register one (resolved) nodule's **declarations** — data types (Pass 1), traits (Pass 1b), and\"] # [doc = \" function signatures (Pass 2) — into its registries, with the same duplicate/arity refusals as the\"] # [doc = \" single-nodule checker (M-662 factors these out of `check_resolved_matured` so the phylum can build\"] # [doc = \" its cross-nodule views before checking any body). Bodies and instances are **not** handled here\"] # [doc = \" (instances need the phylum-wide orphan view; bodies need imports). The prelude `Bool` is included\"] # [doc = \" so intra-nodule resolution is unchanged.\"] # [doc = \"\"] # [doc = \" Widened to `pub(crate)` for the M-1013 STEP 5 differential harness (see [`NoduleRegs`]).\"] pub (crate) fn register_nodule_decls (nodule : & Nodule) -> Result < NoduleRegs , CheckError > { let mut types = BTreeMap :: new () ; let p = prelude () ; types . insert (p . name . clone () , p) ; register_types (& mut types , nodule) ? ; let mut traits = register_traits (& types , nodule) ? ; let fuse_used = nodule . items . iter () . any (| item | matches ! (item , Item :: Impl (id) if id . trait_name == crate :: fuse :: TRAIT_NAME)) ; if fuse_used { if traits . contains_key (crate :: fuse :: TRAIT_NAME) { return Err (CheckError :: new (crate :: fuse :: TRAIT_NAME , \"cannot redeclare the built-in prelude trait `Fuse` (DN-58 §A / M-965 F-A1) — its \\\n                 lawful-merge `join` contract is already provided by the prelude; remove this \\\n                 declaration and `impl Fuse[T] for T { fn join(a: T, b: T) => T = … }` directly\" ,)) ; } traits . insert (crate :: fuse :: TRAIT_NAME . to_owned () , crate :: fuse :: prelude ()) ; } else if traits . contains_key (crate :: fuse :: TRAIT_NAME) { return Err (CheckError :: new (crate :: fuse :: TRAIT_NAME , \"cannot redeclare the built-in prelude trait `Fuse` (DN-58 §A / M-965 F-A1) — its \\\n             lawful-merge `join` contract is already provided by the prelude; remove this \\\n             declaration and `impl Fuse[T] for T { fn join(a: T, b: T) => T = … }` directly\" ,)) ; } let ord3_used = nodule . items . iter () . any (| item | matches ! (item , Item :: Impl (id) if id . trait_name == crate :: ord3 :: TRAIT_NAME)) ; if ord3_used { if traits . contains_key (crate :: ord3 :: TRAIT_NAME) { return Err (CheckError :: new (crate :: ord3 :: TRAIT_NAME , \"cannot redeclare the built-in prelude trait `Ord3` (DN-122 §13 / M-1080 WU-B) — \\\n                 its `cmp` contract is already provided by the prelude; remove this declaration \\\n                 and `impl Ord3[T] for T { fn cmp(a: T, b: T) => Binary{8} = … }` directly\" ,)) ; } traits . insert (crate :: ord3 :: TRAIT_NAME . to_owned () , crate :: ord3 :: prelude ()) ; } else if traits . contains_key (crate :: ord3 :: TRAIT_NAME) { return Err (CheckError :: new (crate :: ord3 :: TRAIT_NAME , \"cannot redeclare the built-in prelude trait `Ord3` (DN-122 §13 / M-1080 WU-B) — its \\\n             `cmp` contract is already provided by the prelude; remove this declaration and \\\n             `impl Ord3[T] for T { fn cmp(a: T, b: T) => Binary{8} = … }` directly\" ,)) ; } let mut fns : BTreeMap < String , FnDecl > = BTreeMap :: new () ; for item in & nodule . items { if let Item :: Fn (fd) = item { if let Some (dup) = first_duplicate (& fd . sig . param_names ()) { return Err (CheckError :: new (& fd . sig . name , format ! (\"duplicate type parameter `{dup}` in `{}`\" , fd . sig . name) ,)) ; } if fns . insert (fd . sig . name . clone () , fd . clone ()) . is_some () { return Err (CheckError :: new (& fd . sig . name , \"duplicate function\")) ; } } } Ok (NoduleRegs { types , fns , traits }) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "register_nodule_decls"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2383,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Resolve one nodule's `use` imports against the phylum-wide [`Exports`] (M-662) — including any\"] # [doc = \" **cross-phylum** `use dep::a.b.Item` (DN-113 Rank 1 / M-1060), resolved through the identical\"] # [doc = \" merge/precedence logic against the SAME `exports` (already phylum-qualified by\"] # [doc = \" [`merge_phyla_exports`] — DRY, one resolver). Builds the per-nodule [`NoduleImports`] — imported\"] # [doc = \" `pub` decls merged by simple name at glob-then-explicit precedence (own decls shadow these later,\"] # [doc = \" in [`check_nodule_with`]) — and enforces every never-silent rule:\"] # [doc = \"\"] # [doc = \" - **unknown name/path** → explicit refusal (distinguishing \\\"no such name\\\" from \\\"exists but\"] # [doc = \"   private\\\", honest + helpful);\"] # [doc = \" - **two explicit `use`s binding the same simple name** → duplicate-import refusal;\"] # [doc = \" - **glob-vs-glob collision** on a name → recorded `ambiguous` (a *reference* to it is refused at\"] # [doc = \"   use-site), never a silent winner;\"] # [doc = \" - **DN-113 v1**: an unknown dependency (`use dep::…` where `dep` is not in `deps`) or a\"] # [doc = \"   cross-phylum **glob** (`use dep::a.b.*` — deferred, folds into M-982) → an explicit refusal.\"] # [doc = \"\"] # [doc = \" (A glob over a prefix with zero `pub` names is allowed — an empty import; an unresolved *reference*\"] # [doc = \" then surfaces the normal unknown-name error.)\"] # [doc = \"\"] # [doc = \" M-1013 STEP 4: widened to `pub(crate)` (zero logic change) so the in-crate self-hosting\"] # [doc = \" differential (`crates/mycelium-l1/src/tests/compiler_stage5_imports.rs`) can call this live\"] # [doc = \" oracle directly (the same widening precedent as `resolve_ctors`/`first_duplicate`, PR #1238).\"] pub (crate) fn resolve_imports (nodule : & Nodule , exports : & Exports , deps : & Phyla ,) -> Result < NoduleImports , CheckError > { let site = qualify (& nodule . path , \"<use>\") ; let mut imp = NoduleImports :: default () ; let mut via_explicit : BTreeSet < String > = BTreeSet :: new () ; let mut via_glob : BTreeSet < String > = BTreeSet :: new () ; for item in & nodule . items { let Item :: Use (UsePath { phylum , path , glob : true , }) = item else { continue ; } ; if let Some (dep) = phylum { return Err (CheckError :: new (& site , format ! (\"`use {dep}::{}.*`: a cross-phylum glob import is not supported in v1 — DN-113 \\\n                     requires an explicit cross-phylum import (`use {dep}::{}.<Item>`); glob \\\n                     disambiguation folds into M-982 (never a silent skip — G2)\" , path . 0 . join (\".\") , path . 0 . join (\".\")) ,)) ; } let prefix = path . 0 . join (\".\") ; let mut any = false ; for qual in exports . declared . keys () { let Some (simple) = direct_child (& prefix , qual) else { continue ; } ; if ! exports_has_pub (exports , qual) { continue ; } any = true ; if via_explicit . contains (& simple) { continue ; } if via_glob . contains (& simple) { imp . ambiguous . insert (simple . clone ()) ; remove_import (& mut imp , & simple) ; continue ; } via_glob . insert (simple . clone ()) ; insert_export (& mut imp , exports , qual , & simple) ; } let _ = any ; } for item in & nodule . items { let Item :: Use (UsePath { phylum , path , glob : false , }) = item else { continue ; } ; let Some ((simple , prefix)) = split_last_seg (path) else { return Err (CheckError :: new (& site , \"a `use` path must name an item (`use a.b.Item`) — an empty `use` path does not name \\\n                 a cross-nodule item (M-662)\" ,)) ; } ; let (qual , display_path) = match phylum { Some (dep) => { if ! deps . has_dep (dep) { return Err (CheckError :: new (& site , format ! (\"`use {dep}::{}`: no such dependency `{dep}` in this phylum's \\\n                             `[dependencies]` (DN-113 §9.5; never a silent skip — G2)\" , path . 0 . join (\".\")) ,)) ; } if prefix . is_empty () { return Err (CheckError :: new (& site , format ! (\"`use {dep}::{simple}`: a cross-phylum import must be nodule-qualified \\\n                             within the dependency — `{simple}` names no nodule of `{dep}`. Write \\\n                             `use {dep}::<nodule>.{simple}` (DN-113 §4)\") ,)) ; } (format ! (\"{dep}::{prefix}.{simple}\") , format ! (\"{dep}::{}\" , path . 0 . join (\".\")) ,) } None => { if prefix . is_empty () { return Err (CheckError :: new (& site , format ! (\"`use {simple}`: a cross-nodule import must be nodule-qualified — \\\n                             `{simple}` names no nodule. Write `use <nodule>.{simple}` (a specific \\\n                             import) or `use <nodule>.*` (a glob) (M-662)\") ,)) ; } (format ! (\"{prefix}.{simple}\") , path . 0 . join (\".\")) } } ; match exports . declared . get (& qual) { None => { return Err (CheckError :: new (& site , format ! (\"`use {display_path}`: no such name `{qual}` {} — {} declares it \\\n                         (M-662/DN-113; never a silent skip — G2)\" , if phylum . is_some () { \"in that dependency phylum\" } else { \"in the phylum\" } , if phylum . is_some () { \"no nodule of the dependency\" } else { \"no nodule\" }) ,)) ; } Some (false) => { return Err (CheckError :: new (& site , format ! (\"`use {display_path}`: `{qual}` exists but is not `pub` — it is private to \\\n                         its nodule and not importable (mark it `pub` to export it; M-662/DN-113)\") ,)) ; } Some (true) => { } } if via_explicit . contains (& simple) { return Err (CheckError :: new (& site , format ! (\"duplicate import of `{simple}` — two `use` declarations bind the same name; \\\n                     import it once (M-662; never a silent shadow — G2)\") ,)) ; } via_explicit . insert (simple . clone ()) ; imp . ambiguous . remove (& simple) ; insert_export (& mut imp , exports , & qual , & simple) ; } let own_ctors : BTreeSet < String > = nodule . items . iter () . filter_map (| it | match it { Item :: Type (td) => Some (td) , _ => None , }) . flat_map (| td | td . ctors . iter () . map (| c | c . name . clone ())) . collect () ; imp . sealed . retain (| c | ! own_ctors . contains (c)) ; let own_fns : BTreeSet < String > = nodule . items . iter () . filter_map (| it | match it { Item :: Fn (fd) => Some (fd . sig . name . clone ()) , _ => None , }) . collect () ; imp . resolved_fn_sigs . retain (| n , _ | ! own_fns . contains (n)) ; imp . cross_phylum_fns . retain (| n | ! own_fns . contains (n)) ; let own_traits : BTreeSet < String > = nodule . items . iter () . filter_map (| it | match it { Item :: Trait (td) => Some (td . name . clone ()) , _ => None , }) . collect () ; imp . cross_phylum_traits . retain (| n | ! own_traits . contains (n)) ; Ok (imp) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "resolve_imports"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2633,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Does the export table mark `qual` as a `pub` (importable) name?\"] fn exports_has_pub (exports : & Exports , qual : & str) -> bool { matches ! (exports . declared . get (qual) , Some (true)) }",
      "reason": "unsupported expression form `matches ! (exports . declared . get (qual) , Some (true))`",
      "item_name": "exports_has_pub"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2638,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" If `qual` is `prefix` + `.` + a **single** further segment, return that segment (a *direct* child\"] # [doc = \" of `prefix`); else `None`. (`\\\"a.b.List\\\"` is a direct child of `\\\"a.b\\\"` ⇒ `List`; `\\\"a.b.c.X\\\"` is\"] # [doc = \" not.) Used to expand a glob `use prefix.*` to exactly the names one level under the prefix.\"] fn direct_child (prefix : & str , qual : & str) -> Option < String > { let rest = qual . strip_prefix (prefix) ? . strip_prefix ('.') ? ; if rest . is_empty () || rest . contains ('.') { None } else { Some (rest . to_owned ()) } }",
      "reason": "unsupported expression form `qual . strip_prefix (prefix) ? . strip_prefix ('.') ?`",
      "item_name": "direct_child"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2650,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Split a `use` path into `(last_segment, prefix_joined)`; `None` for an empty path. `a.b.Item` ⇒\"] # [doc = \" `(\\\"Item\\\", \\\"a.b\\\")`; a single-segment `Item` ⇒ `(\\\"Item\\\", \\\"\\\")`.\"] fn split_last_seg (path : & Path) -> Option < (String , String) > { let (last , init) = path . 0 . split_last () ? ; Some ((last . clone () , init . join (\".\"))) }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "split_last_seg"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2657,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Insert the export `qual` (a `pub` type/fn/trait) into the per-nodule imports under `simple`.\"] # [doc = \" Exactly one of the three export tables holds `qual` (a name is one kind); insert from whichever.\"] fn insert_export (imp : & mut NoduleImports , exports : & Exports , qual : & str , simple : & str) { let is_cross_phylum = qual . contains (\"::\") ; if let Some (info) = exports . types . get (qual) { imp . types . insert (simple . to_owned () , info . clone ()) ; if let Some (sealed) = exports . sealed_ctors . get (qual) { imp . sealed . extend (sealed . iter () . cloned ()) ; } } if let Some (fd) = exports . fns . get (qual) { imp . fns . insert (simple . to_owned () , fd . clone ()) ; if let Some (resolved) = exports . resolved_fn_sigs . get (qual) { imp . resolved_fn_sigs . insert (simple . to_owned () , resolved . clone ()) ; } if is_cross_phylum { imp . cross_phylum_fns . insert (simple . to_owned ()) ; } else { imp . cross_phylum_fns . remove (simple) ; } } if let Some (info) = exports . traits . get (qual) { imp . traits . insert (simple . to_owned () , info . clone ()) ; if is_cross_phylum { imp . cross_phylum_traits . insert (simple . to_owned ()) ; } else { imp . cross_phylum_traits . remove (simple) ; } } }",
      "reason": "mutating method body statement is neither a `let`, a supported `self.<field>`/`*<param>` re-assignment, nor (in tail position) the method's own return value — value-threading deliberately refuses any shape outside a flat re-assignment sequence rather than risk an unsound rebind (DN-125 §6.1, never-silent G2/VR-5)",
      "item_name": "insert_export"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2699,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Remove any import binding for `simple` across all three tables (used when a glob-vs-glob collision\"] # [doc = \" demotes a tentatively-bound name to `ambiguous`).\"] fn remove_import (imp : & mut NoduleImports , simple : & str) { imp . types . remove (simple) ; imp . fns . remove (simple) ; imp . traits . remove (simple) ; imp . resolved_fn_sigs . remove (simple) ; imp . cross_phylum_traits . remove (simple) ; imp . cross_phylum_fns . remove (simple) ; }",
      "reason": "mutating method body statement is neither a `let`, a supported `self.<field>`/`*<param>` re-assignment, nor (in tail position) the method's own return value — value-threading deliberately refuses any shape outside a flat re-assignment sequence rather than risk an unsound rebind (DN-125 §6.1, never-silent G2/VR-5)",
      "item_name": "remove_import"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2712,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Like [`check_nodule`] but with an explicit `matured_scope` flag (RFC-0017 §4.2): when `true`,\"] # [doc = \" every reachable definition whose `thaw == false` must be `Total` (the existing totality\"] # [doc = \" classifier, unchanged) — a non-total non-thaw definition is an explicit `CheckError`. Definitions\"] # [doc = \" marked `thaw` are exempt from the gate (RFC-0017 §4.3). When `matured_scope` is `false` this\"] # [doc = \" is identical to [`check_nodule`].\"] pub fn check_nodule_matured (nodule : & Nodule , matured_scope : bool) -> Result < Env , CheckError > { check_and_resolve_matured (nodule , matured_scope) . map (| (env , _) | env) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_nodule_matured"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2721,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "fn check_and_resolve_matured (nodule : & Nodule , matured_scope : bool ,) -> Result < (Env , Nodule) , CheckError > { mycelium_stack :: with_deep_stack (| | check_and_resolve_matured_inner (nodule , matured_scope)) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_and_resolve_matured"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2732,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "fn check_and_resolve_matured_inner (nodule : & Nodule , matured_scope : bool ,) -> Result < (Env , Nodule) , CheckError > { let resolved = crate :: ambient :: resolve (nodule) ? ; let phylum = Phylum :: of_one (resolved . clone ()) ; let (penv , _exports) = check_phylum_inner (& phylum , & Phyla :: default () , matured_scope) ? ; let env = penv . single () . expect (\"a phylum-of-one yields exactly one Env\") . clone () ; let mut items = Vec :: with_capacity (resolved . items . len ()) ; for item in & resolved . items { match item { Item :: Fn (fd) => { let resolved_fd = env . fns . get (& fd . sig . name) . cloned () . unwrap_or_else (| | fd . clone ()) ; items . push (Item :: Fn (resolved_fd)) ; } other => items . push (other . clone ()) , } } let twin = Nodule { path : resolved . path . clone () , std_sys : resolved . std_sys , items , } ; Ok ((env , twin)) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_and_resolve_matured_inner"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2774,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Like [`check_nodule`], but also returns the **fully-resolved longhand twin** of the program\"] # [doc = \" (paradigm tags filled *and* bare-decimal widths resolved from context) — the source the M-142/LSP\"] # [doc = \" \\\"expand ambient\\\" projection renders (RFC-0012 §5). The returned [`Nodule`] elaborates to the\"] # [doc = \" identical L0 (and content hash) as the original (I2; RFC-0012 §4.3).\"] pub fn check_and_resolve (nodule : & Nodule) -> Result < (Env , Nodule) , CheckError > { check_and_resolve_matured (nodule , false) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_and_resolve"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2782,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" Register a (resolved) nodule's **data declarations** into `types` (Pass 1; RFC-0007 §11): a shell\"] # [doc = \" per type first (so recursive field references resolve), then its constructors. Duplicate type\"] # [doc = \" parameters / duplicate type names are explicit refusals. Extracted (M-662) so the phylum can build\"] # [doc = \" its registries once and reuse them; behavior is byte-identical to the inlined Pass 1.\"] pub (crate) fn register_types (types : & mut BTreeMap < String , DataInfo > , nodule : & Nodule ,) -> Result < () , CheckError > { for arity in collect_tuple_arities (nodule) { let tname = tuple_type_name (arity) ; types . entry (tname) . or_insert_with (| | synthetic_tuple_data (arity)) ; } for item in & nodule . items { if let Item :: Type (td) = item { if let Some (dup) = first_duplicate (& td . params) { return Err (CheckError :: new (& td . name , format ! (\"duplicate type parameter `{dup}` in `{}`\" , td . name) ,)) ; } if types . contains_key (& td . name) { return Err (CheckError :: new (& td . name , \"duplicate type declaration\")) ; } types . insert (td . name . clone () , DataInfo { name : td . name . clone () , home : nodule_home (& nodule . path) , params : td . params . clone () , ctors : vec ! [] , } ,) ; } } for item in & nodule . items { if let Item :: Type (td) = item { let ctors = resolve_ctors (types , td) ? ; types . get_mut (& td . name) . expect (\"registered above\") . ctors = ctors ; } } Ok (()) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "register_types"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2839,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Structural lowering of an `object` declaration (DN-53 §A.3.2, M-811): expand to the equivalent\"] # [doc = \" `Item::Type` + `Item::Impl` + `Item::Fn` items that the existing registration and checking passes\"] # [doc = \" already handle.\"] # [doc = \"\"] # [doc = \" The `via` delegation impls are **not** generated here — they require the trait registry (not yet\"] # [doc = \" built at the point this is called). They are generated by [`expand_object_via_decls`] and injected\"] # [doc = \" as additional `Item::Impl` items before the instance-registration and method-body passes.\"] # [doc = \"\"] # [doc = \" Guarantee: `Declared` (structural one-to-one rewrite; injective by construction — two distinct\"] # [doc = \" `object` declarations never produce the same item set, and the set determines the source uniquely;\"] # [doc = \" DN-53 §A.3.3). The three-way differential (`tests/object_desugar.rs`) provides `Empirical`\"] # [doc = \" agreement between the `object` form and its hand-written lowering.\"] fn desugar_object_structural (od : & ObjectDecl) -> Vec < Item > { let type_decl = TypeDecl { vis : od . vis , name : od . name . clone () , params : od . params . clone () , ctors : vec ! [od . ctor . clone ()] , } ; let mut items : Vec < Item > = vec ! [Item :: Type (type_decl)] ; for impl_decl in & od . impls { items . push (Item :: Impl (impl_decl . clone ())) ; } for fn_decl in & od . fns { items . push (Item :: Fn (fn_decl . clone ())) ; } items }",
      "reason": "struct literal `TypeDecl { .. }` — not an in-file single-ctor struct that emits (no constructor to build)",
      "item_name": "desugar_object_structural"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 2872,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Generate forwarding `ImplDecl`s for each `via` clause of an `object` declaration\"] # [doc = \" (DN-53 §A.3.2, M-811). Called **after** `register_traits` so the trait's method signatures are\"] # [doc = \" available.\"] # [doc = \"\"] # [doc = \" For each `via <field_idx> : TraitName[args]`, generates an `impl TraitName for ObjectName` whose\"] # [doc = \" methods each destructure the object via a `match` to extract the delegate field, then forward the\"] # [doc = \" call. The delegate must implement the trait; the type-checker verifies this when it resolves the\"] # [doc = \" forwarding call.\"] # [doc = \"\"] # [doc = \" Guarantee: `Empirical` — the forwarding bodies are synthesized from the trait signatures; the\"] # [doc = \" three-way differential (`tests/object_desugar.rs`) pins the agreement\"] # [doc = \" `observe(object) == observe(lower(object))` (DN-53 §A.3.3).\"] # [doc = \"\"] # [doc = \" Never-silent (G2): an unknown trait name, an out-of-range field index, or **two `via` clauses on\"] # [doc = \" the same object delegating the same trait** (M-966 — an ambiguous choice between two delegates\"] # [doc = \" with no deterministic tiebreak) is an explicit [`CheckError`], never a silent accept or a\"] # [doc = \" silent first-match pick.\"] fn expand_object_via_decls (od : & ObjectDecl , traits : & BTreeMap < String , TraitInfo > ,) -> Result < Vec < ImplDecl > , CheckError > { let mut first_via_for_trait : BTreeMap < & str , u32 > = BTreeMap :: new () ; for via in & od . via_decls { if let Some (& first_idx) = first_via_for_trait . get (via . trait_name . as_str ()) { return Err (CheckError :: new (& od . name , format ! (\"`object {}`: ambiguous `via` delegation for trait `{}` — both `via {} : {}` \\\n                     and `via {} : {}` claim to provide it, and there is no deterministic tiebreak \\\n                     between two delegates for the same trait (DN-53 §A.3.2; M-966). Fix: keep only \\\n                     one `via` clause per trait on this object, or replace one of them with an \\\n                     explicit `impl {} for {}` that picks the winner by hand — never a silent \\\n                     first-match (G2).\" , od . name , via . trait_name , first_idx , via . trait_name , via . field_idx , via . trait_name , via . trait_name , od . name) ,)) ; } first_via_for_trait . insert (via . trait_name . as_str () , via . field_idx) ; } let mut via_impls : Vec < ImplDecl > = Vec :: new () ; for via in & od . via_decls { let trait_info = traits . get (& via . trait_name) . ok_or_else (| | { CheckError :: new (& od . name , format ! (\"`object {}`: `via {} : {}` — trait `{}` is not declared in scope \\\n                     (DN-53 §A.3.2; `via` requires the trait to be visible — never a silent miss, G2)\" , od . name , via . field_idx , via . trait_name , via . trait_name) ,) }) ? ; let n_fields = od . ctor . fields . len () ; if via . field_idx as usize >= n_fields { return Err (CheckError :: new (& od . name , format ! (\"`object {}`: `via {} : {}` — field index {} is out of range; the constructor \\\n                     `{}` has {} field(s) (0-based; DN-53 §A.3.2; never silent — G2)\" , od . name , via . field_idx , via . trait_name , via . field_idx , od . ctor . name , n_fields) ,)) ; } let obj_ty = TypeRef :: unguaranteed (if od . params . is_empty () { BaseType :: Named (od . name . clone () , vec ! []) } else { BaseType :: Named (od . name . clone () , od . params . iter () . map (| p | TypeRef :: unguaranteed (BaseType :: Named (p . clone () , vec ! []))) . collect () ,) }) ; let binders : Vec < String > = (0 .. n_fields) . map (| i | format ! (\"_f{i}\")) . collect () ; let delegate_var = binders [via . field_idx as usize] . clone () ; let match_pattern = Pattern :: Ctor (od . ctor . name . clone () , binders . iter () . map (| b | Pattern :: Ident (b . clone ())) . collect () ,) ; let mut methods : Vec < FnDecl > = Vec :: new () ; for sig in & trait_info . sigs { let sig = trait_info . params . iter () . zip (via . trait_args . iter ()) . fold (sig . clone () , | acc , (param , concrete) | { subst_type_param_in_sig (& acc , param , concrete) }) ; let self_name = sig . value_params . first () . map (| p | p . name . clone ()) . unwrap_or_else (| | \"self\" . to_string ()) ; let delegate_expr = Expr :: Match { scrutinee : Box :: new (Expr :: Path (Path (vec ! [self_name]))) , arms : vec ! [Arm { pattern : match_pattern . clone () , body : Expr :: Path (Path (vec ! [delegate_var . clone ()])) , }] , } ; let mut call_args : Vec < Expr > = vec ! [delegate_expr] ; for vp in sig . value_params . iter () . skip (1) { call_args . push (Expr :: Path (Path (vec ! [vp . name . clone ()]))) ; } let body = Expr :: App { head : Box :: new (Expr :: Path (Path (vec ! [sig . name . clone ()]))) , args : call_args , } ; methods . push (FnDecl { vis : crate :: ast :: Vis :: Private , thaw : false , tier : None , sig , body , }) ; } via_impls . push (ImplDecl { trait_name : via . trait_name . clone () , trait_args : via . trait_args . clone () , for_ty : obj_ty , methods , }) ; } Ok (via_impls) }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "expand_object_via_decls"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3038,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The core checker for **one nodule of a phylum** (M-662), run on an already ambient-resolved nodule\"] # [doc = \" with its pre-built declaration registries (`regs`), its resolved cross-nodule `imports`, the\"] # [doc = \" phylum-wide pub-blind `coherence` view, and an explicit maturation flag. When `matured_scope` is\"] # [doc = \" true, every fn with `thaw == false` must be `Total` (RFC-0017 §4.2).\"] # [doc = \"\"] # [doc = \" The nodule's checking registries = **its own declarations** merged with the **imported `pub`\"] # [doc = \" declarations** (own decls take precedence — the documented shadowing). The orphan rule is enforced\"] # [doc = \" **phylum-wide** (via `coherence`); name resolution is **never-silent** (an ambiguous glob name is\"] # [doc = \" refused at use-site). A phylum-of-one passes empty imports + a coherence view of just this nodule,\"] # [doc = \" so this reduces *exactly* to the pre-M-662 single-nodule checker.\"] fn check_nodule_with (nodule : & Nodule , regs : NoduleRegs , imports : & NoduleImports , coherence : & CoherenceView , matured_scope : bool , via_objects : & [ObjectDecl] ,) -> Result < Env , CheckError > { let mut types = imports . types . clone () ; types . extend (regs . types . clone ()) ; let mut traits = imports . traits . clone () ; traits . extend (regs . traits . clone ()) ; let mut fns = imports . fns . clone () ; fns . extend (regs . fns . clone ()) ; let via_generated_impls : Vec < ImplDecl > = { let mut all = Vec :: new () ; for od in via_objects { all . extend (expand_object_via_decls (od , & traits) ?) ; } all } ; let via_provenance : BTreeMap < (String , String) , (u32 , String) > = { let mut all = BTreeMap :: new () ; let this_home = nodule_home (& nodule . path) ; for od in via_objects { let head = format ! (\"Data:{}\" , qualify_type_name (& this_home , & od . name)) ; for via in & od . via_decls { all . insert ((via . trait_name . clone () , head . clone ()) , (via . field_idx , od . name . clone ()) ,) ; } } all } ; let mut lower_rules : BTreeMap < String , LowerDecl > = BTreeMap :: new () ; for item in & nodule . items { if let Item :: Lower (ld) = item { check_lower_decl_structural (ld , & lower_rules) ? ; lower_rules . insert (ld . name . clone () , ld . clone ()) ; } } check_lower_rule_acyclicity (& lower_rules , & types , & fns) ? ; let mut derive_generated_impls : Vec < ImplDecl > = Vec :: new () ; let mut derived_provenance : BTreeMap < (String , String) , (String , String) > = BTreeMap :: new () ; let mut derived_seen : BTreeMap < (String , String) , ImplDecl > = BTreeMap :: new () ; for item in & nodule . items { let Item :: Derive (dd) = item else { continue } ; check_derive_application (dd , & lower_rules , & types) ? ; let rule = & lower_rules [& dd . name] ; let Some (template) = rule . impl_rhs () else { continue ; } ; if rule . params . len () != 1 { return Err (CheckError :: new (& dd . name , format ! (\"`derive {} for …` targets an item-shaped rule with {} type parameter(s), but \\\n                     Model-A sibling injection binds the single `derive` target type to exactly one \\\n                     rule parameter (`lower {}[T] = impl … for T`; DN-54 §10.3 / OQ-C multi-param is \\\n                     a future extension — never a silent mis-instantiation, G2)\" , dd . name , rule . params . len () , dd . name) ,)) ; } let concrete = subst_type_param_in_impl (template , & rule . params [0] , & dd . for_ty) ; let (for_ty_resolved , _) = resolve_ty (& dd . name , & types , & [] , & concrete . for_ty) ? ; let Some (head) = type_head (& for_ty_resolved) else { return Err (CheckError :: new (& dd . name , format ! (\"`derive {} for {for_ty_resolved}` instantiates to an `impl … for \\\n                     {for_ty_resolved}` whose target is not a concrete instance head (DN-54 §10.2 / \\\n                     RFC-0019 §4.5 — a derive target must be a concrete type, never a bare type \\\n                     variable; G2)\" , dd . name) ,)) ; } ; let key = (concrete . trait_name . clone () , head) ; match derived_seen . get (& key) { Some (prev) if * prev == concrete => continue , _ => { } } derived_seen . insert (key . clone () , concrete . clone ()) ; derived_provenance . insert (key , (dd . name . clone () , format ! (\"{for_ty_resolved}\"))) ; derive_generated_impls . push (concrete) ; } let effective_nodule_owned : Option < Nodule > = if via_generated_impls . is_empty () && derive_generated_impls . is_empty () { None } else { let mut items = nodule . items . clone () ; for id in & via_generated_impls { items . push (Item :: Impl (id . clone ())) ; } for id in & derive_generated_impls { items . push (Item :: Impl (id . clone ())) ; } Some (Nodule { path : nodule . path . clone () , std_sys : nodule . std_sys , items , }) } ; let effective_nodule : & Nodule = effective_nodule_owned . as_ref () . unwrap_or (nodule) ; let instances = register_instances (& types , & traits , coherence , effective_nodule) ? ; let mut resolved_fns : BTreeMap < String , FnDecl > = fns . clone () ; for fd in regs . fns . values () { let (body , _ret) = check_fn_body (& types , & fns , & traits , & instances , imports , & lower_rules , nodule . std_sys , fd ,) ? ; resolved_fns . insert (fd . sig . name . clone () , FnDecl { vis : fd . vis , thaw : fd . thaw , tier : fd . tier , sig : fd . sig . clone () , body , } ,) ; } let fns = resolved_fns ; let mut resolved_impl_methods : Vec < FnDecl > = Vec :: new () ; let mut impls : BTreeMap < (String , String) , Vec < FnDecl > > = BTreeMap :: new () ; for item in & effective_nodule . items { if let Item :: Impl (id) = item { let methods = check_impl_methods (& types , & fns , & traits , & instances , imports , & lower_rules , effective_nodule . std_sys , id ,) ? ; let (for_ty , _) = resolve_ty (& id . trait_name , & types , & [] , & id . for_ty) ? ; let head = type_head (& for_ty) . expect (\"instance registration refused a non-concrete `for` type head\") ; impls . insert ((id . trait_name . clone () , head) , methods . clone ()) ; resolved_impl_methods . extend (methods) ; } } crate :: fuse :: check_fuse_laws (& types , & fns , & traits , & instances , & impls) ? ; for ld in lower_rules . values () { check_lower_rule_rhs_type (ld , & types , & fns , & traits , & instances , imports , & lower_rules) ? ; } check_effect_coverage (& fns , & regs . fns , & traits , effective_nodule) ? ; crate :: grade :: check_guarantees (& fns , & regs . fns , & resolved_impl_methods) ? ; let totality = crate :: totality :: classify_all (& fns) . map_err (| e | CheckError :: new (& qualify (& nodule . path , \"<totality>\") , e . to_string ())) ? ; if matured_scope { for fd in regs . fns . values () { if ! fd . thaw && totality [& fd . sig . name] != crate :: totality :: Totality :: Total { return Err (CheckError :: new (& fd . sig . name , format ! (\"`{}` is in a matured scope and must be total (RFC-0007 §4.5 / \\\n                         RFC-0017 §4.2) — mark it `thaw fn` to exempt it, or make it total\" , fd . sig . name) ,)) ; } } } Ok (Env { types , fns , totality , traits , instances , impls , lower_rules , derived_provenance , via_provenance , }) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_nodule_with"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3388,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **Structural** validation of a `lower Name[params] = <rhs>` declaration (DN-54 §3/§4.6 /\"] # [doc = \" M-812-cont) — the part that does **not** need the full typing context. Run in the registration\"] # [doc = \" loop *before* acyclicity (§4.2) and the §4.1 RHS type-check (which run over the whole set).\"] # [doc = \"\"] # [doc = \" **Guarantee: `Declared`** (structural checks, not theorems). Checks (all never-silent, G2):\"] # [doc = \" - **Uniqueness**: the rule `name` must not already be registered.\"] # [doc = \" - **Parameter uniqueness**: duplicate param names in `[…]` are refused.\"] # [doc = \" - **§4.6 purity**: the RHS contains no `wild { … }` block — a generative lowering is a *pure*\"] # [doc = \"   compile-time mechanism (the FFI gate is level-independent — DN-38 §3), refused structurally so\"] # [doc = \"   the refusal holds even in an `@std-sys` nodule (where the ordinary `wild` gate would accept it).\"] fn check_lower_decl_structural (ld : & LowerDecl , lower_rules : & BTreeMap < String , LowerDecl > ,) -> Result < () , CheckError > { if lower_rules . contains_key (& ld . name) { return Err (CheckError :: new (& ld . name , format ! (\"duplicate `lower` rule `{}` — a generative-lowering rule name must be unique \\\n                 within a nodule (DN-54 §4.2 / M-812; never silent, G2)\" , ld . name) ,)) ; } if let Some (dup) = first_duplicate (& ld . params) { return Err (CheckError :: new (& ld . name , format ! (\"duplicate type parameter `{dup}` in `lower {}[…]` (DN-54 §3 / M-812; \\\n                 every param name must be distinct)\" , ld . name) ,)) ; } let has_wild = match & ld . rhs { crate :: ast :: LowerRhs :: Expr (e) => { rhs_contains_wild (e) . map_err (| err | CheckError :: new (& ld . name , err . to_string ())) ? } crate :: ast :: LowerRhs :: Impl (id) => { let mut found = false ; for m in & id . methods { if rhs_contains_wild (& m . body) . map_err (| err | CheckError :: new (& ld . name , err . to_string ())) ? { found = true ; break ; } } found } } ; if has_wild { return Err (CheckError :: new (& ld . name , format ! (\"`lower {}`'s RHS contains a `wild {{ … }}` block — a generative-lowering rule is a \\\n                 pure compile-time mechanism and may not perform host/FFI calls (DN-54 §4.6 / \\\n                 §3.3; the `wild` gate is level-independent — DN-38 §3; never silent, G2)\" , ld . name) ,)) ; } Ok (()) }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "check_lower_decl_structural"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3460,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **§4.1 IL-grammar RHS type-check** of a registered `lower` rule (DN-54 §4.1 / M-812-cont),\"] # [doc = \" **shared** (M-1054 Stage 1b) with [`Cx::check_sugar_call`]'s call-site accept path — see\"] # [doc = \" [`infer_expr_rule_rhs_type`], the one `Cx::infer` pass both now run. The RHS must type-check as\"] # [doc = \" an L1 expression with the rule's type parameters in scope as abstract type-variables (the rule is\"] # [doc = \" parametric over the type it is derived for) **and**, since M-1054 Stage 1b, its declared **value**\"] # [doc = \" parameters ([`LowerDecl::value_params`]) bound to their declared types — see\"] # [doc = \" [`infer_expr_rule_rhs_type`]'s own doc comment for why. An ill-typed / ill-formed RHS is an\"] # [doc = \" explicit refusal at definition time — so no `derive` site (nor, since Stage 1b, a value-parametric\"] # [doc = \" call site) can invoke a rule that would produce broken L0. Run over the whole rule set *after*\"] # [doc = \" §4.2 acyclicity (so a cyclic set reports the cycle, not an \\\"unknown name\\\" for the cycle's\"] # [doc = \" rule-reference edge).\"] # [doc = \"\"] # [doc = \" **Guarantee: `Declared`** (a structural type/grammar check, not a theorem — VR-5) **layered with\"] # [doc = \" `Empirical`** (M-919; DN-71 Model S §4.2) for any `Substrate`-typed binding the RHS introduces —\"] # [doc = \" see [`infer_expr_rule_rhs_type`]'s affine-tracker note. No expected type is pinned (a rule may\"] # [doc = \" lower to any well-typed term) — pure inference. The `@std-sys` gate is held **closed**: a `lower`\"] # [doc = \" rule is never an FFI escape (the §4.6 structural check already refused any direct `wild`; this is\"] # [doc = \" defense in depth).\"] fn check_lower_rule_rhs_type (ld : & LowerDecl , types : & BTreeMap < String , DataInfo > , fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , instances : & BTreeMap < (String , String) , InstanceInfo > , imports : & NoduleImports , lower_rules : & BTreeMap < String , LowerDecl > ,) -> Result < () , CheckError > { if let crate :: ast :: LowerRhs :: Impl (id) = & ld . rhs { let Some (tr) = traits . get (& id . trait_name) else { return Err (CheckError :: new (& ld . name , format ! (\"`lower {}`'s item RHS is `impl {} for …`, but trait `{}` is not declared in \\\n                     scope (DN-54 §10.3 Model A; a derive template's trait must be visible — never \\\n                     silent, G2)\" , ld . name , id . trait_name , id . trait_name) ,)) ; } ; check_impl_method_set (tr , id) . map_err (| e | { CheckError :: new (& ld . name , format ! (\"`lower {}`'s item RHS `impl {} for …`: {}\" , ld . name , id . trait_name , e . message) ,) }) ? ; return Ok (()) ; } let rhs = ld . expr_rhs () . expect (\"item-shaped rules returned above; this is the expression arm\") ; infer_expr_rule_rhs_type (ld , rhs , types , fns , traits , instances , imports , lower_rules) ? ; Ok (()) }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "check_lower_rule_rhs_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3527,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" **M-1054 Stage 1b** — the one `Cx::infer` pass shared by a `lower` rule's **definition-time**\"] # [doc = \" validation ([`check_lower_rule_rhs_type`]) and a value-parametric call site's **accept path**\"] # [doc = \" ([`Cx::check_sugar_call`]). Binds `ld`'s declared **value** parameters\"] # [doc = \" ([`LowerDecl::value_params`]) into the initial scope at their declared types (a nullary rule's\"] # [doc = \" empty `value_params` makes this identical to the pre-Stage-1b empty-scope walk — no behavioral\"] # [doc = \" change for that case) and infers the RHS's type once.\"] # [doc = \"\"] # [doc = \" **Why the call site reuses this instead of re-checking per-argument types (Option B, DN-116):**\"] # [doc = \" the checker has **no coercion** (`want == got`, exact match — see the ordinary user-fn accept\"] # [doc = \" branch's own `if want != got` gate) so a monomorphic value-parametric rule's RHS result type is\"] # [doc = \" **fixed at definition** — inferring it once, with the params bound to their *declared* types\"] # [doc = \" (never the calling site's own argument nodes), gives the one answer every call site shares.\"] # [doc = \" [`Cx::check_sugar_call`] still separately re-checks each *argument's* type against the declared\"] # [doc = \" parameter type at every call site (so a bare-decimal argument still takes its parameter's width,\"] # [doc = \" and an argument/parameter mismatch is still reported there) — this function only fixes the\"] # [doc = \" **result**.\"] # [doc = \"\"] # [doc = \" **Affine tracking is ACTIVE here, seeded from the value-param scope (M-919 fix; DN-71 Model S\"] # [doc = \" §4.2), mirroring [`check_fn_body`]'s own `Tracker::seeded(&scope)` call.** Before M-1054 Stage\"] # [doc = \" 1b a `lower` rule had no value parameters in v0, so the tracker seeded empty; now a declared\"] # [doc = \" value parameter is tracked exactly like an ordinary function parameter (a call-site argument\"] # [doc = \" pass counts as the caller's move-in — M-903). The RHS can **also** still legally introduce a\"] # [doc = \" *local* `Substrate` binding mid-body (DN-54 §3.3 permits calling other already-checked top-level\"] # [doc = \" `fn`s, and nothing in v0 restricts those callees to non-affine return types) — an **inert**\"] # [doc = \" tracker would silently exempt every such binding from the double-consume check ordinary function\"] # [doc = \" bodies get (a real, previously-verified coverage gap: a probe reproduction confirmed a same-rule\"] # [doc = \" double-consume via a helper-fn-acquired `Substrate` type-checked under an inert tracker). Seeding\"] # [doc = \" from the (now possibly non-empty) value-param scope closes both: every `let`/lambda/match binder\"] # [doc = \" pushed during the RHS walk — value param **or** RHS-local — is tracked exactly as it would be\"] # [doc = \" inside an ordinary function body, so a double-consume of either is refused here too, never\"] # [doc = \" silently. **This is the M-919 *upper-bound* (duplication) check only, over the RHS-local\"] # [doc = \" bindings and the *unsubstituted* value-param references.** It does not by itself make a *call\"] # [doc = \" site* with a `Substrate`-**containing** value parameter safe to accept — a value-parametric\"] # [doc = \" expansion *splices* the caller's argument node into the RHS at every occurrence of its param,\"] # [doc = \" which is a different hazard than an ordinary function call (each param used only once here,\"] # [doc = \" against its own *declared* type, not the call site's actual argument content). Closing that is\"] # [doc = \" [`Cx::check_sugar_call`]'s own Stage-3 (OQ-H4/DN-117) job: the real substituted-`Expr` affine\"] # [doc = \" walk (triggered by [`Cx::ty_structurally_contains_substrate`] or a per-argument tracker-touch\"] # [doc = \" signal — see that function's doc comment).\"] # [doc = \"\"] # [doc = \" **Stage 3's RHS-local-affine-binding case needs no separate structural gate (DN-117 §3.3,\"] # [doc = \" 2026-07-11 — superseding the earlier two-part-gate design).** This function's own active\"] # [doc = \" tracker, seeded from the value-param scope and walking the *unsubstituted* `rhs`, already and\"] # [doc = \" unconditionally catches a duplicated RHS-local affine acquisition (a `let`-bound helper-fn call\"] # [doc = \" returning `Substrate`, or a bare `consume`, referenced more than once) — that hazard does not\"] # [doc = \" depend on value-parameter substitution at all, so no additional structural pre-check\"] # [doc = \" (`Cx::rhs_first_affine_binding`, since removed) is needed; a **dropped** RHS-local affine binding\"] # [doc = \" is correctly *accepted* here too (DN-117 §4.3 — the static pass enforces only the use-at-most-once\"] # [doc = \" upper bound), where the earlier structural gate wrongly refused it wholesale.\"] # [allow (clippy :: too_many_arguments)] fn infer_expr_rule_rhs_type (ld : & LowerDecl , rhs : & Expr , types : & BTreeMap < String , DataInfo > , fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , instances : & BTreeMap < (String , String) , InstanceInfo > , imports : & NoduleImports , lower_rules : & BTreeMap < String , LowerDecl > ,) -> Result < Ty , CheckError > { let mut scope : Vec < (String , Ty) > = Vec :: with_capacity (ld . value_params . len ()) ; for p in & ld . value_params { let (pty , _) = resolve_ty (& ld . name , types , & ld . params , & p . ty) ? ; scope . push ((p . name . clone () , pty)) ; } let cx = Cx { site : & ld . name , types , fns , traits , instances , imports , lower_rules , tyvars : & ld . params , bounds : & [] , std_sys : false , depth : Cell :: new (0) , affine : Tracker :: seeded (& scope) , # [cfg (test)] stage3_sabotage_skip_substitution : false , } ; cx . infer (& mut scope , rhs) . map_err (| e | { CheckError :: new (& ld . name , format ! (\"`lower {}`'s RHS fails the IL-grammar / type check (DN-54 §4.1): {}\" , ld . name , e . message) ,) }) }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "infer_expr_rule_rhs_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3619,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Does the expression tree contain a `wild { … }` block anywhere? (DN-54 §4.6 — a `lower` rule's\"] # [doc = \" RHS must be pure.) Returns `true` at the first `wild` found. Uses the shared stateless\"] # [doc = \" [`crate::totality::walk_expr`] traversal (DRY — same tree walk every pass uses), which carries\"] # [doc = \" its own explicit recursion-depth budget (M-674) — a pathologically-nested RHS refuses cleanly\"] # [doc = \" (propagated to the caller) rather than overflowing the host stack.\"] fn rhs_contains_wild (rhs : & Expr) -> Result < bool , crate :: totality :: WalkDepthExceeded > { let mut found = false ; crate :: totality :: walk_expr (rhs , & mut | x | { if matches ! (x , Expr :: Wild (_)) { found = true ; } }) ? ; Ok (found) }",
      "reason": "qualified type path `crate :: totality :: WalkDepthExceeded` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "rhs_contains_wild"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3634,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Collect every identifier a surface [`Pattern`] binds, appending to `out` (M-1054 Stage 1b's\"] # [doc = \" Stage-2 free-identifier gate, [`Cx::rhs_first_free_id`]). Mirrors the constructor-arm binding\"] # [doc = \" shape the real decision-tree compiler (`crate::decision`) and elaborator already use — every\"] # [doc = \" `Pattern::Ident`/`Ctor`/`Tuple` field becomes a binder in the arm body's scope; `Wildcard`/`Lit`\"] # [doc = \" bind nothing. `Or`'s alternatives are collected from **every** branch (a union, not just the\"] # [doc = \" first) — conservative rather than relying on the \\\"every alternative binds the same identifier\"] # [doc = \" set\\\" invariant holding at this point in the pipeline (that invariant is itself validated later,\"] # [doc = \" downstream, by `check_match`/`normalize_pattern` — see that function's own doc comment — not yet\"] # [doc = \" by the time this Stage 1b gate runs over the raw surface tree).\"] fn collect_pattern_binders (pat : & Pattern , out : & mut Vec < String >) { match pat { Pattern :: Wildcard | Pattern :: Lit (_) => { } Pattern :: Ident (name) => out . push (name . clone ()) , Pattern :: Ctor (_ , fields) | Pattern :: Tuple (fields) => { for f in fields { collect_pattern_binders (f , out) ; } } Pattern :: Or (alts) => { for a in alts { collect_pattern_binders (a , out) ; } } } }",
      "reason": "mutating method's tail expression is neither a threaded-binding field/whole-value re-assignment nor (with the method's original return type already `()`/self-chain, and either multiple threaded bindings or one already assigned) an extra value — value-threading only supports a flat sequence of `self.<field> = ..`/`*<param> = ..` re-assignments plus, when the source return type is non-unit, one trailing value expression (DN-125 §5, conservative scope per §6.1)",
      "item_name": "collect_pattern_binders"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3673,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Substitute the rule's type parameter `param` with the concrete type `concrete` throughout a\"] # [doc = \" [`TypeRef`] (DN-54 §10 Model A instantiation; M-973). A bare `Named(param, [])` occurrence becomes\"] # [doc = \" `concrete` (keeping the occurrence's own guarantee index if written — `T @ Exact` ↦ `C @ Exact`);\"] # [doc = \" every structural form recurses; reprs / `Substrate` / `Bytes` / `Float` / VSA / dense carry no\"] # [doc = \" nested type-name and are cloned verbatim. Total, allocation-bounded by the input's size.\"] pub (crate) fn subst_type_param_in_typeref (tr : & TypeRef , param : & str , concrete : & TypeRef ,) -> TypeRef { match & tr . base { BaseType :: Named (name , args) if name == param && args . is_empty () => TypeRef { base : concrete . base . clone () , guarantee : tr . guarantee . or (concrete . guarantee) , } , BaseType :: Named (name , args) => TypeRef { base : BaseType :: Named (name . clone () , args . iter () . map (| a | subst_type_param_in_typeref (a , param , concrete)) . collect () ,) , guarantee : tr . guarantee , } , BaseType :: Seq { elem , len } => TypeRef { base : BaseType :: Seq { elem : Box :: new (subst_type_param_in_typeref (elem , param , concrete)) , len : * len , } , guarantee : tr . guarantee , } , BaseType :: Fn (a , b) => TypeRef { base : BaseType :: Fn (Box :: new (subst_type_param_in_typeref (a , param , concrete)) , Box :: new (subst_type_param_in_typeref (b , param , concrete)) ,) , guarantee : tr . guarantee , } , BaseType :: Tuple (elems) => TypeRef { base : BaseType :: Tuple (elems . iter () . map (| e | subst_type_param_in_typeref (e , param , concrete)) . collect () ,) , guarantee : tr . guarantee , } , BaseType :: Binary (_) | BaseType :: Ternary (_) | BaseType :: Dense (_ , _) | BaseType :: Vsa { .. } | BaseType :: Substrate (_) | BaseType :: Bytes | BaseType :: Float | BaseType :: Ambient (_) => tr . clone () , } }",
      "reason": "field access on a non-`self` base — the transpiler tracks no local types, so the projection cannot be resolved to a constructor position (only `self.<field>` desugars to a `match`)",
      "item_name": "subst_type_param_in_typeref"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3736,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Substitute `param` ↦ `concrete` in a function **signature** (value-parameter types + return type;\"] # [doc = \" M-973). The signature's own type parameters (`sig.params`) and effect annotations are cloned\"] # [doc = \" verbatim — a method's own generics are a separate, shadowing scope, and effects carry no type.\"] fn subst_type_param_in_sig (sig : & FnSig , param : & str , concrete : & TypeRef) -> FnSig { FnSig { name : sig . name . clone () , params : sig . params . clone () , value_params : sig . value_params . iter () . map (| p | Param { name : p . name . clone () , ty : subst_type_param_in_typeref (& p . ty , param , concrete) , }) . collect () , ret : subst_type_param_in_typeref (& sig . ret , param , concrete) , effects : sig . effects . clone () , effect_budgets : sig . effect_budgets . clone () , } }",
      "reason": "struct literal `FnSig { .. }` — not an in-file single-ctor struct that emits (no constructor to build)",
      "item_name": "subst_type_param_in_sig"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3757,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Substitute `param` ↦ `concrete` throughout an **expression** (a derived method body; M-973). The\"] # [doc = \" only positions an [`Expr`] carries a [`TypeRef`] are `Let`'s ascription, `Swap`'s target,\"] # [doc = \" `Lambda`'s parameter types, and `Ascribe`; everything else recurses structurally. Total; a\"] # [doc = \" [`Literal::List`]'s element expressions recurse, other literals are cloned.\"] fn subst_type_param_in_expr (e : & Expr , param : & str , concrete : & TypeRef) -> Expr { let boxed = | x : & Expr | Box :: new (subst_type_param_in_expr (x , param , concrete)) ; match e { Expr :: Let { name , ty , bound , body , } => Expr :: Let { name : name . clone () , ty : ty . as_ref () . map (| t | subst_type_param_in_typeref (t , param , concrete)) , bound : boxed (bound) , body : boxed (body) , } , Expr :: If { cond , conseq , alt } => Expr :: If { cond : boxed (cond) , conseq : boxed (conseq) , alt : boxed (alt) , } , Expr :: Match { scrutinee , arms } => Expr :: Match { scrutinee : boxed (scrutinee) , arms : arms . iter () . map (| a | Arm { pattern : a . pattern . clone () , body : subst_type_param_in_expr (& a . body , param , concrete) , }) . collect () , } , Expr :: For { x , xs , acc , init , body , } => Expr :: For { x : x . clone () , xs : boxed (xs) , acc : acc . clone () , init : boxed (init) , body : boxed (body) , } , Expr :: Swap { value , target , policy , } => Expr :: Swap { value : boxed (value) , target : subst_type_param_in_typeref (target , param , concrete) , policy : policy . clone () , } , Expr :: WithParadigm { paradigm , body } => Expr :: WithParadigm { paradigm : * paradigm , body : boxed (body) , } , Expr :: Wild (b) => Expr :: Wild (boxed (b)) , Expr :: Spore (b) => Expr :: Spore (boxed (b)) , Expr :: Consume (b) => Expr :: Consume (boxed (b)) , Expr :: Wrapping (b) => Expr :: Wrapping (boxed (b)) , Expr :: Try (b) => Expr :: Try (boxed (b)) , Expr :: Colony (hyphae) => Expr :: Colony (hyphae . iter () . map (| h | Hypha { forage : h . forage . as_ref () . map (| f | boxed (f)) , body : subst_type_param_in_expr (& h . body , param , concrete) , }) . collect () ,) , Expr :: Lambda { params , body } => Expr :: Lambda { params : params . iter () . map (| p | Param { name : p . name . clone () , ty : subst_type_param_in_typeref (& p . ty , param , concrete) , }) . collect () , body : boxed (body) , } , Expr :: App { head , args } => Expr :: App { head : boxed (head) , args : args . iter () . map (| a | subst_type_param_in_expr (a , param , concrete)) . collect () , } , Expr :: Fuse { left , right } => Expr :: Fuse { left : boxed (left) , right : boxed (right) , } , Expr :: Reclaim { policy , body } => Expr :: Reclaim { policy : boxed (policy) , body : boxed (body) , } , Expr :: Ascribe (inner , ty) => Expr :: Ascribe (boxed (inner) , subst_type_param_in_typeref (ty , param , concrete) ,) , Expr :: TupleLit (elems) => Expr :: TupleLit (elems . iter () . map (| x | subst_type_param_in_expr (x , param , concrete)) . collect () ,) , Expr :: Lit (Literal :: List (elems)) => Expr :: Lit (Literal :: List (elems . iter () . map (| x | subst_type_param_in_expr (x , param , concrete)) . collect () ,)) , Expr :: Path (_) | Expr :: Lit (_) => e . clone () , } }",
      "reason": "qualified/associated-function call `Box :: new` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin)",
      "item_name": "subst_type_param_in_expr"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3878,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Instantiate an item-shaped `lower`-rule template at a concrete target type — the Model-A\"] # [doc = \" substitution `param ↦ concrete` over an entire [`ImplDecl`] (DN-54 §10.3; M-973). Substitutes the\"] # [doc = \" trait arguments, the `for` type, and each method's signature + body. **Hygiene (never-silent\"] # [doc = \" correctness):** a method that shadows the rule parameter with its **own** type parameter of the\"] # [doc = \" same name is left untouched (the inner binding wins; the rule param does not leak past the shadow).\"] fn subst_type_param_in_impl (id : & ImplDecl , param : & str , concrete : & TypeRef) -> ImplDecl { ImplDecl { trait_name : id . trait_name . clone () , trait_args : id . trait_args . iter () . map (| a | subst_type_param_in_typeref (a , param , concrete)) . collect () , for_ty : subst_type_param_in_typeref (& id . for_ty , param , concrete) , methods : id . methods . iter () . map (| m | { if m . sig . param_names () . iter () . any (| n | n == param) { m . clone () } else { FnDecl { vis : m . vis , thaw : m . thaw , tier : m . tier , sig : subst_type_param_in_sig (& m . sig , param , concrete) , body : subst_type_param_in_expr (& m . body , param , concrete) , } } }) . collect () , } }",
      "reason": "struct literal `ImplDecl { .. }` — not an in-file single-ctor struct that emits (no constructor to build)",
      "item_name": "subst_type_param_in_impl"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3914,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Validate a `derive Name for T` use-site application (DN-54 §4 / M-812 / DN-38 §8.1).\"] # [doc = \"\"] # [doc = \" **Guarantee: `Declared`** (name-resolution + target-type + arity checks; the RHS instantiation\"] # [doc = \" + elaboration run at elaboration time, `Empirical` — VR-5).\"] # [doc = \"\"] # [doc = \" Checks (all never-silent, G2):\"] # [doc = \" - **Rule resolution**: `Name` must resolve to a registered `lower` rule in `lower_rules`\"] # [doc = \"   (earlier declarations in the same Pass 3e loop are already registered).\"] # [doc = \" - **Target type well-formedness**: `for_ty` must resolve to a real type (DN-54 §4).\"] fn check_derive_application (dd : & DeriveDecl , lower_rules : & BTreeMap < String , LowerDecl > , types : & BTreeMap < String , DataInfo > ,) -> Result < () , CheckError > { if ! lower_rules . contains_key (& dd . name) { return Err (CheckError :: new (& dd . name , format ! (\"`derive {}` references an unknown generative-lowering rule — declare it first \\\n                 with `lower {} […] = <rhs>` (DN-54 §4 / M-812; rule must be visible before use, \\\n                 G2). Did you spell the rule name correctly?\" , dd . name , dd . name) ,)) ; } resolve_ty (& dd . name , types , & [] , & dd . for_ty) ? ; Ok (()) }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "check_derive_application"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 3946,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **§4.2 cross-rule acyclicity** (DN-54 §4.2 / M-812-cont). Reject a `lower` rule set in which the\"] # [doc = \" rules' RHSs reference one another in a cycle (mutual recursion among rules), which would diverge\"] # [doc = \" the elaboration pipeline (the lowering passes must stay a DAG — RFC-0006/DN-38 §2). A rule's\"] # [doc = \" edges are the names it references in its RHS **that are themselves rule names** (a conservative\"] # [doc = \" superset over the single-segment paths in the RHS — the same edge set [`crate::elab`] would\"] # [doc = \" expand). A strongly-connected component of size > 1, or a direct self-reference, is a cycle and\"] # [doc = \" is refused with a never-silent diagnostic naming a rule on the cycle (G2). Guarantee: `Declared`\"] # [doc = \" (a structural acyclicity check, not a theorem).\"] fn check_lower_rule_acyclicity (lower_rules : & BTreeMap < String , LowerDecl > , types : & BTreeMap < String , DataInfo > , fns : & BTreeMap < String , FnDecl > ,) -> Result < () , CheckError > { let is_ctor = | name : & str | -> bool { types . values () . any (| d | d . ctors . iter () . any (| c | c . name == name)) } ; let is_rule_edge = | name : & str | -> bool { lower_rules . contains_key (name) && ! fns . contains_key (name) && ! is_ctor (name) } ; let edges : BTreeMap < & str , BTreeSet < String > > = lower_rules . iter () . map (| (name , ld) | { let mut refs = BTreeSet :: new () ; let mut collect = | x : & Expr | { if let Expr :: Path (p) = x { if p . 0 . len () == 1 && is_rule_edge (& p . 0 [0]) { refs . insert (p . 0 [0] . clone ()) ; } } } ; match & ld . rhs { crate :: ast :: LowerRhs :: Expr (e) => crate :: totality :: walk_expr (e , & mut collect) . map_err (| err | CheckError :: new (name , err . to_string ())) ? , crate :: ast :: LowerRhs :: Impl (id) => { for m in & id . methods { crate :: totality :: walk_expr (& m . body , & mut collect) . map_err (| err | CheckError :: new (name , err . to_string ())) ? ; } } } Ok :: < _ , CheckError > ((name . as_str () , refs)) }) . collect :: < Result < _ , _ > > () ? ; for (name , refs) in & edges { if refs . contains (* name) { return Err (CheckError :: new (name , format ! (\"`lower {name}` references itself — a `lower` rule may not be (mutually) \\\n                     recursive (DN-54 §4.2: the lowering-rule graph must be acyclic so `derive` \\\n                     terminates; use `Fix` for user-level recursion *inside* the RHS instead). \\\n                     Never silent (G2).\") ,)) ; } } # [derive (Clone , Copy , PartialEq)] enum Color { White , Grey , Black , } let mut color : BTreeMap < & str , Color > = edges . keys () . map (| k | (* k , Color :: White)) . collect () ; for & root in edges . keys () { if color [root] != Color :: White { continue ; } let mut stack : Vec < (& str , usize) > = vec ! [(root , 0)] ; color . insert (root , Color :: Grey) ; while let Some (& (node , idx)) = stack . last () { let succs : Vec < & str > = edges [node] . iter () . map (String :: as_str) . collect () ; if idx < succs . len () { stack . last_mut () . expect (\"non-empty\") . 1 += 1 ; let next = succs [idx] ; match color [next] { Color :: White => { color . insert (next , Color :: Grey) ; stack . push ((next , 0)) ; } Color :: Grey => { return Err (CheckError :: new (next , format ! (\"`lower` rules form a cycle through `{next}` — mutually-recursive \\\n                                 lowering rules are refused (DN-54 §4.2: the lowering-rule graph \\\n                                 must be acyclic so `derive` terminates). Break the cycle, or use \\\n                                 `Fix` for user-level recursion *inside* a rule's RHS. Never \\\n                                 silent (G2).\") ,)) ; } Color :: Black => { } } } else { color . insert (node , Color :: Black) ; stack . pop () ; } } } Ok (()) }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "check_lower_rule_acyclicity"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4072,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" **Effect-coverage pass** (RFC-0014 §3.4/§4.5 I3; M-660 — guarantee: `Declared`). For every\"] # [doc = \" top-level function **and every `impl`-method body**, the effects it **performs** must be a subset\"] # [doc = \" of the effects it **declares**. The *performed* set is the union, over every call in the body, of\"] # [doc = \" the declared effects of the callee — a **known top-level fn** `g` (in `fns`) OR an unqualified\"] # [doc = \" **trait-method** call (the declaring trait method's effects, from `traits`; an ambiguous method\"] # [doc = \" name was already refused at the call site — M-659, so the name maps to one trait in any program\"] # [doc = \" reaching here). This is the v0 \\\"manual-declare + compositional-check\\\" line (RFC-0014 §8): the\"] # [doc = \" checker *composes* declared effects up the call graph as a **check**, never *infers* an undeclared\"] # [doc = \" one. (In M-660 the only effect sources are these declarations: `wild`-sourced effects arrive with\"] # [doc = \" M-661, and the runtime budget ledger is the separate M-353 concern.)\"] # [doc = \"\"] # [doc = \" **Under-declaration** — performing an effect not declared — is an explicit [`CheckError`] naming\"] # [doc = \" both the missing effect and the callee that introduces it (G2/RFC-0014 I3 \\\"no undeclared\"] # [doc = \" effects\\\"). **Over-declaration** is allowed: declaring an effect the body never performs is a\"] # [doc = \" *contract*, not an error (RFC-0014 I5 default-tightly-scoped). Checking `impl`-method bodies too\"] # [doc = \" (their declared set == the trait method's, by conformance) is what keeps an effect from being\"] # [doc = \" **hidden** from a caller — the core RFC-0014 invariant that \\\"an effect a function performs is\"] # [doc = \" visible in its signature\\\".\"] fn check_effect_coverage (fns : & BTreeMap < String , FnDecl > , own_fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , nodule : & Nodule ,) -> Result < () , CheckError > { for fd in own_fns . values () { check_body_effect_coverage (fns , traits , & fd . sig . name , & fd . sig . effects , & fd . body) ? ; } for item in & nodule . items { if let Item :: Impl (id) = item { for m in & id . methods { check_body_effect_coverage (fns , traits , & m . sig . name , & m . sig . effects , & m . body) ? ; } } } Ok (()) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "check_effect_coverage"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4114,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" What introduced a performed effect, for the coverage diagnostic. `Ord` so the `(effect, source)`\"] # [doc = \" set is deterministic (a stable first-miss). A **`Call`** is a top-level fn or trait-method call\"] # [doc = \" (M-660); **`Wild`** is the `wild` FFI floor (M-661 — `wild` performs `ffi`).\"] # [derive (Debug , Clone , PartialEq , Eq , PartialOrd , Ord)] enum EffectSource { # [doc = \" A call to a named callee (top-level fn or unqualified trait method) — M-660.\"] Call (String) , # [doc = \" A `wild` block — the FFI floor; it performs the `ffi` effect (M-661).\"] Wild , }",
      "reason": "dropped non-doc attribute(s) on enum `EffectSource`: # [derive (Debug , Clone , PartialEq , Eq , PartialOrd , Ord)]",
      "item_name": "EffectSource"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4125,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" One body's effect coverage: `declared ⊇ performed`, where *performed* is the union of each\"] # [doc = \" callee's declared effects — a known top-level fn (`fns`), else an unqualified trait method\"] # [doc = \" (`traits`, by name) — **plus the `ffi` effect contributed by any `wild` block** (M-661). Owned-\"] # [doc = \" `String` sets keep lifetimes simple; the structural walk is shared with totality (one traversal,\"] # [doc = \" no bespoke depth-guarded recursion). Deterministic order ⇒ a stable first-miss diagnostic. The\"] # [doc = \" M-353 runtime budget ledger is a separate concern (not consulted here).\"] fn check_body_effect_coverage (fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , name : & str , declared_effs : & [String] , body : & Expr ,) -> Result < () , CheckError > { let declared : std :: collections :: BTreeSet < String > = declared_effs . iter () . cloned () . collect () ; let mut performed : std :: collections :: BTreeSet < (String , EffectSource) > = std :: collections :: BTreeSet :: new () ; crate :: totality :: walk_expr (body , & mut | x | { match x { Expr :: App { head , .. } => { if let Expr :: Path (p) = head . as_ref () { if p . 0 . len () == 1 { let callee = & p . 0 [0] ; if let Some (g) = fns . get (callee) { for eff in & g . sig . effects { performed . insert ((eff . clone () , EffectSource :: Call (callee . clone ()))) ; } } else { for tr in traits . values () { for s in & tr . sigs { if & s . name == callee { for eff in & s . effects { performed . insert ((eff . clone () , EffectSource :: Call (callee . clone ()) ,)) ; } } } } } } } } Expr :: Wild (_) => { performed . insert ((\"ffi\" . to_owned () , EffectSource :: Wild)) ; } _ => { } } }) . map_err (| e | CheckError :: new (name , e . to_string ())) ? ; for (eff , source) in & performed { if ! declared . contains (eff) { let via = match source { EffectSource :: Call (callee) => format ! (\"via calling `{callee}`\") , EffectSource :: Wild => \"via a `wild` block (the FFI floor — M-661)\" . to_owned () , } ; return Err (CheckError :: new (name , format ! (\"`{name}` performs effect `{eff}` ({via}) but does not declare it — add it to \\\n                     the `!{{…}}` effect annotation (RFC-0014 §4.5 I3: no undeclared effects; never \\\n                     silent — G2)\") ,)) ; } } Ok (()) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "check_body_effect_coverage"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4205,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Render an effect set for a diagnostic in the surface `!{a, b}` form (`!{}` for the empty/pure\"] # [doc = \" set). Used only for never-silent error messages (M-660); preserves the written source order.\"] fn render_effects (effects : & [String]) -> String { format ! (\"!{{{}}}\" , effects . join (\", \")) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "render_effects"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4211,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "pub (crate) fn resolve_ctors (types : & BTreeMap < String , DataInfo > , td : & TypeDecl ,) -> Result < Vec < CtorInfo > , CheckError > { let mut ctors = Vec :: new () ; for c in & td . ctors { if ctors . iter () . any (| x : & CtorInfo | x . name == c . name) { return Err (CheckError :: new (& td . name , format ! (\"duplicate constructor `{}`\" , c . name) ,)) ; } if c . sealed && td . vis == crate :: ast :: Vis :: Private { return Err (CheckError :: new (& td . name , format ! (\"constructor `{}` is marked `priv`, but type `{}` is not `pub` — the seal is \\\n                     redundant (a nodule-private type's constructors are already unreachable \\\n                     cross-nodule). Make the type `pub` to export its name while withholding this \\\n                     constructor, or drop the `priv` marker (DN-104 §4; never-silent, G2)\" , c . name , td . name) ,)) ; } let mut fields = Vec :: new () ; for f in & c . fields { let (ty , _) = resolve_ty (& td . name , types , & td . params , f) ? ; fields . push (ty) ; } ctors . push (CtorInfo { name : c . name . clone () , fields , }) ; } Ok (ctors) }",
      "reason": "qualified/associated-function call `Vec :: new` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin)",
      "item_name": "resolve_ctors"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4254,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The first value that appears more than once in `xs` (left to right), if any. Used to reject\"] # [doc = \" duplicate type-parameter names — an explicit error, never a silently-shadowed binding (G2).\"] pub (crate) fn first_duplicate (xs : & [String]) -> Option < & String > { let mut seen = std :: collections :: BTreeSet :: new () ; xs . iter () . find (| x | ! seen . insert ((* x) . clone ())) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "first_duplicate"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4261,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" **Trait pass** (RFC-0019 §4.2; LR-2 — guarantee: `Declared`, a structural registry check, not a\"] # [doc = \" theorem). Register each `trait Tr<params> { fn … }` as a [`TraitInfo`]: reject a duplicate trait\"] # [doc = \" name; reject duplicate type-parameter names and duplicate method names; and check that **every**\"] # [doc = \" method signature *resolves* with the trait's params in scope as abstract type-variables (its\"] # [doc = \" value-param types and return type via [`resolve_ty`]). A trait is a registry entry, never a\"] # [doc = \" kernel node (KC-3). Every refusal is an explicit [`CheckError`] (G2).\"] pub (crate) fn register_traits (types : & BTreeMap < String , DataInfo > , nodule : & Nodule ,) -> Result < BTreeMap < String , TraitInfo > , CheckError > { let mut traits : BTreeMap < String , TraitInfo > = BTreeMap :: new () ; for item in & nodule . items { let Item :: Trait (td) = item else { continue } ; let site = & td . name ; if let Some (dup) = first_duplicate (& td . params) { return Err (CheckError :: new (site , format ! (\"duplicate type parameter `{dup}` in trait `{}`\" , td . name) ,)) ; } if traits . contains_key (& td . name) { return Err (CheckError :: new (site , \"duplicate trait declaration\")) ; } let mut seen_methods : std :: collections :: BTreeSet < & str > = std :: collections :: BTreeSet :: new () ; for s in & td . sigs { if ! seen_methods . insert (s . name . as_str ()) { return Err (CheckError :: new (site , format ! (\"duplicate method `{}` in trait `{}`\" , s . name , td . name) ,)) ; } let mut tyvars = td . params . clone () ; tyvars . extend (s . param_names ()) ; check_sig_resolves (types , site , & tyvars , s) ? ; } traits . insert (td . name . clone () , TraitInfo { name : td . name . clone () , params : td . params . clone () , sigs : td . sigs . clone () , } ,) ; } for tr in traits . values () { for s in & tr . sigs { for tp in & s . params { for b in & tp . bounds { if ! traits . contains_key (& b . name) { return Err (CheckError :: new (& tr . name , format ! (\"trait `{}` method `{}`: unknown trait `{}` in the bound `{}: {}` \\\n                                 (RFC-0019 §4.1 — a method's type-parameter bound must name a \\\n                                 declared trait)\" , tr . name , s . name , b . name , tp . name , b . name) ,)) ; } } } } } Ok (traits) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "register_traits"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4336,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Confirm a [`FnSig`]'s value-parameter types and return type all resolve under `tyvars` (the\"] # [doc = \" abstract type-variables in scope). Shared by the trait pass (method requirements) and the\"] # [doc = \" bounded-fn body checker (signature validation). Does not check the body — only the signature.\"] fn check_sig_resolves (types : & BTreeMap < String , DataInfo > , site : & str , tyvars : & [String] , sig : & FnSig ,) -> Result < () , CheckError > { for p in & sig . value_params { resolve_ty (site , types , tyvars , & p . ty) ? ; } resolve_ty (site , types , tyvars , & sig . ret) ? ; Ok (()) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "check_sig_resolves"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4352,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" **MED closure (2026-07-11, adversarial-verification follow-up to the CRITICAL ctor-field fix\"] # [doc = \" above):** a **foreign** (cross-phylum-imported) trait's method signatures are surface\"] # [doc = \" [`TypeRef`]s, resolved **lazily** — at `impl`-check time, in [`check_impl_methods`]'s\"] # [doc = \" `resolve_ty(site, types, &tr.params, &rp.ty)` — against the IMPLEMENTING phylum's OWN type\"] # [doc = \" registry. They are never re-homed against the trait's own declaring phylum the way a ctor field\"] # [doc = \" now is (the CRITICAL fix in [`merge_phyla_exports`]) or a fn-sig always was\"] # [doc = \" (`Exports::resolved_fn_sigs`). A concrete named type mentioned in a foreign trait's signature\"] # [doc = \" that is NOT one of the trait's own generic params — e.g. `trait Trt[A] { fn get(x: A) => Bar; }`\"] # [doc = \" where `Bar` is some concrete type in the trait's *declaring* phylum, not a param — is therefore\"] # [doc = \" silently re-resolved against whatever bare name the CONSUMER's own registry happens to bind:\"] # [doc = \" exactly the M-1036/DN-112 bare-name collapse, one level up, for a **trait signature** rather\"] # [doc = \" than a ctor field.\"] # [doc = \"\"] # [doc = \" **Confirmed reachable in v1** by a scratch differential (`use dep::Trait; impl Trait for\"] # [doc = \" LocalType { .. }`, no cross-phylum `instances` merge required at all — the confusion is entirely\"] # [doc = \" local to [`check_impl_methods`]'s own re-resolution). This is narrower than what the CRITICAL\"] # [doc = \" fix's own follow-up note assumed (\\\"gated-unreachable because v1 does not merge instances\\\") —\"] # [doc = \" that assumption covered *querying* another phylum's registered instances, not *registering* a\"] # [doc = \" local instance of an *imported* trait, which needs no instance-merge at all.\"] # [doc = \"\"] # [doc = \" The real fix is DN-113/DN-122 territory: re-home a foreign trait's sigs against its own\"] # [doc = \" declaring phylum, the way [`merge_phyla_exports`] now does for ctor fields — out of scope here\"] # [doc = \" (needs the instances-merge machinery DN-122 tracks). Until it lands, this closes the *soundness*\"] # [doc = \" gap the narrow, conservative way [`lookup_data_home_checked`] does for CRITICAL #2: **refuse**\"] # [doc = \" rather than silently re-resolve. `!trait_local` (computed in [`register_instances`], just below)\"] # [doc = \" is exactly \\\"this trait is not declared anywhere in this phylum\\\" — the only way `traits.get`\"] # [doc = \" could have found it is a cross-phylum import (traits are never a prelude/ambient construct, so\"] # [doc = \" there is no other source for a phylum-non-local hit). A trait whose signature only ever\"] # [doc = \" references its own params (e.g. `Cmp[A] { fn cmp(a: A, b: A) => Binary{2}; }`) carries no\"] # [doc = \" concrete-type reference at all and is **unaffected** — only a signature that actually names a\"] # [doc = \" concrete type beyond its params is refused, so this does not regress the common \\\"impl a foreign\"] # [doc = \" trait for your own type\\\" pattern the orphan rule exists to allow.\"] fn foreign_trait_sig_names_a_concrete_type (tr : & TraitInfo) -> Option < & str > { let params : BTreeSet < & str > = tr . params . iter () . map (String :: as_str) . collect () ; tr . sigs . iter () . find_map (| sig | fn_sig_names_a_concrete_type (sig , & params)) }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "foreign_trait_sig_names_a_concrete_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4391,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" **HOLE B closure (M-1060 fix-cycle-3, 2026-07-11, adversarial-verification 3rd-cycle finding):**\"] # [doc = \" the fn-signature analogue of [`foreign_trait_sig_names_a_concrete_type`] — does a **foreign**\"] # [doc = \" (cross-phylum-imported) fn's OWN signature mention a concrete named type beyond its own generic\"] # [doc = \" type parameters, when [`NoduleImports::resolved_fn_sigs`] has **no baked entry** for it (the\"] # [doc = \" un-bakeable case: [`resolve_fn_sig`] only resolves against the declaring nodule's own registered\"] # [doc = \" types, so a signature that itself references a type the declaring nodule imports from a sibling\"] # [doc = \" nodule fails to bake — see that fn's doc comment)? [`Self::check_app`] /\"] # [doc = \" [`Self::check_app_generic_fn`]'s fallback then re-resolves the un-bakeable `TypeRef` fresh\"] # [doc = \" against `self.types` (the CALLER's own registry) — exactly the M-1036/DN-112 bare-name collapse,\"] # [doc = \" one level up, for a **fn signature** across the phylum boundary rather than intra-phylum. Shares\"] # [doc = \" [`fn_sig_names_a_concrete_type`]'s core with the trait-sig guard (DRY — one recognizer, not two);\"] # [doc = \" `sig.params` (the fn's own `Vec<TypeParam>` — empty for a monomorphic fn) is the \\\"safe\\\" set here,\"] # [doc = \" exactly as `tr.params` is for a trait.\"] fn foreign_fn_sig_names_a_concrete_type (sig : & FnSig) -> Option < & str > { let params : BTreeSet < & str > = sig . params . iter () . map (| p | p . name . as_str ()) . collect () ; fn_sig_names_a_concrete_type (sig , & params) }",
      "reason": "`let` binding uses an unsupported pattern (only simple `let x = e;` is supported)",
      "item_name": "foreign_fn_sig_names_a_concrete_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4409,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" Shared core of [`foreign_trait_sig_names_a_concrete_type`] /\"] # [doc = \" [`foreign_fn_sig_names_a_concrete_type`]: does `sig`'s value-param/return types reference a\"] # [doc = \" concrete named type beyond `params` (the enclosing trait's or fn's own generic parameter names)?\"] # [doc = \" `None` if `sig` is built entirely from primitives/params (the safe case).\"] fn fn_sig_names_a_concrete_type < 'a > (sig : & 'a FnSig , params : & BTreeSet < & str >) -> Option < & 'a str > { for p in & sig . value_params { if let Some (name) = typeref_names_concrete_type (& p . ty , params) { return Some (name) ; } } typeref_names_concrete_type (& sig . ret , params) }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "fn_sig_names_a_concrete_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4422,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" Recursively find a [`BaseType::Named`] in `tr` that is not one of `params` — the \\\"concrete named\"] # [doc = \" type\\\" a foreign trait signature might reference beyond its own generic parameters. `None` if\"] # [doc = \" `tr` is built entirely from primitives/params (the safe case).\"] fn typeref_names_concrete_type < 'a > (tr : & 'a TypeRef , params : & BTreeSet < & str >) -> Option < & 'a str > { match & tr . base { BaseType :: Named (name , args) => { if ! params . contains (name . as_str ()) { return Some (name . as_str ()) ; } args . iter () . find_map (| a | typeref_names_concrete_type (a , params)) } BaseType :: Seq { elem , .. } => typeref_names_concrete_type (elem , params) , BaseType :: Fn (a , b) => typeref_names_concrete_type (a , params) . or_else (| | typeref_names_concrete_type (b , params)) , BaseType :: Tuple (elems) => elems . iter () . find_map (| e | typeref_names_concrete_type (e , params)) , _ => None , } }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "typeref_names_concrete_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4444,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" **Impl pass — registration + coherence** (RFC-0019 §4.5; LR-2 — guarantee: `Declared`, the\"] # [doc = \" coherence argument is Declared-with-argument per RFC-0019, not machine-checked). For each\"] # [doc = \" `impl Trait<args> for T`:\"] # [doc = \" - resolve `for_ty` and each `trait_args` to a **concrete** [`Ty`] (no type-variables in scope);\"] # [doc = \" - the trait must exist and the argument **arity** must match the trait's params (else explicit);\"] # [doc = \" - **global uniqueness:** key `(trait, type_head(for_ty))` must be free, else an overlapping-\"] # [doc = \"   instance / coherence refusal naming the pair (RFC-0019 §4.5; ADR-003);\"] # [doc = \" - **orphan rule (phylum-wide, pub-blind — M-662):** the trait is declared in *some* nodule of the\"] # [doc = \"   phylum, OR `for_ty`'s head is a `Data` declared in *some* nodule of the phylum, OR `for_ty` is a\"] # [doc = \"   primitive repr type (`Binary`/`Ternary`/`Dense`/`Substrate`); otherwise an explicit orphan\"] # [doc = \"   refusal. The locality view is pub-blind (coherence is enforcement authority, not the `pub`\"] # [doc = \"   namespace — RFC-0019 §4.5). For a phylum-of-one this reduces to the prior single-nodule test.\"] # [doc = \"\"] # [doc = \" Method *bodies* are not checked here (that is [`check_impl_methods`], after the whole instance\"] # [doc = \" set is known); method *presence* (exact-match against the trait's sigs) IS checked here.\"] pub (crate) fn register_instances (types : & BTreeMap < String , DataInfo > , traits : & BTreeMap < String , TraitInfo > , coherence : & CoherenceView , nodule : & Nodule ,) -> Result < BTreeMap < (String , String) , InstanceInfo > , CheckError > { let phylum_traits = & coherence . traits ; let phylum_types = & coherence . types ; let mut instances : BTreeMap < (String , String) , InstanceInfo > = BTreeMap :: new () ; for item in & nodule . items { let Item :: Impl (id) = item else { continue } ; let site = & id . trait_name ; let Some (tr) = traits . get (& id . trait_name) else { return Err (CheckError :: new (site , format ! (\"`impl` for unknown trait `{}` (RFC-0019 §4.5)\" , id . trait_name) ,)) ; } ; let (for_ty , _) = resolve_ty (site , types , & [] , & id . for_ty) ? ; let mut trait_args = Vec :: with_capacity (id . trait_args . len ()) ; for a in & id . trait_args { trait_args . push (resolve_ty (site , types , & [] , a) ? . 0) ; } if trait_args . len () != tr . params . len () { return Err (CheckError :: new (site , format ! (\"trait `{}` takes {} type argument(s), but this `impl` supplies {} \\\n                     (RFC-0019 §4.5)\" , tr . name , tr . params . len () , trait_args . len ()) ,)) ; } let Some (head) = type_head (& for_ty) else { return Err (CheckError :: new (site , format ! (\"an `impl … for {for_ty}` over a bare type variable is not a legal instance \\\n                     head in stage-1 (no blanket instances — RFC-0019 §4.5); the `for` type must \\\n                     be concrete\") ,)) ; } ; let trait_local = phylum_traits . contains (id . trait_name . as_str ()) ; let type_local = match & for_ty { Ty :: Data (n , _) => phylum_types . contains (ty_local_name (n)) , Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Dense (_ , _) | Ty :: Vsa { .. } | Ty :: Substrate (_) | Ty :: Seq (_ , _) | Ty :: Bytes | Ty :: Float => true , Ty :: Var (_) | Ty :: Fn (_ , _) => false , } ; if ! trait_local && ! type_local { return Err (CheckError :: new (site , format ! (\"orphan instance: `impl {} for {for_ty}` — neither the trait `{}` nor the type \\\n                     `{for_ty}` is declared in any nodule of this phylum (RFC-0019 §4.5 orphan rule, \\\n                     phylum-wide; M-662). Declare one of them in the phylum, or move the impl.\" , id . trait_name , id . trait_name) ,)) ; } if ! trait_local { if let Some (name) = foreign_trait_sig_names_a_concrete_type (tr) { return Err (CheckError :: new (site , format ! (\"`impl {} for {for_ty}` — the foreign (cross-phylum) trait `{}`'s \\\n                         signature mentions a concrete type `{name}` beyond its own generic \\\n                         parameter(s); a cross-phylum trait's signature is not yet re-homed \\\n                         against its declaring phylum (DN-113 §7 / DN-122 residual — a bare-name \\\n                         reference to `{name}` here cannot be verified against the correct \\\n                         phylum's `{name}` and would risk the M-1036/DN-112 bare-name collapse one \\\n                         level up; refused rather than silently resolved against a possibly-wrong \\\n                         declaration, G2/VR-5). Declare a local wrapper trait instead, or await the \\\n                         tracked follow-up for full cross-phylum trait-sig re-homing.\" , id . trait_name , tr . name) ,)) ; } } let key = (id . trait_name . clone () , head . clone ()) ; if instances . contains_key (& key) { return Err (CheckError :: new (site , format ! (\"overlapping instance — coherence/global-uniqueness violation (RFC-0019 §4.5): \\\n                     a second `impl {} for` a `{head}` type. Stage-1 keys per (trait, type-head), so \\\n                     two instances on the same head (even at different widths) conflict.\" , id . trait_name) ,)) ; } check_impl_method_set (tr , id) ? ; instances . insert (key , InstanceInfo { trait_name : id . trait_name . clone () , trait_args , for_ty , methods : id . methods . iter () . map (| m | m . sig . name . clone ()) . collect () , } ,) ; } Ok (instances) }",
      "reason": "fn parameter `nodule` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "register_instances"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4613,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The impl's method **set** must match the trait's requirement set **exactly** (RFC-0019 §4.5):\"] # [doc = \" a missing method and an extra method are both explicit refusals (never silently filled or\"] # [doc = \" dropped — G2). Signature/body agreement is [`check_impl_methods`]; this is only presence/names.\"] fn check_impl_method_set (tr : & TraitInfo , id : & ImplDecl) -> Result < () , CheckError > { let site = & id . trait_name ; let required : std :: collections :: BTreeSet < & str > = tr . sigs . iter () . map (| s | s . name . as_str ()) . collect () ; let provided : std :: collections :: BTreeSet < & str > = id . methods . iter () . map (| m | m . sig . name . as_str ()) . collect () ; for need in & required { if ! provided . contains (need) { return Err (CheckError :: new (site , format ! (\"impl of `{}` is missing method `{need}`\" , tr . name) ,)) ; } } for have in & provided { if ! required . contains (have) { return Err (CheckError :: new (site , format ! (\"impl of `{}` has method `{have}` not in trait `{}`\" , tr . name , tr . name) ,)) ; } } if let Some (dup) = first_duplicate (& id . methods . iter () . map (| m | m . sig . name . clone ()) . collect :: < Vec < _ > > () ,) { return Err (CheckError :: new (site , format ! (\"impl of `{}` provides method `{dup}` more than once\" , tr . name) ,)) ; } Ok (()) }",
      "reason": "unit type `()` has no representable value in this grammar fragment",
      "item_name": "check_impl_method_set"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4659,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Check one `impl`'s method bodies against their **expected** signatures (RFC-0019 §4.5; guarantee:\"] # [doc = \" `Declared` — a structural check). The expected signature of method `m` is the trait's sig for `m`\"] # [doc = \" with the trait's params substituted by this impl's `trait_args`; the method body is checked the\"] # [doc = \" normal fn-body way against those substituted value-param/return types. A signature mismatch\"] # [doc = \" (wrong param types or arity, wrong return) is an explicit refusal, never silently accepted.\"] # [allow (clippy :: too_many_arguments)] fn check_impl_methods (types : & BTreeMap < String , DataInfo > , fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , instances : & BTreeMap < (String , String) , InstanceInfo > , imports : & NoduleImports , lower_rules : & BTreeMap < String , LowerDecl > , std_sys : bool , id : & ImplDecl ,) -> Result < Vec < FnDecl > , CheckError > { let tr = traits . get (& id . trait_name) . expect (\"instance registration checked the trait exists\") ; let mut resolved : Vec < FnDecl > = Vec :: with_capacity (id . methods . len ()) ; let (for_ty , _) = resolve_ty (& id . trait_name , types , & [] , & id . for_ty) ? ; let mut trait_args = Vec :: with_capacity (id . trait_args . len ()) ; for a in & id . trait_args { trait_args . push (resolve_ty (& id . trait_name , types , & [] , a) ? . 0) ; } let s = param_subst (& tr . params , & trait_args) ; for method in & id . methods { let req = tr . sigs . iter () . find (| x | x . name == method . sig . name) . expect (\"method-set match checked at registration\") ; let site = & method . sig . name ; if method . sig . value_params . len () != req . value_params . len () { return Err (CheckError :: new (site , format ! (\"impl method `{}` of `{}` takes {} parameter(s), but the trait requires {}\" , method . sig . name , tr . name , method . sig . value_params . len () , req . value_params . len ()) ,)) ; } for (mp , rp) in method . sig . value_params . iter () . zip (& req . value_params) { let (got , _) = resolve_ty (site , types , & [] , & mp . ty) ? ; let want = subst_ty (& resolve_ty (site , types , & tr . params , & rp . ty) ? . 0 , & s) ; if got != want { return Err (CheckError :: new (site , format ! (\"impl method `{}` parameter `{}`: {}\" , method . sig . name , mp . name , edge_mismatch (\"type\" , & want , & got)) ,)) ; } } let (got_ret , _) = resolve_ty (site , types , & [] , & method . sig . ret) ? ; let want_ret = subst_ty (& resolve_ty (site , types , & tr . params , & req . ret) ? . 0 , & s) ; if got_ret != want_ret { return Err (CheckError :: new (site , format ! (\"impl method `{}` return: {}\" , method . sig . name , edge_mismatch (\"type\" , & want_ret , & got_ret)) ,)) ; } let req_effects : std :: collections :: BTreeSet < & str > = req . effects . iter () . map (String :: as_str) . collect () ; let got_effects : std :: collections :: BTreeSet < & str > = method . sig . effects . iter () . map (String :: as_str) . collect () ; if req_effects != got_effects { return Err (CheckError :: new (site , format ! (\"impl method `{}` declares effects {} but trait `{}` requires {} — an impl \\\n                     method's effect annotation must match the trait method's exactly (RFC-0014 \\\n                     §4.5 I3; never silently widened or narrowed — G2)\" , method . sig . name , render_effects (& method . sig . effects) , tr . name , render_effects (& req . effects) ,) ,)) ; } let (body , _ret) = check_fn_body (types , fns , traits , instances , imports , lower_rules , std_sys , method ,) ? ; resolved . push (FnDecl { vis : method . vis , thaw : method . thaw , tier : method . tier , sig : method . sig . clone () , body , }) ; } let _ = for_ty ; Ok (resolved) }",
      "reason": "field access on a non-`self` base — the transpiler tracks no local types, so the projection cannot be resolved to a constructor position (only `self.<field>` desugars to a `match`)",
      "item_name": "check_impl_methods"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4791,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Check a function (or impl method) body against its declared signature (RFC-0007 §11; RFC-0019\"] # [doc = \" §4.1). Validates the type-parameter bounds, builds the `tyvars`/`bounds` scopes, resolves the\"] # [doc = \" value-param + return types, and runs the bidirectional [`Cx::check`]. Returns the **resolved**\"] # [doc = \" body (ambient bare-decimals filled) and the resolved return type. Shared by Pass 3 (top-level\"] # [doc = \" fns) and [`check_impl_methods`] (impl methods) — DRY.\"] # [allow (clippy :: too_many_arguments)] fn check_fn_body (types : & BTreeMap < String , DataInfo > , fns : & BTreeMap < String , FnDecl > , traits : & BTreeMap < String , TraitInfo > , instances : & BTreeMap < (String , String) , InstanceInfo > , imports : & NoduleImports , lower_rules : & BTreeMap < String , LowerDecl > , std_sys : bool , fd : & FnDecl ,) -> Result < (Expr , Ty) , CheckError > { let site = & fd . sig . name ; let tyvars = fd . sig . param_names () ; let bounds = check_bounds (types , traits , site , & tyvars , & fd . sig . params) ? ; let mut scope : Vec < (String , Ty) > = Vec :: new () ; for p in & fd . sig . value_params { let (ty , _) = resolve_ty (site , types , & tyvars , & p . ty) ? ; scope . push ((p . name . clone () , ty)) ; } let (ret , _) = resolve_ty (site , types , & tyvars , & fd . sig . ret) ? ; let cx = Cx { site , types , fns , traits , instances , imports , lower_rules , tyvars : & tyvars , bounds : & bounds , std_sys , depth : Cell :: new (0) , affine : Tracker :: seeded (& scope) , # [cfg (test)] stage3_sabotage_skip_substitution : false , } ; let (got , body) = cx . check (& mut scope , & fd . body , Some (& ret)) ? ; if got != ret { return Err (CheckError :: new (site , edge_mismatch (\"body\" , & ret , & got))) ; } Ok ((body , ret)) }",
      "reason": "field access on a non-`self` base — the transpiler tracks no local types, so the projection cannot be resolved to a constructor position (only `self.<field>` desugars to a `match`)",
      "item_name": "check_fn_body"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4845,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Validate a function/method's type-parameter **bounds** (RFC-0019 §4.1): each bound must name a\"] # [doc = \" registered trait with the correct type-argument arity, and the bound's trait-args must resolve\"] # [doc = \" under `tyvars`. Returns the `(param, bounds)` pairs (only the bounded params) for the checking\"] # [doc = \" context. Every refusal is explicit (G2). The dictionary the bound stands for is staged to\"] # [doc = \" elaboration (RFC-0007 §12.3 / M-673) — the checker only validates satisfiability (\\\"typing\\\").\"] fn check_bounds (types : & BTreeMap < String , DataInfo > , traits : & BTreeMap < String , TraitInfo > , site : & str , tyvars : & [String] , params : & [crate :: ast :: TypeParam] ,) -> Result < Vec < (String , Vec < TraitRef >) > , CheckError > { let mut bounds : Vec < (String , Vec < TraitRef >) > = Vec :: new () ; for p in params { for b in & p . bounds { let Some (tr) = traits . get (& b . name) else { return Err (CheckError :: new (site , format ! (\"bound `{}: {}` names unknown trait `{}` (RFC-0019 §4.1)\" , p . name , b . name , b . name) ,)) ; } ; let elided_self = b . args . is_empty () && tr . params . len () == 1 ; if ! elided_self && b . args . len () != tr . params . len () { return Err (CheckError :: new (site , format ! (\"bound `{}: {}<…>` supplies {} type argument(s), but trait `{}` takes {} \\\n                         (write `{}: {}` for the single-parameter self-bound, or supply all args)\" , p . name , b . name , b . args . len () , b . name , tr . params . len () , p . name , b . name) ,)) ; } for a in & b . args { resolve_ty (site , types , tyvars , a) ? ; } } if ! p . bounds . is_empty () { bounds . push ((p . name . clone () , p . bounds . clone ())) ; } } Ok (bounds) }",
      "reason": "unsupported Rust type form `[String]`",
      "item_name": "check_bounds"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4901,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" A minimal, per-call fresh-name generator for [`Cx::stage3_substitute_expr`]'s (A)-style\"] # [doc = \" RHS-local-binder renaming (M-1054 Stage 3, DN-117 §2 step 1) — the check-time-only analogue of\"] # [doc = \" [`crate::elab::Elab::fresh`]. `%`-prefixed (surface-illegal — `crate::lexer::is_ident_start`\"] # [doc = \" forbids `%`, mirrored by every other synthetic-name scheme in this module/`elab.rs`), so a\"] # [doc = \" minted name can never collide with a real surface identifier; the counter makes distinct mints\"] # [doc = \" within one substitution walk distinct from each other. Not persisted past one\"] # [doc = \" `check_sugar_call` invocation — a fresh instance every time (no cross-call state, unlike\"] # [doc = \" `elab.rs`'s `SUGAR_EXPANSION_SITE`, which needs cross-invocation uniqueness for the *real*,\"] # [doc = \" elaboration-time expansion; this walk's renamed binders are discarded with the rest of the\"] # [doc = \" substituted term).\"] # [derive (Default)] struct Stage3Fresh (u64) ;",
      "reason": "dropped non-doc attribute(s) on struct `Stage3Fresh`: # [derive (Default)]",
      "item_name": "Stage3Fresh"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4914,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Stage3Fresh { fn mint (& mut self) -> String { let n = self . 0 ; self . 0 += 1 ; format ! (\"%stage3-subst%{n}\") } }",
      "reason": "no member of this impl block could be transpiled (1 sub-issue(s)): impl method `mint` body: unsupported expression form `format ! (\"%stage3-subst%{n}\")`",
      "item_name": "Stage3Fresh"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4922,
      "col": 1,
      "category": "GenericBound",
      "rust_construct": "GenericBound",
      "snippet": "# [doc = \" The checking context for one function body.\"] struct Cx < 'a > { site : & 'a str , types : & 'a BTreeMap < String , DataInfo > , fns : & 'a BTreeMap < String , FnDecl > , # [doc = \" Trait registry (RFC-0019 §4.2) — for resolving bounded-generic-call satisfiability and\"] # [doc = \" unqualified trait-method calls (`Tr::m`). Empty in re-inference (`infer_type`).\"] traits : & 'a BTreeMap < String , TraitInfo > , # [doc = \" Instance registry (RFC-0019 §4.5), keyed by `(trait, type-head)` — the coherence map a\"] # [doc = \" bounded call / trait-method call resolves against. Empty in re-inference.\"] instances : & 'a BTreeMap < (String , String) , InstanceInfo > , # [doc = \" The nodule's resolved cross-nodule imports (M-662) — consulted **only** at unresolved-name\"] # [doc = \" sites to raise the never-silent glob-vs-glob ambiguity error (a *reference* to a name brought\"] # [doc = \" in by ≥2 globs and not shadowed). Imported `pub` decls themselves are already merged into\"] # [doc = \" `types`/`fns`/`traits` (by simple name), so ordinary resolution sees them directly; this field\"] # [doc = \" only carries the `ambiguous` set so a reference to an ambiguous name is refused, never a silent\"] # [doc = \" winner (G2). Empty (`ambiguous` empty) in re-inference and in a phylum-of-one. **Also**\"] # [doc = \" carries [`NoduleImports::resolved_fn_sigs`] (DN-112 Rank 1 / M-1036) — [`Self::check_app`]'s\"] # [doc = \" monomorphic-callee path consults it in preference to re-resolving a callee's signature\"] # [doc = \" fresh against `self.types` (the caller's own registry), so an imported function's\"] # [doc = \" parameter/return types stay the *declaring* nodule's, never re-derived at the call site.\"] imports : & 'a NoduleImports , # [doc = \" **M-1054 Stage 0** — the nodule's registered `lower` rules (DN-110 §5-A), consulted **only**\"] # [doc = \" after every existing call-resolution path (fn/HOF/ctor/trait-method/prim families) has\"] # [doc = \" already missed [`Self::check_app`], so a name that resolves any other way is completely\"] # [doc = \" unaffected by this field's presence (regression-safe by construction). Lets an expression\"] # [doc = \" position call site `Name(args)` be *recognized* as a sugar-rule invocation and arity/type-\"] # [doc = \" matched against [`LowerDecl::value_params`] — see [`Self::check_sugar_call`]. Always empty in\"] # [doc = \" `infer_type`'s re-inference context exactly like `traits`/`instances` above (re-inference runs\"] # [doc = \" over already-checked terms, which — in v0 — never contain a sugar-rule call, since Stage 0\"] # [doc = \" never accepts one; kept non-empty there anyway, for uniformity, at zero behavioral cost).\"] lower_rules : & 'a BTreeMap < String , LowerDecl > , # [doc = \" The type parameters in scope for this body (RFC-0007 §11.2) — empty for a monomorphic\"] # [doc = \" function. A bare `Named` type that matches one of these resolves to [`Ty::Var`].\"] tyvars : & 'a [String] , # [doc = \" The **bounds** on the type parameters in scope (`T: Cmp` ⇒ `(\\\"T\\\", TraitRef{Cmp})`), so an\"] # [doc = \" unqualified trait-method call inside a bounded body can be typed through a bound (the\"] # [doc = \" dictionary it stands for is staged to elaboration — RFC-0007 §12.3 / M-673). Parallel to\"] # [doc = \" `tyvars`; empty for an unbounded/monomorphic body.\"] bounds : & 'a [(String , Vec < TraitRef >)] , # [doc = \" Whether the enclosing nodule carries the `@std-sys` FFI-floor marker (M-661; RFC-0016 §8-Q6).\"] # [doc = \" A `wild` block (the denied-by-default unsafe escape, LR-9/S6) type-checks **only** when this is\"] # [doc = \" `true`; in a non-`@std-sys` nodule a `wild` is a hard [`CheckError`] (never a silent escape —\"] # [doc = \" G2). Threaded down from the nodule header through [`check_fn_body`] / [`check_impl_methods`].\"] std_sys : bool , # [doc = \" Live expression-nesting depth for the explicit [`MAX_CHECK_DEPTH`] budget (interior\"] # [doc = \" mutability so [`Self::check`] stays `&self`). Reset per body; accounted by [`DepthGuard`].\"] depth : Cell < u32 > , # [doc = \" The **affine `Substrate` use-once tracker** (M-903; DN-71 §4.2) — mirrors `scope` by index\"] # [doc = \" (interior mutability, matching `depth`'s pattern, so [`Self::check`] stays `&self`). **Active**\"] # [doc = \" (seeded via [`Tracker::seeded`]) in both [`check_fn_body`] (seeded from the parameter scope)\"] # [doc = \" **and** [`check_lower_rule_rhs_type`] (seeded from the empty initial scope — M-919 fix, DN-71\"] # [doc = \" Model S §4.2: a `lower` rule's RHS can still bind a `Substrate` via a helper-fn call, so it is\"] # [doc = \" use-once-checked too); **inert** (a guaranteed no-op) only in the `infer_type` context — see\"] # [doc = \" `crate::affine`'s module docs for why. (FLAG-6 fix, M-973: this doc previously read that the\"] # [doc = \" tracker was inert in `check_lower_rule_rhs_type`, stale since M-919 made it active there.)\"] affine : Tracker , # [doc = \" **Test-only non-vacuity sabotage toggle (M-1054 Stage 3, DN-117 §5 item 2).** When `true`,\"] # [doc = \" [`Self::check_sugar_call`]'s Stage-3 walk feeds the walk the **unsubstituted** `rhs` instead\"] # [doc = \" of the real, spliced [`Self::stage3_substitute_expr`] output — simulating \\\"the substitution\"] # [doc = \" never ran\\\" to prove the substituted-`Expr` walk is load-bearing: a fixture that must\"] # [doc = \" genuinely REFUSE (e.g. the composite/hidden-affine classes no type-based gate alone can\"] # [doc = \" catch) should wrongly ACCEPT under this sabotage. **Does not exist in a non-test build at\"] # [doc = \" all** (`#[cfg(test)]`-gated field — zero production footprint); every non-test `Cx`\"] # [doc = \" construction site sets it `false` (also `#[cfg(test)]`-gated, so it only compiles under\"] # [doc = \" `cfg(test)` too — see each `Cx { .. }` literal). Set `true` only by the dedicated\"] # [doc = \" `#[cfg(test)]`-only entry point in `src/tests/affine_stage3.rs`.\"] # [cfg (test)] stage3_sabotage_skip_substitution : bool , }",
      "reason": "lifetime parameter `'a` has no grammar surface",
      "item_name": "Cx"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 4993,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" RFC-0040 (M-977): if `expected` is a **cons-list-shaped** user ADT, return its `(nil, cons)`\"] # [doc = \" constructor names — the trigger for desugaring a `[…]` literal into a right-nested `Cons` chain\"] # [doc = \" (see [`Cx::check_list`]). \\\"Cons-list-shaped\\\" is a purely *structural* recognition, requiring no\"] # [doc = \" annotation: the type is a `Ty::Data(name, _)` whose declaration has **exactly two** constructors —\"] # [doc = \" one **nullary** (the \\\"nil\\\": `Nil`/`GLNil`/`TNil`/`BNil`/…) and one **binary** whose SECOND field is\"] # [doc = \" the recursive `Self` type `Data(name, …)` (the \\\"cons\\\": `Cons(A, Self)`). This matches every\"] # [doc = \" `lib/std` list type (`Vec`, `Trits`, `ByteList`, `GRowList`, …) uniformly. A type not of this shape\"] # [doc = \" yields `None`, so the `Seq{T,N}` and no-context paths are untouched (never a silent reinterpret).\"] pub (crate) fn cons_list_ctors (types : & std :: collections :: BTreeMap < String , DataInfo > , expected : & Ty ,) -> Option < (String , String) > { let Ty :: Data (name , _args) = expected else { return None ; } ; let di = lookup_data (types , name) ? ; if di . ctors . len () != 2 { return None ; } let mut nil : Option < String > = None ; let mut cons : Option < String > = None ; for c in & di . ctors { match c . fields . len () { 0 => nil = Some (c . name . clone ()) , 2 => { if let Ty :: Data (fname , fargs) = & c . fields [1] { if fname == name && fargs . len () == di . params . len () { cons = Some (c . name . clone ()) ; } } } _ => { } } } match (nil , cons) { (Some (n) , Some (c)) => Some ((n , c)) , _ => None , } }",
      "reason": "qualified type path `std :: collections :: BTreeMap < String , DataInfo >` — collapsing to its last segment would risk colliding with an unrelated same-named local type (e.g. `std::cmp::Ordering` vs a local `Ordering`); left an explicit gap rather than guessed (VR-5)",
      "item_name": "cons_list_ctors"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 5039,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl Cx < '_ > { fn err < T > (& self , msg : impl Into < String >) -> Result < T , CheckError > { Err (CheckError :: new (self . site , msg)) } # [doc = \" Map a match-analysis over-budget refusal (usefulness / decision-tree, RFC-0041 §4.7) into the\"] # [doc = \" checker's never-silent [`CheckError`] surface — an explicit refusal, never a host-stack\"] # [doc = \" overflow (G2/VR-5). Shared by the exhaustiveness/redundancy (`usefulness::useful`) and the\"] # [doc = \" decision-tree (`decision::compile`) callsites so the mapping is written once (DRY).\"] fn match_budget_err (& self , e : mycelium_workstack :: BudgetError) -> CheckError { CheckError :: new (self . site , format ! (\"match analysis exceeded the recursion budget: {e} — an explicit over-budget refusal \\\n                 (RFC-0041 §4.7), never a host-stack overflow (G2/VR-5)\") ,) } # [doc = \" Enter one level of `check` recursion against the explicit [`MAX_CHECK_DEPTH`] budget\"] # [doc = \" (banked guard 4): charge a level, refuse with a clean [`CheckError`] past the budget (never a\"] # [doc = \" host-stack overflow), and return a [`DepthGuard`] that releases the level on **any** exit path.\"] fn enter (& self) -> Result < DepthGuard < '_ > , CheckError > { let d = self . depth . get () + 1 ; if d > MAX_CHECK_DEPTH { return self . err (format ! (\"expression nesting exceeds the checker depth budget ({MAX_CHECK_DEPTH}) — an \\\n                 explicit budget (banked guard 4), refused cleanly rather than overflowing the \\\n                 host stack (RFC-0007 §4.6 clocked-recursion discipline)\")) ; } self . depth . set (d) ; Ok (DepthGuard (& self . depth)) } fn ctor (& self , name : & str) -> Option < (& DataInfo , usize) > { self . types . values () . find_map (| d | d . ctors . iter () . position (| c | c . name == name) . map (| i | (d , i))) } # [doc = \" M-1027 / DN-104 §4: the **cross-nodule construction seal**. If `name` is a constructor **imported\"] # [doc = \" by name** (`use home.Ctor`) from another nodule where it is `priv`-sealed, **constructing** it\"] # [doc = \" here is a never-silent [`CheckError`]. Returns `Err` at the two constructor-application sites for\"] # [doc = \" a withheld name; `Ok(())` otherwise (an unsealed ctor, or a home constructor — own decls are\"] # [doc = \" subtracted from the withheld set in `resolve_imports`). The type **name** and **pattern-matching**\"] # [doc = \" are unaffected — only *construction* is withheld (DN-104 §4).\"] # [doc = \"\"] # [doc = \" **Not an enforced security/capability boundary (known gap, M-1036)** — this check is keyed by\"] # [doc = \" bare constructor name in [`NoduleImports::sealed`], so a caller that declares its own same-named\"] # [doc = \" **local** (unsealed) type/ctor — rather than importing the real one — never populates its\"] # [doc = \" withheld set and bypasses this check entirely (own decls shadow imports, RFC-0006 §4.3; pinned\"] # [doc = \" by `tests/ctor_seal.rs::known_gap_a_same_named_local_shadow_type_bypasses_the_seal`). It refuses\"] # [doc = \" a well-behaved cross-nodule caller that actually goes through `use`; it does not defend against\"] # [doc = \" an adversarial or accidentally-colliding same name.\"] fn check_ctor_seal (& self , name : & str , ty_name : & str) -> Result < () , CheckError > { if self . imports . sealed . contains (name) { return self . err (format ! (\"constructor `{name}` of `{ty_name}` is `priv`-sealed in its home nodule — it is \\\n                 exported for use in signatures and pattern-matching, but **withheld from \\\n                 cross-nodule construction** (the capability-gate: only its home nodule may mint one; \\\n                 M-1027 / DN-104 §4). Construct it in its home nodule, or call a `pub fn` factory the \\\n                 home nodule exposes (never a silent forge — G2/VR-5)\")) ; } Ok (()) } # [doc = \" Infer the type of `e` under `scope` (a lexical stack; shadowing = later wins). A thin wrapper\"] # [doc = \" over the bidirectional [`Self::check`] with no expected type — used where only the type is\"] # [doc = \" wanted and `e` carries no ambient bare-decimal needing context (e.g. the elaborator's\"] # [doc = \" re-inference over already-resolved terms).\"] fn infer (& self , scope : & mut Vec < (String , Ty) > , e : & Expr) -> Result < Ty , CheckError > { self . check (scope , e , None) . map (| (ty , _) | ty) } # [doc = \" **Bidirectional check** (RFC-0012 §4.3): type `e` under `scope`, optionally *against* an\"] # [doc = \" `expected` type, and return the type together with a **resolved** expression — any ambient\"] # [doc = \" bare decimal ([`Literal::AmbientInt`]) whose width the context determines is rewritten to a\"] # [doc = \" concrete `Binary`/`Ternary` literal, so the evaluator and elaborator see only explicit forms.\"] # [doc = \" Where the width is *not* determined, the refusal is an explicit `UnresolvedWidth` (never a\"] # [doc = \" built-in default). A cross-paradigm edge surfaces as a `MissingConversion` (never silent).\"] fn check (& self , scope : & mut Vec < (String , Ty) > , e : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let _depth = self . enter () ? ; match e { Expr :: Lit (Literal :: AmbientInt (p , v)) => { let lit = self . resolve_ambient_int (* p , * v , expected) ? ; let ty = lit_ty_of (self . site , & lit) ? ; Ok ((ty , Expr :: Lit (lit))) } Expr :: Lit (Literal :: List (elems)) => self . check_list (scope , elems , expected) , Expr :: Lit (l) => Ok ((self . lit_ty (l) ? , Expr :: Lit (l . clone ()))) , Expr :: Path (p) => self . check_path (scope , p , e , expected) , Expr :: Let { name , ty , bound , body , } => self . check_let (scope , name , ty . as_ref () , bound , body , expected) , Expr :: If { cond , conseq , alt } => self . check_if (scope , cond , conseq , alt , expected) , Expr :: Match { scrutinee , arms } => self . check_match (scope , scrutinee , arms , expected) , Expr :: For { x , xs , acc , init , body , } => self . check_for (scope , x , xs , acc , init , body , expected) , Expr :: Swap { value , target , policy , } => self . check_swap (scope , value , target , policy) , Expr :: Wild (body) => self . check_wild (body , expected) , Expr :: Spore (_) => { self . err (\"`spore` is deferred to the reconstruction-manifest work (E2-5/M-260)\") } Expr :: Wrapping (body) => self . check_wrapping (scope , body , expected) , Expr :: Consume (operand) => self . check_consume (scope , operand , expected) , Expr :: Try (_) => self . err (\"`?` (the try-operator) is only supported as a `let`-binder RHS in v0 — write \\\n                 `let x = e? in body` (DN-102 §5). A `?` in any other position (a nested `g(f()?)`, a \\\n                 repeated `e??`, or a tail `e?`) needs the general-position CPS lift, which is deferred \\\n                 (FLAG-try-1); rewrite it as `let tmp = <inner>? in <continuation using tmp>`.\" ,) , Expr :: Colony (hyphae) => self . check_colony (scope , hyphae , expected) , Expr :: Lambda { params , body } => self . check_lambda (scope , params , body , expected) , Expr :: WithParadigm { .. } => self . err (\"internal: a `with paradigm` block reached the checker — the ambient resolution \\\n                 pass should have stripped it (RFC-0012 §4.4)\" ,) , Expr :: Ascribe (inner , t) => self . check_ascribe (scope , inner , t) , Expr :: App { head , args } => self . check_app (scope , head , args , expected) , Expr :: TupleLit (elems) => self . check_tuple_lit (scope , elems , expected) , Expr :: Fuse { left , right } => self . check_fuse (scope , left , right , expected) , Expr :: Reclaim { policy , body } => self . check_reclaim (scope , policy , body , expected) , } } # [doc = \" `consume <expr>` (DN-03 §1 / LR-8 / M-664) — affine acquisition of a `Substrate` value. The\"] # [doc = \" operand must have a `Substrate{tag}` type; any other operand type is an explicit refusal\"] # [doc = \" (never silent — G2). The result is the moved substrate (`Substrate{tag}`), now exclusively\"] # [doc = \" owned by the consumer.\"] # [doc = \"\"] # [doc = \" Guarantee **`Empirical`** (M-903; DN-71 Model S §4.2/§4.4 — updated from the M-664-era\"] # [doc = \" `Declared`, note that stated the pre-M-903 baseline honestly and is stale as of this\"] # [doc = \" affine-tracker landing, VR-5 requires this update rather than leaving it): the *single-use*\"] # [doc = \" property is now **checked**, not merely asserted — every reference to a `Substrate`-typed\"] # [doc = \" binding is recorded in `self.affine` (`crate::affine::Tracker`, wired below via `use_at` at\"] # [doc = \" every scope reference) and a second move on any reachable path is refused here, naming both\"] # [doc = \" sites. `Empirical`, not `Proven`: the tracker's own module docs are explicit that it is a\"] # [doc = \" sound-over-precise conservative approximation (verified by an exhaustive sweep, not a\"] # [doc = \" mechanized soundness proof) with a known, documented loop/closure multiplicity gap closed only\"] # [doc = \" by [`crate::substrate::SubstrateHandle::try_consume`]'s runtime backstop (M-903; wired into\"] # [doc = \" execution by M-904, DN-71 §4.3).\"] fn check_consume (& self , scope : & mut Vec < (String , Ty) > , operand : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let (oty , oexpr) = self . check (scope , operand , None) ? ; let Ty :: Substrate (tag) = & oty else { return self . err (format ! (\"`consume` requires an affine `Substrate{{…}}` operand (DN-03 §1 / LR-8), but its \\\n                 operand has type `{oty}` — only a `Substrate` value can be consumed \\\n                 (never silent — G2)\")) ; } ; let ty = Ty :: Substrate (tag . clone ()) ; if let Some (exp) = expected { if exp != & ty { return self . err (format ! (\"`consume` of `{ty}` yields `{ty}`, but the context expects `{exp}` \\\n                     (never silent — G2)\")) ; } } Ok ((ty , Expr :: Consume (Box :: new (oexpr)))) } fn check_path (& self , scope : & [(String , Ty)] , p : & Path , e : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { if p . 0 . len () != 1 { return self . err (format ! (\"dotted path `{}` does not resolve — multi-segment qualified-path *syntax* is \\\n                 deferred in v0; bring the name into scope with a `use` (`use {}`) and reference it \\\n                 by its final segment (M-662)\" , p . 0 . join (\".\") , p . 0 . join (\".\"))) ; } let name = & p . 0 [0] ; if let Some (idx) = scope . iter () . rposition (| (n , _) | n == name) { let ty = & scope [idx] . 1 ; match self . affine . use_at (idx) { UseOutcome :: NotAffine | UseOutcome :: FirstUse => { } UseOutcome :: DoubleUse { tag , first_ordinal , this_ordinal , } => { return self . err (format ! (\"double-consume: `{name}` (`Substrate{{{tag}}}`) is used again here \\\n                         (reference #{this_ordinal} to `{name}` in `{site}`), but it was already \\\n                         moved earlier (reference #{first_ordinal} to `{name}`) — an affine \\\n                         `Substrate` value may be consumed, passed, returned, or captured **at \\\n                         most once** along any execution path (DN-71 Model S §4.2; RFC-0006 LR-8). \\\n                         This is a static checker refusal naming both the first move and this \\\n                         violating use — never silent (G2/VR-5).\" , site = self . site)) ; } } return Ok ((ty . clone () , e . clone ())) ; } if let Some (err) = self . imports . ambiguity_error (self . site , name) { return Err (err) ; } if let Some ((d , i)) = self . ctor (name) { self . check_ctor_seal (name , & d . name) ? ; if d . ctors [i] . fields . is_empty () { let qname = qualify_type_name (& d . home , & d . name) ; let targs = if d . params . is_empty () { vec ! [] } else { match expected { Some (Ty :: Data (en , eargs)) if * en == qname && eargs . len () == d . params . len () => { eargs . clone () } _ => { return self . err (format ! (\"constructor `{name}` of generic `{}<…>` needs its type \\\n                                 argument(s) from context — ascribe the value (RFC-0007 §11.3, \\\n                                 never a guess)\" , d . name)) } } } ; return Ok ((Ty :: Data (qname , targs) , e . clone ())) ; } return self . err (format ! (\"constructor `{name}` takes {} field(s) — apply it (W6 saturation)\" , d . ctors [i] . fields . len ())) ; } if let Some (fd) = self . fns . get (name) { if fd . sig . value_params . is_empty () { return self . err (format ! (\"`{name}` takes 0 value parameters — a nullary function is not a \\\n                     first-class value; apply it directly (never a silent coercion — G2)\")) ; } let baked = self . imports . resolved_fn_sigs . get (name) ; if baked . is_none () && self . imports . cross_phylum_fns . contains (name) { if let Some (concrete) = foreign_fn_sig_names_a_concrete_type (& fd . sig) { return self . err (format ! (\"`{name}` — the foreign (cross-phylum) fn's signature mentions a concrete \\\n                         type `{concrete}` beyond its own generic parameter(s), and its signature \\\n                         could not be pre-resolved against its declaring phylum (`resolve_fn_sig` \\\n                         only bakes a signature that references no type the declaring nodule itself \\\n                         imports — best-effort, never a hard error there); a bare-name reference to \\\n                         `{concrete}` here (as a first-class value) cannot be verified against the \\\n                         correct phylum's `{concrete}` and would risk the M-1036/DN-112 bare-name \\\n                         collapse one level up (DN-113 §7 / DN-122 residual; refused rather than \\\n                         silently resolved against a possibly-wrong phylum's registry, G2/VR-5).\")) ; } } if fd . sig . value_params . len () > 1 { if ! fd . sig . params . is_empty () { return self . err (format ! (\"`{name}` is generic and multi-parameter — using a generic multi-parameter \\\n                         function as a first-class value requires type arguments from context; \\\n                         ascribe the value (RFC-0024 §4A.5, never a silent coercion — G2)\")) ; } let vparams = fd . sig . value_params . clone () ; let mut param_tys : Vec < Ty > = Vec :: with_capacity (vparams . len ()) ; for (i , p) in vparams . iter () . enumerate () { let t = match baked . and_then (| (params , _) | params . get (i)) { Some (w) => w . clone () , None => resolve_ty (self . site , self . types , & [] , & p . ty) ? . 0 , } ; param_tys . push (t) ; } let ret_ty = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & [] , & fd . sig . ret) ? . 0 , } ; let curried_ty = param_tys . iter () . rev () . fold (ret_ty . clone () , | acc , t | { Ty :: Fn (Box :: new (t . clone ()) , Box :: new (acc)) }) ; if let Some (exp) = expected { if exp != & curried_ty { return self . err (format ! (\"`{name}` has curried type `{curried_ty}`, but the context expects \\\n                             `{exp}` (type mismatch — RFC-0024 §4A.5, never a silent coercion)\")) ; } } let call = Expr :: App { head : Box :: new (e . clone ()) , args : vparams . iter () . map (| p | Expr :: Path (Path (vec ! [p . name . clone ()]))) . collect () , } ; let mut body : Expr = call ; for p in vparams . iter () . rev () { body = Expr :: Lambda { params : vec ! [p . clone ()] , body : Box :: new (body) , } ; } return Ok ((curried_ty , body)) ; } if fd . sig . params . is_empty () { let param_ty = match baked . and_then (| (params , _) | params . first ()) { Some (w) => w . clone () , None => resolve_ty (self . site , self . types , & [] , & fd . sig . value_params [0] . ty) ? . 0 , } ; let ret_ty = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & [] , & fd . sig . ret) ? . 0 , } ; return Ok ((Ty :: Fn (Box :: new (param_ty) , Box :: new (ret_ty)) , e . clone ())) ; } let callee_vars = fd . sig . param_names () ; let mut subst : BTreeMap < String , Ty > = BTreeMap :: new () ; if let Some (Ty :: Fn (ea , er)) = expected { let want_a = match baked . and_then (| (params , _) | params . first ()) { Some (w) => w . clone () , None => { resolve_ty (self . site , self . types , & callee_vars , & fd . sig . value_params [0] . ty ,) ? . 0 } } ; let want_r = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & callee_vars , & fd . sig . ret) ? . 0 , } ; let _ = unify (self . site , & want_a , ea , & mut subst) ; let _ = unify (self . site , & want_r , er , & mut subst) ; } for v in & callee_vars { if ! subst . contains_key (v) { return self . err (format ! (\"`{name}` is generic over `{v}`, but its type arguments cannot be \\\n                         determined from context — ascribe the value or the call site \\\n                         (RFC-0024 §5, RFC-0007 §11.3, never a guessed default)\")) ; } } let want_a = match baked . and_then (| (params , _) | params . first ()) { Some (w) => w . clone () , None => { resolve_ty (self . site , self . types , & callee_vars , & fd . sig . value_params [0] . ty ,) ? . 0 } } ; let want_r = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & callee_vars , & fd . sig . ret) ? . 0 , } ; let param_ty = subst_ty (& want_a , & subst) ; let ret_ty = subst_ty (& want_r , & subst) ; return Ok ((Ty :: Fn (Box :: new (param_ty) , Box :: new (ret_ty)) , e . clone ())) ; } self . err (teach_unknown (name , & format ! (\"unknown name `{name}`\"))) } fn check_let (& self , scope : & mut Vec < (String , Ty) > , name : & str , ty : Option < & TypeRef > , bound : & Expr , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { if let Expr :: Try (inner) = bound { if ty . is_some () { return self . err (format ! (\"let `{name}`: an explicit ascription on a `?`-binder is not supported in v0 — \\\n                     the success type is taken from the operand's `Result`/`Option` payload \\\n                     (DN-102 §5). Drop the `: …` or bind and ascribe on a separate `let`.\")) ; } return self . check_try_let (scope , name , inner , body , expected) ; } let want = match ty { Some (t) => Some (resolve_ty (self . site , self . types , self . tyvars , t) ? . 0) , None => None , } ; let (bty , bound2) = self . check (scope , bound , want . as_ref ()) ? ; if let Some (w) = & want { if w != & bty { return self . err (format ! (\"let `{name}`: {}\" , edge_mismatch (\"bound\" , w , & bty))) ; } } scope . push ((name . to_owned () , bty . clone ())) ; self . affine . push (& bty) ; let r = self . check (scope , body , expected) ; scope . pop () ; self . affine . pop () ; let (rty , body2) = r ? ; Ok ((rty , Expr :: Let { name : name . to_owned () , ty : ty . cloned () , bound : Box :: new (bound2) , body : Box :: new (body2) , } ,)) } # [doc = \" `let x = e? in body` — the **try-operator desugar** (DN-102 / M-1025 ENB-2). The `?` on the\"] # [doc = \" binder RHS propagates the operand's error/absence channel: it rewrites, **type-directed on the\"] # [doc = \" operand's checked `Result`/`Option` type**, to the existing `match` bind with the continuation\"] # [doc = \" `body` inside the binding arm —\"] # [doc = \"\"] # [doc = \" ```text\"] # [doc = \" let x = e? in body                       let x = e? in body\"] # [doc = \"   ⇓  (e : Result[A, E])                    ⇓  (e : Option[A])\"] # [doc = \" match e { Ok(x) => body,                 match e { Some(x) => body,\"] # [doc = \"           Err($try_err) => Err($try_err) }          None => None }\"] # [doc = \" ```\"] # [doc = \"\"] # [doc = \" Because `body` is in tail position its type is the enclosing function's return type\"] # [doc = \" `Result[B, E]` (resp. `Option[B]`), so the `Err($f) => Err($f)` arm (`Result[B, E]`) **unifies**\"] # [doc = \" with it — **no early return, no never-type** (`->!` stays deferred, DN-99 #88; DN-102 §2). The\"] # [doc = \" **error-type unification rule** (DN-102 §3) falls out of that arm-type unification: `E` on both\"] # [doc = \" sides must match, forcing the function to return the same error channel — a mismatch is the\"] # [doc = \" ordinary never-silent `match`-arm `CheckError` (G2). KC-3: no new L0 node — the returned\"] # [doc = \" rewritten `Match` is what the evaluator/elaborator consume, so a `Try` never survives past the\"] # [doc = \" checker (elab/eval keep only a defensive residual). Guarantee: `Declared` (surface contract)\"] # [doc = \" until the DN-102 §7 differential witnesses `?` ≡ the hand-`match` oracle (VR-5).\"] fn check_try_let (& self , scope : & mut Vec < (String , Ty) > , name : & str , inner : & Expr , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let affine_snap = self . affine . snapshot () ; let ity = self . infer (scope , inner) ? ; self . affine . restore (& affine_snap) ; const ERRB : & str = \"$try_err\" ; let arms = match & ity { Ty :: Data (n , args) if ty_local_name (n) == \"Result\" && args . len () == 2 => vec ! [Arm { pattern : Pattern :: Ctor (\"Ok\" . to_owned () , vec ! [Pattern :: Ident (name . to_owned ())]) , body : body . clone () , } , Arm { pattern : Pattern :: Ctor (\"Err\" . to_owned () , vec ! [Pattern :: Ident (ERRB . to_owned ())]) , body : Expr :: App { head : Box :: new (Expr :: Path (Path (vec ! [\"Err\" . to_owned ()]))) , args : vec ! [Expr :: Path (Path (vec ! [ERRB . to_owned ()]))] , } , } ,] , Ty :: Data (n , args) if ty_local_name (n) == \"Option\" && args . len () == 1 => vec ! [Arm { pattern : Pattern :: Ctor (\"Some\" . to_owned () , vec ! [Pattern :: Ident (name . to_owned ())] ,) , body : body . clone () , } , Arm { pattern : Pattern :: Ident (\"None\" . to_owned ()) , body : Expr :: Path (Path (vec ! [\"None\" . to_owned ()])) , } ,] , _ => { return self . err (format ! (\"`?` operand must have a `Result[_, _]` or `Option[_]` type, got `{ity}` — the \\\n                     try-operator propagates an error/absence channel (DN-102 §3; M-1025)\")) ; } } ; self . check_match (scope , inner , & arms , expected) } # [doc = \" **Lambda / closure typing** (RFC-0024 §4A.6, M-704). A `lambda(p: A) => body` checks to\"] # [doc = \" `Ty::Fn(A, B)` where `B = infer(body)` under `scope ∪ {p: A}`. The capture set (free variables\"] # [doc = \" of `body` bound in the enclosing `scope`) is well-typed *by construction* — each free name is\"] # [doc = \" looked up in `scope` during body checking, so an unbound free variable is a never-silent\"] # [doc = \" `unknown name` refusal from `check_path` (G2), not a guess. The closure's *lowering* (the\"] # [doc = \" tag-sum struct + the generated `apply` dispatcher) is performed by monomorphization (`mono.rs`),\"] # [doc = \" which reuses this typing via re-inference (`infer_type`) — no new `Ty` variant here (the closure\"] # [doc = \" struct is an ordinary `Ty::Data` post-mono; §4A.6).\"] # [doc = \"\"] # [doc = \" **Multi-argument currying (M-822 / RFC-0024 §4A.5/§4A.8).** A multi-parameter lambda\"] # [doc = \" `lambda(p1: A, p2: B, …, pN: Z) => body` is treated as curried:\"] # [doc = \" `lambda(p1: A) => lambda(p2: B) => … => lambda(pN: Z) => body`. This is the RFC-0024 §4A.5\"] # [doc = \" partial-application path — each arrow in the curried chain lowers by the existing §4A single-arg\"] # [doc = \" machinery. Zero-parameter lambdas are a never-silent refusal (G2) — a zero-arg closure has\"] # [doc = \" no type without a unit type, which is a separate surface decision.\"] # [doc = \"\"] # [doc = \" **Bidirectional.** When the context supplies an expected `Ty::Fn(ea, er)`, the written param\"] # [doc = \" type must equal `ea` (a mismatch is a never-silent refusal, not a coercion), and the body is\"] # [doc = \" checked against `er` so a bare-decimal in the body takes its width from the codomain. A\"] # [doc = \" non-arrow expected type for a lambda is an explicit refusal.\"] fn check_lambda (& self , scope : & mut Vec < (String , Ty) > , params : & [Param] , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { if params . is_empty () { return self . err (\"a `lambda` requires at least 1 parameter — a zero-argument lambda has no type \\\n                 without a unit/nullary type, which is a separate surface decision (never a silent \\\n                 accept — G2)\" . to_owned () ,) ; } if params . len () > 1 { let (first , rest) = params . split_first () . expect (\"len > 1\") ; let inner_body = Expr :: Lambda { params : rest . to_vec () , body : Box :: new (body . clone ()) , } ; return self . check_lambda (scope , std :: slice :: from_ref (first) , & inner_body , expected) ; } let [param] = params else { unreachable ! (\"len == 1 after the multi-arg and zero-arg branches above\") } ; let (param_ty , _) = resolve_ty (self . site , self . types , self . tyvars , & param . ty) ? ; let expected_ret : Option < Ty > = match expected { Some (Ty :: Fn (ea , er)) => { if ea . as_ref () != & param_ty { return self . err (format ! (\"this `lambda`'s parameter `{}` has type `{param_ty}`, but the context \\\n                         expects a `{ea} => {er}` (arrow-domain mismatch — RFC-0024 §4A.6, never a \\\n                         silent coercion)\" , param . name)) ; } Some (er . as_ref () . clone ()) } Some (other) => { return self . err (format ! (\"a `lambda` has function type, but the context expects `{other}` (a `lambda` is \\\n                     not a `{other}` — RFC-0024 §4A.6, never a silent coercion)\")) ; } None => None , } ; scope . push ((param . name . clone () , param_ty . clone ())) ; self . affine . push (& param_ty) ; let r = self . check (scope , body , expected_ret . as_ref ()) ; scope . pop () ; self . affine . pop () ; let (body_ty , body2) = r ? ; if let Some (er) = & expected_ret { if er != & body_ty { return self . err (format ! (\"this `lambda`'s body has type `{body_ty}`, but the context expects the codomain \\\n                     `{er}` (arrow-codomain mismatch — RFC-0024 §4A.6, never a silent coercion)\")) ; } } Ok ((Ty :: Fn (Box :: new (param_ty) , Box :: new (body_ty)) , Expr :: Lambda { params : params . to_vec () , body : Box :: new (body2) , } ,)) } fn check_if (& self , scope : & mut Vec < (String , Ty) > , cond : & Expr , conseq : & Expr , alt : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let bool_ty = Ty :: Data (\"Bool\" . to_owned () , vec ! []) ; let (c , cond2) = self . check (scope , cond , Some (& bool_ty)) ? ; if c != bool_ty { return self . err (format ! (\"if-condition must be Bool, got {c}\")) ; } let pre_branch = self . affine . snapshot () ; let (t , conseq2) = self . check (scope , conseq , expected) ? ; let post_conseq = self . affine . snapshot () ; self . affine . restore (& pre_branch) ; let (f , alt2) = self . check (scope , alt , expected . or (Some (& t))) ? ; self . affine . merge_alt (post_conseq) ; if t != f { return self . err (format ! (\"if-branches disagree: {}\" , edge_mismatch (\"else\" , & t , & f))) ; } Ok ((t , Expr :: If { cond : Box :: new (cond2) , conseq : Box :: new (conseq2) , alt : Box :: new (alt2) , } ,)) } fn check_swap (& self , scope : & mut Vec < (String , Ty) > , value : & Expr , target : & TypeRef , policy : & Path ,) -> Result < (Ty , Expr) , CheckError > { let (vty , value2) = self . check (scope , value , None) ? ; if ! matches ! (vty , Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Dense (_ , _)) { return self . err (format ! (\"swap source must be a representation type, got {vty}\")) ; } let (tty , _) = resolve_ty (self . site , self . types , self . tyvars , target) ? ; if ! matches ! (tty , Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Dense (_ , _)) { return self . err (format ! (\"swap target must be a representation type, got {tty}\")) ; } Ok ((tty , Expr :: Swap { value : Box :: new (value2) , target : target . clone () , policy : policy . clone () , } ,)) } # [doc = \" Type a `wild { body }` block — the **audited FFI floor** (M-661; RFC-0016 §8-Q6; LR-9/S6;\"] # [doc = \" ADR-014). Guarantee: **`Declared`** — this is a structural + audited *context* gate, never a\"] # [doc = \" theorem (VR-5). The rule (settled by the maintainer; RFC-0016 §8-Q6 amendment):\"] # [doc = \"\"] # [doc = \" 1. **Context gate.** A `wild` block is legal **only** inside a `@std-sys` nodule. In any other\"] # [doc = \"    nodule it is a hard [`CheckError`] — the audited FFI floor lives only in `std-sys` (LR-9),\"] # [doc = \"    never a silent escape from safe code (G2). This is a hard refusal, **not** a lint.\"] # [doc = \" 2. **Type by ascription, never synthesis.** The `wild` body is the **trusted/opaque FFI\"] # [doc = \"    escape** — it is **not** recursively type-checked (it conforms to the expected type; it is\"] # [doc = \"    *audited*, not *verified* — VR-5/ADR-014). So a result type must be supplied by the context\"] # [doc = \"    (`expected`); in a synthesis position the checker refuses with \\\"ascribe the `wild` block's\"] # [doc = \"    result type\\\" (never a guessed type — G2). The block then **has** that expected type.\"] # [doc = \" 3. **Effect source.** `wild` is the `ffi` effect source (M-660 binding): the enclosing fn must\"] # [doc = \"    declare `!{ffi}`. That is enforced separately, in the effect-coverage pass\"] # [doc = \"    ([`check_body_effect_coverage`]) — which credits a `wild` with performing `ffi` — so it\"] # [doc = \"    composes with the M-660 machinery rather than duplicating it here.\"] # [doc = \" 4. **Execution is staged.** There is no FFI host in v0, so `wild` *type-checks + gates + is\"] # [doc = \"    audited* now; actually *running* it elaborates to an explicit [`crate::elab::ElabError::Residual`]\"] # [doc = \"    (a future capability) — consistent with M-657/659/660 staging. The body is preserved\"] # [doc = \"    **verbatim** in the returned expression (opaque — no interior resolution).\"] fn check_wild (& self , body : & Expr , expected : Option < & Ty >) -> Result < (Ty , Expr) , CheckError > { if ! self . std_sys { return self . err (\"`wild` is denied outside a `@std-sys` nodule — the audited FFI floor lives only in \\\n                 `std-sys` (RFC-0016 §8-Q6, LR-9); never a silent escape — G2. Mark the nodule's \\\n                 header `@std-sys` to author the FFI floor.\" ,) ; } let Some (want) = expected else { return self . err (\"a `wild` block has no synthesizable type — its body is the trusted/opaque FFI escape \\\n                 (not type-checked, only audited; ADR-014/VR-5). Ascribe the `wild` block's result \\\n                 type — `(wild { … }) : Binary{8}` (a special form takes the ascription parenthesized) \\\n                 — or use it in a typed position (a fn body / a `let` with an annotation, e.g. \\\n                 `let v: Binary{8} = wild { … }`) — never a guess (G2).\" ,) ; } ; Ok ((want . clone () , Expr :: Wild (Box :: new (body . clone ())))) } # [doc = \" Type a `wrapping { <expr> }` block (RFC-0034 §10/§10.1; CU-5) — the named, explicit Axis-B\"] # [doc = \" modular-arithmetic opt-out. Two gates, both never-silent (G2):\"] # [doc = \"\"] # [doc = \" 1. **Structural gate ([`Self::gate_wrapping_body`]).** The body must be a **`Binary` add/sub/mul\"] # [doc = \"    arithmetic tree** — internal nodes are the surface ops whose kernel name is exactly\"] # [doc = \"    `bin.add`/`bin.sub`/`bin.mul` (the three [`mycelium_interp::prims::eval_wrapping`] supports —\"] # [doc = \"    surface `add_s`/`sub_s`/`mul_s`; the infix `+`/`-`/`*` are the *ternary* `trit.*`, refused\"] # [doc = \"    here), and leaves are variable/literal operands. Any other enclosed operation (a division, a\"] # [doc = \"    comparison, a **function call**, a `let`/`if`/`match`) is refused **up front**. Refusing calls\"] # [doc = \"    is load-bearing: it makes the region a call-free arithmetic tree, so the evaluator's dynamic\"] # [doc = \"    wrapping-region bracket ([`crate::eval`]) coincides exactly with the *lexical* block (the\"] # [doc = \"    opt-out is never ambient — it cannot leak through a call).\"] # [doc = \" 2. **Normal typing.** The body is then type-checked normally, which enforces the `Binary`-operand\"] # [doc = \"    contract and the **equal-width** requirement via [`prim_sig`] (a width mismatch, or a\"] # [doc = \"    non-`Binary` operand, is a never-silent [`CheckError`] there). The construct's type is the\"] # [doc = \"    body's type, **preserved** (`Binary{N}`).\"] # [doc = \"\"] # [doc = \" Guarantee posture: `wrapping` opts out of the *range* refusal only; the enclosed ops evaluate to\"] # [doc = \" `Declared`-tagged modular results at runtime (VR-5 — the developer's explicit opt-in, never a\"] # [doc = \" fabricated stronger claim). The static shape is width-preserving exactly as the non-wrapping\"] # [doc = \" `bin.add`/`bin.sub`/`bin.mul` (the modular semantics is a *runtime* mode, not a type change).\"] fn check_wrapping (& self , scope : & mut Vec < (String , Ty) > , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { self . gate_wrapping_body (body) ? ; let (ty , rbody) = self . check (scope , body , expected) ? ; Ok ((ty , Expr :: Wrapping (Box :: new (rbody)))) } # [doc = \" The structural gate for a `wrapping { … }` body (RFC-0034 §10.1): accept only a `Binary`\"] # [doc = \" add/sub/mul arithmetic tree — internal nodes are single-segment applications whose\"] # [doc = \" [`prim_kernel_name`] is `bin.add`/`bin.sub`/`bin.mul` (arity 2), leaves are `Path`/`Lit`\"] # [doc = \" operands — and refuse everything else with an explicit, teaching refusal (never-silent — G2).\"] # [doc = \" The operand types (`Binary`, equal width) are *not* checked here — that is the normal-typing\"] # [doc = \" pass's job ([`prim_sig`]); this gate only constrains the *shape* so the region is a call-free\"] # [doc = \" arithmetic tree (see [`Self::check_wrapping`]).\"] fn gate_wrapping_body (& self , e : & Expr) -> Result < () , CheckError > { match e { Expr :: Path (_) | Expr :: Lit (_) => Ok (()) , Expr :: App { head , args } => { if let Expr :: Path (p) = head . as_ref () { if p . 0 . len () == 1 && args . len () == 2 && matches ! (prim_kernel_name (& p . 0 [0]) , Some (\"bin.add\" | \"bin.sub\" | \"bin.mul\")) { self . gate_wrapping_body (& args [0]) ? ; return self . gate_wrapping_body (& args [1]) ; } } self . err (\"a `wrapping { … }` block accepts only an enclosed `Binary` add/sub/mul tree \\\n                     (surface `add_s`/`sub_s`/`mul_s` — `eval_wrapping` supports exactly \\\n                     `bin.add`/`bin.sub`/`bin.mul`; the infix `+`/`-`/`*` are the *ternary* ops and are \\\n                     not wrapping-eligible). This enclosed operation is not one of them — a modular \\\n                     arithmetic region cannot contain a different op or a function call (RFC-0034 \\\n                     §10.1; never-silent — G2).\" ,) } _ => self . err (\"a `wrapping { … }` block's body must be a `Binary` add/sub/mul arithmetic expression \\\n                 — its operands are variables/literals or nested add/sub/mul, never a \\\n                 `let`/`if`/`match`/`swap`/other form (RFC-0034 §10.1; the opt-out is a modular \\\n                 *arithmetic* region, not a general scope — never-silent, G2).\" ,) , } } # [doc = \" Type a `colony { hypha e1, …, hypha eN }` block (RFC-0008 §4.7; M-666). Every `hypha` body\"] # [doc = \" is type-checked under the **current** scope (RT1: hyphae share no state — each closes over\"] # [doc = \" the lexical environment by value, never over a mutable cell), and the colony's result type is\"] # [doc = \" the **last** hypha's type — the **RT2 spawn-order sequentialization**'s final observable\"] # [doc = \" (RFC-0008 §4.6 R1: the deterministic fragment's reference semantics is the sequentialization).\"] # [doc = \" The `expected` type, if any, applies to that last hypha (the colony's value); the earlier\"] # [doc = \" hyphae are checked with no expected. A colony must hold **≥ 1** hypha (defense-in-depth — the\"] # [doc = \" parser already requires it); an empty colony is an explicit refusal, never a silent unit.\"] # [doc = \"\"] # [doc = \" Honesty (Declared): this typing is the conservative v0 surface for RFC-0008 §4.7. With no\"] # [doc = \" product/tuple type in the v0 calculus, the colony cannot yet yield *all* hyphae's joined\"] # [doc = \" results as one heterogeneous value — that is later work (a join-result product; RFC-0008\"] # [doc = \" RT6/§4.7). The last-hypha-as-observable rule keeps the type honest and matches the RT2\"] # [doc = \" sequential reference; it never invents a product type or silently discards a type mismatch.\"] fn check_colony (& self , scope : & mut Vec < (String , Ty) > , hyphae : & [Hypha] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let Some ((last , leading)) = hyphae . split_last () else { return self . err (\"a `colony` must contain at least one `hypha` (RFC-0008 §4.7 — a colony is a \\\n                 grouping of *active* hyphae); an empty `colony { }` has no observable\" ,) ; } ; let mut checked : Vec < Hypha > = Vec :: with_capacity (hyphae . len ()) ; for h in leading { let forage = match & h . forage { Some (p) => Some (Box :: new (self . check_forage_policy (scope , p) ?)) , None => None , } ; let (_ty , body2) = self . check (scope , & h . body , None) ? ; checked . push (Hypha { forage , body : body2 , }) ; } let forage = match & last . forage { Some (p) => Some (Box :: new (self . check_forage_policy (scope , p) ?)) , None => None , } ; let (rty , last_body2) = self . check (scope , & last . body , expected) ? ; checked . push (Hypha { forage , body : last_body2 , }) ; Ok ((rty , Expr :: Colony (checked))) } # [doc = \" D-lite `@forage(policy)` policy check (RFC-0008 RT3; DN-63 §3.5; M-906/DN-70 D1). The\"] # [doc = \" D-lite subset narrows DN-63's open `policy: PlacementPolicy` expression sketch (§3.5) to a\"] # [doc = \" **literal binary bitmask**: each set bit `i` names one local worker candidate `worker-i`\"] # [doc = \" (DN-63 FLAG-13, narrowed for the subset, not resolved — the full node-level `Meta` signal\"] # [doc = \" set stays open, owned by the H2 `forage` maturity work, DN-70 §5 R-6). This is a\"] # [doc = \" *checker*-level narrowing, not a grammar one (`parse_hypha` accepts any expression),\"] # [doc = \" because the D-lite mechanism builds a `mycelium_select::SelectionPolicy` **statically**, at\"] # [doc = \" elaboration time, straight from the literal's bits (so `elaborate`/`elaborate_colony` and\"] # [doc = \" the L1 evaluator can agree identically on the DN-63 FLAG-14 empty-candidate refusal — see\"] # [doc = \" [`crate::elab`]'s `elab_colony` and [`crate::eval::ForageError`]). Reusing `mycelium-select`\"] # [doc = \" for an *arbitrary evaluated* Mycelium policy value is exactly the DN-70 §5 R-5 mechanized\"] # [doc = \" capture-and-set surface, explicitly deferred to H2. A non-literal or non-`Binary` policy is\"] # [doc = \" refused here, explicitly (never silently coerced or ignored — G2).\"] # [doc = \"\"] # [doc = \" Guarantee: `Declared` — the literal-bitmask convention is this D-lite subset's own,\"] # [doc = \" narrower-than-DN-63 choice (FLAG, per the M-906 task brief: \\\"uses the existing\"] # [doc = \" SelectionPolicy — no new mechanism\\\"; the mechanism itself is unchanged, only its D-lite\"] # [doc = \" input surface is narrowed).\"] fn check_forage_policy (& self , scope : & mut Vec < (String , Ty) > , policy : & Expr ,) -> Result < Expr , CheckError > { let (ty , policy2) = self . check (scope , policy , None) ? ; if ! matches ! (ty , Ty :: Binary (_)) { return self . err (format ! (\"`@forage(policy)` requires a binary-bitmask policy in the D-lite subset (got \\\n                 `{ty}`) — each set bit names one local worker candidate (DN-63 §3.5 FLAG-13, \\\n                 narrowed; RFC-0008 RT3); the general `PlacementPolicy` expression surface is the \\\n                 DN-70 §5 R-5 H2 mechanized-capture work (never-silent — G2)\")) ; } if ! matches ! (policy2 , Expr :: Lit (Literal :: Bin (_))) { return self . err (\"`@forage(policy)` requires a *literal* binary-bitmask policy in the D-lite \\\n                 subset (e.g. `@forage(0b101) hypha …`) — a computed `Binary` expression is not \\\n                 yet supported; the general dynamic-policy surface is the DN-70 §5 R-5 H2 \\\n                 mechanized `SelectionPolicy` capture-and-set work (never-silent — G2)\" . to_owned () ,) ; } Ok (policy2) } # [doc = \" DN-58 §A (M-667, M-965): `fuse(a, b)` — lawful binary merge over the `Fuse` semilattice\"] # [doc = \" instance carried by the type. Both operands must have the same type `T`; the result type is\"] # [doc = \" also `T`. Repr types (Binary/Ternary/Dense/Bytes/Seq) are always fusible — the semilattice\"] # [doc = \" join is bitwise-and for Binary/Ternary (commutative/associative/idempotent by semantics —\"] # [doc = \" Empirical). Named Data types require a `Fuse` trait instance; absent one the error is\"] # [doc = \" never-silent (G2).\"] # [doc = \"\"] # [doc = \" Guarantee: `Empirical` — the type-homogeneity check is Exact; the \\\"repr types are fusible\\\"\"] # [doc = \" claim is Empirical (bitwise-and semilattice laws are property-tested, not mechanized-Proven\"] # [doc = \" here). The Data-type Fuse-instance check is Declared (trait registry lookup, not a proof) —\"] # [doc = \" **unless** [`crate::fuse::check_fuse_laws`] (M-965 F-A2) exhaustively verified the instance's\"] # [doc = \" `join` (a finite nullary-constructor `for_ty`), in which case that instance's laws are\"] # [doc = \" `Empirical`-checked, not merely Declared (VR-5: the checker runs at registration, before this\"] # [doc = \" `fuse`-call site is ever reached, so a law-violating instance never gets this far — it was\"] # [doc = \" already refused when it was `impl`ed).\"] # [doc = \" **Landed (M-965):** F-A1 — `Fuse` is now a built-in prelude trait\"] # [doc = \" (`crate::fuse::prelude`), so a program `impl Fuse[T] for T { … }`s directly, with no\"] # [doc = \" `trait Fuse` declaration of its own. F-A2 — the semilattice law checker\"] # [doc = \" (`crate::fuse::check_fuse_laws`) is wired into `check_nodule_with`'s impl pass; it is\"] # [doc = \" exhaustive only over a **finite, enumerable** `for_ty` (a nullary-constructor Data type) —\"] # [doc = \" a composite/parametric `for_ty`'s laws are honestly left unchecked (DN-58 §A.6 F-A3,\"] # [doc = \" deferred), never silently assumed (VR-5).\"] fn check_fuse (& self , scope : & mut Vec < (String , Ty) > , left : & Expr , right : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let (lty , left2) = self . check (scope , left , expected) ? ; let (rty , right2) = self . check (scope , right , Some (& lty)) ? ; if lty != rty { return self . err (format ! (\"`fuse` requires both operands to have the same type; left is `{lty}`, right is \\\n                 `{rty}` (DN-58 §A.4 — the lawful-merge instance is per type; RFC-0008 RT6; \\\n                 never-silent — G2)\")) ; } let fusible = matches ! (& lty , Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Dense (_ , _) | Ty :: Bytes | Ty :: Seq (_ , _)) ; if ! fusible { let has_fuse_instance = if let Some (head) = type_head (& lty) { self . instances . contains_key (& (\"Fuse\" . to_owned () , head . clone ())) } else { false } ; if ! has_fuse_instance { return self . err (format ! (\"`fuse` requires a `Fuse` semilattice instance for `{lty}` — `Fuse` is a \\\n                     built-in prelude trait (M-965 F-A1), so declare \\\n                     `impl Fuse[{lty}] for {lty} {{ fn join(a: {lty}, b: {lty}) => {lty} = … }}` \\\n                     with a commutative/associative/idempotent `join`; RFC-0008 RT6 / DN-58 §A.4 \\\n                     (never-silent — G2). If `{lty}` is a finite nullary-constructor type, the \\\n                     three semilattice laws are exhaustively checked at `impl` time (M-965 F-A2) — \\\n                     a law violation refuses right there, before this `fuse` call is ever reached.\")) ; } } Ok ((lty , Expr :: Fuse { left : Box :: new (left2) , right : Box :: new (right2) , } ,)) } # [doc = \" DN-58 §B (M-667): `reclaim(policy) { body }` — attach a reified reclamation/supervision\"] # [doc = \" policy to a structured scope. `policy` is any expression (the policy type is open in v0 —\"] # [doc = \" FLAG F-B2: the `SupervisionPolicy` surface type is not yet committed in the type system);\"] # [doc = \" `body` is the supervised expression. The result type is the body's type. Never-silent on an\"] # [doc = \" ill-typed `policy` or `body` (G2). Guarantee: `Empirical` (M-713 property-tested).\"] fn check_reclaim (& self , scope : & mut Vec < (String , Ty) > , policy : & Expr , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let (_ , policy2) = self . check (scope , policy , None) ? ; let (bty , body2) = self . check (scope , body , expected) ? ; Ok ((bty , Expr :: Reclaim { policy : Box :: new (policy2) , body : Box :: new (body2) , } ,)) } # [doc = \" M-826 — check a tuple literal `(a, b, …)` (arity ≥ 2).\"] # [doc = \"\"] # [doc = \" Desugars to a constructor application on the synthetic `Tuple$N<A, B, …>` data type\"] # [doc = \" (KC-3: no new L0 node). Each element is checked, then the whole is rewritten to an\"] # [doc = \" equivalent `Expr::App { head: Path(\\\"MkTuple$N\\\"), args }` so the downstream mono/elab passes\"] # [doc = \" see only ordinary data-constructor applications. Guarantee: `Empirical` (round-trip tested).\"] # [doc = \"\"] # [doc = \" Never-silent (G2):\"] # [doc = \" - Arity < 2 cannot arise (the parser rejects it).\"] # [doc = \" - If an expected type is provided and disagrees with the inferred `Tuple$N<…>`, an explicit\"] # [doc = \"   mismatch error is returned.\"] fn check_tuple_lit (& self , scope : & mut Vec < (String , Ty) > , elems : & [Expr] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let n = elems . len () ; debug_assert ! (n >= 2 , \"parser must reject 0- and 1-tuples\") ; let tname = tuple_type_name (n) ; let ctor_name = tuple_ctor_name (n) ; let expected_elems : Option < Vec < Ty > > = match expected { Some (Ty :: Data (tn , targs)) if * tn == tname && targs . len () == n => Some (targs . clone ()) , _ => None , } ; let mut elem_tys = Vec :: with_capacity (n) ; let mut rebuilt = Vec :: with_capacity (n) ; for (i , el) in elems . iter () . enumerate () { let exp_i = expected_elems . as_ref () . map (| v | & v [i]) ; let (ety , el2) = self . check (scope , el , exp_i) ? ; elem_tys . push (ety) ; rebuilt . push (el2) ; } let result_ty = Ty :: Data (tname . clone () , elem_tys) ; if let Some (exp) = expected { if exp != & result_ty { return self . err (format ! (\"tuple literal has type `{result_ty}`, but the context expects `{exp}` \\\n                     (M-826 — never a silent coercion, G2)\")) ; } } let head = Expr :: Path (Path (vec ! [ctor_name])) ; Ok ((result_ty , app_node (& head , rebuilt))) } fn check_ascribe (& self , scope : & mut Vec < (String , Ty) > , inner : & Expr , t : & TypeRef ,) -> Result < (Ty , Expr) , CheckError > { let (want , _) = resolve_ty (self . site , self . types , self . tyvars , t) ? ; let (ity , inner2) = self . check (scope , inner , Some (& want)) ? ; if ity != want { return self . err (format ! (\"ascription: {}\" , edge_mismatch (\"expression\" , & want , & ity))) ; } Ok ((want , Expr :: Ascribe (Box :: new (inner2) , t . clone ()))) } fn check_app (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , args : & [Expr] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let Expr :: Path (p) = head else { let (hty , h2) = self . check (scope , head , None) ? ; let Ty :: Fn (param_ty , ret_ty) = hty else { return self . err (format ! (\"application head is not a function — the expression before `(…)` has type \\\n                     `{hty}`, which is not callable (M-826 §Part2 — never silent, G2)\")) ; } ; if args . len () != 1 { return self . err (format ! (\"higher-order application requires exactly 1 argument in stage-1; \\\n                     got {} — partial application / multi-arg HOF is deferred (RFC-0024 §5, G2)\" , args . len ())) ; } let (got , a2) = self . check (scope , & args [0] , Some (& param_ty)) ? ; if got != * param_ty { return self . err (format ! (\"higher-order call: argument has type `{got}` but callee expects `{param_ty}` \\\n                     (arrow-type mismatch — M-826, never a silent coercion, G2)\")) ; } return Ok ((* ret_ty , app_node (& h2 , vec ! [a2]))) ; } ; if p . 0 . len () != 1 { return self . err (format ! (\"dotted call `{}` does not resolve — multi-segment qualified-path *syntax* is \\\n                 deferred in v0; `use {}` and call by its final segment (M-662)\" , p . 0 . join (\".\") , p . 0 . join (\".\"))) ; } let name = & p . 0 [0] ; if let Some ((_ , Ty :: Fn (param_ty , ret_ty))) = scope . iter () . rev () . find (| (n , _) | n == name) { let param_ty = param_ty . as_ref () . clone () ; let ret_ty = ret_ty . as_ref () . clone () ; if args . len () != 1 { return self . err (format ! (\"`{name}` has function type and takes exactly 1 argument in stage-1; \\\n                     got {} (partial application / multi-arg HOF is deferred — RFC-0024 §5, \\\n                     never a silent coercion)\" , args . len ())) ; } let (got , a2) = self . check (scope , & args [0] , Some (& param_ty)) ? ; if got != param_ty { return self . err (format ! (\"`{name}` has type `{param_ty} -> {ret_ty}`; argument has type `{got}` \\\n                     (arrow-type mismatch — RFC-0024 §3, never a silent coercion)\")) ; } return Ok ((ret_ty , app_node (head , vec ! [a2]))) ; } if let Some (fd) = self . fns . get (name) { if fd . sig . value_params . len () != args . len () { return self . err (format ! (\"`{name}` takes {} argument(s), got {}\" , fd . sig . value_params . len () , args . len ())) ; } if fd . sig . params . is_empty () { let baked = self . imports . resolved_fn_sigs . get (name) ; if baked . is_none () && self . imports . cross_phylum_fns . contains (name) { if let Some (concrete) = foreign_fn_sig_names_a_concrete_type (& fd . sig) { return self . err (format ! (\"`{name}` — the foreign (cross-phylum) fn's signature mentions a \\\n                             concrete type `{concrete}` beyond its own generic parameter(s), and \\\n                             its signature could not be pre-resolved against its declaring phylum \\\n                             (`resolve_fn_sig` only bakes a signature that references no type the \\\n                             declaring nodule itself imports — best-effort, never a hard error there); \\\n                             a bare-name reference to `{concrete}` here cannot be verified against \\\n                             the correct phylum's `{concrete}` and would risk the M-1036/DN-112 \\\n                             bare-name collapse one level up (DN-113 §7 / DN-122 residual; refused \\\n                             rather than silently resolved against a possibly-wrong phylum's \\\n                             registry, G2/VR-5).\")) ; } } let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (i , (pm , a)) in fd . sig . value_params . iter () . zip (args) . enumerate () { let want = match baked . and_then (| (params , _) | params . get (i)) { Some (w) => w . clone () , None => resolve_ty (self . site , self . types , & [] , & pm . ty) ? . 0 , } ; let (got , a2) = self . check (scope , a , Some (& want)) ? ; if want != got { return self . err (format ! (\"`{name}` parameter `{}`: {}\" , pm . name , edge_mismatch (\"argument\" , & want , & got))) ; } rebuilt . push (a2) ; } let ret = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & [] , & fd . sig . ret) ? . 0 , } ; return Ok ((ret , app_node (head , rebuilt))) ; } return self . check_app_generic_fn (scope , head , name , fd , args) ; } if let Some ((d , i)) = self . ctor (name) { self . check_ctor_seal (name , & d . name) ? ; let arity = d . ctors [i] . fields . len () ; let dname = qualify_type_name (& d . home , & d . name) ; let params = d . params . clone () ; if arity != args . len () { return self . err (format ! (\"constructor `{name}` takes {arity} field(s), got {} (W6 saturation)\" , args . len ())) ; } if params . is_empty () { let fields = d . ctors [i] . fields . clone () ; let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (want , a) in fields . iter () . zip (args) { let (got , a2) = self . check (scope , a , Some (want)) ? ; if want != & got { return self . err (format ! (\"constructor `{name}` field: {}\" , edge_mismatch (\"argument\" , want , & got))) ; } rebuilt . push (a2) ; } return Ok ((Ty :: Data (dname , vec ! []) , app_node (head , rebuilt))) ; } let fields = d . ctors [i] . fields . clone () ; return self . check_app_generic_ctor (scope , head , name , dname , params , fields , args , expected) ; } if self . is_trait_method (name) { return self . check_trait_method_call (scope , head , name , args , expected) ; } if let Some (err) = self . imports . ambiguity_error (self . site , name) { return Err (err) ; } if matches ! (name . as_str () , \"eq\" | \"lt\" | \"lt_s\") { if args . len () != 2 { return self . err (format ! (\"`{name}` takes two operands (got {}); RFC-0032 D1\" , args . len ())) ; } let mut typed : Vec < Option < (Ty , Expr) > > = vec ! [None , None] ; let mut anchor : Option < Ty > = None ; for (i , a) in args . iter () . enumerate () { if matches ! (a , Expr :: Lit (Literal :: AmbientInt (_ , _))) { continue ; } let (t , a2) = self . check (scope , a , None) ? ; if anchor . is_none () { anchor = Some (t . clone ()) ; } typed [i] = Some ((t , a2)) ; } let mut tys = Vec :: with_capacity (2) ; let mut rebuilt = Vec :: with_capacity (2) ; for (i , a) in args . iter () . enumerate () { let (t , a2) = match typed [i] . take () { Some (x) => x , None => { let anchor_ty = anchor . clone () . ok_or_else (| | { CheckError :: new (self . site , format ! (\"both operands of `{name}` are bare decimals, so neither pins a \\\n                                     width — and because comparison is width-collapsing (RFC-0032 D1) \\\n                                     the `Binary{{1}}` result cannot anchor them either. Ascribe one \\\n                                     operand or write it explicitly (RFC-0012 §4.3, never a default \\\n                                     width)\") ,) }) ? ; self . check (scope , a , Some (& anchor_ty)) ? } } ; tys . push (t) ; rebuilt . push (a2) ; } match (& tys [0] , & tys [1]) { (Ty :: Binary (x) , Ty :: Binary (y)) if x == y => { } (Ty :: Ternary (x) , Ty :: Ternary (y)) if x == y && name == \"lt_s\" => { return self . err (\"`lt_s` is the Binary two's-complement order (RFC-0033 §4.1.2; ADR-028); \\\n                         balanced ternary is inherently signed, so `lt` already orders it by \\\n                         signed value — use `lt`\" . to_owned () ,) ; } (Ty :: Ternary (x) , Ty :: Ternary (y)) if x == y => { } (Ty :: Float , Ty :: Float) => { return self . err (format ! (\"`{name}` is the RFC-0032 D1 Binary/Ternary comparison; float ordering \\\n                         is partial (NaN is unordered — ADR-040 §2.4), so floats have their own \\\n                         explicit prims: use `flt_lt`/`flt_le`/`flt_gt`/`flt_ge`/`flt_eq` (any \\\n                         NaN operand → false) or the named total order `flt_total_le` for \\\n                         sorting/keying (M-899)\")) ; } _ => { return self . err (format ! (\"`{name}` compares two equal-width operands of the same paradigm \\\n                         (Binary/Ternary), got {:?} and {:?} (RFC-0032 D1)\" , tys [0] , tys [1])) ; } } return Ok ((Ty :: Binary (Width :: Lit (1)) , app_node (head , rebuilt))) ; } if let Some (ret) = self . try_check_seq_bytes_prim (scope , head , name , args) ? { return Ok (ret) ; } if let Some (ret) = self . try_check_dense_prim (scope , head , name , args) ? { return Ok (ret) ; } if let Some (ret) = self . try_check_vsa_prim (scope , head , name , args) ? { return Ok (ret) ; } if let Some (ret) = self . try_check_float_prim (scope , head , name , args) ? { return Ok (ret) ; } let Some (fam) = prim_family (name) else { if let Some (rule) = self . lower_rules . get (name) { return self . check_sugar_call (head , name , rule , scope , args) ; } return self . err (teach_unknown (name , & format ! (\"unknown function/constructor/prim `{name}`\") ,)) ; } ; let mut typed : Vec < Option < (Ty , Expr) > > = vec ! [None ; args . len ()] ; let mut anchor : Option < u32 > = expected . and_then (| t | fam . width_of (t)) ; for (i , a) in args . iter () . enumerate () { if matches ! (a , Expr :: Lit (Literal :: AmbientInt (_ , _))) { continue ; } let (t , a2) = self . check (scope , a , None) ? ; if anchor . is_none () { anchor = fam . width_of (& t) ; } typed [i] = Some ((t , a2)) ; } let mut arg_tys = Vec :: with_capacity (args . len ()) ; let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (i , a) in args . iter () . enumerate () { let (t , a2) = match typed [i] . take () { Some (x) => x , None => { let w = anchor . ok_or_else (| | { CheckError :: new (self . site , format ! (\"UnresolvedWidth: a bare decimal operand of `{name}` has no width \\\n                                 here — no concrete operand or expected type pins it. Ascribe it, \\\n                                 or write it explicitly (RFC-0012 §4.3, never a default width)\") ,) }) ? ; self . check (scope , a , Some (& fam . ty (w))) ? } } ; arg_tys . push (t) ; rebuilt . push (a2) ; } match prim_sig (name , & arg_tys) { Some (ret) => Ok ((ret , app_node (head , rebuilt))) , None => self . err (format ! (\"`{name}` does not accept argument types {arg_tys:?} (T-Op; RFC-0007 §4.4)\")) , } } # [doc = \" **M-1054 Stage 1b — expression-position sugar-rule call-site ACCEPT** (DN-110 §5-A;\"] # [doc = \" DN-116, Option B — the def-time RHS type-scheme). A `lower`-declared rule with **value**\"] # [doc = \" parameters ([`LowerDecl::value_params`] — distinct from the `derive`-facing *type*\"] # [doc = \" parameters in [`LowerDecl::params`]) is recognized at an ordinary call site `Name(args)`,\"] # [doc = \" arity- and type-matched exactly like a user-fn call (DRY with the fn branch above — same\"] # [doc = \" `resolve_ty` + bidirectional `self.check` + [`edge_mismatch`] diagnostic shape), then —\"] # [doc = \" unlike Stage 0 — **accepted**, mirroring the ordinary user-fn accept branch above\"] # [doc = \" (`Ok((ret, app_node(head, rebuilt)))`).\"] # [doc = \"\"] # [doc = \" **Why a type-only accept is sound (Option B).** The checker has **no coercion**: every\"] # [doc = \" accept path in this module is an exact `want == got` match. So a monomorphic\"] # [doc = \" value-parametric rule's RHS result type is **fixed at definition**, never dependent on the\"] # [doc = \" calling site's own argument *nodes* — [`infer_expr_rule_rhs_type`] (shared with\"] # [doc = \" [`check_lower_rule_rhs_type`]'s definition-time validation) infers it **once**, from the\"] # [doc = \" value params bound to their *declared* types, giving the one answer every call site shares.\"] # [doc = \" This function TYPES the call; it never re-derives or re-expands the RHS — the hygienic\"] # [doc = \" **expansion** is [`crate::elab::Elab::app`]'s job (M-1054 Stage 1b §5.2 wiring), reusing the\"] # [doc = \" landed Stage 1 [`crate::elab::elaborate_lower_rule_with_args`] machinery. This honors the\"] # [doc = \" ratified two-phase split: L1 check-phase for typing/def-site/affine, L0 elab-phase for the\"] # [doc = \" hygienic expansion (never doubly-expanding, never dragging `%`-freshening into the checker).\"] # [doc = \"\"] # [doc = \" **Two honest, never-silent gates keep this accept sound (G2/VR-5) — see DN-116/DN-117:**\"] # [doc = \" - **Stage 3 (OQ-H4, affine soundness — DN-117, real linear check, landed).** A value\"] # [doc = \"   parameter (or RHS-local binding) with an affine surface no longer refuses *wholesale*.\"] # [doc = \"   Instead: [`Self::ty_structurally_contains_substrate`] is retained only as a cheap\"] # [doc = \"   **trigger** (DN-117 §3.2), together with a precise per-argument \\\"did checking this\"] # [doc = \"   argument touch the affine tracker at all\\\" signal (a strict superset of the trigger DN-117\"] # [doc = \"   §3.2's literal text names — needed to catch the \\\"affine-hiding-non-affine-type\\\" class,\"] # [doc = \"   DN-117 §2/§5 R3, that no *type*-based trigger alone can see). When triggered, the real\"] # [doc = \"   M-919 [`crate::affine::Tracker`] walks the **substituted** `Expr` — each argument spliced\"] # [doc = \"   at every unshadowed occurrence of its value param, the check-time-only analogue of\"] # [doc = \"   [`crate::elab::sugar_expand`]'s (B) substitution (see [`Self::stage3_substitute_expr`]) —\"] # [doc = \"   and the call is accepted **iff no [`UseOutcome::DoubleUse`] is produced**. An ordinary\"] # [doc = \"   (non-affine) argument may safely be spliced into the RHS at more than one occurrence (see\"] # [doc = \"   [`crate::elab::sugar_expand`]'s own doc comment — re-evaluation, not a soundness hazard,\"] # [doc = \"   for a pure v0 value); an *affine* argument may not — [`crate::elab::Elab::app`]'s ordinary\"] # [doc = \"   fn-call inlining `Let`-binds each argument **exactly once**, but Stage 1's (B) substitution\"] # [doc = \"   splices the raw argument node verbatim at **every** RHS occurrence of its value param, so\"] # [doc = \"   an affine value param genuinely used more than once would be double-consumed by expansion\"] # [doc = \"   alone — this is exactly what the substituted-`Expr` walk now catches precisely (accept\"] # [doc = \"   linear-or-dropped, refuse duplicated — DN-117 §4.3: **a dropped affine value param is\"] # [doc = \"   ACCEPTED**, not refused; the static pass enforces only the use-at-most-once upper bound,\"] # [doc = \"   the must-consume lower bound is an M-904 runtime concern, unchanged by this gate).\"] # [doc = \" - **Stage 2 (OQ-H1, def-site resolution).** Refuses an RHS identifier that is genuinely\"] # [doc = \"   **free** — neither a value parameter, an RHS-local binder, nor a same-nodule,\"] # [doc = \"   unambiguous top-level fn/constructor/`lower`-rule name (see [`Self::rhs_first_free_id`]).\"] # [doc = \"   Stage 1b is scoped to the single-nodule, params-and-globally-unambiguous-fns/ctors-only\"] # [doc = \"   fragment; cross-nodule / import-ambiguous def-site resolution is Stage 2's own job.\"] # [doc = \"\"] # [doc = \" An item-shaped rule (DN-54 §10 Model A — `derive`-only, no expression-position form at all)\"] # [doc = \" is refused with its own clearer diagnostic rather than falling through to arity matching.\"] fn check_sugar_call (& self , head : & Expr , name : & str , rule : & LowerDecl , scope : & mut Vec < (String , Ty) > , args : & [Expr] ,) -> Result < (Ty , Expr) , CheckError > { let crate :: ast :: LowerRhs :: Expr (rhs) = & rule . rhs else { return self . err (format ! (\"`{name}` is a `lower` rule with an item-shaped (`impl … for …`) RHS (DN-54 §10 \\\n                 Model A) — it has no expression-position form; apply it with `derive {name} for \\\n                 <Type>`, not `{name}(…)` (never a silent reinterpretation, G2)\")) ; } ; if rule . value_params . len () != args . len () { return self . err (format ! (\"`{name}` (a `lower` sugar rule) takes {} value parameter(s), got {} argument(s) \\\n                 — arity mismatch (M-1054 call-site recognition; never a silent \\\n                 truncation/padding, G2)\" , rule . value_params . len () , args . len ())) ; } let mut has_affine_surface = rule . value_params . iter () . any (| pm | { resolve_ty (self . site , self . types , & [] , & pm . ty) . map (| (pty , _) | { let mut visiting = BTreeSet :: new () ; self . ty_structurally_contains_substrate (& pty , & mut visiting) }) . unwrap_or (false) }) ; let pre_args_snapshot = self . affine . snapshot () ; let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (pm , a) in rule . value_params . iter () . zip (args) { let (want , _) = resolve_ty (self . site , self . types , & [] , & pm . ty) ? ; let pre_arg = self . affine . snapshot () ; let (got , a2) = self . check (scope , a , Some (& want)) ? ; if self . affine . snapshot () != pre_arg { has_affine_surface = true ; } if want != got { return self . err (format ! (\"`{name}` parameter `{}`: {}\" , pm . name , edge_mismatch (\"argument\" , & want , & got))) ; } rebuilt . push (a2) ; } let bound : Vec < String > = rule . value_params . iter () . map (| p | p . name . clone ()) . collect () ; if let Some (free) = self . rhs_first_free_id (rhs , & bound , false , 0) ? { if self . lower_rules . contains_key (free) { return self . err (format ! (\"`{name}`'s RHS references the `lower`-rule `{free}` in value position — a \\\n                     `lower` rule has no L0 form of its own to fall through to (M-1054 Stage 1b \\\n                     §5.2 wiring; DN-116) and resolves only when it is the head of a call \\\n                     (`{free}(...)`). Refused here, at check, with this specific diagnostic \\\n                     (DN-115 §4.2/G2) rather than falling through to a later, generic \\\"unknown \\\n                     name\\\" refusal from this rule's own RHS re-type-check — never-silent either \\\n                     way, but this names the actual mismatch (G2).\")) ; } return self . err (format ! (\"`{name}`'s RHS references `{free}`, which is neither a value parameter, an \\\n                 RHS-local binder, nor a same-nodule fn/constructor/prim/`lower`-rule (in \\\n                 call-head position) — M-1054 Stage 2 (OQ-H1) accepts only the single-nodule, \\\n                 params-and-globally-unambiguous-fns/ctors/prims/lower-rule-calls-only fragment; \\\n                 cross-nodule / ambiguous def-site resolution is Stage 4 work (DN-113/DN-115), not \\\n                 built yet (never a silent free-variable splice — G2).\")) ; } if has_affine_surface { self . affine . restore (& pre_args_snapshot) ; let params_for_subst : Vec < (String , Expr) > = rule . value_params . iter () . map (| p | p . name . clone ()) . zip (rebuilt . iter () . cloned ()) . collect () ; let mut shadow : Vec < (String , String) > = Vec :: new () ; let mut fresh = Stage3Fresh :: default () ; # [cfg_attr (not (test) , allow (unused_mut))] let mut substituted = self . stage3_substitute_expr (rhs , & params_for_subst , & mut shadow , & mut fresh , 0) ? ; # [cfg (test)] if self . stage3_sabotage_skip_substitution { substituted = rhs . clone () ; } let depth = scope . len () ; for pm in & rule . value_params { let (pty , _) = resolve_ty (self . site , self . types , & [] , & pm . ty) ? ; scope . push ((pm . name . clone () , pty . clone ())) ; self . affine . push (& pty) ; } let walk_result = self . check (scope , & substituted , None) ; scope . truncate (depth) ; self . affine . truncate (depth) ; walk_result . map_err (| e | { CheckError :: new (self . site , format ! (\"`{name}` (a `lower` sugar rule) rejects this call — M-1054 Stage 3 \\\n                         (OQ-H4/DN-117) real affine re-check over the substituted expansion: {}\" , e . message) ,) }) ? ; } let ret_ty = infer_expr_rule_rhs_type (rule , rhs , self . types , self . fns , self . traits , self . instances , self . imports , self . lower_rules ,) ? ; Ok ((ret_ty , app_node (head , rebuilt))) } # [doc = \" **M-1054 Stage 3 (OQ-H4/DN-117) gate — the Stage-3 *trigger*'s recursive core** — does `ty`\"] # [doc = \" **structurally contain** [`Ty::Substrate`] anywhere: at the top level, nested inside a\"] # [doc = \" registered [`Data`](Ty::Data) type's constructor fields (recursively, resolving each field's\"] # [doc = \" abstract [`Ty::Var`]s against `ty`'s own type arguments via [`subst_ty`]), or inside a\"] # [doc = \" [`Seq`](Ty::Seq) element / [`Fn`](Ty::Fn) argument-or-return position.\"] # [doc = \"\"] # [doc = \" **No longer the accept/refuse decision (DN-117 §3.2, superseding the earlier DN-116 §3.2\"] # [doc = \" design).** This predicate used to *refuse outright* any value parameter it matched; it is now\"] # [doc = \" one of two conditions [`Self::check_sugar_call`] OR's together to decide whether to run the\"] # [doc = \" real substituted-`Expr` affine walk (the other being a precise per-argument tracker-touch\"] # [doc = \" signal — that function's own doc comment) — the walk itself is what accepts or refuses. A\"] # [doc = \" `Data(\\\"Handle\\\")` whose sole constructor wraps a `Substrate` field (e.g. `Wrap(Substrate{gpu})`)\"] # [doc = \" still matches here even though `pty` itself is `Data(\\\"Handle\\\", [])`, not `Ty::Substrate` —\"] # [doc = \" this is exactly what makes the composite/nested-affine case (a caller's argument\"] # [doc = \" pattern-matched twice to extract the affine field) trigger the real walk, which then decides\"] # [doc = \" linear-use (accept) vs. duplication (refuse) precisely, rather than refusing every composite\"] # [doc = \" argument wholesale as the earlier design did.\"] # [doc = \"\"] # [doc = \" **Termination (never a silent infinite loop, G2).** `visiting` accumulates the [`Data`]\"] # [doc = \" **type names** currently on the walk's call stack (on-path cycle detection, not a permanent\"] # [doc = \" memo): re-entering a name already on the stack — a directly or mutually recursive `Data`\"] # [doc = \" type — returns `false` for that occurrence *without recursing further*, since any affine\"] # [doc = \" field reachable through that name was (or will be) found via the first, non-cyclic entry to\"] # [doc = \" it in the same walk. Because the type registry (`self.types`) is finite, the recursion depth\"] # [doc = \" is bounded by the number of distinct registered type names — no separate depth counter is\"] # [doc = \" needed (unlike the AST-walking helpers in this module, which bound *expression* nesting\"] # [doc = \" against [`MAX_CHECK_DEPTH`]; this walk bounds *type* nesting against the finite type\"] # [doc = \" registry instead).\"] # [doc = \"\"] # [doc = \" **Honest scope (VR-5) — not over-refusing.** A plain `Data` type with no affine field\"] # [doc = \" anywhere in its structure (including its own recursive occurrences) stays accepted: the walk\"] # [doc = \" returns `false` for it. An **abstract** field type (`Ty::Var` left unresolved because the\"] # [doc = \" owning `Data`'s parameter list and `ty`'s own argument list don't cover it — e.g. a\"] # [doc = \" value-parametric rule whose value-param type is itself an unsubstituted generic parameter)\"] # [doc = \" is conservatively treated as **not** containing `Substrate` (`Ty::Var(_) => false`): Stage 1b\"] # [doc = \" is the single-nodule, concrete-argument fragment (OQ-H1's own scope note), so a genuinely\"] # [doc = \" abstract value-parameter type is not yet reachable through this gate in practice; if that\"] # [doc = \" ever changes, this is the seam to revisit (FLAG, not silently widened).\"] # [doc = \"\"] # [doc = \" **M-1036 residual-close audit (B).** The `lookup_data(self.types, name)` call below (in the\"] # [doc = \" `Ty::Data` arm) is left unguarded — audited and confirmed **not exploitable** for a\"] # [doc = \" wrong-value silent *accept*, because this fn is (per this doc comment's own opening\"] # [doc = \" paragraph) only ever a **cheap pre-filter TRIGGER**, OR'd in [`Self::check_sugar_call`] with\"] # [doc = \" a second, fully **independent** precise signal: whether checking the *actual argument\"] # [doc = \" expression* at this call site touched the real affine tracker (`self.affine.snapshot() !=\"] # [doc = \" pre_arg`, computed via the ordinary — already home-check-guarded, per `resolve_ty`/\"] # [doc = \" `normalize_pattern`'s M-1036 fixes — `self.check` call, never through this fn's own lookup).\"] # [doc = \" The two possible mismatch directions are both harmless:\"] # [doc = \" - **Over-trigger** (a wrong `d` looks *more* Substrate-shaped than the real declared type):\"] # [doc = \"   the real substituted-`Expr` walk just runs unnecessarily and still decides correctly from\"] # [doc = \"   the real `Expr`, never this fn's structural read.\"] # [doc = \" - **Under-trigger** (a wrong `d` — e.g. a same-nodule shadow's `DataInfo` — looks *less*\"] # [doc = \"   Substrate-shaped, missing a field the real declared type has): the independent\"] # [doc = \"   per-argument tracker-touch signal still fires whenever the call site actually passes a\"] # [doc = \"   live affine value there, since that signal is computed from the argument's own real\"] # [doc = \"   checked shape, not from this fn's `d`. A trigger this fn alone would miss is exactly the\"] # [doc = \"   \\\"affine-hiding-non-affine-type\\\" class DN-117 already designed the OR'd signal to catch\"] # [doc = \"   (see the class comment above `has_affine_surface`'s definition) — so a shadow-mismatch here\"] # [doc = \"   is strictly narrower than a gap the design already covers, not a new one.\"] # [doc = \"\"] # [doc = \" Guarantee: `Declared` (reasoned, not mechanized) — this fn's own accept/refuse authority is\"] # [doc = \" itself only `Declared`/heuristic by design (it is a trigger, not the decision — see this\"] # [doc = \" comment's opening paragraph); the real decision (the substituted-`Expr` walk) carries its own\"] # [doc = \" separate guarantee.\"] fn ty_structurally_contains_substrate (& self , ty : & Ty , visiting : & mut BTreeSet < String >) -> bool { match ty { Ty :: Substrate (_) => true , Ty :: Data (name , args) => { if args . iter () . any (| a | self . ty_structurally_contains_substrate (a , visiting)) { return true ; } if ! visiting . insert (name . clone ()) { return false ; } let found = match lookup_data (self . types , name) { Some (info) => { let subst : BTreeMap < String , Ty > = info . params . iter () . cloned () . zip (args . iter () . cloned ()) . collect () ; info . ctors . iter () . any (| ctor | { ctor . fields . iter () . any (| fty | { let resolved = subst_ty (fty , & subst) ; self . ty_structurally_contains_substrate (& resolved , visiting) }) }) } None => false , } ; visiting . remove (name) ; found } Ty :: Seq (elem , _) => self . ty_structurally_contains_substrate (elem , visiting) , Ty :: Fn (param , ret) => { self . ty_structurally_contains_substrate (param , visiting) || self . ty_structurally_contains_substrate (ret , visiting) } Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Dense (_ , _) | Ty :: Vsa { .. } | Ty :: Bytes | Ty :: Float | Ty :: Var (_) => false , } } # [doc = \" **M-1054 Stage 1b/2 Stage-2 gate helper (OQ-H1)** — depth-first, left-to-right search for\"] # [doc = \" the first RHS identifier that is genuinely **free**: not `bound` (the value parameters, plus\"] # [doc = \" every RHS-local binder introduced by an ancestor `let`/`lambda`/`for`/match-arm-pattern), and\"] # [doc = \" not a same-nodule, unambiguous top-level `fn`, constructor name (a bare nullary-constructor\"] # [doc = \" `Var` reference, or any constructor named in call-head position, resolves through the\"] # [doc = \" ordinary global namespace exactly like a `fn` reference does — [`crate::elab::Elab::expr`]'s\"] # [doc = \" own `Expr::Path` handling treats them identically), or recognized prim\"] # [doc = \" ([`prim_name_is_recognized`] — DN-115 §4.1/G1). `None` means every RHS identifier is within\"] # [doc = \" the accepted fragment; `Some(name)` names the first offender.\"] # [doc = \"\"] # [doc = \" **`call_head` (DN-115 §4.2/G2) — a nullary `lower`-rule resolves only in call-head\"] # [doc = \" position.** `true` iff `e` is exactly the `head` of an enclosing [`Expr::App`] (mirroring\"] # [doc = \" [`crate::elab::Elab::app`]'s own dispatch, `elab.rs`, which pattern-matches\"] # [doc = \" `Expr::App{head: Expr::Path(..), ..}` **literally** and has no `lower_rules` lookup in its\"] # [doc = \" value-position `Expr::Path` arm — `Elab::app` is the only place that consults `lower_rules`\"] # [doc = \" at all). This gate previously accepted a bare (non-call, value-position) reference to a\"] # [doc = \" nullary `lower`-rule name position-agnostically; it now **narrows** (KISS, not the\"] # [doc = \" alternative of extending elab's `Expr::Path` arm — DN-115 §4.2) to name the mismatch with\"] # [doc = \" its own clear, check-phase diagnostic. **Honesty note (VR-5):** the narrowing is a\"] # [doc = \" diagnostic-quality + gate/mechanism-self-consistency fix, not a closed soundness gap — a\"] # [doc = \" bare reference was *already* refused before this fix too (via a later, generic \\\"unknown\"] # [doc = \" name\\\" error from this rule's own RHS re-type-check, `infer_expr_rule_rhs_type` →\"] # [doc = \" `Cx::check_path`, which never resolves a `lower`-rule name either); see DN-115's\"] # [doc = \" Ratification section for the adversarial verification. Every non-`Expr::Path` recursive\"] # [doc = \" call passes `false` — no other syntactic position is ever a call head.\"] # [doc = \"\"] # [doc = \" **Single-nodule def-site-resolution invariant (DN-115 §3).** This gate is a pure\"] # [doc = \" *admissibility* test — it does not itself resolve anything. Every id it accepts is resolved\"] # [doc = \" by Pass-1 elaboration ([`crate::elab::elaborate_value_parametric_rule_inner`]) against the\"] # [doc = \" **def-site** global env, which coincides with the **use-site** env only because a `lower`\"] # [doc = \" rule and its call today live in the **same nodule** (no cross-nodule import is consulted —\"] # [doc = \" `self.imports` is never read here). That coincidence is what makes Pass-1's use-env lookup\"] # [doc = \" def-site-correct; Stage 4's cross-nodule case (where the two envs diverge) must thread an\"] # [doc = \" explicit def-site env rather than silently reuse this same mechanism (G2) — this gate keeps\"] # [doc = \" refusing every genuinely cross-nodule/phylum free id so that widening is deliberate, never\"] # [doc = \" silent.\"] # [doc = \"\"] # [doc = \" Exhaustively recurses into every [`Expr`] child so no subtree is silently skipped (G2) — an\"] # [doc = \" unrecognized/future `Expr` variant is a compile error here (no wildcard arm), not a silent\"] # [doc = \" gap. Depth-bounded by [`MAX_CHECK_DEPTH`] like every other recursive walk in this module.\"] fn rhs_first_free_id < 'e > (& self , e : & 'e Expr , bound : & [String] , call_head : bool , depth : u32 ,) -> Result < Option < & 'e str > , CheckError > { if depth > MAX_CHECK_DEPTH { return Err (CheckError :: new (self . site , format ! (\"expression nesting exceeds the checker depth budget ({MAX_CHECK_DEPTH}) — a \\\n                     pathologically-nested `lower` rule RHS (M-1054 Stage 1b free-identifier walk)\") ,)) ; } let d = depth + 1 ; match e { Expr :: Path (p) if p . 0 . len () == 1 => { let name = p . 0 [0] . as_str () ; if bound . iter () . any (| b | b == name) { return Ok (None) ; } if self . lower_rules . contains_key (name) { return if call_head { Ok (None) } else { Ok (Some (name)) } ; } if self . fns . contains_key (name) || self . ctor (name) . is_some () || prim_name_is_recognized (name) { return Ok (None) ; } Ok (Some (name)) } Expr :: Path (_) | Expr :: Lit (_) => Ok (None) , Expr :: Let { bound : b , body , name , .. } => { if let Some (f) = self . rhs_first_free_id (b , bound , false , d) ? { return Ok (Some (f)) ; } let mut bound2 = bound . to_vec () ; bound2 . push (name . clone ()) ; self . rhs_first_free_id (body , & bound2 , false , d) } Expr :: If { cond , conseq , alt } => { for sub in [cond , conseq , alt] { if let Some (f) = self . rhs_first_free_id (sub , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: Match { scrutinee , arms } => { if let Some (f) = self . rhs_first_free_id (scrutinee , bound , false , d) ? { return Ok (Some (f)) ; } for arm in arms { let mut bound2 = bound . to_vec () ; collect_pattern_binders (& arm . pattern , & mut bound2) ; if let Some (f) = self . rhs_first_free_id (& arm . body , & bound2 , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: For { x , xs , acc , init , body , } => { if let Some (f) = self . rhs_first_free_id (xs , bound , false , d) ? { return Ok (Some (f)) ; } if let Some (f) = self . rhs_first_free_id (init , bound , false , d) ? { return Ok (Some (f)) ; } let mut bound2 = bound . to_vec () ; bound2 . push (x . clone ()) ; bound2 . push (acc . clone ()) ; self . rhs_first_free_id (body , & bound2 , false , d) } Expr :: Lambda { params , body } => { let mut bound2 = bound . to_vec () ; bound2 . extend (params . iter () . map (| p | p . name . clone ())) ; self . rhs_first_free_id (body , & bound2 , false , d) } Expr :: App { head , args } => { if let Some (f) = self . rhs_first_free_id (head , bound , true , d) ? { return Ok (Some (f)) ; } for a in args { if let Some (f) = self . rhs_first_free_id (a , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: Fuse { left , right } => { for sub in [left , right] { if let Some (f) = self . rhs_first_free_id (sub , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: Reclaim { policy , body } => { for sub in [policy , body] { if let Some (f) = self . rhs_first_free_id (sub , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: Swap { value , .. } => self . rhs_first_free_id (value , bound , false , d) , Expr :: WithParadigm { body , .. } | Expr :: Wild (body) | Expr :: Spore (body) | Expr :: Wrapping (body) | Expr :: Consume (body) | Expr :: Try (body) => self . rhs_first_free_id (body , bound , false , d) , Expr :: Ascribe (inner , _) => self . rhs_first_free_id (inner , bound , false , d) , Expr :: TupleLit (items) => { for it in items { if let Some (f) = self . rhs_first_free_id (it , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } Expr :: Colony (hyphae) => { for h in hyphae { if let Some (forage) = & h . forage { if let Some (f) = self . rhs_first_free_id (forage , bound , false , d) ? { return Ok (Some (f)) ; } } if let Some (f) = self . rhs_first_free_id (& h . body , bound , false , d) ? { return Ok (Some (f)) ; } } Ok (None) } } } # [doc = \" **M-1054 Stage 3 (OQ-H4), DN-117 §2 step 1 — the check-time-only substituted `Expr`\"] # [doc = \" builder.** The `Expr`-level analogue of [`crate::elab::sugar_expand`]'s (B) substitution\"] # [doc = \" (which operates on L0 `Node`s at *elaboration* time): splices each `params[i].1` (an\"] # [doc = \" already-checked argument `Expr`) at every **unshadowed** occurrence of `params[i].0` (a\"] # [doc = \" value-parameter name) inside `e`. The result is discarded after the Stage-3 affine verdict\"] # [doc = \" ([`Self::check_sugar_call`]) — Option B (DN-116) is untouched; `Elab::app` still re-derives\"] # [doc = \" the real expansion independently at L0 (no double-expansion, DRY).\"] # [doc = \"\"] # [doc = \" **Shadowing (B).** An occurrence bound by an inner `let`/`match`-arm/`for`/`lambda` binder of\"] # [doc = \" the same spelling as a value param is *not* substituted — it refers to that inner binder, not\"] # [doc = \" the param (mirrors [`crate::elab::sugar_expand`]'s own unshadowed-occurrence logic,\"] # [doc = \" `elab.rs:942`).\"] # [doc = \"\"] # [doc = \" **(A)-style fresh-renaming of RHS-local binders — a necessary refinement beyond DN-117 §2's\"] # [doc = \" literal (B)-only text (flagged in this leaf's report/ratification, not silently added).**\"] # [doc = \" Every RHS-local binder this walk introduces (`Let`/`Lambda`/`For`/match-arm pattern idents)\"] # [doc = \" is renamed to a fresh, `%`-prefixed synthetic name (guaranteed disjoint from any surface\"] # [doc = \" spelling — `%` is lexer-illegal, `is_ident_start`/`is_ident_continue`), exactly mirroring\"] # [doc = \" [`crate::elab::sugar_expand`]'s own (A) step. **Why this is needed, not optional:** without\"] # [doc = \" it, a spliced argument's own free variable (e.g. a caller's `s` inside `consume s`) could be\"] # [doc = \" *captured* by an RHS-local binder that coincidentally shares its spelling (e.g.\"] # [doc = \" `let s = 5 in p`) — the substituted term would then wrongly resolve the spliced `s` to the\"] # [doc = \" RHS's own local `s`, producing a spurious type-mismatch refusal (a false REFUSE of a\"] # [doc = \" linear-use ACCEPT case) rather than the correct verdict. This capture hazard cannot arise for\"] # [doc = \" the *real*, elaboration-time expansion (Stage 1's own (A) freshening already prevents it\"] # [doc = \" there) — it is specific to this check-time-only, separately-built substituted term, so it\"] # [doc = \" needs its own (A) step. `%`-renaming a `Let`/`Lambda`/`For` binder is always unambiguously\"] # [doc = \" safe (those never denote anything but a fresh binder); a match-arm pattern identifier that is\"] # [doc = \" genuinely ambiguous between \\\"binder\\\" and \\\"nullary constructor reference\\\" without a scrutinee\"] # [doc = \" type cannot be safely `%`-renamed either way — see\"] # [doc = \" [`Self::stage3_substitute_pattern`]'s own doc comment: this ambiguous case now **refuses the\"] # [doc = \" whole sugar call** (fixed 2026-07-11, a CRITICAL false-accept found by adversarial review;\"] # [doc = \" FLAG-pattern-ctor-collision, DN-117 ratification note) rather than leaving the binder\"] # [doc = \" unrenamed, which is exactly the capture hazard this paragraph describes.\"] # [doc = \"\"] # [doc = \" Depth-bounded by [`MAX_CHECK_DEPTH`] like every other recursive walk in this module (never a\"] # [doc = \" silent stack overflow on a pathological RHS, G2).\"] # [allow (clippy :: too_many_arguments)] fn stage3_substitute_expr (& self , e : & Expr , params : & [(String , Expr)] , shadow : & mut Vec < (String , String) > , fresh : & mut Stage3Fresh , depth : u32 ,) -> Result < Expr , CheckError > { if depth > MAX_CHECK_DEPTH { return Err (CheckError :: new (self . site , format ! (\"expression nesting exceeds the checker depth budget ({MAX_CHECK_DEPTH}) — a \\\n                     pathologically-nested `lower` rule RHS (M-1054 Stage 3 substituted-Expr walk)\") ,)) ; } let d = depth + 1 ; Ok (match e { Expr :: Path (p) if p . 0 . len () == 1 => { let n = & p . 0 [0] ; if let Some ((_ , f)) = shadow . iter () . rev () . find (| (orig , _) | orig == n) { Expr :: Path (Path (vec ! [f . clone ()])) } else if let Some ((_ , arg)) = params . iter () . find (| (pn , _) | pn == n) { arg . clone () } else { e . clone () } } Expr :: Path (_) | Expr :: Lit (_) => e . clone () , Expr :: Let { name , ty , bound , body , } => { let bound2 = self . stage3_substitute_expr (bound , params , shadow , fresh , d) ? ; let f = fresh . mint () ; shadow . push ((name . clone () , f . clone ())) ; let body2 = self . stage3_substitute_expr (body , params , shadow , fresh , d) ? ; shadow . pop () ; Expr :: Let { name : f , ty : ty . clone () , bound : Box :: new (bound2) , body : Box :: new (body2) , } } Expr :: If { cond , conseq , alt } => Expr :: If { cond : Box :: new (self . stage3_substitute_expr (cond , params , shadow , fresh , d) ?) , conseq : Box :: new (self . stage3_substitute_expr (conseq , params , shadow , fresh , d) ?) , alt : Box :: new (self . stage3_substitute_expr (alt , params , shadow , fresh , d) ?) , } , Expr :: Match { scrutinee , arms } => { let scrutinee2 = self . stage3_substitute_expr (scrutinee , params , shadow , fresh , d) ? ; let mut arms2 = Vec :: with_capacity (arms . len ()) ; for arm in arms { arms2 . push (self . stage3_substitute_arm (arm , params , shadow , fresh , d) ?) ; } Expr :: Match { scrutinee : Box :: new (scrutinee2) , arms : arms2 , } } Expr :: For { x , xs , acc , init , body , } => { let xs2 = self . stage3_substitute_expr (xs , params , shadow , fresh , d) ? ; let init2 = self . stage3_substitute_expr (init , params , shadow , fresh , d) ? ; let fx = fresh . mint () ; let facc = fresh . mint () ; shadow . push ((x . clone () , fx . clone ())) ; shadow . push ((acc . clone () , facc . clone ())) ; let body2 = self . stage3_substitute_expr (body , params , shadow , fresh , d) ? ; shadow . pop () ; shadow . pop () ; Expr :: For { x : fx , xs : Box :: new (xs2) , acc : facc , init : Box :: new (init2) , body : Box :: new (body2) , } } Expr :: Swap { value , target , policy , } => Expr :: Swap { value : Box :: new (self . stage3_substitute_expr (value , params , shadow , fresh , d) ?) , target : target . clone () , policy : policy . clone () , } , Expr :: WithParadigm { paradigm , body } => Expr :: WithParadigm { paradigm : * paradigm , body : Box :: new (self . stage3_substitute_expr (body , params , shadow , fresh , d) ?) , } , Expr :: Wild (b) => Expr :: Wild (Box :: new (self . stage3_substitute_expr (b , params , shadow , fresh , d) ? ,)) , Expr :: Spore (b) => Expr :: Spore (Box :: new (self . stage3_substitute_expr (b , params , shadow , fresh , d) ? ,)) , Expr :: Wrapping (b) => Expr :: Wrapping (Box :: new (self . stage3_substitute_expr (b , params , shadow , fresh , d) ? ,)) , Expr :: Consume (b) => Expr :: Consume (Box :: new (self . stage3_substitute_expr (b , params , shadow , fresh , d) ? ,)) , Expr :: Try (b) => Expr :: Try (Box :: new (self . stage3_substitute_expr (b , params , shadow , fresh , d) ? ,)) , Expr :: Colony (hyphae) => { let mut out = Vec :: with_capacity (hyphae . len ()) ; for h in hyphae { let forage = match & h . forage { Some (f) => Some (Box :: new (self . stage3_substitute_expr (f , params , shadow , fresh , d) ? ,)) , None => None , } ; let body = self . stage3_substitute_expr (& h . body , params , shadow , fresh , d) ? ; out . push (Hypha { forage , body }) ; } Expr :: Colony (out) } Expr :: Lambda { params : lparams , body , } => { let mut renamed = Vec :: with_capacity (lparams . len ()) ; for p in lparams { let f = fresh . mint () ; shadow . push ((p . name . clone () , f . clone ())) ; renamed . push (Param { name : f , ty : p . ty . clone () , }) ; } let body2 = self . stage3_substitute_expr (body , params , shadow , fresh , d) ? ; for _ in lparams { shadow . pop () ; } Expr :: Lambda { params : renamed , body : Box :: new (body2) , } } Expr :: App { head , args } => { let head2 = self . stage3_substitute_expr (head , params , shadow , fresh , d) ? ; let mut args2 = Vec :: with_capacity (args . len ()) ; for a in args { args2 . push (self . stage3_substitute_expr (a , params , shadow , fresh , d) ?) ; } Expr :: App { head : Box :: new (head2) , args : args2 , } } Expr :: Fuse { left , right } => Expr :: Fuse { left : Box :: new (self . stage3_substitute_expr (left , params , shadow , fresh , d) ?) , right : Box :: new (self . stage3_substitute_expr (right , params , shadow , fresh , d) ?) , } , Expr :: Reclaim { policy , body } => Expr :: Reclaim { policy : Box :: new (self . stage3_substitute_expr (policy , params , shadow , fresh , d) ?) , body : Box :: new (self . stage3_substitute_expr (body , params , shadow , fresh , d) ?) , } , Expr :: Ascribe (inner , t) => Expr :: Ascribe (Box :: new (self . stage3_substitute_expr (inner , params , shadow , fresh , d) ?) , t . clone () ,) , Expr :: TupleLit (items) => { let mut out = Vec :: with_capacity (items . len ()) ; for it in items { out . push (self . stage3_substitute_expr (it , params , shadow , fresh , d) ?) ; } Expr :: TupleLit (out) } }) } # [doc = \" [`Self::stage3_substitute_expr`]'s `Arm` companion — renames the arm's pattern binders (see\"] # [doc = \" [`Self::stage3_substitute_pattern`]) before substituting the body, popping exactly as many\"] # [doc = \" shadow entries as the ORIGINAL (pre-rename) pattern bound.\"] fn stage3_substitute_arm (& self , arm : & Arm , params : & [(String , Expr)] , shadow : & mut Vec < (String , String) > , fresh : & mut Stage3Fresh , depth : u32 ,) -> Result < Arm , CheckError > { let mut binder_count = 0usize ; let pattern2 = self . stage3_substitute_pattern (& arm . pattern , shadow , fresh , & mut binder_count) ? ; let body2 = self . stage3_substitute_expr (& arm . body , params , shadow , fresh , depth) ? ; for _ in 0 .. binder_count { shadow . pop () ; } Ok (Arm { pattern : pattern2 , body : body2 , }) } # [doc = \" [`Self::stage3_substitute_expr`]'s `Pattern` companion — fresh-renames a pattern's binder\"] # [doc = \" identifiers, pushing each `(orig, fresh)` pair onto `shadow` and incrementing `binder_count`\"] # [doc = \" (so the caller pops exactly as many as were pushed).\"] # [doc = \"\"] # [doc = \" **`Pattern::Ident` ambiguity (FLAG-pattern-ctor-collision, DN-117 ratification note) — now a\"] # [doc = \" refusal, not a silent \\\"leave unrenamed\\\" (fixed 2026-07-11, post-ratification adversarial\"] # [doc = \" review — CRITICAL false-accept, see this note's own errata entry).** A bare\"] # [doc = \" `Pattern::Ident(name)` is *either* a binder *or* a nullary-constructor reference,\"] # [doc = \" disambiguated only at the real `Cx::check_match` (`Self::resolve_pattern`) against the\"] # [doc = \" **scrutinee's type** — type information this pure `Expr`-level substitution walk does not\"] # [doc = \" have (no scrutinee type is threaded through). [`Self::is_any_nullary_ctor`] — is `name` the\"] # [doc = \" spelling of *any* registered nullary constructor, anywhere in the type registry (not\"] # [doc = \" scrutinee-scoped)? — is the only conservative, type-independent test available here, and it\"] # [doc = \" is genuinely ambiguous: `name` could be a binder for *this* pattern's (unrelated) scrutinee\"] # [doc = \" type, or a real ctor reference. **The previously-shipped choice (\\\"leave it unrenamed,\"] # [doc = \" treating it as a possible ctor reference\\\") was UNSOUND, not merely narrow:** when `name` is\"] # [doc = \" actually a genuine binder here, leaving it unrenamed skips this walk's own capture-avoidance\"] # [doc = \" discipline (see [`Self::stage3_substitute_expr`]'s doc comment on why every RHS-local binder\"] # [doc = \" must be `%`-freshened) — a spliced argument's free variable of the same spelling (e.g. a\"] # [doc = \" caller-outer local also named `name`) is then *captured* by this unrenamed pattern binder\"] # [doc = \" instead of referring to the caller's value, so the tracker never sees the real double-consume\"] # [doc = \" this produces. **Confirmed reproducible** (`stage3_pattern_ctor_collision_*` tests,\"] # [doc = \" `src/tests/affine_stage3.rs`): a nullary ctor spelled `s`, a rule `Pick3(h: Handle, q:\"] # [doc = \" Substrate) = match h { Wrap(s) => consume q }`, called as `(Pick3(Wrap(consume h_backing),\"] # [doc = \" s), consume s)` with a caller-outer `s` — accepted a genuine double-consume of the outer `s`.\"] # [doc = \" **Fix: refuse the whole sugar call** with a never-silent diagnostic (G2) rather than guess\"] # [doc = \" which reading is meant — a conservative false-REFUSE here is sound; the false-ACCEPT was not.\"] # [doc = \" The full close (scrutinee-type-directed disambiguation, mirroring `resolve_pattern`'s own\"] # [doc = \" logic) is out of proportion to this fix's scope and remains open, tracked by\"] # [doc = \" FLAG-pattern-ctor-collision.\"] fn stage3_substitute_pattern (& self , pat : & Pattern , shadow : & mut Vec < (String , String) > , fresh : & mut Stage3Fresh , binder_count : & mut usize ,) -> Result < Pattern , CheckError > { Ok (match pat { Pattern :: Wildcard | Pattern :: Lit (_) => pat . clone () , Pattern :: Ident (name) => { if self . is_any_nullary_ctor (name) { return Err (CheckError :: new (self . site , format ! (\"this `lower` sugar rule's RHS binds `{name}` in a match-arm pattern, \\\n                             but `{name}` is also the spelling of a registered nullary \\\n                             constructor elsewhere — ambiguous between \\\"binder\\\" and \\\n                             \\\"constructor reference\\\" without the scrutinee's type, which this \\\n                             check-time substitution walk does not have (M-1054 Stage 3 \\\n                             (OQ-H4)/DN-117, FLAG-pattern-ctor-collision). Refused rather than \\\n                             risk capturing a spliced argument's free variable of the same \\\n                             spelling (a confirmed false-accept of a genuine double-consume) — \\\n                             rename this binder, or the colliding constructor, to disambiguate \\\n                             (never a silent guess, G2).\") ,)) ; } let f = fresh . mint () ; shadow . push ((name . clone () , f . clone ())) ; * binder_count += 1 ; Pattern :: Ident (f) } Pattern :: Ctor (name , fields) => Pattern :: Ctor (name . clone () , fields . iter () . map (| f | self . stage3_substitute_pattern (f , shadow , fresh , binder_count)) . collect :: < Result < Vec < _ > , _ > > () ? ,) , Pattern :: Tuple (fields) => Pattern :: Tuple (fields . iter () . map (| f | self . stage3_substitute_pattern (f , shadow , fresh , binder_count)) . collect :: < Result < Vec < _ > , _ > > () ? ,) , Pattern :: Or (alts) => Pattern :: Or (alts . iter () . map (| a | self . stage3_substitute_pattern (a , shadow , fresh , binder_count)) . collect :: < Result < Vec < _ > , _ > > () ? ,) , }) } # [doc = \" Conservative, type-independent check: does `name` spell **any** registered nullary\"] # [doc = \" constructor, anywhere in the type registry (not scoped to a particular scrutinee type)? Used\"] # [doc = \" only by [`Self::stage3_substitute_pattern`]'s ambiguity gate — see that function's doc\"] # [doc = \" comment for why a scrutinee-scoped `resolve_pattern`-style resolution is not available at\"] # [doc = \" this (pure `Expr`, no type context) walk, and why the ambiguous case now refuses rather than\"] # [doc = \" guesses.\"] fn is_any_nullary_ctor (& self , name : & str) -> bool { self . types . values () . any (| d | { d . ctors . iter () . any (| c | c . name == name && c . fields . is_empty ()) }) } # [doc = \" Check a call to a **generic** function (RFC-0007 §11.3): resolve the callee's signature with\"] # [doc = \" the *callee's* type parameters as abstract variables, **unify** the declared parameter types\"] # [doc = \" against the actual argument types to infer the type arguments, and substitute the solution\"] # [doc = \" into the return type. An unsolved type parameter is an explicit error — never a guessed\"] # [doc = \" default (G2/VR-5). Extracted (`#[inline(never)]`) so [`Self::check_app`]'s frame stays within\"] # [doc = \" the A4-02 host-stack-depth bound on the common monomorphic/prim path.\"] # [inline (never)] fn check_app_generic_fn (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , fd : & FnDecl , args : & [Expr] ,) -> Result < (Ty , Expr) , CheckError > { let callee_vars = fd . sig . param_names () ; let baked = self . imports . resolved_fn_sigs . get (name) ; if baked . is_none () && self . imports . cross_phylum_fns . contains (name) { if let Some (concrete) = foreign_fn_sig_names_a_concrete_type (& fd . sig) { return self . err (format ! (\"`{name}` — the foreign (cross-phylum) fn's signature mentions a concrete type \\\n                     `{concrete}` beyond its own generic parameter(s), and its signature could not \\\n                     be pre-resolved against its declaring phylum (`resolve_fn_sig` only bakes a \\\n                     signature that references no type the declaring nodule itself imports — \\\n                     best-effort, never a hard error there); a bare-name reference to `{concrete}` \\\n                     here cannot be verified against the correct phylum's `{concrete}` and would \\\n                     risk the M-1036/DN-112 bare-name collapse one level up (DN-113 §7 / DN-122 \\\n                     residual; refused rather than silently resolved against a possibly-wrong \\\n                     phylum's registry, G2/VR-5).\")) ; } } let mut subst : BTreeMap < String , Ty > = BTreeMap :: new () ; let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (i , (pm , a)) in fd . sig . value_params . iter () . zip (args) . enumerate () { let want = match baked . and_then (| (params , _) | params . get (i)) { Some (w) => w . clone () , None => resolve_ty (self . site , self . types , & callee_vars , & pm . ty) ? . 0 , } ; let want_now = subst_ty (& want , & subst) ; let exp = if has_var (& want_now) { None } else { Some (& want_now) } ; let (got , a2) = self . check (scope , a , exp) ? ; unify (self . site , & want_now , & got , & mut subst) . map_err (| e | { CheckError :: new (self . site , format ! (\"`{name}` argument `{}`: {}\" , pm . name , e . message) ,) }) ? ; rebuilt . push (a2) ; } for v in & callee_vars { if ! subst . contains_key (v) { return self . err (format ! (\"`{name}` is generic over `{v}`, but this call does not determine it — ascribe an argument or the result (RFC-0007 §11.3, never a guessed default)\")) ; } } let callee_wvars = fd . sig . width_param_names () ; for v in & callee_wvars { if ! subst . contains_key (v) { return self . err (format ! (\"`{name}` is width-generic over `{v}`, but this call does not determine the width — ascribe an argument (DN-42 §4, never a guessed default / VR-5)\")) ; } } for p in & fd . sig . params { for b in & p . bounds { let concrete = subst . get (& p . name) . cloned () . unwrap_or (Ty :: Var (p . name . clone ())) ; self . require_instance (& b . name , & concrete , & format ! (\"required by `{name}`'s bound `{}: {}`\" , p . name , b . name) ,) ? ; } } let ret_base = match baked { Some ((_ , ret)) => ret . clone () , None => resolve_ty (self . site , self . types , & callee_vars , & fd . sig . ret) ? . 0 , } ; let ret = subst_ty (& ret_base , & subst) ; Ok ((ret , app_node (head , rebuilt))) } # [doc = \" Require that an instance `(trait_name, type_head(concrete))` exists, or refuse explicitly\"] # [doc = \" (RFC-0019 §4.5; G2). Used for bounded-generic-call satisfiability and concrete trait-method\"] # [doc = \" resolution. If `concrete` is still abstract (a bare `Ty::Var` — the call is itself inside a\"] # [doc = \" bounded generic whose bound already guarantees the instance at the eventual concrete type),\"] # [doc = \" the requirement is **discharged by the bound in scope**: it is satisfied iff that same\"] # [doc = \" `(var: trait)` bound is present in `self.bounds` (else an explicit \\\"no instance/bound\\\" error).\"] # [doc = \" The dictionary value is staged to elaboration (M-673) — this is satisfiability only.\"] fn require_instance (& self , trait_name : & str , concrete : & Ty , because : & str ,) -> Result < () , CheckError > { match type_head (concrete) { Some (head) => match self . instances . get (& (trait_name . to_owned () , head)) { Some (info) if info . for_ty == * concrete => Ok (()) , Some (info) => self . err (format ! (\"no instance `{trait_name}` for `{concrete}` ({because}) — the `{trait_name}` \\\n                     instance on this type head is declared for `{}`, not `{concrete}` (RFC-0019 §4.5)\" , info . for_ty)) , None => self . err (format ! (\"no instance `{trait_name}` for `{concrete}` ({because}) — declare \\\n                     `impl {trait_name}<…> for {concrete} {{ … }}` (RFC-0019 §4.5, never assumed)\")) , } , None => { let Ty :: Var (v) = concrete else { return self . err (format ! (\"no instance `{trait_name}` for `{concrete}` ({because})\")) ; } ; let satisfied = self . bounds . iter () . any (| (pv , bs) | pv == v && bs . iter () . any (| b | b . name == trait_name)) ; if satisfied { Ok (()) } else { self . err (format ! (\"no instance/bound provides `{trait_name}` for type variable `{v}` \\\n                         ({because}) — add the bound `{v}: {trait_name}` (RFC-0019 §4.1)\")) } } } } # [doc = \" Is `name` a method of some registered trait? (The call-resolution path uses this to decide\"] # [doc = \" whether to try trait-method resolution — fn/ctor names were already dispatched, so a name\"] # [doc = \" reaching here that matches a trait method is an unqualified trait-method call.)\"] fn is_trait_method (& self , name : & str) -> bool { self . traits . values () . any (| tr | tr . sigs . iter () . any (| s | s . name == name)) } # [doc = \" Resolve and type an **unqualified trait-method call** `m(args)` (RFC-0019 §4.4; RFC-0007\"] # [doc = \" §12.1; guarantee: `Declared`). The method must belong to **exactly one** trait (ambiguity\"] # [doc = \" across traits is an explicit error — never a guess). The trait's single type-parameter is\"] # [doc = \" determined by **unifying** the trait method's signature against the actual argument types;\"] # [doc = \" then an instance must exist — either a concrete `(Tr, head(C))` or a `T: Tr` **bound in\"] # [doc = \" scope** (dictionary staged to elaboration — M-673). The call types at the (substituted)\"] # [doc = \" method return type. Extracted (`#[inline(never)]`) for the frame-size reason as the other\"] # [doc = \" generic paths (A4-02).\"] # [inline (never)] fn check_trait_method_call (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , args : & [Expr] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let owners : Vec < & TraitInfo > = self . traits . values () . filter (| tr | tr . sigs . iter () . any (| s | s . name == name)) . collect () ; let tr = match owners . as_slice () { [one] => * one , [] => unreachable ! (\"is_trait_method gated this call\") , many => { let names : Vec < & str > = many . iter () . map (| t | t . name . as_str ()) . collect () ; return self . err (format ! (\"ambiguous trait-method call `{name}` — declared by multiple traits ({}); \\\n                     stage-1 has no qualified-call syntax, so this is an explicit refusal, never a \\\n                     guess (RFC-0019 §4.4)\" , names . join (\", \"))) ; } } ; if tr . params . len () != 1 { return self . err (format ! (\"trait-method resolution for `{name}` needs a single-parameter trait in stage-1 \\\n                 (trait `{}` has {} parameters — multi-parameter traits are v2, RFC-0019 §10)\" , tr . name , tr . params . len ())) ; } let sig = tr . sigs . iter () . find (| s | s . name == name) . expect (\"owner has the method\") ; if sig . value_params . len () != args . len () { return self . err (format ! (\"trait method `{}::{name}` takes {} argument(s), got {}\" , tr . name , sig . value_params . len () , args . len ())) ; } if self . imports . cross_phylum_traits . contains (& tr . name) { if let Some (name) = foreign_trait_sig_names_a_concrete_type (tr) { return self . err (format ! (\"trait-method call `{}` — the foreign (cross-phylum) trait `{}`'s signature \\\n                     mentions a concrete type `{name}` beyond its own generic parameter(s); a \\\n                     cross-phylum trait's signature is not yet re-homed against its declaring \\\n                     phylum (DN-113 §7 / DN-122 residual — a bare-name reference to `{name}` here \\\n                     cannot be verified against the correct phylum's `{name}` and would risk the \\\n                     M-1036/DN-112 bare-name collapse one level up; refused rather than silently \\\n                     resolved against a possibly-wrong phylum's declaration, G2/VR-5). Declare a \\\n                     local wrapper trait instead, or await the tracked follow-up for full \\\n                     cross-phylum trait-sig re-homing.\" , name , tr . name)) ; } } let tparam = & tr . params [0] ; let trait_vars = std :: slice :: from_ref (tparam) ; let mut subst : BTreeMap < String , Ty > = BTreeMap :: new () ; if let Some (exp) = expected { let ret_abs = resolve_ty (self . site , self . types , trait_vars , & sig . ret) ? . 0 ; let _ = unify (self . site , & ret_abs , exp , & mut subst) ; } let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (pm , a) in sig . value_params . iter () . zip (args) { let want = resolve_ty (self . site , self . types , trait_vars , & pm . ty) ? . 0 ; let want_now = subst_ty (& want , & subst) ; let exp = if has_var (& want_now) { None } else { Some (& want_now) } ; let (got , a2) = self . check (scope , a , exp) ? ; unify (self . site , & want_now , & got , & mut subst) . map_err (| e | { CheckError :: new (self . site , format ! (\"trait method `{}::{name}` argument `{}`: {}\" , tr . name , pm . name , e . message) ,) }) ? ; rebuilt . push (a2) ; } let Some (receiver) = subst . get (tparam) . cloned () else { return self . err (format ! (\"trait-method call `{name}` does not determine trait `{}`'s type parameter `{tparam}` \\\n                 from its arguments — ascribe an argument or the result (RFC-0019 §4.4, never a guess)\" , tr . name)) ; } ; self . require_instance (& tr . name , & receiver , & format ! (\"no instance/bound provides `{}::{name}` for these arguments\" , tr . name) ,) ? ; let ret = subst_ty (& resolve_ty (self . site , self . types , trait_vars , & sig . ret) ? . 0 , & subst ,) ; Ok ((ret , app_node (head , rebuilt))) } # [doc = \" Check a saturated application of a **generic** data constructor (RFC-0007 §11.2/§11.3). The\"] # [doc = \" constructor's declared fields are abstract over the type's parameters; the type arguments are\"] # [doc = \" taken from `expected` when it pins this data type (bidirectional), otherwise **inferred** from\"] # [doc = \" the field arguments via [`unify`]. An undetermined parameter is an explicit \\\"ascribe it\\\" error\"] # [doc = \" — never a guess. Extracted (`#[inline(never)]`) for the same frame-size reason as\"] # [doc = \" [`Self::check_app_generic_fn`].\"] # [inline (never)] # [allow (clippy :: too_many_arguments)] fn check_app_generic_ctor (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , dname : String , params : Vec < String > , fields : Vec < Ty > , args : & [Expr] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let mut subst : BTreeMap < String , Ty > = BTreeMap :: new () ; if let Some (Ty :: Data (en , eargs)) = expected { if * en == dname && eargs . len () == params . len () { for (p , ea) in params . iter () . zip (eargs) { subst . insert (p . clone () , ea . clone ()) ; } } } let mut rebuilt = Vec :: with_capacity (args . len ()) ; for (want , a) in fields . iter () . zip (args) { let want_now = subst_ty (want , & subst) ; let exp = if has_var (& want_now) { None } else { Some (& want_now) } ; let (got , a2) = self . check (scope , a , exp) ? ; unify (self . site , & want_now , & got , & mut subst) . map_err (| e | { CheckError :: new (self . site , format ! (\"constructor `{name}` field: {}\" , e . message) ,) }) ? ; rebuilt . push (a2) ; } let mut targs = Vec :: with_capacity (params . len ()) ; for p in & params { match subst . get (p) { Some (t) => targs . push (t . clone ()) , None => { return self . err (format ! (\"constructor `{name}` does not determine type parameter `{p}` of \\\n                         `{dname}<…>` — ascribe the value (RFC-0007 §11.3, never a guess)\")) } } } Ok ((Ty :: Data (dname , targs) , app_node (head , rebuilt))) } # [doc = \" T-For (RFC-0007 §4.8): `xs` must be a *linearly recursive* data type (nil/cons shape);\"] # [doc = \" `init : A`; `body : A` under `x : E, acc : A`; the whole expression is `A`. Every shape\"] # [doc = \" violation is an explicit refusal — general catamorphisms are an L2 concern.\"] # [allow (clippy :: too_many_arguments)] fn check_for (& self , scope : & mut Vec < (String , Ty) > , x : & str , xs : & Expr , acc : & str , init : & Expr , body : & Expr , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let (sty , xs2) = self . check (scope , xs , None) ? ; let Ty :: Data (tname , targs) = & sty else { return self . err (format ! (\"`for` iterates a linearly recursive data value, got {sty} (RFC-0007 §4.8)\")) ; } ; let elem = linear_elem_ty (self . site , self . types , tname , targs) ? ; let (aty , init2) = self . check (scope , init , expected) ? ; scope . push ((x . to_owned () , elem . clone ())) ; self . affine . push (& elem) ; scope . push ((acc . to_owned () , aty . clone ())) ; self . affine . push (& aty) ; let r = self . check (scope , body , Some (& aty)) ; scope . pop () ; self . affine . pop () ; scope . pop () ; self . affine . pop () ; let (bty , body2) = r ? ; if bty != aty { return self . err (format ! (\"`for` body must yield the accumulator type {}\" , edge_mismatch (\"body\" , & aty , & bty))) ; } Ok ((aty , Expr :: For { x : x . to_owned () , xs : Box :: new (xs2) , acc : acc . to_owned () , init : Box :: new (init2) , body : Box :: new (body2) , } ,)) } # [doc = \" Type a `match` over a data, `Binary`, or `Ternary` scrutinee with **nested** patterns\"] # [doc = \" (RFC-0007 §4.4/§4.7). Each arm's pattern is checked against the scrutinee type (binders enter\"] # [doc = \" scope at their field types), the arm bodies must agree, and coverage is decided by the\"] # [doc = \" **Maranget usefulness** algorithm ([`crate::usefulness`]): the match must be **exhaustive**\"] # [doc = \" (a `_` is not useful — otherwise the witness names a missing case) and every arm must be\"] # [doc = \" **reachable** (an arm covered by the earlier ones is a redundancy error). This unifies the data\"] # [doc = \" match and the M-320 literal match: a `Binary`/`Ternary` value domain is never enumerated, so a\"] # [doc = \" literal match still needs a `_`/binder default (W7 — coverage is checked, never assumed).\"] # [doc = \" Bare-decimal literal patterns take their width from the scrutinee/field type (RFC-0012 §4.3).\"] fn check_match (& self , scope : & mut Vec < (String , Ty) > , scrutinee : & Expr , arms : & [crate :: ast :: Arm] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { let (sty , scrut2) = self . check (scope , scrutinee , None) ? ; if ! matches ! (sty , Ty :: Data (_ , _) | Ty :: Binary (_) | Ty :: Ternary (_) | Ty :: Bytes) { return self . err (format ! (\"match scrutinee must be a data, Binary, Ternary, or Bytes type, got {sty} \\\n                 (DN-105 / RFC-0007 §4.7)\")) ; } if arms . is_empty () { return self . err (\"a match needs at least one arm\") ; } let flat_arms : Vec < crate :: ast :: Arm > = arms . iter () . try_fold (Vec :: new () , | mut acc , arm | { match & arm . pattern { Pattern :: Or (alts) => { if alts . len () < 2 { return self . err (\"internal: Pattern::Or must have at least 2 alternatives \\\n                                 (or-pattern invariant violation — report this)\" ,) ; } let mut ref_binds : Option < Vec < (String , Ty) > > = None ; for (alt_idx , alt) in alts . iter () . enumerate () { let resolved = self . resolve_pattern (alt , & sty) ? ; let mut binds : Vec < (String , Ty , Vec < usize >) > = Vec :: new () ; self . check_pattern (& resolved , & sty , & mut binds) ? ; let binder_sig : Vec < (String , Ty) > = binds . iter () . map (| (n , t , _) | (n . clone () , t . clone ())) . collect () ; match & ref_binds { None => ref_binds = Some (binder_sig) , Some (ref_sig) => { let mut got = binder_sig . clone () ; let mut want = ref_sig . clone () ; got . sort_by (| a , b | a . 0 . cmp (& b . 0)) ; want . sort_by (| a , b | a . 0 . cmp (& b . 0)) ; if got != want { return self . err (format ! (\"or-pattern alternative {alt_idx} binds {got:?} but \\\n                                             alternative 0 binds {ref_sig:?} — every alternative \\\n                                             of an or-pattern must bind the same variable names at \\\n                                             the same types (RFC-0020 §9 / R20-Q3, never-silent G2)\")) ; } } } acc . push (crate :: ast :: Arm { pattern : resolved , body : arm . body . clone () , }) ; } } _ => acc . push (arm . clone ()) , } Ok (acc) }) ? ; let col = [sty . clone ()] ; let mut rows : Vec < Vec < crate :: usefulness :: Pat > > = Vec :: new () ; let mut result : Option < Ty > = None ; let mut arms2 : Vec < crate :: ast :: Arm > = Vec :: with_capacity (flat_arms . len ()) ; let pre_match = self . affine . snapshot () ; let mut merged = pre_match . clone () ; for arm in & flat_arms { let pattern = self . resolve_pattern (& arm . pattern , & sty) ? ; let mut binds : Vec < (String , Ty , Vec < usize >) > = Vec :: new () ; let pat = self . check_pattern (& pattern , & sty , & mut binds) ? ; self . check_linear (& binds) ? ; if crate :: usefulness :: useful (self . types , & rows , std :: slice :: from_ref (& pat) , & col) . map_err (| e | self . match_budget_err (e)) ? . is_none () { return self . err (\"this arm is unreachable — earlier arms already cover it (W7: every arm must \\\n                     be reachable)\" ,) ; } let depth = scope . len () ; self . affine . restore (& pre_match) ; for (name , ty , _occ) in & binds { scope . push ((name . clone () , ty . clone ())) ; self . affine . push (ty) ; } let body_expected = expected . or (result . as_ref ()) ; let r = self . check (scope , & arm . body , body_expected) ; scope . truncate (depth) ; self . affine . truncate (depth) ; let (bty , body2) = r ? ; if let (Some (m) , Some (cur)) = (merged . as_mut () , self . affine . snapshot ()) { crate :: affine :: union_merge_into (m , & cur) ; } match & result { None => result = Some (bty) , Some (r) if * r != bty => { return self . err (format ! (\"match arms disagree: {}\" , edge_mismatch (\"arm\" , r , & bty))) } Some (_) => { } } rows . push (vec ! [pat]) ; arms2 . push (crate :: ast :: Arm { pattern , body : body2 , }) ; } self . affine . restore (& merged) ; if let Some (witness) = crate :: usefulness :: useful (self . types , & rows , & [crate :: usefulness :: Pat :: Wild] , & col) . map_err (| e | self . match_budget_err (e)) ? { return self . err (format ! (\"non-exhaustive match on {sty}: missing {} (W7 — coverage is checked, never assumed)\" , crate :: usefulness :: render (& witness [0]))) ; } let arm_ix : Vec < usize > = (0 .. rows . len ()) . collect () ; let occ = [Vec :: < usize > :: new ()] ; let tree = crate :: decision :: compile (self . types , & rows , & arm_ix , & occ , & col) . map_err (| e | self . match_budget_err (e)) ? ; if crate :: decision :: has_reachable_fail (& tree) { return self . err (\"internal: an exhaustive match compiled to a decision tree with a reachable Fail \\\n                 (the usefulness check and the Maranget compiler disagree — report this)\" ,) ; } let rty = result . ok_or_else (| | CheckError :: new (self . site , \"a match needs at least one arm\")) ? ; Ok ((rty , Expr :: Match { scrutinee : Box :: new (scrut2) , arms : arms2 , } ,)) } # [doc = \" Normalize a surface [`Pattern`] against its `expected` type into the **canonical checked\"] # [doc = \" form** stored in the resolved AST, type-directed at every position (the scrutinee type at the\"] # [doc = \" root, each constructor field's type as it recurses):\"] # [doc = \"\"] # [doc = \" 1. **Ambient literals** — resolve a bare-decimal (`AmbientInt`) pattern to a concrete\"] # [doc = \"    `Binary`/`Ternary` literal at the width `expected` pins. A literal under a\"] # [doc = \"    non-repr/cross-paradigm position is left unchanged so [`normalize_pattern`] raises the\"] # [doc = \"    precise W7 error.\"] # [doc = \" 2. **Nullary-ctor idents** — rewrite a bare `Pattern::Ident(name)` that names a *nullary\"] # [doc = \"    constructor of the scrutinee data type* into an explicit `Pattern::Ctor(name, vec![])`.\"] # [doc = \"    This makes the checked AST **unambiguous**: a residual `Pattern::Ident` is always a true\"] # [doc = \"    binder, a `Pattern::Ctor` always a constructor — so the type-free downstream passes\"] # [doc = \"    (guarantee grading Pass 3d, totality Pass 4) need no type information to tell them apart.\"] # [doc = \"    The checker (which alone knows the *expected scrutinee type*) is the single source of truth\"] # [doc = \"    for this resolution, mirroring [`normalize_pattern`]; a binder whose name merely collides\"] # [doc = \"    with a nullary ctor of an *unrelated* type stays a binder (no global ctor scan — that\"] # [doc = \"    over-broad scan was an unsound grade-upgrade, M-663 / Copilot review).\"] fn resolve_pattern (& self , pat : & Pattern , expected : & Ty) -> Result < Pattern , CheckError > { Ok (match pat { Pattern :: Lit (Literal :: AmbientInt (p , v)) => { Pattern :: Lit (self . resolve_ambient_int (* p , * v , Some (expected)) ?) } Pattern :: Ident (name) if matches ! (expected , Ty :: Data (tn , _) if lookup_data (self . types , tn) . is_some_and (| d | d . ctors . iter () . any (| c | c . name == * name && c . fields . is_empty ()))) => { Pattern :: Ctor (name . clone () , vec ! []) } Pattern :: Ctor (name , subs) => { let field_tys = match expected { Ty :: Data (tn , targs) => lookup_data (self . types , tn) . and_then (| d | { d . ctors . iter () . find (| c | c . name == * name) . filter (| c | c . fields . len () == subs . len ()) . map (| c | { let s = param_subst (& d . params , targs) ; c . fields . iter () . map (| f | subst_ty (f , & s)) . collect :: < Vec < _ > > () }) }) , _ => None , } ; let mut out = Vec :: with_capacity (subs . len ()) ; for (i , s) in subs . iter () . enumerate () { match & field_tys { Some (fts) => out . push (self . resolve_pattern (s , & fts [i]) ?) , None => out . push (s . clone ()) , } } Pattern :: Ctor (name . clone () , out) } Pattern :: Tuple (subs) => { let n = subs . len () ; let ctor_name = tuple_ctor_name (n) ; let tname = tuple_type_name (n) ; let field_tys = match expected { Ty :: Data (tn , targs) if * tn == tname => { lookup_data (self . types , tn) . and_then (| d | { d . ctors . iter () . find (| c | c . name == ctor_name) . filter (| c | c . fields . len () == subs . len ()) . map (| c | { let s = param_subst (& d . params , targs) ; c . fields . iter () . map (| f | subst_ty (f , & s)) . collect :: < Vec < _ > > () }) }) } _ => None , } ; let mut out = Vec :: with_capacity (subs . len ()) ; for (i , s) in subs . iter () . enumerate () { let resolved = match & field_tys { Some (fts) => self . resolve_pattern (s , & fts [i]) ? , None => s . clone () , } ; out . push (resolved) ; } Pattern :: Ctor (ctor_name , out) } Pattern :: Wildcard | Pattern :: Lit (_) | Pattern :: Ident (_) => pat . clone () , Pattern :: Or (_) => { return Err (CheckError :: new (self . site , \"internal: Pattern::Or reached resolve_pattern — or-patterns must be desugared \\\n                     in check_match before any downstream pass (invariant violation — report this)\" ,)) ; } }) } # [doc = \" Resolve a bare decimal (`AmbientInt`) to a concrete `Binary`/`Ternary` literal at the width\"] # [doc = \" the `expected` type pins (RFC-0012 §4.3). Never a built-in default: an absent or\"] # [doc = \" cross-paradigm context is an explicit refusal.\"] fn resolve_ambient_int (& self , p : Paradigm , v : i64 , expected : Option < & Ty > ,) -> Result < Literal , CheckError > { match (p , expected) { (Paradigm :: Binary , Some (Ty :: Binary (Width :: Lit (w)))) => encode_binary (self . site , v , * w) , (Paradigm :: Ternary , Some (Ty :: Ternary (Width :: Lit (w)))) => encode_balanced_ternary (self . site , v , * w) , (_ , Some (other)) => self . err (format ! (\"a bare `{p}` decimal cannot fill a {other} context — {} (RFC-0012 §4.3)\" , match paradigm_name (other) { Some (o) => format ! (\"`{p}` and `{o}` are different paradigms; write an explicit \\\n                                        `swap` or a tagged literal\") , None => \"a bare decimal only resolves in a Binary/Ternary context\" . to_owned () , })) , (_ , None) => self . err (format ! (\"UnresolvedWidth: a bare `{p}` decimal `{v}` has no width here — its width must come \\\n                 from the checked context (an ascription, a parameter/return type, or another \\\n                 operand). Ascribe it or write it explicitly (RFC-0012 §4.3; never a default width)\")) , } } # [doc = \" Type-check `pat` against `expected`, accumulating its binders (`name: ty @ occurrence`) into\"] # [doc = \" `binds`, and return the normalized [`crate::usefulness::Pat`] for the coverage matrix.\"] # [doc = \" Delegates to the free [`normalize_pattern`] (shared with the elaborator), starting at the root\"] # [doc = \" occurrence `[]`.\"] fn check_pattern (& self , pat : & Pattern , expected : & Ty , binds : & mut Vec < (String , Ty , Vec < usize >) > ,) -> Result < crate :: usefulness :: Pat , CheckError > { normalize_pattern (self . types , self . site , pat , expected , & [] , binds) } # [doc = \" A pattern must bind each name at most once (linearity) — a repeated binder is ambiguous, so it\"] # [doc = \" is an explicit error rather than a silent last-wins.\"] fn check_linear (& self , binds : & [(String , Ty , Vec < usize >)]) -> Result < () , CheckError > { for (i , (n , _ , _)) in binds . iter () . enumerate () { if binds [.. i] . iter () . any (| (m , _ , _) | m == n) { return self . err (format ! (\"pattern binds `{n}` more than once (bindings must be linear)\")) ; } } Ok (()) } # [doc = \" Check a list literal `[e1, …]` into a [`Ty::Seq`] (RFC-0032 D3; M-749). The element type is\"] # [doc = \" **inferred from the elements**, which must be **homogeneous** (a mismatch is a never-silent\"] # [doc = \" [`CheckError`], never a silent coercion — G2); the length is the element count. When an\"] # [doc = \" `expected` [`Ty::Seq`] type is supplied, each element is checked **against** its element type\"] # [doc = \" and the count must match the expected length (never-silent). An empty `[]` against a\"] # [doc = \" `Seq{T, 0}` is the well-formed empty sequence; an empty `[]` with **no** expected `Seq` type\"] # [doc = \" has no inferable element type and is an explicit refusal (G2 — never a guessed element).\"] # [doc = \"\"] # [doc = \" Returns the resolved literal so any ambient bare-decimal element (resolved against the\"] # [doc = \" element type) is rewritten to a concrete `Binary`/`Ternary` form for the evaluator/elaborator.\"] fn check_list (& self , scope : & mut Vec < (String , Ty) > , elems : & [Expr] , expected : Option < & Ty > ,) -> Result < (Ty , Expr) , CheckError > { if let Some (exp) = expected { if let Some ((nil_ctor , cons_ctor)) = cons_list_ctors (self . types , exp) { let concrete = matches ! (exp , Ty :: Data (_ , args) if ! args . iter () . any (has_var)) ; if concrete { return self . check_cons_list (scope , elems , exp , & nil_ctor , & cons_ctor) ; } let mut acc = Expr :: Path (Path (vec ! [nil_ctor])) ; for e in elems . iter () . rev () { acc = Expr :: App { head : Box :: new (Expr :: Path (Path (vec ! [cons_ctor . clone ()]))) , args : vec ! [e . clone () , acc] , } ; } return self . check (scope , & acc , expected) ; } } let expected_elem : Option < Ty > = match expected { Some (Ty :: Seq (elem , _)) => Some ((* * elem) . clone ()) , _ => None , } ; if let Some (Ty :: Seq (_ , n)) = expected { if * n as usize != elems . len () { return self . err (format ! (\"list literal has {} element(s) but the expected `Seq` length is {} \\\n                     (RFC-0032 D3 — never a silent truncation/padding)\" , elems . len () , n)) ; } } let mut elem_ty : Option < Ty > = expected_elem . clone () ; let mut rebuilt = Vec :: with_capacity (elems . len ()) ; for (i , e) in elems . iter () . enumerate () { let (t , e2) = self . check (scope , e , elem_ty . as_ref ()) ? ; match & elem_ty { Some (want) if * want != t => { return self . err (format ! (\"list literal element {i} has type {t}, but the sequence's element type is \\\n                         {want} — list elements must be homogeneous (RFC-0032 D3, never a silent \\\n                         coercion)\")) ; } Some (_) => { } None => elem_ty = Some (t) , } rebuilt . push (e2) ; } let Some (elem) = elem_ty else { return self . err (\"an empty list literal `[]` has no inferable element type here — ascribe it to a \\\n                 `Seq{T, 0}` (RFC-0032 D3, never a guessed element type)\" ,) ; } ; let len = u32 :: try_from (rebuilt . len ()) . map_err (| _ | { CheckError :: new (self . site , \"list literal length exceeds u32 (RFC-0032 D3)\") }) ? ; Ok ((Ty :: Seq (Box :: new (elem) , len) , Expr :: Lit (Literal :: List (rebuilt)) ,)) } # [doc = \" Check a list literal `[e1, …, en]` against a **concrete** cons-list ADT (`exp`, a two-ctor\"] # [doc = \" nil/`Cons(A, Self)` type — RFC-0040/M-977), routing the list's *data*-spine depth through\"] # [doc = \" ITERATION rather than control recursion (RFC-0041 §4.7, the data-vs-control fix).\"] # [doc = \"\"] # [doc = \" The desugared value is still the right-nested `Cons(e1, Cons(…, Nil))` chain (so elaboration /\"] # [doc = \" eval / AOT see byte-for-byte the RFC-0040 representation), but it is **built and checked in a\"] # [doc = \" flat loop**: each element is checked against the constructor's (already fully-determined)\"] # [doc = \" element type via a single, shallow `Cons(e, <typed placeholder tail>)` check — so the checker's\"] # [doc = \" control-recursion depth stays O(1) per element and a large list is bounded by the work-step\"] # [doc = \" budget, never refused as if it were `n`-deep control recursion. Behaviour is byte-identical to\"] # [doc = \" the desugared-chain check for the concrete `exp` case (each element's expected type comes from\"] # [doc = \" `exp` alone, not the tail): the produced resolved elements and the produced chain match exactly.\"] # [doc = \"\"] # [doc = \" Never-silent (G2/VR-5): a heterogeneous / mistyped element surfaces the same explicit\"] # [doc = \" `CheckError` the desugared-chain check would (both flow through `check_app`'s constructor path).\"] fn check_cons_list (& self , scope : & mut Vec < (String , Ty) > , elems : & [Expr] , exp : & Ty , nil_ctor : & str , cons_ctor : & str ,) -> Result < (Ty , Expr) , CheckError > { let budget = mycelium_workstack :: RecursionBudget :: default () ; let cons_head = | | Expr :: Path (Path (vec ! [cons_ctor . to_owned ()])) ; let (_nil_ty , nil_expr) = self . check (scope , & Expr :: Path (Path (vec ! [nil_ctor . to_owned ()])) , Some (exp) ,) ? ; const TAIL_SLOT : & str = \"$cons_list_tail$\" ; let base = scope . len () ; let tail_ref = Expr :: Path (Path (vec ! [TAIL_SLOT . to_owned ()])) ; scope . push ((TAIL_SLOT . to_owned () , exp . clone ())) ; self . affine . push (exp) ; let mut resolved_elems = Vec :: with_capacity (elems . len ()) ; let mut result_ty : Option < Ty > = None ; for e in elems { if let Err (be) = budget . charge_steps (1) { scope . truncate (base) ; self . affine . truncate (base) ; return self . err (format ! (\"list literal exceeded the work-step budget: {be} — an explicit over-budget \\\n                     refusal (RFC-0041 §4.7), never a host-stack overflow (G2/VR-5)\")) ; } let layer = Expr :: App { head : Box :: new (cons_head ()) , args : vec ! [e . clone () , tail_ref . clone ()] , } ; let (lty , lresolved) = match self . check (scope , & layer , Some (exp)) { Ok (x) => x , Err (err) => { scope . truncate (base) ; self . affine . truncate (base) ; return Err (err) ; } } ; let elem_resolved = match lresolved { Expr :: App { mut args , .. } if ! args . is_empty () => args . swap_remove (0) , other => other , } ; resolved_elems . push (elem_resolved) ; result_ty = Some (lty) ; } scope . truncate (base) ; self . affine . truncate (base) ; let mut acc = nil_expr ; for e in resolved_elems . into_iter () . rev () { acc = Expr :: App { head : Box :: new (cons_head ()) , args : vec ! [e , acc] , } ; } let ty = result_ty . unwrap_or_else (| | exp . clone ()) ; Ok ((ty , acc)) } # [doc = \" Type a `Seq`/`Bytes` indexing/length/slice/concat/eq/hash prim (RFC-0032 D3/D4; M-749/M-750/\"] # [doc = \" M-799; M-912), or `Ok(None)` if `name` is not one of them (so the caller falls through to the\"] # [doc = \" width-preserving prim path).\"] # [doc = \" The operands are checked, the receiver's repr type is verified (a non-`Seq`/`Bytes` receiver,\"] # [doc = \" or a wrong arity, is an explicit never-silent refusal — G2), and the result type is returned.\"] fn try_check_seq_bytes_prim (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , args : & [Expr] ,) -> Result < Option < (Ty , Expr) > , CheckError > { let check_index = | scope : & mut Vec < (String , Ty) > , a : & Expr | -> Result < Expr , CheckError > { let (ity , a2) = self . check (scope , a , None) ? ; match ity { Ty :: Binary (_) => Ok (a2) , _ => self . err (format ! (\"`{name}` index must be a `Binary{{W}}` (unsigned magnitude), got {ity} \\\n                     (RFC-0032 D3/D4)\")) , } } ; let arity_err = | want : usize | -> Result < Option < (Ty , Expr) > , CheckError > { self . err (format ! (\"`{name}` takes {want} operand(s), got {} (RFC-0032 D3/D4)\" , args . len ())) } ; match name { \"seq_len\" | \"bytes_len\" => { if args . len () != 1 { return arity_err (1) ; } let (recv , recv2) = self . check (scope , & args [0] , None) ? ; let ok = match name { \"seq_len\" => matches ! (recv , Ty :: Seq (_ , _)) , _ => matches ! (recv , Ty :: Bytes) , } ; if ! ok { return self . err (format ! (\"`{name}` expects a {} operand, got {recv} (RFC-0032 D3/D4)\" , if name == \"seq_len\" { \"Seq\" } else { \"Bytes\" })) ; } Ok (Some ((Ty :: Binary (Width :: Lit (32)) , app_node (head , vec ! [recv2]) ,))) } \"seq_get\" => { if args . len () != 2 { return arity_err (2) ; } let (recv , recv2) = self . check (scope , & args [0] , None) ? ; let Ty :: Seq (elem , _) = recv else { return self . err (format ! (\"`seq_get` expects a `Seq` operand, got {recv} (RFC-0032 D3)\")) ; } ; let idx2 = check_index (scope , & args [1]) ? ; Ok (Some ((* elem , app_node (head , vec ! [recv2 , idx2])))) } \"bytes_get\" => { if args . len () != 2 { return arity_err (2) ; } let (recv , recv2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (recv , Ty :: Bytes) { return self . err (format ! (\"`bytes_get` expects a `Bytes` operand, got {recv} (RFC-0032 D4)\")) ; } let idx2 = check_index (scope , & args [1]) ? ; Ok (Some ((Ty :: Binary (Width :: Lit (8)) , app_node (head , vec ! [recv2 , idx2]) ,))) } \"bytes_slice\" => { if args . len () != 3 { return arity_err (3) ; } let (recv , recv2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (recv , Ty :: Bytes) { return self . err (format ! (\"`bytes_slice` expects a `Bytes` operand, got {recv} (RFC-0032 D4)\")) ; } let start2 = check_index (scope , & args [1]) ? ; let end2 = check_index (scope , & args [2]) ? ; Ok (Some ((Ty :: Bytes , app_node (head , vec ! [recv2 , start2 , end2])))) } \"bytes_concat\" => { if args . len () != 2 { return arity_err (2) ; } let (a , a2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (a , Ty :: Bytes) { return self . err (format ! (\"`bytes_concat` expects a `Bytes` first operand, got {a} (RFC-0032 D4)\")) ; } let (b , b2) = self . check (scope , & args [1] , None) ? ; if ! matches ! (b , Ty :: Bytes) { return self . err (format ! (\"`bytes_concat` expects a `Bytes` second operand, got {b} (RFC-0032 D4)\")) ; } Ok (Some ((Ty :: Bytes , app_node (head , vec ! [a2 , b2])))) } \"width_cast\" => { if args . len () != 2 { return arity_err (2) ; } let (vty , v2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (vty , Ty :: Binary (_)) { return self . err (format ! (\"`width_cast` value operand must be a concrete `Binary{{N}}`, got {vty} \\\n                         (DN-41; never a default width)\")) ; } let (wty , w2) = self . check (scope , & args [1] , None) ? ; let Ty :: Binary (Width :: Lit (m)) = wty else { return self . err (format ! (\"`width_cast` width witness must be a concrete `Binary{{M}}` (only its width is used), \\\n                         got {wty} (DN-41; a width-variable witness is refused — DN-42/M-753)\")) ; } ; Ok (Some ((Ty :: Binary (Width :: Lit (m)) , app_node (head , vec ! [v2 , w2]) ,))) } \"truncate\" => { if args . len () != 2 { return arity_err (2) ; } let (vty , v2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (vty , Ty :: Binary (_)) { return self . err (format ! (\"`truncate` value operand must be a concrete `Binary{{N}}`, got {vty} \\\n                         (DN-51; never a default width)\")) ; } let (wty , w2) = self . check (scope , & args [1] , None) ? ; let Ty :: Binary (Width :: Lit (m)) = wty else { return self . err (format ! (\"`truncate` width witness must be a concrete `Binary{{M}}` (only its width is used), \\\n                         got {wty} (DN-51; a width-variable witness is refused — DN-42/M-753)\")) ; } ; Ok (Some ((Ty :: Binary (Width :: Lit (m)) , app_node (head , vec ! [v2 , w2]) ,))) } \"bytes_eq\" => { if args . len () != 2 { return arity_err (2) ; } let (a , a2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (a , Ty :: Bytes) { return self . err (format ! (\"`bytes_eq` expects a `Bytes` first operand, got {a} (M-912)\")) ; } let (b , b2) = self . check (scope , & args [1] , None) ? ; if ! matches ! (b , Ty :: Bytes) { return self . err (format ! (\"`bytes_eq` expects a `Bytes` second operand, got {b} (M-912)\")) ; } Ok (Some ((Ty :: Binary (Width :: Lit (1)) , app_node (head , vec ! [a2 , b2]) ,))) } \"hash_blake3\" => { if args . len () != 1 { return arity_err (1) ; } let (recv , recv2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (recv , Ty :: Bytes) { return self . err (format ! (\"`hash_blake3` expects a `Bytes` operand, got {recv} (M-912)\")) ; } Ok (Some ((Ty :: Bytes , app_node (head , vec ! [recv2])))) } _ => Ok (None) , } } # [doc = \" Try to check a call to a **dense prim** (RFC-0001 §4.1 / RFC-0002 §5; M-890/M-891,\"] # [doc = \" `enb` Gap C) — the tensor-valued group `dense_add`/`dense_sub`/`dense_neg`/`dense_scale`\"] # [doc = \" (kernel `dense.add`/`dense.sub`/`dense.neg`/`dense.scale`, the `mycelium-dense` surface)\"] # [doc = \" plus the measurement pair `dense_dot`/`dense_similarity` (kernel `dense.dot`/\"] # [doc = \" `dense.similarity`, M-891). Returns `Ok(None)` if `name` is not one of them.\"] # [doc = \"\"] # [doc = \" Dim and dtype are part of the type (`Dense{d, s}`), so the never-silent shape contract is\"] # [doc = \" **static** here: `dense_add`/`dense_sub` require two `Dense{d, s}` operands with *equal*\"] # [doc = \" dim and dtype (a mismatch is an explicit refusal naming both types — never a broadcast or\"] # [doc = \" re-round; G2), and the result is the same `Dense{d, s}`. `dense_neg` is unary,\"] # [doc = \" dim/dtype-preserving. `dense_scale(a: Dense{d, s}, c: Dense{1, s})` takes its factor as a\"] # [doc = \" **`Dense{1, s}` scalar** of the *same* dtype — the pre-Gap-A scalar form (no scalar-float\"] # [doc = \" type exists until the M-895/M-896 float ADR lands; FLAGged at the kernel wrapper,\"] # [doc = \" `mycelium-interp/src/prims.rs`). `dense_dot`/`dense_similarity` (M-891) take the same\"] # [doc = \" equal-dim/dtype operand pair and return **`Dense{1, F64}`** — the measurement-result form:\"] # [doc = \" the binary64 the kernel computed, delivered exactly (never re-rounded onto the operand\"] # [doc = \" grid), carrying the kernel's `Proven` absolute accumulation bound at runtime; `F64` has no\"] # [doc = \" dense op set (kernel v1 scope), so a measurement cannot silently feed back into dense\"] # [doc = \" arithmetic — re-entry is an explicit runtime `UnsupportedDtype` refusal (G2). The\"] # [doc = \" numeric-domain contracts (off-grid/non-finite\"] # [doc = \" elements, overflow, subnormal results, approximate sources) are *runtime* refusals owned by\"] # [doc = \" the kernel, not static rules — exactly as `add_u`'s overflow is (RFC-0032 D2 precedent).\"] # [doc = \" A bare-decimal operand has no Dense anchor (RFC-0012 §4.3 gives Dense no bare-decimal\"] # [doc = \" encoding), so it is refused by the operand check itself, never defaulted.\"] fn try_check_dense_prim (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , args : & [Expr] ,) -> Result < Option < (Ty , Expr) > , CheckError > { if ! matches ! (name , \"dense_add\" | \"dense_sub\" | \"dense_neg\" | \"dense_scale\" | \"dense_dot\" | \"dense_similarity\") { return Ok (None) ; } let arity_err = | want : usize | -> Result < Option < (Ty , Expr) > , CheckError > { self . err (format ! (\"`{name}` takes {want} operand(s), got {} (RFC-0001 §4.1; M-890)\" , args . len ())) } ; let check_dense = | scope : & mut Vec < (String , Ty) > , a : & Expr , which : & str | -> Result < (u32 , Scalar , Expr) , CheckError > { let (ty , a2) = self . check (scope , a , None) ? ; let Ty :: Dense (d , s) = ty else { return Err (CheckError :: new (self . site , format ! (\"`{name}` {which} operand must be a `Dense{{dim, scalar}}`, got {ty} \\\n                         (RFC-0001 §4.1; M-890 — never a silent conversion; a cross-paradigm \\\n                         edge needs an explicit `swap`)\") ,)) ; } ; Ok ((d , s , a2)) } ; match name { \"dense_add\" | \"dense_sub\" => { if args . len () != 2 { return arity_err (2) ; } let (da , sa , a2) = check_dense (scope , & args [0] , \"first\") ? ; let (db , sb , b2) = check_dense (scope , & args [1] , \"second\") ? ; if da != db || sa != sb { return self . err (format ! (\"`{name}` operands must share one dim and dtype, got Dense{{{da}, \\\n                         {sa:?}}} and Dense{{{db}, {sb:?}}} (M-890 — a shape/dtype mismatch is \\\n                         an explicit refusal, never a broadcast or re-round; G2)\")) ; } Ok (Some ((Ty :: Dense (da , sa) , app_node (head , vec ! [a2 , b2])))) } \"dense_dot\" | \"dense_similarity\" => { if args . len () != 2 { return arity_err (2) ; } let (da , sa , a2) = check_dense (scope , & args [0] , \"first\") ? ; let (db , sb , b2) = check_dense (scope , & args [1] , \"second\") ? ; if da != db || sa != sb { return self . err (format ! (\"`{name}` operands must share one dim and dtype, got Dense{{{da}, \\\n                         {sa:?}}} and Dense{{{db}, {sb:?}}} (M-891 — a shape/dtype mismatch is \\\n                         an explicit refusal, never a broadcast or re-round; G2)\")) ; } Ok (Some ((Ty :: Dense (1 , Scalar :: F64) , app_node (head , vec ! [a2 , b2]) ,))) } \"dense_neg\" => { if args . len () != 1 { return arity_err (1) ; } let (d , s , a2) = check_dense (scope , & args [0] , \"single\") ? ; Ok (Some ((Ty :: Dense (d , s) , app_node (head , vec ! [a2])))) } \"dense_scale\" => { if args . len () != 2 { return arity_err (2) ; } let (d , s , a2) = check_dense (scope , & args [0] , \"vector\") ? ; let (dc , sc , c2) = check_dense (scope , & args [1] , \"scale-factor\") ? ; if dc != 1 { return self . err (format ! (\"`dense_scale` factor must be a `Dense{{1, scalar}}` (the pre-Gap-A \\\n                         scalar form), got Dense{{{dc}, {sc:?}}} (M-890)\")) ; } if sc != s { return self . err (format ! (\"`dense_scale` factor dtype {sc:?} must match the vector dtype {s:?} \\\n                         (M-890 — never a silent re-round; G2)\")) ; } Ok (Some ((Ty :: Dense (d , s) , app_node (head , vec ! [a2 , c2])))) } _ => unreachable ! (\"gated by the matches! above\") , } } # [doc = \" Try to check a call to a **VSA prim** (RFC-0003 §3/§4/§5/§6; ADR-008) — the M-892 bind\"] # [doc = \" group `vsa_bind`/`vsa_unbind` (binary, hypervector × hypervector) and `vsa_permute`\"] # [doc = \" (hypervector × `Binary{W}` shift), kernel `vsa.bind`/`vsa.unbind`/`vsa.permute`, plus the\"] # [doc = \" M-893 **certified superposition** `vsa_bundle` (`Seq{VSA{…}, N≥1}` × `Float` δ →\"] # [doc = \" `VSA{…}`), kernel `vsa.bundle`, plus the M-894 **cleanup/reconstruction pair and the\"] # [doc = \" capacity query** — `vsa_cleanup` (`VSA{…}` × `Seq{VSA{…}, N≥1}` → `Seq{Float, 3}`,\"] # [doc = \" the `[index, confidence, margin]` decision triple), `vsa_reconstruct`\"] # [doc = \" (`VSA{…}` × `VSA{…}` × `Seq{VSA{…}, N≥1}` × `Float` threshold → `Seq{Float, 3}`), and\"] # [doc = \" `vsa_required_dim` (`Binary{W}` items × `Float` δ → `Binary{64}`), kernel\"] # [doc = \" `vsa.cleanup`/`vsa.reconstruct`/`vsa.required_dim` (surface names `_`-joined like\"] # [doc = \" `dense_add`). Returns `Ok(None)` if `name` is not one of them.\"] # [doc = \"\"] # [doc = \" Model + dim + sparsity live in the type (`VSA{model, dim, sparsity}`), so the static rules\"] # [doc = \" are: every hypervector operand must be a `VSA` type with **one shared model + dim +\"] # [doc = \" sparsity** (a mismatch is an explicit refusal, never a coercion — G2); the model must be\"] # [doc = \" in the **introduction dispatch set MAP-I / FHRR / BSC** (an out-of-set model — HRR/MAP-B/\"] # [doc = \" SBC mentions included — is a static refusal naming the set; widening is append-only and\"] # [doc = \" mirrors the interpreter's dispatch); and the sparsity must be `Dense` at introduction\"] # [doc = \" (the kernel's Value-level ops construct dense-class results — a `Sparse` claim would\"] # [doc = \" diverge from the runtime value, so it is refused, not silently re-classed). `vsa_permute`'s\"] # [doc = \" shift is any unsigned `Binary{W}` magnitude (rotation is cyclic — the inverse permute is\"] # [doc = \" the complementary shift `dim − s`); a bare-decimal shift has no width anchor here and must\"] # [doc = \" be written/ascribed explicitly (RFC-0012 §4.3 — never a default width).\"] # [doc = \"\"] # [doc = \" `vsa_bundle` adds two static rules of its own: the model must additionally be in the\"] # [doc = \" **certified-bundle set {MAP-I}** — the only introduction-set model with a *certified*\"] # [doc = \" Value-level bundle (`bundle_values_certified`, the M-131 checked-instantiation pattern);\"] # [doc = \" an FHRR/BSC bundle is a **static** refusal naming the certified set (their kernel bundles\"] # [doc = \" are `Empirical`-profile ops — routing them here would silently re-tag their evidence,\"] # [doc = \" VR-5; surfacing them is a distinct append-only extension) — and the item count `N` lives\"] # [doc = \" in the `Seq` type, so an **empty bundle (`N = 0`) is a static refusal** too (no\"] # [doc = \" superposition is defined). The capacity side-condition `dim ≥ requiredDim(N, δ)` stays\"] # [doc = \" **runtime** (δ is a run-time `Float`): the kernel refuses `InsufficientCapacity` naming\"] # [doc = \" the required dim rather than issuing an unbacked `Proven` bound.\"] # [doc = \"\"] # [doc = \" The M-894 prims add their own static rules: `vsa_cleanup`/`vsa_reconstruct`'s codebook is\"] # [doc = \" a `Seq{VSA{…}, N ≥ 1}` sharing the query/record's model + dim (`N` lives in the type, so\"] # [doc = \" an **empty codebook is a static refusal** — nothing to clean up against); `vsa_reconstruct`\"] # [doc = \" additionally restricts the model to the **reconstruct dispatch set {MAP-I, BSC}** — an\"] # [doc = \" FHRR record is a *static* refusal naming the ground (FHRR's `Empirical` unbind profile is\"] # [doc = \" trial-validated only for a single bind product, not a reconstruction record — VR-5; its\"] # [doc = \" surfacing is an append-only extension under a reconstruction-regime profile of its own),\"] # [doc = \" and its threshold is a `Float` (the RFC-0003 §6 manifest's `cleanup_threshold ∈ [0, 1]`\"] # [doc = \" made an explicit operand — the domain check stays runtime, δ-style). `vsa_required_dim`'s\"] # [doc = \" items ride any unsigned `Binary{W}` magnitude (like `vsa_permute`'s shift — a bare decimal\"] # [doc = \" has no width anchor, RFC-0012 §4.3) and the result is the `Binary{64}` sufficient\"] # [doc = \" dimension carrying the kernel's `Proven` `CapacityBound`. Both retrieval prims return the\"] # [doc = \" `Seq{Float, 3}` decision triple — the retrieval is never a silent nearest-neighbour pick\"] # [doc = \" (FR-S4/G2: confidence + margin are first-class results).\"] # [doc = \"\"] # [doc = \" The numeric-domain contracts stay **runtime**, owned by the kernel and carried through the\"] # [doc = \" interpreter wrapper: alphabet violations, the FHRR unbind regime gate, non-`Exact`\"] # [doc = \" operand composition, the RFC-0010 §4.4 identifiability tie, and the below-threshold\"] # [doc = \" retrieval are explicit runtime refusals; per-op tags ride the runtime value\"] # [doc = \" **per model** (MAP-I/BSC ops `Exact`; FHRR `unbind` `Empirical` with its trial-validated\"] # [doc = \" δ bound; the certified `vsa.bundle` result `Proven` with its checked `CapacityBound`;\"] # [doc = \" the M-894 decision triple carrying the query/record's own (strength, bound) pair —\"] # [doc = \" VR-5, carried, never re-stamped; `mycelium-interp/src/prims.rs`).\"] fn try_check_vsa_prim (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , args : & [Expr] ,) -> Result < Option < (Ty , Expr) > , CheckError > { if ! VSA_PRIM_NAMES . contains (& name) { return Ok (None) ; } const VSA_PRIM_MODELS : [& str ; 3] = [\"MAP-I\" , \"FHRR\" , \"BSC\"] ; let arity_err = | want : usize | -> Result < Option < (Ty , Expr) > , CheckError > { self . err (format ! (\"`{name}` takes {want} operand(s), got {} (RFC-0003 §3; M-892)\" , args . len ())) } ; let check_hv = | scope : & mut Vec < (String , Ty) > , a : & Expr , which : & str | -> Result < (String , u32 , Sparsity , Expr) , CheckError > { let (ty , a2) = self . check (scope , a , None) ? ; let Ty :: Vsa { model , dim , sparsity , } = ty else { return Err (CheckError :: new (self . site , format ! (\"`{name}` {which} operand must be a `VSA{{model, dim, sparsity}}` \\\n                         hypervector, got {ty} (RFC-0003 §3; M-892 — never a silent conversion; \\\n                         a cross-paradigm edge needs an explicit `swap`)\") ,)) ; } ; if ! VSA_PRIM_MODELS . contains (& model . as_str ()) { return Err (CheckError :: new (self . site , format ! (\"`{name}` model {model:?} is outside the vsa prim dispatch set at \\\n                         introduction (MAP-I, FHRR, BSC — M-892); the type is a legal mention, \\\n                         but its algebra is not surfaced — widening the set is an append-only \\\n                         extension, never a guessed algebra (G2)\") ,)) ; } if sparsity != Sparsity :: Dense { return Err (CheckError :: new (self . site , format ! (\"`{name}` requires a `Dense`-sparsity hypervector at introduction — the \\\n                         kernel's Value-level ops construct dense-class results, so a Sparse \\\n                         operand type would diverge from the runtime value (M-892; refused, \\\n                         never silently re-classed — G2)\") ,)) ; } Ok ((model , dim , sparsity , a2)) } ; let check_codebook = | scope : & mut Vec < (String , Ty) > , a : & Expr , model : & str , dim : u32 | -> Result < Expr , CheckError > { let (sty , s2) = self . check (scope , a , None) ? ; let Ty :: Seq (elem , n) = sty else { return Err (CheckError :: new (self . site , format ! (\"`{name}` codebook must be a `Seq{{VSA{{model, dim, sparsity}}, N}}` of \\\n                         clean atoms, got {sty} (RFC-0003 §3/§6; M-894 — never a silent \\\n                         conversion)\") ,)) ; } ; let Ty :: Vsa { model : cm , dim : cd , sparsity : cs , } = * elem else { return Err (CheckError :: new (self . site , format ! (\"`{name}` codebook atoms must be `VSA{{model, dim, sparsity}}` \\\n                         hypervectors, got a Seq of {elem} (M-894 — never a silent conversion; \\\n                         a cross-paradigm edge needs an explicit `swap`)\") ,)) ; } ; if cm != model || cd != dim { return Err (CheckError :: new (self . site , format ! (\"`{name}` codebook atoms must share the query's model and dim, got \\\n                         VSA{{{cm}, {cd}}} atoms against a VSA{{{model}, {dim}}} query/record \\\n                         (M-894 — a model or dim mismatch is an explicit refusal, never a \\\n                         cross-model coercion; G2)\") ,)) ; } if cs != Sparsity :: Dense { return Err (CheckError :: new (self . site , format ! (\"`{name}` requires `Dense`-sparsity codebook atoms at introduction — \\\n                         the kernel's cleanup memory holds dense-class atoms, so a Sparse atom \\\n                         type would diverge from the runtime value (M-894; refused, never \\\n                         silently re-classed — G2)\") ,)) ; } if n == 0 { return Err (CheckError :: new (self . site , format ! (\"`{name}` requires at least one codebook atom — an empty cleanup memory \\\n                         has nothing to clean up against (FR-S4; M-894 — refused statically, \\\n                         never a defaulted retrieval; G2)\") ,)) ; } Ok (s2) } ; let triple_ty = | | Ty :: Seq (Box :: new (Ty :: Float) , 3) ; match name { \"vsa_bind\" | \"vsa_unbind\" => { if args . len () != 2 { return arity_err (2) ; } let (ma , da , spa , a2) = check_hv (scope , & args [0] , \"first\") ? ; let (mb , db , _spb , b2) = check_hv (scope , & args [1] , \"second\") ? ; if ma != mb || da != db { return self . err (format ! (\"`{name}` operands must share one model and dim, got VSA{{{ma}, {da}}} \\\n                         and VSA{{{mb}, {db}}} (M-892 — a model or dim mismatch is an explicit \\\n                         refusal, never a cross-model coercion; G2)\")) ; } Ok (Some ((Ty :: Vsa { model : ma , dim : da , sparsity : spa , } , app_node (head , vec ! [a2 , b2]) ,))) } \"vsa_permute\" => { if args . len () != 2 { return arity_err (2) ; } let (m , d , sp , a2) = check_hv (scope , & args [0] , \"hypervector\") ? ; let (sty , s2) = self . check (scope , & args [1] , None) ? ; let Ty :: Binary (_) = sty else { return self . err (format ! (\"`vsa_permute` shift must be an unsigned `Binary{{W}}` magnitude, got \\\n                         {sty} (M-892 — rotation is cyclic, so the inverse permute is the \\\n                         complementary shift `dim − s`; a bare decimal has no width anchor here \\\n                         and must be written/ascribed explicitly, RFC-0012 §4.3)\")) ; } ; Ok (Some ((Ty :: Vsa { model : m , dim : d , sparsity : sp , } , app_node (head , vec ! [a2 , s2]) ,))) } \"vsa_bundle\" => { if args . len () != 2 { return arity_err (2) ; } let (sty , s2) = self . check (scope , & args [0] , None) ? ; let Ty :: Seq (elem , n) = sty else { return self . err (format ! (\"`vsa_bundle` first operand must be a `Seq{{VSA{{model, dim, \\\n                         sparsity}}, N}}` of hypervectors to superpose, got {sty} (RFC-0003 \\\n                         §4/§5; M-893 — never a silent conversion)\")) ; } ; let Ty :: Vsa { model , dim , sparsity , } = * elem else { return self . err (format ! (\"`vsa_bundle` items must be `VSA{{model, dim, sparsity}}` \\\n                         hypervectors, got a Seq of {elem} (RFC-0003 §4/§5; M-893 — never a \\\n                         silent conversion; a cross-paradigm edge needs an explicit `swap`)\")) ; } ; if ! VSA_PRIM_MODELS . contains (& model . as_str ()) { return self . err (format ! (\"`vsa_bundle` model {model:?} is outside the vsa prim dispatch set at \\\n                         introduction (MAP-I, FHRR, BSC — M-892); the type is a legal mention, \\\n                         but its algebra is not surfaced — widening the set is an append-only \\\n                         extension, never a guessed algebra (G2)\")) ; } if model != \"MAP-I\" { return self . err (format ! (\"`vsa_bundle` is the certified superposition path and its dispatch set \\\n                         at introduction is the certified singleton {{MAP-I}} — model \\\n                         {model:?} has no certified Value-level bundle (M-893); the FHRR/BSC \\\n                         empirical-profile bundles are a distinct, append-only surfacing under \\\n                         their own name, never a silent re-tag (VR-5)\")) ; } if sparsity != Sparsity :: Dense { return self . err (\"`vsa_bundle` requires `Dense`-sparsity hypervectors at introduction — \\\n                         the kernel's Value-level ops construct dense-class results, so a \\\n                         Sparse item type would diverge from the runtime value (M-893; \\\n                         refused, never silently re-classed — G2)\" ,) ; } if n == 0 { return self . err (\"`vsa_bundle` requires at least one item — no superposition is defined \\\n                         over a `Seq{…, 0}` (RFC-0003 §4; M-893 — refused statically, never a \\\n                         defaulted value — G2)\" . to_owned () ,) ; } let (dty , d2) = self . check (scope , & args [1] , Some (& Ty :: Float)) ? ; if ! matches ! (dty , Ty :: Float) { return self . err (format ! (\"`vsa_bundle` target failure probability δ must be a `Float`, got \\\n                         {dty} (M-893 — the certified capacity bound targets an explicit δ; \\\n                         never a defaulted or coerced parameter — G2)\")) ; } Ok (Some ((Ty :: Vsa { model , dim , sparsity , } , app_node (head , vec ! [s2 , d2]) ,))) } \"vsa_cleanup\" => { if args . len () != 2 { return arity_err (2) ; } let (m , d , _sp , q2) = check_hv (scope , & args [0] , \"query\") ? ; let cb2 = check_codebook (scope , & args [1] , & m , d) ? ; Ok (Some ((triple_ty () , app_node (head , vec ! [q2 , cb2])))) } \"vsa_reconstruct\" => { if args . len () != 4 { return arity_err (4) ; } let (m , d , _sp , r2) = check_hv (scope , & args [0] , \"record\") ? ; if m != \"MAP-I\" && m != \"BSC\" { return self . err (format ! (\"`vsa_reconstruct` model {m:?} is outside the reconstruct dispatch set \\\n                         at introduction ({{MAP-I, BSC}}) — FHRR's unbind tag is Empirical and \\\n                         trial-validated only for a single bind product, not a reconstruction \\\n                         record (M-894; VR-5 — never a stretched profile; surfacing it is a \\\n                         distinct, append-only extension)\")) ; } let (mb , db , _spb , role2) = check_hv (scope , & args [1] , \"role\") ? ; if mb != m || db != d { return self . err (format ! (\"`vsa_reconstruct` record and role must share one model and dim, got \\\n                         VSA{{{m}, {d}}} and VSA{{{mb}, {db}}} (M-894 — a model or dim mismatch \\\n                         is an explicit refusal, never a cross-model coercion; G2)\")) ; } let cb2 = check_codebook (scope , & args [2] , & m , d) ? ; let (tty , t2) = self . check (scope , & args [3] , Some (& Ty :: Float)) ? ; if ! matches ! (tty , Ty :: Float) { return self . err (format ! (\"`vsa_reconstruct` cleanup threshold must be a `Float`, got {tty} \\\n                         (RFC-0003 §6; M-894 — a below-threshold retrieval is an explicit \\\n                         refusal, never a defaulted or coerced threshold; G2)\")) ; } Ok (Some ((triple_ty () , app_node (head , vec ! [r2 , role2 , cb2 , t2]) ,))) } \"vsa_required_dim\" => { if args . len () != 2 { return arity_err (2) ; } let (ity , i2) = self . check (scope , & args [0] , None) ? ; let Ty :: Binary (_) = ity else { return self . err (format ! (\"`vsa_required_dim` items must be an unsigned `Binary{{W}}` magnitude, \\\n                         got {ity} (M-894 — a bare decimal has no width anchor here and must be \\\n                         written/ascribed explicitly, RFC-0012 §4.3)\")) ; } ; let (dty , d2) = self . check (scope , & args [1] , Some (& Ty :: Float)) ? ; if ! matches ! (dty , Ty :: Float) { return self . err (format ! (\"`vsa_required_dim` target failure probability δ must be a `Float`, \\\n                         got {dty} (M-894 — the capacity theorem instantiates an explicit δ; \\\n                         never a defaulted or coerced parameter — G2)\")) ; } Ok (Some ((Ty :: Binary (Width :: Lit (64)) , app_node (head , vec ! [i2 , d2]) ,))) } _ => unreachable ! (\"gated by the matches! above\") , } } # [doc = \" Try to check a call to a **scalar-float prim** (ADR-040 §2.4/§2.5; M-898/M-899, `enb`\"] # [doc = \" Gap A) — the arithmetic group `flt_add`/`flt_sub`/`flt_mul`/`flt_div` (binary) and\"] # [doc = \" `flt_neg` (unary), kernel `flt.add`/`flt.sub`/`flt.mul`/`flt.div`/`flt.neg`, plus the\"] # [doc = \" comparison group `flt_lt`/`flt_le`/`flt_gt`/`flt_ge`/`flt_eq` and the named total order\"] # [doc = \" `flt_total_le` (all binary), kernel `flt.lt`/…/`flt.eq`/`flt.total_le` (surface names are\"] # [doc = \" `_`-joined like `dense_add`, since `.` is the path separator in the lexer). Returns\"] # [doc = \" `Ok(None)` if `name` is not one of them.\"] # [doc = \"\"] # [doc = \" `Float` is nullary (binary64 only at introduction — ADR-040 FLAG-1), so the static rule is\"] # [doc = \" the simplest of the prim branches: every operand must be exactly `Float`, and the result\"] # [doc = \" is `Float` for the arithmetic ops and **`Binary{1}`** — the realized `Bool` of the\"] # [doc = \" RFC-0032 D1 engineering note, exactly the `eq`/`lt` shape — for the comparison ops. A\"] # [doc = \" non-`Float` operand is an explicit refusal (a cross-paradigm edge needs an\"] # [doc = \" explicit `swap`; a bare decimal has no `Float` anchor — RFC-0012 §4.3 gives it none — so it\"] # [doc = \" is refused, never defaulted; write a float literal `1.0`, M-897). The numeric-domain\"] # [doc = \" behaviour is deliberately **not** a static rule and **not** a runtime refusal either:\"] # [doc = \" arithmetic specials (overflow → ±inf, `x/0` → ±inf, `0/0` → NaN) are **in-band,\"] # [doc = \" inspectable, propagating values** per the ratified ADR-040 §2.4 FLAG-2 — the ops are total\"] # [doc = \" over `Float` (contrast `div_u`, whose integer div-by-zero has no in-band sentinel and\"] # [doc = \" must refuse at runtime) — and **comparison NaN semantics are value semantics, not types**:\"] # [doc = \" the five predicates deliver the IEEE-754 §5.11 *defined* result `false` on any NaN operand\"] # [doc = \" (NaN is unordered; `flt_eq(NaN, NaN)` is false), while `flt_total_le` is the IEEE-754\"] # [doc = \" §5.10 `totalOrder` — total and reflexive, NaN placed last, `−0` before `+0` (ADR-040\"] # [doc = \" §2.4; M-899). Per-op tag: `Empirical` per ADR-040 §2.6, carried on the runtime value with\"] # [doc = \" its zero-deviation-vs-spec bound; for `flt_total_le` the total-order *property* stays\"] # [doc = \" `Empirical` until the M-511 proof debt is discharged (`mycelium-interp/src/prims.rs`).\"] fn try_check_float_prim (& self , scope : & mut Vec < (String , Ty) > , head : & Expr , name : & str , args : & [Expr] ,) -> Result < Option < (Ty , Expr) > , CheckError > { if name == \"bin_to_flt\" { if args . len () != 1 { return self . err (format ! (\"`bin_to_flt` takes 1 operand, got {} (ADR-040 §2.4; CU-3)\" , args . len ())) ; } let (vty , v2) = self . check (scope , & args [0] , None) ? ; if ! matches ! (vty , Ty :: Binary (_)) { return self . err (format ! (\"`bin_to_flt` operand must be a concrete `Binary{{N}}`, got {vty} (ADR-040 \\\n                     §2.4 — a checked-exact conversion; never a default width)\")) ; } return Ok (Some ((Ty :: Float , app_node (head , vec ! [v2])))) ; } if name == \"flt_to_bin\" { if args . len () != 2 { return self . err (format ! (\"`flt_to_bin` takes 2 operands, got {} (ADR-040 §2.4; CU-3)\" , args . len ())) ; } let (vty , v2) = self . check (scope , & args [0] , Some (& Ty :: Float)) ? ; if ! matches ! (vty , Ty :: Float) { return self . err (format ! (\"`flt_to_bin` first (value) operand must be a `Float` (IEEE-754 binary64 — \\\n                     ADR-040 §2.1), got {vty} (never a silent conversion; write a float literal \\\n                     like `1.0`)\")) ; } let (wty , w2) = self . check (scope , & args [1] , None) ? ; let Ty :: Binary (Width :: Lit (m)) = wty else { return self . err (format ! (\"`flt_to_bin` width witness must be a concrete `Binary{{M}}` (only its width \\\n                     is used), got {wty} (ADR-040 §2.4, the DN-41 witness shape; a width-variable \\\n                     witness is refused)\")) ; } ; return Ok (Some ((Ty :: Binary (Width :: Lit (m)) , app_node (head , vec ! [v2 , w2]) ,))) ; } let is_cmp = FLOAT_CMP_PRIM_NAMES . contains (& name) ; let is_class = FLOAT_CLASS_PRIM_NAMES . contains (& name) ; if ! is_cmp && ! is_class && ! FLOAT_ARITH_PRIM_NAMES . contains (& name) { return Ok (None) ; } let want = if name == \"flt_neg\" || is_class { 1 } else { 2 } ; if args . len () != want { return self . err (format ! (\"`{name}` takes {want} operand(s), got {} (ADR-040 §2.4/§2.5; M-898/M-899)\" , args . len ())) ; } let mut rebuilt = Vec :: with_capacity (want) ; for (i , a) in args . iter () . enumerate () { let (ty , a2) = self . check (scope , a , Some (& Ty :: Float)) ? ; if ! matches ! (ty , Ty :: Float) { let which = match (want , i) { (1 , _) => \"single\" , (_ , 0) => \"first\" , _ => \"second\" , } ; return self . err (format ! (\"`{name}` {which} operand must be a `Float` (IEEE-754 binary64 — ADR-040 \\\n                     §2.1), got {ty} (M-898/M-899 — never a silent conversion; a cross-paradigm \\\n                     edge needs an explicit `swap`, and a bare decimal has no Float anchor — \\\n                     write a float literal like `1.0`)\")) ; } rebuilt . push (a2) ; } let ret = if is_cmp || is_class { Ty :: Binary (Width :: Lit (1)) } else { Ty :: Float } ; Ok (Some ((ret , app_node (head , rebuilt)))) } # [doc = \" Literal typing (Q6): a literal *is* its representation — a binary literal's width is its\"] # [doc = \" digit count, a ternary literal's trit count its width. Bare integers need context v0 does not\"] # [doc = \" yet give them → explicit refusal, never a cross-family default. (List literals are checked by\"] # [doc = \" [`Self::check_list`], which has the element-expression context `lit_ty_of` lacks.)\"] fn lit_ty (& self , l : & Literal) -> Result < Ty , CheckError > { lit_ty_of (self . site , l) } }",
      "reason": "impl target type `Cx < '_ >` has no confirmed mapping (generic type path `Cx < '_ >` — type argument `'_` is not a type (lifetime / const-generic / associated-type binding-or-constraint); `type_args` admits only type_refs, so left an explicit gap (VR-5))",
      "item_name": "Cx < '_ >"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9493,
      "col": 1,
      "category": "Closure",
      "rust_construct": "Closure",
      "snippet": "# [doc = \" The literal-typing rule (Q6), as a free function so the elaborator can reuse it without a\"] # [doc = \" checking context. A literal *is* its representation (width = digit count); bare integers and\"] # [doc = \" lists are explicit refusals.\"] pub (crate) fn lit_ty_of (site : & str , l : & Literal) -> Result < Ty , CheckError > { match l { Literal :: Bin (s) => { let n = s . chars () . filter (| c | * c == '0' || * c == '1') . count () ; if n == 0 { return Err (CheckError :: new (site , \"empty binary literal\")) ; } Ok (Ty :: Binary (Width :: Lit (u32 :: try_from (n) . expect (\"digit count fits u32\") ,))) } Literal :: Trit (s) => { if s . is_empty () { return Err (CheckError :: new (site , \"empty ternary literal\")) ; } Ok (Ty :: Ternary (Width :: Lit (u32 :: try_from (s . len ()) . expect (\"trit count fits u32\") ,))) } Literal :: Int (_) => Err (CheckError :: new (site , \"a bare integer literal has no representation family (no cross-family defaulting, \\\n             Q6) — write a binary/ternary literal, declare a `default paradigm` (RFC-0012), or \\\n             ascribe a Dense element\" ,)) , Literal :: AmbientInt (_ , _) => Err (CheckError :: new (site , \"internal: an unresolved ambient bare decimal reached `lit_ty_of` — the checker \\\n             resolves its width from context first (RFC-0012 §4.3)\" ,)) , Literal :: Bytes (_) => Ok (Ty :: Bytes) , Literal :: Float (_) => Ok (Ty :: Float) , Literal :: Str (_) => Ok (Ty :: Bytes) , Literal :: List (_) => Err (CheckError :: new (site , \"a list/sequence pattern is not supported in the v0 surface (RFC-0032 D3 — `[…]` is a \\\n             constructor literal, not a pattern)\" ,)) , } }",
      "reason": "closure parameter `c` has no explicit type annotation — Mycelium's `lambda` parameters are always `name: Type` (grammar `lambda_expr` / `param`) and this transpiler has no type-inference pass to recover an omitted Rust closure-param type",
      "item_name": "lit_ty_of"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9546,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Normalize a surface [`Pattern`] against its `expected` type into a [`crate::usefulness::Pat`]\"] # [doc = \" (the coverage-matrix shape), collecting its binders as `(name, type, occurrence)` — the\"] # [doc = \" **occurrence** is the path of field indices from the scrutinee root to the binder's position.\"] # [doc = \" Shared by the checker (`Cx::check_pattern`, occurrence `[]`) and the **elaborator** (which needs\"] # [doc = \" the matrix + the binder occurrences to lower a `match` to flat L0 `Match`/binders — RFC-0011\"] # [doc = \" §4.4). Nested constructor/literal patterns recurse (RFC-0007 §4.7); a binder is a wildcard for\"] # [doc = \" coverage (it refines nothing), a nullary constructor an empty `Ctor`.\"] pub (crate) fn normalize_pattern (types : & BTreeMap < String , DataInfo > , site : & str , pat : & Pattern , expected : & Ty , occ : & [usize] , binds : & mut Vec < (String , Ty , Vec < usize >) > ,) -> Result < crate :: usefulness :: Pat , CheckError > { use crate :: usefulness :: Pat ; match pat { Pattern :: Wildcard => Ok (Pat :: Wild) , Pattern :: Ident (n) => { if let Ty :: Data (tn , _) = expected { let d = lookup_data_home_checked (types , tn , site) ? ; if let Some (c) = d . ctors . iter () . find (| c | c . name == * n) { if ! c . fields . is_empty () { return Err (CheckError :: new (site , format ! (\"constructor pattern `{n}` must bind its {} field(s) (W7)\" , c . fields . len ()) ,)) ; } return Ok (Pat :: Ctor (n . clone () , vec ! [])) ; } } binds . push ((n . clone () , expected . clone () , occ . to_vec ())) ; Ok (Pat :: Wild) } Pattern :: Ctor (n , subs) => { let Ty :: Data (tn , targs) = expected else { return Err (CheckError :: new (site , format ! (\"constructor pattern `{n}` on a {expected} scrutinee — match a literal or `_`\") ,)) ; } ; let d = lookup_data_home_checked (types , tn , site) ? . clone () ; let Some (c) = d . ctors . iter () . find (| c | c . name == * n) else { return Err (CheckError :: new (site , format ! (\"`{n}` is not a constructor of {tn}\") ,)) ; } ; if subs . len () != c . fields . len () { return Err (CheckError :: new (site , format ! (\"pattern `{n}` binds {} of {} field(s) (W7: exactly the arity)\" , subs . len () , c . fields . len ()) ,)) ; } let s = param_subst (& d . params , targs) ; let mut out = Vec :: with_capacity (subs . len ()) ; for (i , (sub , fty)) in subs . iter () . zip (& c . fields) . enumerate () { let mut child = occ . to_vec () ; child . push (i) ; let fty = subst_ty (fty , & s) ; out . push (normalize_pattern (types , site , sub , & fty , & child , binds) ?) ; } Ok (Pat :: Ctor (n . clone () , out)) } Pattern :: Tuple (subs) => { let n = subs . len () ; let ctor_name = tuple_ctor_name (n) ; normalize_pattern (types , site , & Pattern :: Ctor (ctor_name . clone () , subs . clone ()) , expected , occ , binds ,) . map_err (| e | { CheckError :: new (site , format ! (\"tuple pattern `({})` — {}\" , subs . iter () . map (| _ | \"_\") . collect ::< Vec < _ >> () . join (\", \") , e . message) ,) }) } Pattern :: Lit (lit) => { if matches ! (lit , Literal :: Float (_)) { return Err (CheckError :: new (site , \"a float literal is not a legal match pattern (ADR-040 FLAG-4: IEEE equality \\\n                     and content identity diverge on floats — `-0.0`/NaN; match on the result of \\\n                     an explicit comparison instead — never a silent equality choice, G2)\" ,)) ; } let lty = lit_ty_of (site , lit) ? ; if lty != * expected { return Err (CheckError :: new (site , format ! (\"literal pattern has type {lty} but the scrutinee is {expected} \\\n                         (W7: a literal arm must match the scrutinee's repr and width)\") ,)) ; } Ok (Pat :: Lit (literal_key (lit))) } Pattern :: Or (_) => Err (CheckError :: new (site , \"internal: Pattern::Or reached normalize_pattern — or-patterns must be desugared \\\n             in check_match before any downstream pass (invariant violation — report this)\" ,)) , } }",
      "reason": "unsupported Rust type form `[usize]`",
      "item_name": "normalize_pattern"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9694,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Re-infer an expression's type against a checked [`Env`] (the elaborator needs the scrutinee type\"] # [doc = \" to lower a `match`, and a `let`-bound's type to track its scope — RFC-0011). The program is\"] # [doc = \" already checked, so this recomputes a type the checker validated; it does not re-litigate errors.\"] pub (crate) fn infer_type (env : & Env , scope : & mut Vec < (String , Ty) > , e : & Expr ,) -> Result < Ty , CheckError > { let no_imports = NoduleImports :: default () ; let cx = Cx { site : \"<elaborate>\" , types : & env . types , fns : & env . fns , traits : & env . traits , instances : & env . instances , imports : & no_imports , lower_rules : & env . lower_rules , tyvars : & [] , bounds : & [] , std_sys : true , depth : Cell :: new (0) , affine : Tracker :: inert () , # [cfg (test)] stage3_sabotage_skip_substitution : false , } ; cx . infer (scope , e) }",
      "reason": "qualified/associated-function call `NoduleImports :: default` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin)",
      "item_name": "infer_type"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9748,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" **Test-only** twin of [`infer_type`] with an **active** affine tracker\"] # [doc = \" ([`Tracker::seeded`], seeded from `scope` — the caller's own pre-existing local bindings, e.g.\"] # [doc = \" a `Substrate`-typed local a fixture wants tracked), instead of `infer_type`'s deliberate\"] # [doc = \" [`Tracker::inert`]. Needed because every M-1054 Stage 1b/Stage 3 white-box fixture checks a bare\"] # [doc = \" call `Expr` directly (no surface grammar for `value_params` yet — DN-110 §8.6, so there is no\"] # [doc = \" `check_nodule`/`fn`-body route to a real, active-tracker `Cx`) via `infer_type`, whose `inert`\"] # [doc = \" tracker is exactly right for its own (post-check re-inference) job but makes every `self.affine`\"] # [doc = \" interaction inside [`Cx::check_sugar_call`]'s Stage-3 (OQ-H4/DN-117) mechanism a no-op — the\"] # [doc = \" per-argument tracker-touch trigger and the substituted-`Expr` double-consume walk both need a\"] # [doc = \" real, active tracker to do anything. `#[cfg(test)]`-gated (never a shipped alternate entry\"] # [doc = \" point — the one production `infer_type` stays `Tracker::inert()`, unconditionally).\"] # [cfg (test)] pub (crate) fn infer_type_with_active_affine (env : & Env , scope : & mut Vec < (String , Ty) > , e : & Expr ,) -> Result < Ty , CheckError > { infer_type_with_active_affine_inner (env , scope , e , false) }",
      "reason": "dropped non-doc attribute(s) on fn `infer_type_with_active_affine`: # [cfg (test)]",
      "item_name": "infer_type_with_active_affine"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9768,
      "col": 1,
      "category": "DeriveAttr",
      "rust_construct": "DeriveAttr",
      "snippet": "# [doc = \" **Test-only, non-vacuity sabotage twin** of [`infer_type_with_active_affine`] (M-1054 Stage 3,\"] # [doc = \" DN-117 §5 item 2): identical, except every `Cx::check_sugar_call` reached while checking `e`\"] # [doc = \" runs its Stage-3 walk over the **unsubstituted** RHS (see\"] # [doc = \" `Cx::stage3_sabotage_skip_substitution`'s own doc comment) — simulating \\\"the substituted-`Expr`\"] # [doc = \" splice never happened,\\\" to prove that splice is load-bearing: a fixture that must genuinely\"] # [doc = \" REFUSE under the real mechanism (composite/hidden-affine duplication) should wrongly **ACCEPT**\"] # [doc = \" here.\"] # [cfg (test)] pub (crate) fn infer_type_with_active_affine_sabotaged (env : & Env , scope : & mut Vec < (String , Ty) > , e : & Expr ,) -> Result < Ty , CheckError > { infer_type_with_active_affine_inner (env , scope , e , true) }",
      "reason": "dropped non-doc attribute(s) on fn `infer_type_with_active_affine_sabotaged`: # [cfg (test)]",
      "item_name": "infer_type_with_active_affine_sabotaged"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9784,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [cfg (test)] fn infer_type_with_active_affine_inner (env : & Env , scope : & mut Vec < (String , Ty) > , e : & Expr , sabotage : bool ,) -> Result < Ty , CheckError > { let no_imports = NoduleImports :: default () ; let cx = Cx { site : \"<test:active-affine>\" , types : & env . types , fns : & env . fns , traits : & env . traits , instances : & env . instances , imports : & no_imports , lower_rules : & env . lower_rules , tyvars : & [] , bounds : & [] , std_sys : true , depth : Cell :: new (0) , affine : Tracker :: seeded (scope) , stage3_sabotage_skip_substitution : sabotage , } ; cx . infer (scope , e) }",
      "reason": "qualified/associated-function call `NoduleImports :: default` — no established Mycelium surface form for a Rust conversion-op body; emitting the bare last-segment name would fabricate a call (e.g. `from(...)` is not a Mycelium builtin)",
      "item_name": "infer_type_with_active_affine_inner"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9810,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" A canonical key for de-duplicating literal patterns (M-320): normalize away `_` separators so\"] # [doc = \" `0b1010` and `0b10_10` collide as the *same* literal. `Bin`/`Trit`/`Bytes`/`Str` reach here (the\"] # [doc = \" caller type-checks the literal first, which rejects `Int`/`List`).\"] fn literal_key (lit : & Literal) -> String { match lit { Literal :: Bin (s) => format ! (\"b:{}\" , s . chars () . filter (| c | * c == '0' || * c == '1') . collect ::< String > ()) , Literal :: Trit (s) => format ! (\"t:{s}\") , Literal :: Int (i) => format ! (\"i:{i}\") , Literal :: AmbientInt (p , i) => format ! (\"amb:{p}:{i}\") , Literal :: Bytes (s) => { format ! (\"by:{}\" , s . chars () . filter (| c | * c != '_') . collect ::< String > ()) } Literal :: Str (s) => format ! (\"s:{s}\") , Literal :: Float (s) => format ! (\"f:{s}\") , Literal :: List (_) => \"list\" . to_owned () , } }",
      "reason": "unsupported expression form `format ! (\"b:{}\" , s . chars () . filter (| c | * c == '0' || * c == '1') . collect ::< String > ())`",
      "item_name": "literal_key"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9841,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The teaching diagnostic for imperative control-flow words used as names (RFC-0007 §4.8):\"] # [doc = \" the error was happening anyway (unknown name) — make it teach instead of confuse.\"] fn teach_unknown (name : & str , base : & str) -> String { if matches ! (name , \"while\" | \"loop\" | \"break\" | \"continue\" | \"return\") { format ! (\"{base} — `{name}` is not a Mycelium form; iterate by recursion or \\\n             `for x in xs, acc = init => body` (RFC-0007 §4.8)\") } else { base . to_owned () } }",
      "reason": "unsupported expression form `matches ! (name , \"while\" | \"loop\" | \"break\" | \"continue\" | \"return\")`",
      "item_name": "teach_unknown"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9854,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The v0 linear-recursion shape check (RFC-0007 §4.8): every constructor of `tname` is either\"] # [doc = \" a **nil** (no fields) or a **cons** (exactly one spine field of type `tname` + exactly one\"] # [doc = \" element field), with one element type across all cons constructors. Returns the element\"] # [doc = \" type; anything else is an explicit refusal.\"] fn linear_elem_ty (site : & str , types : & BTreeMap < String , DataInfo > , tname : & str , targs : & [Ty] ,) -> Result < Ty , CheckError > { let d = lookup_data (types , tname) . ok_or_else (| | CheckError :: new (site , format ! (\"unknown type `{tname}`\"))) ? ; let s = param_subst (& d . params , targs) ; let mut elem : Option < Ty > = None ; let mut has_cons = false ; for c in & d . ctors { if c . fields . is_empty () { continue ; } let (spine , rest) : (Vec < & Ty > , Vec < & Ty >) = c . fields . iter () . partition (| f | matches ! (f , Ty :: Data (n , _) if n == tname)) ; if spine . len () != 1 || rest . len () != 1 { return Err (CheckError :: new (site , format ! (\"`for` needs a linearly recursive type: constructor `{}` of `{tname}` must \\\n                     have exactly one `{tname}` field and one element field (general \\\n                     catamorphisms are an L2 concern — RFC-0007 §4.8)\" , c . name) ,)) ; } has_cons = true ; let elem_ty = subst_ty (rest [0] , & s) ; match & elem { None => elem = Some (elem_ty) , Some (e) if * e == elem_ty => { } Some (e) => { return Err (CheckError :: new (site , format ! (\"`for` needs one element type across `{tname}`'s constructors: \\\n                         {e} vs {elem_ty}\") ,)) } } } if ! has_cons { return Err (CheckError :: new (site , format ! (\"`{tname}` has no recursive constructor — nothing for `for` to iterate\") ,)) ; } Ok (elem . expect (\"has_cons implies an element type\")) }",
      "reason": "unsupported Rust type form `[Ty]`",
      "item_name": "linear_elem_ty"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9917,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Rebuild an [`Expr::App`] node from its head and (resolved) arguments.\"] fn app_node (head : & Expr , args : Vec < Expr >) -> Expr { Expr :: App { head : Box :: new (head . clone ()) , args , } }",
      "reason": "struct literal `App { .. }` — not an in-file single-ctor struct that emits (no constructor to build)",
      "item_name": "app_node"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9925,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The paradigm name of a representation type (for the never-silent cross-paradigm framing).\"] # [doc = \"\"] # [doc = \" `pub(crate)` (widened from private, zero logic change) so the M-1013 Stage-5 self-hosting\"] # [doc = \" differential (`compiler_stage5_classify.rs`) can call this real oracle directly, exactly as the\"] # [doc = \" eval PR-1 leaf widened `try_match` (DN-26 §10.2 harness marshalling).\"] pub (crate) fn paradigm_name (t : & Ty) -> Option < & 'static str > { match t { Ty :: Binary (_) => Some (\"Binary\") , Ty :: Ternary (_) => Some (\"Ternary\") , Ty :: Dense (_ , _) => Some (\"Dense\") , Ty :: Vsa { .. } => Some (\"VSA\") , Ty :: Seq (_ , _) | Ty :: Bytes | Ty :: Float | Ty :: Data (_ , _) | Ty :: Substrate (_) | Ty :: Var (_) | Ty :: Fn (_ , _) => None , } }",
      "reason": "match pattern constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "paradigm_name"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9954,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Frame a type-edge mismatch (RFC-0012 §4.4): a **cross-paradigm** edge (`want` and `got` are\"] # [doc = \" representation types of *different* paradigms) becomes an explicit `MissingConversion` pointing\"] # [doc = \" at writing a `swap` — the never-silent guarantee. A same-paradigm mismatch (e.g. two `Binary`\"] # [doc = \" widths) keeps the plain wording.\"] fn edge_mismatch (edge : & str , want : & Ty , got : & Ty) -> String { match (paradigm_name (want) , paradigm_name (got)) { (Some (w) , Some (g)) if w != g => format ! (\"MissingConversion: {edge} is {got} but {want} is required — a cross-paradigm edge needs \\\n             an explicit `swap(…, to: {want}, policy: …)` (RFC-0012 §4.4; never silently converted)\") , _ => format ! (\"{edge} has type {got}, expected {want}\") , } }",
      "reason": "match-arm guard (`if ...`) has no Mycelium equivalent (arm grammar has no guard slot)",
      "item_name": "edge_mismatch"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9968,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" A bare-decimal-bearing prim family — every v0 prim is width-polymorphic and width-preserving, so\"] # [doc = \" the result width (or a concrete operand's) anchors a bare-decimal operand (RFC-0012 §4.3).\"] # [derive (Clone , Copy)] enum PrimFam { Binary , Ternary , }",
      "reason": "enum variant/constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "PrimFam"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9976,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl PrimFam { # [doc = \" The width of `t` if it is this family's representation type, else `None`.\"] fn width_of (self , t : & Ty) -> Option < u32 > { match (self , t) { (PrimFam :: Binary , Ty :: Binary (Width :: Lit (w))) | (PrimFam :: Ternary , Ty :: Ternary (Width :: Lit (w))) => Some (* w) , _ => None , } } # [doc = \" This family's type at width `w`.\"] fn ty (self , w : u32) -> Ty { match self { PrimFam :: Binary => Ty :: Binary (Width :: Lit (w)) , PrimFam :: Ternary => Ty :: Ternary (Width :: Lit (w)) , } } }",
      "reason": "no member of this impl block could be transpiled (2 sub-issue(s)): impl method `width_of` body: match pattern constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5); impl method `ty` body: match pattern constructor `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "PrimFam"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 9995,
      "col": 1,
      "category": "ReservedWord",
      "rust_construct": "ReservedWord",
      "snippet": "# [doc = \" The family of a builtin prim, or `None` if `name` is not a known prim.\"] fn prim_family (name : & str) -> Option < PrimFam > { Some (match name { \"not\" | \"popcount\" | \"clz\" | \"ctz\" | \"xor\" | \"and\" | \"or\" | \"add_u\" | \"sub_u\" | \"mul_u\" | \"mul_s\" | \"div_u\" | \"rem_u\" | \"shl_u\" | \"shr_u\" | \"add_s\" | \"sub_s\" | \"neg_s\" | \"div_s\" | \"rem_s\" | \"shr_s\" => PrimFam :: Binary , \"add\" | \"sub\" | \"mul\" | \"neg\" => PrimFam :: Ternary , _ => return None , }) }",
      "reason": "value/constructor reference `Binary` collides with a Mycelium reserved word — emitting it verbatim would fail to parse (the lexer tokenizes it as a keyword, not an identifier); no sanctioned auto-rename in this PoC, so flagged rather than emitted (G2/VR-5)",
      "item_name": "prim_family"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10027,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **M-1054 Stage 2 (G1, DN-115 §4.1)** — the VSA-prim dispatch set's **name view**, factored out\"] # [doc = \" of [`Cx::try_check_vsa_prim`] so the checker's own argument-shape-matching dispatcher and the\"] # [doc = \" Stage-2 free-identifier gate ([`Cx::rhs_first_free_id`], via [`prim_name_is_recognized`]) share\"] # [doc = \" **one** source of truth for \\\"is this name a VSA prim at all\\\" (DRY) — never two lists that could\"] # [doc = \" drift (M-892/M-894's introduction + codebook dispatch set).\"] const VSA_PRIM_NAMES : & [& str] = & [\"vsa_bind\" , \"vsa_unbind\" , \"vsa_permute\" , \"vsa_bundle\" , \"vsa_cleanup\" , \"vsa_reconstruct\" , \"vsa_required_dim\" ,] ;",
      "reason": "top-level `const VSA_PRIM_NAMES` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "VSA_PRIM_NAMES"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10042,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" **M-1054 Stage 2 (G1, DN-115 §4.1)** — the float-prim dispatch set's **name view**, factored\"] # [doc = \" into four sub-group consts so [`Cx::try_check_float_prim`] and\"] # [doc = \" [`float_prim_name_is_recognized`] share **one** source of truth per sub-group (DRY) instead of\"] # [doc = \" two independently-maintained lists that could drift (ADR-040 §2.1/§2.4/§2.5: the Binary↔Float\"] # [doc = \" conversions, comparisons, classification predicates, and scalar arithmetic).\"] const FLOAT_CMP_PRIM_NAMES : & [& str] = & [\"flt_lt\" , \"flt_le\" , \"flt_gt\" , \"flt_ge\" , \"flt_eq\" , \"flt_total_le\" ,] ;",
      "reason": "top-level `const FLOAT_CMP_PRIM_NAMES` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "FLOAT_CMP_PRIM_NAMES"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10055,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "const FLOAT_CLASS_PRIM_NAMES : & [& str] = & [\"flt_is_nan\" , \"flt_is_finite\" , \"flt_is_infinite\"] ;",
      "reason": "top-level `const FLOAT_CLASS_PRIM_NAMES` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "FLOAT_CLASS_PRIM_NAMES"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10056,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "const FLOAT_ARITH_PRIM_NAMES : & [& str] = & [\"flt_add\" , \"flt_sub\" , \"flt_mul\" , \"flt_div\" , \"flt_neg\"] ;",
      "reason": "top-level `const FLOAT_ARITH_PRIM_NAMES` — no const item production in the grammar (`item` covers use/default/type/trait/impl/fn/object/lower/derive only)",
      "item_name": "FLOAT_ARITH_PRIM_NAMES"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10058,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The float-prim name view used by [`prim_name_is_recognized`] — the union of the three\"] # [doc = \" sub-group consts above (kept as a function, not a `const &[&str]`, because Rust has no stable\"] # [doc = \" const slice-concatenation) plus the two Binary↔Float conversion names\"] # [doc = \" [`Cx::try_check_float_prim`] matches individually (`bin_to_flt`/`flt_to_bin`, each its own\"] # [doc = \" dedicated pre-branch there with a distinct arity — CU-3). Those two names are the one place in\"] # [doc = \" this pair of functions not threaded through a shared const (a literal two-name duplication,\"] # [doc = \" narrow and stable — ADR-040 §2.4 fixes both spellings), everything else is a single source of\"] # [doc = \" truth per sub-group (DRY).\"] fn float_prim_name_is_recognized (name : & str) -> bool { matches ! (name , \"bin_to_flt\" | \"flt_to_bin\") || FLOAT_CMP_PRIM_NAMES . contains (& name) || FLOAT_CLASS_PRIM_NAMES . contains (& name) || FLOAT_ARITH_PRIM_NAMES . contains (& name) }",
      "reason": "unsupported expression form `matches ! (name , \"bin_to_flt\" | \"flt_to_bin\")`",
      "item_name": "float_prim_name_is_recognized"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10088,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Encode a non-negative decimal `v` as an **unsigned** `Binary{width}` literal (MSB-first), or an\"] # [doc = \" explicit refusal if it does not fit (RFC-0012 §4.3 — never a silent wrap/truncation).\"] fn encode_binary (site : & str , v : i64 , width : u32) -> Result < Literal , CheckError > { if width == 0 { return Err (CheckError :: new (site , \"cannot encode a decimal at Binary{0} (zero width)\" ,)) ; } let vu = u128 :: try_from (v) . map_err (| _ | { CheckError :: new (site , format ! (\"negative decimal `{v}` has no unsigned `Binary` encoding\") ,) }) ? ; if width < 128 && (vu >> width) != 0 { return Err (CheckError :: new (site , format ! (\"decimal `{v}` does not fit Binary{{{width}}} (unsigned range 0..2^{width})\") ,)) ; } let mut s = String :: with_capacity (width as usize) ; for i in (0 .. width) . rev () { let bit = if i < 128 { (vu >> i) & 1 } else { 0 } ; s . push (if bit == 1 { '1' } else { '0' }) ; } Ok (Literal :: Bin (s)) }",
      "reason": "function body has a semicolon-terminated (value-discarding) statement expression before the tail — a `let`-chain body maps only simple `let x = e;` bindings plus a single trailing expression",
      "item_name": "encode_binary"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10117,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Encode a decimal `v` as a **balanced-ternary** `Ternary{width}` literal (MSB-first, digits in\"] # [doc = \" `{-,0,+}`), or an explicit refusal if it does not fit the symmetric range (RFC-0012 §4.3).\"] fn encode_balanced_ternary (site : & str , v : i64 , width : u32) -> Result < Literal , CheckError > { if width == 0 { return Err (CheckError :: new (site , \"cannot encode a decimal at Ternary{0} (zero width)\" ,)) ; } let mut n = i128 :: from (v) ; let mut digits : Vec < i8 > = Vec :: new () ; while n != 0 { let mut r = (n % 3) as i8 ; n /= 3 ; if r == 2 { r = - 1 ; n += 1 ; } else if r == - 2 { r = 1 ; n -= 1 ; } digits . push (r) ; } if digits . len () > width as usize { return Err (CheckError :: new (site , format ! (\"decimal `{v}` does not fit Ternary{{{width}}} (balanced range ±(3^{width}-1)/2)\") ,)) ; } digits . resize (width as usize , 0) ; let s : String = digits . iter () . rev () . map (| d | match d { - 1 => '-' , 0 => '0' , 1 => '+' , _ => unreachable ! (\"a balanced trit is in {{-1, 0, 1}}\") , }) . collect () ; Ok (Literal :: Trit (s)) }",
      "reason": "function body has a semicolon-terminated (value-discarding) statement expression before the tail — a `let`-chain body maps only simple `let x = e;` bindings plus a single trailing expression",
      "item_name": "encode_balanced_ternary"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10162,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The builtin prim signature table `Π` (RFC-0007 §4.4 T-Op), width-polymorphic. Surface names\"] # [doc = \" map onto the trusted interpreter's registry (`bit.*`/`trit.*`).\"] # [must_use] pub fn prim_sig (name : & str , args : & [Ty]) -> Option < Ty > { match (name , args) { (\"not\" | \"neg_s\" | \"popcount\" | \"clz\" | \"ctz\" , [Ty :: Binary (w)]) => { Some (Ty :: Binary (w . clone ())) } (\"xor\" , [Ty :: Binary (a) , Ty :: Binary (b)]) if a == b => Some (Ty :: Binary (a . clone ())) , (\"and\" | \"or\" | \"add_u\" | \"sub_u\" | \"mul_u\" | \"mul_s\" | \"div_u\" | \"rem_u\" | \"shl_u\" | \"shr_u\" | \"add_s\" | \"sub_s\" | \"div_s\" | \"rem_s\" | \"shr_s\" , [Ty :: Binary (a) , Ty :: Binary (b)] ,) if a == b => Some (Ty :: Binary (a . clone ())) , (\"add\" | \"sub\" | \"mul\" , [Ty :: Ternary (a) , Ty :: Ternary (b)]) if a == b => { Some (Ty :: Ternary (a . clone ())) } (\"neg\" , [Ty :: Ternary (m)]) => Some (Ty :: Ternary (m . clone ())) , _ => None , } }",
      "reason": "unsupported Rust type form `[Ty]`",
      "item_name": "prim_sig"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10199,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Canonicalize a surface VSA model ident to the **kernel model id** (`Repr::Vsa { model }` —\"] # [doc = \" M-892). A surface ident cannot carry the `-` some kernel ids use (`MAP-I`, `MAP-B`), so the\"] # [doc = \" underscore spelling maps onto it; every other ident passes through **verbatim** (an unknown\"] # [doc = \" model is a legal type *mention* — core type-checks hypervector mentions without the algebra,\"] # [doc = \" ADR-008 — and the `vsa_*` prims gate their own dispatch set with an explicit refusal, so\"] # [doc = \" nothing here guesses). The mapping is total and append-only.\"] # [must_use] pub fn vsa_kernel_model_id (surface : & str) -> String { match surface { \"MAP_I\" => \"MAP-I\" . to_owned () , \"MAP_B\" => \"MAP-B\" . to_owned () , other => other . to_owned () , } }",
      "reason": "Rust ownership/identity-conversion no-op method `.to_owned()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `to_owned(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `to_owned`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5)",
      "item_name": "vsa_kernel_model_id"
    },
    {
      "file": "crates/mycelium-l1/src/checkty.rs",
      "line": 10214,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" The surface→kernel prim-name mapping (the `Op` node's `prim` — RFC-0007 §4.1).\"] # [must_use] pub fn prim_kernel_name (name : & str) -> Option < & 'static str > { Some (match name { \"not\" => \"bit.not\" , \"popcount\" => \"bit.popcount\" , \"clz\" => \"bit.clz\" , \"ctz\" => \"bit.ctz\" , \"xor\" => \"bit.xor\" , \"and\" => \"bit.and\" , \"or\" => \"bit.or\" , \"add_u\" => \"bit.add\" , \"sub_u\" => \"bit.sub\" , \"mul_s\" => \"bin.mul\" , \"mul_u\" => \"bit.mul\" , \"div_u\" => \"bin.div\" , \"rem_u\" => \"bin.rem\" , \"shl_u\" => \"bin.shl\" , \"shr_u\" => \"bin.shr\" , \"add_s\" => \"bin.add\" , \"sub_s\" => \"bin.sub\" , \"neg_s\" => \"bin.neg\" , \"div_s\" => \"bin.div_s\" , \"rem_s\" => \"bin.rem_s\" , \"shr_s\" => \"bin.shr_s\" , \"eq\" => \"cmp.eq\" , \"lt\" => \"cmp.lt\" , \"lt_s\" => \"cmp.lt_s\" , \"seq_len\" => \"seq.len\" , \"seq_get\" => \"seq.get\" , \"bytes_len\" => \"bytes.len\" , \"bytes_get\" => \"bytes.get\" , \"bytes_slice\" => \"bytes.slice\" , \"bytes_concat\" => \"bytes.concat\" , \"bytes_eq\" => \"bytes.eq\" , \"hash_blake3\" => \"hash.blake3\" , \"width_cast\" => \"bit.width_cast\" , \"truncate\" => \"bit.truncate\" , \"dense_add\" => \"dense.add\" , \"dense_sub\" => \"dense.sub\" , \"dense_neg\" => \"dense.neg\" , \"dense_scale\" => \"dense.scale\" , \"dense_dot\" => \"dense.dot\" , \"dense_similarity\" => \"dense.similarity\" , \"flt_add\" => \"flt.add\" , \"flt_sub\" => \"flt.sub\" , \"flt_mul\" => \"flt.mul\" , \"flt_div\" => \"flt.div\" , \"flt_neg\" => \"flt.neg\" , \"flt_lt\" => \"flt.lt\" , \"flt_le\" => \"flt.le\" , \"flt_gt\" => \"flt.gt\" , \"flt_ge\" => \"flt.ge\" , \"flt_eq\" => \"flt.eq\" , \"flt_total_le\" => \"flt.total_le\" , \"flt_is_nan\" => \"flt.is_nan\" , \"flt_is_finite\" => \"flt.is_finite\" , \"flt_is_infinite\" => \"flt.is_infinite\" , \"bin_to_flt\" => \"bin.to_flt\" , \"flt_to_bin\" => \"flt.to_bin\" , \"vsa_bind\" => \"vsa.bind\" , \"vsa_unbind\" => \"vsa.unbind\" , \"vsa_permute\" => \"vsa.permute\" , \"vsa_bundle\" => \"vsa.bundle\" , \"vsa_cleanup\" => \"vsa.cleanup\" , \"vsa_reconstruct\" => \"vsa.reconstruct\" , \"vsa_required_dim\" => \"vsa.required_dim\" , \"add\" => \"trit.add\" , \"sub\" => \"trit.sub\" , \"mul\" => \"trit.mul\" , \"neg\" => \"trit.neg\" , _ => return None , }) }",
      "reason": "unsupported expression form `return None`",
      "item_name": "prim_kernel_name"
    }
  ],
  "total_top_level_items": 147
}
