//! Shared CLI plumbing for the Mycelium toolchain binaries (M-643).
//!
//! Scaffold registered by the orchestrator so the Phase-8 release-mechanics epic branches from a
//! buildable workspace base (CLAUDE.md swarm step 3). The DRY helpers — input reading,
//! source collection, and shared argument handling extracted from `mycfmt` / `myc-check` /
//! `myc-lint` / `myc-sec` — are populated by M-643. Until then this crate is intentionally empty.
